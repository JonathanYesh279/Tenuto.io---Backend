---
phase: 67-component-standardization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/ui/badge.tsx
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/ui/Card.tsx
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/ui/Table.tsx
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/ui/DesignSystem.tsx
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/ui/ConfirmDeleteDialog.tsx
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/ui/ConfirmationModal.tsx
autonomous: true

must_haves:
  truths:
    - "No gray-NNN Tailwind classes remain in any file under src/components/ui/"
    - "Badge success variant uses bg-success semantic token instead of bg-green-500"
    - "Badge inactive variant uses bg-neutral-100 instead of bg-gray-100"
    - "Card component accepts a variant prop with default, elevated, and outlined options"
    - "Card default variant produces identical visual output to current Card (shadow-1, same border/bg)"
    - "Card hover prop continues to work independently of variant prop"
    - "Table cells use density token spacing (px-table-cell-px py-table-cell-py)"
  artifacts:
    - path: "src/components/ui/badge.tsx"
      provides: "Semantic color badge variants"
      contains: "bg-success"
    - path: "src/components/ui/Card.tsx"
      provides: "CVA Card with variant prop"
      contains: "cardVariants"
    - path: "src/components/ui/Table.tsx"
      provides: "Token-backed table density"
      contains: "table-cell-px"
  key_links:
    - from: "src/components/ui/badge.tsx"
      to: "tailwind.config.js"
      via: "semantic success/neutral token classes"
      pattern: "bg-success|bg-neutral"
    - from: "src/components/ui/Card.tsx"
      to: "tailwind.config.js"
      via: "shadow token classes"
      pattern: "shadow-0|shadow-1|shadow-2"
    - from: "src/components/ui/Table.tsx"
      to: "tailwind.config.js"
      via: "density token spacing classes"
      pattern: "table-cell-px|table-cell-py|table-header-py"
---

<objective>
Migrate all gray-NNN classes in src/components/ui/ to neutral-NNN, add CVA variants to Card component, migrate Badge to semantic color tokens, and standardize Table with density tokens.

Purpose: First wave of token adoption -- replaces all hardcoded gray colors with tokenized neutral scale, adds structured variants to Card and Badge, and applies density tokens to Table.
Output: 6 modified component files with zero gray-NNN references and token-backed variants.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-design-system-foundation/66-01-SUMMARY.md
@.planning/phases/66-design-system-foundation/66-02-SUMMARY.md
@.planning/phases/67-component-standardization/67-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate gray-NNN to neutral-NNN across all ui/ files + migrate Badge to semantic colors</name>
  <files>
    src/components/ui/badge.tsx
    src/components/ui/Table.tsx
    src/components/ui/DesignSystem.tsx
    src/components/ui/ConfirmDeleteDialog.tsx
    src/components/ui/ConfirmationModal.tsx
  </files>
  <action>
Replace every `gray-NNN` Tailwind class in src/components/ui/ with the equivalent `neutral-NNN` class. Specific replacements:

**badge.tsx:**
- Line 22: `bg-gray-100 text-gray-700` -> `bg-neutral-100 text-neutral-700` (inactive variant)
- Line 19: `bg-green-500 text-white hover:bg-green-600` -> `bg-success text-success-foreground hover:bg-success/80` (success variant)
- NOTE: `active` and `completed` variants use `bg-green-100 text-green-800` -- these should stay as success-100/success-800 (already mapped in Phase 66 hex scale). Keep as-is since they already reference the success palette.
- `graduated` (purple) and `pending` (orange) have no semantic token equivalent -- keep as-is per research.

**Table.tsx:**
- StatusBadge inside Table.tsx: `bg-gray-100 text-gray-800` on inactive -> `bg-neutral-100 text-neutral-800`
- Table cell padding: Replace `px-4 py-3` with `px-table-cell-px py-table-cell-py` on all `<th>` and `<td>` elements (header and body cells)
- Table header: Replace `py-3` with `py-table-header-py` on `<th>` elements specifically

**DesignSystem.tsx:**
- StatusBadge: `bg-gray-100 text-gray-800 border-gray-200` on inactive -> `bg-neutral-100 text-neutral-800 border-neutral-200`

**ConfirmDeleteDialog.tsx:**
- Line 94: `text-gray-900` -> `text-foreground` (semantic token preferred over neutral-900 for primary text)

**ConfirmationModal.tsx:**
- Line 86: `text-gray-700` -> `text-muted-foreground` (semantic token preferred for secondary text)

IMPORTANT: Do NOT use neutral classes with opacity modifiers (e.g., bg-neutral-500/50) -- the neutral CSS vars contain full hsl() values and opacity modifiers will not work. Use semantic tokens (bg-muted, text-muted-foreground) for opacity needs.
  </action>
  <verify>
Run: `cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && grep -r "gray-[0-9]" src/components/ui/ --include="*.tsx"` -- should return zero matches.
Run: `grep "bg-success" src/components/ui/badge.tsx` -- should show the success variant using semantic token.
  </verify>
  <done>Zero gray-NNN classes in src/components/ui/. Badge success uses bg-success, inactive uses bg-neutral-100. Table uses density tokens for cell padding. ConfirmDeleteDialog and ConfirmationModal use semantic text tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Add CVA variants to Card component</name>
  <files>
    src/components/ui/Card.tsx
  </files>
  <action>
Add CVA variant support to Card.tsx while preserving all existing behavior.

1. Import `cva` and `VariantProps` from `class-variance-authority`
2. Create `cardVariants` CVA definition:
   - Base classes: `"rounded-lg border bg-card text-card-foreground transition-shadow duration-200"`
   - Variants:
     - `default`: `"shadow-1"` (current behavior)
     - `elevated`: `"shadow-2"` (pre-elevated)
     - `outlined`: `"shadow-none border-2"` (no shadow, thicker border)
   - Default variant: `default`

3. Update CardProps interface:
   - Extend with `VariantProps<typeof cardVariants>`
   - Keep existing `hover?: boolean` prop (NOT a CVA variant -- it's a separate boolean)

4. Update Card component:
   - Destructure `variant` from props
   - Use `cardVariants({ variant })` in cn() call
   - Keep `hover && "hover:shadow-2"` logic after the CVA call
   - className override still last in cn()

5. Update CardTitle: Replace `text-2xl font-semibold` with `text-h2` (both are 24px/600 -- identical output). Keep `leading-none tracking-tight` as overrides since card titles are single-line.

6. Export `cardVariants` alongside existing exports.

CRITICAL: The default variant MUST produce identical visual output to the current Card. Current: `shadow-1`. New default: `shadow-1`. No visual change.
  </action>
  <verify>
Run: `cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && grep "cardVariants" src/components/ui/Card.tsx` -- should show the CVA definition and usage.
Run: `grep "variant.*default.*elevated.*outlined" src/components/ui/Card.tsx` -- should show all three variants.
Run: `grep "hover" src/components/ui/Card.tsx` -- should show hover prop still exists.
Run: `npx tsc --noEmit --project tsconfig.json 2>&1 | head -20` -- should show no type errors in Card.tsx.
  </verify>
  <done>Card.tsx has CVA variants (default/elevated/outlined), hover prop preserved, CardTitle uses text-h2, cardVariants exported. Default variant visually identical to current Card.</done>
</task>

</tasks>

<verification>
1. `grep -r "gray-[0-9]" src/components/ui/ --include="*.tsx"` returns zero matches
2. `grep "bg-success" src/components/ui/badge.tsx` shows semantic token usage
3. `grep "cardVariants" src/components/ui/Card.tsx` shows CVA definition
4. `grep "table-cell" src/components/ui/Table.tsx` shows density token usage
5. TypeScript compiles without errors in modified files
</verification>

<success_criteria>
- Zero gray-NNN classes in src/components/ui/
- Badge success variant uses bg-success semantic token
- Card has three CVA variants (default, elevated, outlined) with backward-compatible default
- Card hover prop works independently of variant
- Table uses density tokens for cell padding
- CardTitle uses text-h2 semantic type scale
- All modified files compile without TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/67-component-standardization/67-01-SUMMARY.md`
</output>
