---
phase: 67-component-standardization
plan: 03
type: execute
wave: 3
depends_on: ["67-02"]
files_modified:
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/ui/Calendar.tsx
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/ui/InputModal.tsx
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/ui/ConfirmDeleteDialog.tsx
autonomous: true

must_haves:
  truths:
    - "Calendar heading uses text-h3 semantic type scale"
    - "Calendar cell spacing uses design system spacing values"
    - "Calendar event color map references semantic tokens where equivalent exists"
    - "InputModal heading uses text-h3 semantic type scale"
    - "ConfirmDeleteDialog heading (ConfirmDeleteModal h2 inside it) uses semantic foreground token"
  artifacts:
    - path: "src/components/ui/Calendar.tsx"
      provides: "Calendar with token overrides"
      contains: "text-h3"
    - path: "src/components/ui/InputModal.tsx"
      provides: "InputModal with semantic heading"
      contains: "text-h3"
  key_links:
    - from: "src/components/ui/Calendar.tsx"
      to: "tailwind.config.js"
      via: "semantic type scale and spacing tokens"
      pattern: "text-h3|spacing"
---

<objective>
Apply semantic type scale to remaining shared heading components and add calendar CSS token overrides.

Purpose: Complete the token adoption across all src/components/ui/ files -- headings use the semantic type scale, Calendar uses design system tokens for colors and spacing.
Output: Calendar, InputModal, and ConfirmDeleteDialog fully token-integrated.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-component-standardization/67-02-SUMMARY.md
@.planning/phases/67-component-standardization/67-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply semantic type scale to Calendar and InputModal headings</name>
  <files>
    src/components/ui/Calendar.tsx
    src/components/ui/InputModal.tsx
  </files>
  <action>
**Calendar.tsx headings:**

1. Line 86: `<h3 className="text-lg font-semibold text-foreground">` -> `<h3 className="text-h3 text-foreground">`
   - text-h3 = 20px, fontWeight 600, lineHeight 1.4 (includes font-semibold equivalent)
   - text-lg was 18px -- this is a deliberate 2px increase for Hebrew readability (Phase 66 decision)

2. Line 165: `<h4 className="font-medium text-foreground mb-3">` -> `<h4 className="text-body font-medium text-foreground mb-3">`
   - text-body = 14px, lineHeight 1.6 -- this is a section sub-heading, keep font-medium override since text-body defaults to fontWeight 400

**Calendar.tsx spacing and color token overrides:**

3. Calendar day cells: `min-h-[100px]` -> keep as-is (no spacing token for calendar cell height -- it's domain-specific)

4. Event badge padding: `px-2 py-1` in event items -> keep as-is (fine-grained event badge spacing, no matching token)

5. Calendar header day cells: `p-3` -> `p-spacing-element` (12px = p-3, exact match, now token-backed)

6. Calendar "add event" button: `px-3 py-1.5` -> keep as-is (small button sizing, matches Button sm variant proportions)

7. Color map in getColorClasses -- the blue and green entries already use semantic tokens (bg-muted, bg-success-100). The orange and purple entries use raw Tailwind colors (bg-orange-100, bg-purple-100) -- keep as-is since there are no semantic equivalents for orange/purple event types. Add a comment above the colorMap:
```typescript
// Event colors: blue and green use semantic tokens; orange and purple
// have no semantic equivalent and use Tailwind palette directly.
```

8. Navigation buttons: `p-1 rounded hover:bg-muted` -> already uses semantic bg-muted token. Keep as-is.

**InputModal.tsx heading:**

9. Line 43: `<h3 className="text-lg font-semibold text-foreground">` -> `<h3 className="text-h3 text-foreground">`
   - Same 18px -> 20px upgrade as Calendar, using semantic type scale

10. InputModal input element: `px-3 py-2` -> `px-spacing-element py-2` (matching the Input component token migration from Plan 02)

11. InputModal submit button: `px-4 py-2 bg-primary text-primary-foreground rounded hover:bg-neutral-800` -> keep as-is (this is a one-off inline button style, consistent with current design. Could use Button component but that's a larger refactor outside this plan's scope)
  </action>
  <verify>
Run: `cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && grep "text-h3" src/components/ui/Calendar.tsx` -- should show semantic heading.
Run: `grep "text-h3" src/components/ui/InputModal.tsx` -- should show semantic heading.
Run: `grep "spacing-element" src/components/ui/Calendar.tsx` -- should show token usage in header.
Run: `grep "spacing-element" src/components/ui/InputModal.tsx` -- should show token usage in input.
  </verify>
  <done>Calendar h3 uses text-h3 semantic type scale. Calendar header cells use p-spacing-element. Calendar color map has documentation comment. InputModal h3 uses text-h3. InputModal input uses px-spacing-element. Orange/purple calendar colors kept as raw Tailwind (no semantic equivalent).</done>
</task>

<task type="auto">
  <name>Task 2: Final verification sweep -- confirm zero gray-NNN and full token coverage</name>
  <files>
    src/components/ui/ConfirmDeleteDialog.tsx
  </files>
  <action>
1. ConfirmDeleteDialog.tsx: Verify the gray-900 replacement from Plan 01 is in place (should already be text-foreground). If Plan 01 was executed correctly, no changes needed here. This task is a verification checkpoint.

2. Run a comprehensive grep across ALL files in src/components/ui/ for any remaining hardcoded patterns that should use tokens:
   - `grep -r "gray-[0-9]" src/components/ui/` -- must return zero
   - `grep -r "text-2xl" src/components/ui/` -- should return zero (all migrated to text-h2)
   - `grep -r "text-lg font-semibold" src/components/ui/` -- should return zero (all migrated to text-h3, except any that are not headings)

3. If any remaining issues found in step 2, fix them. Expected: ConfirmDeleteDialog's ConfirmDeleteModal-style heading inside (the `text-xl font-bold` on h2 if present) should use `text-h3` equivalent. Check the actual content -- ConfirmDeleteDialog uses DialogTitle which was already migrated to text-h3 in Plan 02. No extra h2 exists.

4. Verify TypeScript compilation: `npx tsc --noEmit --project tsconfig.json 2>&1 | head -30` -- no errors expected.

5. If the ConfirmDeleteDialog still has any remaining hardcoded color classes (red-600, yellow-500, blue-600 in variantIconColors/variantBgColors) -- these are domain-specific severity colors, NOT gray scale, and do NOT need migration. They intentionally use specific hues for danger/warning/info severity indicators.
  </action>
  <verify>
Run: `cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && grep -r "gray-[0-9]" src/components/ui/ --include="*.tsx" | wc -l` -- must be 0.
Run: `grep -r "text-2xl" src/components/ui/ --include="*.tsx" | wc -l` -- must be 0.
Run: `npx tsc --noEmit --project tsconfig.json 2>&1 | grep -i "error" | head -5` -- must be 0 errors.
  </verify>
  <done>Zero gray-NNN in src/components/ui/. Zero text-2xl (all migrated to text-h2). Zero text-lg font-semibold heading patterns (all migrated to text-h3). TypeScript compiles clean. Phase 67 success criteria fully met.</done>
</task>

</tasks>

<verification>
Phase-level verification -- all 7 success criteria from the roadmap:

1. `grep -r "gray-[0-9]" src/components/ui/ --include="*.tsx" | wc -l` = 0 (SC-1: no gray-NNN)
2. `grep "<Button" src/components/ui/DesignSystem.tsx` shows ActionButton delegates to CVA Button (SC-2)
3. `grep "cardVariants" src/components/ui/Card.tsx` shows CVA variants exist (SC-3)
4. `grep "bg-success" src/components/ui/badge.tsx` shows semantic tokens (SC-4)
5. `grep "spacing-element\|spacing-card" src/components/ui/input.tsx src/components/ui/dialog.tsx` shows token-backed sizing (SC-5)
6. `grep "text-h2\|text-h3" src/components/ui/Card.tsx src/components/ui/dialog.tsx src/components/ui/Calendar.tsx src/components/ui/InputModal.tsx` shows semantic type scale (SC-6)
7. `grep "text-h3\|spacing-element" src/components/ui/Calendar.tsx` shows calendar token overrides (SC-7)
</verification>

<success_criteria>
- Calendar and InputModal headings use text-h3 semantic type scale
- Calendar header spacing uses spacing-element token
- Calendar color map documented (semantic where available, raw Tailwind where no semantic equivalent)
- Zero gray-NNN across all src/components/ui/ files
- Zero text-2xl (migrated to text-h2)
- TypeScript compiles without errors
- All 7 phase success criteria verified
</success_criteria>

<output>
After completion, create `.planning/phases/67-component-standardization/67-03-SUMMARY.md`
</output>
