---
phase: 25-ensemble-import-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/ImportData.tsx
autonomous: false

must_haves:
  truths:
    - "Import page shows a 4th tab labeled 'הרכבים' alongside teachers, students, and conservatory tabs"
    - "Clicking the ensemble tab resets state and shows the upload step with ensemble-specific file guide"
    - "Uploading an .xlsx file calls the ensemble preview API and displays ensemble rows with conductor match badges"
    - "Preview shows conductor summary cards (resolved/unresolved/ambiguous counts in green/red/amber gradients)"
    - "Preview shows orchestra match summary cards (new/updated/no-change counts)"
    - "Preview shows warnings for unrecognized ensemble names and other parse issues"
    - "Each ensemble row displays: name, type, subType, conductor badge, orchestra match status, participant count, schedule, hours"
    - "After execute, results step shows createdCount, updatedCount, noChangeCount, skippedCount, errorCount"
    - "Page subtitle updates to 'ייבוא הרכבים מקובץ Excel' when ensemble tab is active"
  artifacts:
    - path: "/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js"
      provides: "previewEnsembleImport method in importService"
      contains: "previewEnsembleImport"
    - path: "/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/ImportData.tsx"
      provides: "Ensemble import tab with row-level preview, conductor badges, and results rendering"
      contains: "ensembles"
  key_links:
    - from: "ImportData.tsx handleFile"
      to: "importService.previewEnsembleImport"
      via: "activeTab === 'ensembles' branch"
      pattern: "previewEnsembleImport"
    - from: "ImportData.tsx handleExecute"
      to: "importService.executeImport"
      via: "ensemblePreview.importLogId branch"
      pattern: "ensemblePreview.*importLogId"
    - from: "importService.previewEnsembleImport"
      to: "POST /api/import/ensembles/preview"
      via: "fetch with FormData"
      pattern: "import/ensembles/preview"
---

<objective>
Add ensemble import as a 4th tab on the existing import page, with a row-level preview showing each ensemble's decomposed name, conductor match badge (green/red/amber), orchestra match status (new/existing-updated/existing-no-change), schedule data, reporting hours, and warnings for unrecognized names. After execute, show results with created/updated/skipped/error counts.

Purpose: Completes the v1.4 milestone by enabling admins to import ensemble data through the same familiar import interface used for teachers, students, and conservatory imports.
Output: Modified ImportData.tsx with 4-tab layout, ensemble preview with conductor badges, and ensemble results display; apiService.js with previewEnsembleImport method.
</objective>

<execution_context>
@/home/yona279/.claude/get-shit-done/workflows/execute-plan.md
@/home/yona279/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-ensemble-import-frontend/25-RESEARCH.md

# Source files to modify
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/ImportData.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ensemble API method and extend ImportData with 4th tab, ensemble preview, and results</name>
  <files>
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/ImportData.tsx
  </files>
  <action>
**apiService.js changes:**

Add `previewEnsembleImport(file)` method to the `importService` object (after `previewConservatoryImport`, before `executeImport`). Follow the exact same pattern as `previewConservatoryImport` (lines 5134-5163):
- Create FormData, append file
- POST to `${apiClient.baseURL}/import/ensembles/preview`
- Authorization header with `apiClient.getStoredToken()`
- Return parsed JSON response
- Console log: `'Uploading ensemble import file for preview'` and `'Ensemble import preview ready'`
- Error message: `'Error importing ensemble file'`

**ImportData.tsx changes:**

1. **Type updates (top of file):**
   - Change `ImportTab` type from `'teachers' | 'students' | 'conservatory'` to `'teachers' | 'students' | 'conservatory' | 'ensembles'`
   - Add `EnsemblePreviewEnsemble` interface matching the backend response shape:
     ```typescript
     interface EnsemblePreviewEnsemble {
       row: number
       rawName: string
       name: string
       type: string        // 'תזמורת' | 'הרכב'
       subType: string | null
       performanceLevel: string | null
       participantCount: number
       conductorMatch: {
         status: 'resolved' | 'unresolved' | 'ambiguous' | 'none'
         teacherId?: string
         teacherName?: string
         candidateCount?: number
         importedName?: string
       }
       schedule: {
         activity1: { day: string; dayOfWeek: string; startTime: string; endTime: string; actualHours: number } | null
         activity2: { day: string; dayOfWeek: string; startTime: string; endTime: string; actualHours: number } | null
       }
       hours: { totalActual: number; coordinationHours: number; totalReporting: number }
       ministryUseCode: number | null
       orchestraMatch: {
         status: 'new' | 'existing-updated' | 'existing-no-change'
         orchestraId?: string
         orchestraName?: string
       }
     }
     ```
   - Add `EnsemblePreviewData` interface:
     ```typescript
     interface EnsemblePreviewData {
       importLogId: string
       preview: {
         totalRows: number
         ensembles: EnsemblePreviewEnsemble[]
         conductorSummary: { resolved: number; unresolved: number; ambiguous: number; none: number }
         orchestraMatchSummary: { new: number; existingUpdated: number; existingNoChange: number }
         analytics: any | null
         warnings: Array<{ row: number; field: string; message: string }>
         errors: any[]
       }
     }
     ```
   - Add `EnsembleImportResult` interface:
     ```typescript
     interface EnsembleImportResult {
       totalRows: number
       createdCount: number
       updatedCount: number
       skippedCount: number
       noChangeCount: number
       errorCount: number
       skipped: Array<{ row: number; name: string; reason: string }>
       errors: Array<{ row: number; name: string; error: string }>
     }
     ```

2. **Import `MusicNotesIcon` from `@phosphor-icons/react`** (already used in TeacherTabNavigation.tsx and other teacher detail pages for conducting/orchestras -- fits semantically for ensemble import).

3. **Add `EnsembleFileGuide` component** (before the default export, after `ConservatoryFileGuide`):
   - A card with the title "קובץ הרכבי ביצוע"
   - A blue info banner: "תומך בקבצי מימשק משרד החינוך - גיליון הרכבי ביצוע"
   - Subtitle: "המערכת מזהה אוטומטית את סוג ההרכב, מנצח, לוח זמנים ושעות דיווח"
   - A simple description: "יש להעלות את קובץ ה-Excel של הרכבי הביצוע כפי שהתקבל ממשרד החינוך. המערכת תזהה את הגיליון 'הרכבי ביצוע' אוטומטית."
   - A 2-column grid listing the parsed fields: שם הרכב, סוג (תזמורת/הרכב), מנצח/מדריך, מספר משתתפים, יום פעילות, שעת התחלה, שעת סיום, שעות בפועל, שעות ריכוז, סה"כ שעות דיווח

4. **Add state for ensemble-specific data (inside the ImportData component):**
   - `const [ensemblePreview, setEnsemblePreview] = useState<EnsemblePreviewData | null>(null)`
   - `const [ensembleResults, setEnsembleResults] = useState<EnsembleImportResult | null>(null)`
   - Update `resetState()` to also clear `setEnsemblePreview(null)` and `setEnsembleResults(null)`

5. **Add helper: `getConductorMatchBadge(conductorMatch)`** (near the existing `getTeacherMatchBadge` function):
   - Reuse the EXACT same badge pattern as `getTeacherMatchBadge` (lines 318-348) but with ensemble-specific labels:
     - `resolved`: green badge, text: `מנצח: ${conductorMatch.teacherName}`
     - `unresolved`: red badge, text: `מנצח לא נמצא` (and show `conductorMatch.importedName` if available)
     - `ambiguous`: amber badge, text: `${conductorMatch.candidateCount} מנצחים אפשריים`
     - `none`: return null

6. **Add helper: `getOrchestraMatchBadge(orchestraMatch)`**:
   - `new`: blue badge with PlusIcon, text: `הרכב חדש`
   - `existing-updated`: green badge with ArrowsClockwiseIcon, text: `עדכון`
   - `existing-no-change`: gray badge, text: `ללא שינוי`

7. **Tab button (in the flex gap-2 mb-6 div around line 809-843):**
   - Add a 4th tab button after the conservatory button following the exact same pattern
   - Icon: `<MusicNotesIcon size={16} weight="regular" />`
   - Label: `הרכבים`
   - onClick: `handleTabChange('ensembles')`
   - Same conditional className pattern as existing tabs

8. **Page subtitle (around line 800-804):**
   - Add: `{activeTab === 'ensembles' && 'ייבוא הרכבים מקובץ Excel'}`

9. **Upload step file guide:**
   - Add: `{activeTab === 'ensembles' && <EnsembleFileGuide />}`

10. **handleFile callback (around line 671-699):**
    - Add ensemble branch BEFORE the existing conservatory check:
      ```
      if (activeTab === 'ensembles') {
        const result = await importService.previewEnsembleImport(file)
        setEnsemblePreview(result)
      } else if (activeTab === 'conservatory') {
        ...existing...
      }
      ```
    - The `else` branch for teachers/students remains unchanged

11. **handleExecute modification (around line 723-753):**
    - Update the importLogId resolution to include ensembles:
      ```
      const importLogId = activeTab === 'ensembles'
        ? ensemblePreview?.importLogId
        : activeTab === 'conservatory'
          ? conservatoryPreview?.importLogId
          : previewData?.importLogId
      ```
    - Add ensemble results branch (BEFORE the conservatory branch):
      ```
      if (activeTab === 'ensembles') {
        setEnsembleResults(result)
        setImportState('results')
        const parts = []
        if (result.createdCount > 0) parts.push(`${result.createdCount} נוצרו`)
        if (result.updatedCount > 0) parts.push(`${result.updatedCount} עודכנו`)
        if (result.noChangeCount > 0) parts.push(`${result.noChangeCount} ללא שינוי`)
        toast.success(`ייבוא הרכבים הושלם: ${parts.join(', ')}`)
      } else if (activeTab === 'conservatory') {
        ...existing...
      }
      ```

12. **allPreviewRows guard (around line 783-789):**
    - Update the guard to exclude ensembles: `(activeTab !== 'conservatory' && activeTab !== 'ensembles' && previewData)`

13. **Preview State -- Ensembles (NEW BLOCK):**
    Add `{importState === 'preview' && activeTab === 'ensembles' && ensemblePreview && (` block BEFORE the existing conservatory preview block. This renders:

    a. **Summary stat cards (4 cards, grid cols-2 md:grid-cols-4)**:
       - Total ensembles: `ensemblePreview.preview.totalRows` in indigo gradient
       - New: `ensemblePreview.preview.orchestraMatchSummary.new` in blue gradient, label "הרכבים חדשים"
       - Updates: `ensemblePreview.preview.orchestraMatchSummary.existingUpdated` in green gradient, label "עדכונים"
       - No Change: `ensemblePreview.preview.orchestraMatchSummary.existingNoChange` in gray gradient, label "ללא שינוי"

    b. **Conductor summary cards (3 cards, grid cols-3)** -- only show if at least one is > 0:
       - Resolved: green gradient, count `ensemblePreview.preview.conductorSummary.resolved`, label "מנצחים שובצו"
       - Unresolved: red gradient, count `ensemblePreview.preview.conductorSummary.unresolved`, label "מנצחים לא נמצאו"
       - Ambiguous: amber gradient, count `ensemblePreview.preview.conductorSummary.ambiguous`, label "מנצחים מעורפלים"
       This follows the EXACT pattern of the teacher match summary in student preview (lines 1122-1153).

    c. **Warnings section** if `ensemblePreview.preview.warnings.length > 0`:
       - Reuse existing warning pattern (amber card with WarningIcon)
       - Each warning: `שורה ${w.row}: ${w.message}` (warnings have `{ row, field, message }` shape)
       - Show first 10 with "ועוד X אזהרות..." overflow

    d. **Ensemble preview table** in a `rounded-3xl shadow-sm bg-white overflow-hidden` card:
       - Header: "תצוגה מקדימה"
       - Table columns: "שורה" | "סטטוס" | "שם הרכב" | "סוג" | "מנצח" | "משתתפים" | "לוח זמנים" | "ש"ש דיווח"
       - Iterate `ensemblePreview.preview.ensembles.slice(0, 50)`:
         - Row number: `ens.row` in gray mono text
         - Status: `getOrchestraMatchBadge(ens.orchestraMatch)`
         - Name: `ens.name` in font-medium, and below it in text-xs text-gray-500: `ens.subType` (if exists) and `ens.performanceLevel` (if exists)
         - Type: `ens.type` as a small pill badge (purple for תזמורת, indigo for הרכב)
         - Conductor: `getConductorMatchBadge(ens.conductorMatch)`
         - Participants: `ens.participantCount`
         - Schedule: Render activity1 if exists: `${ens.schedule.activity1.dayOfWeek} ${ens.schedule.activity1.startTime}-${ens.schedule.activity1.endTime}`. If activity2 exists, add second line.
         - Reporting hours: `ens.hours.totalReporting`
       - If more than 50 rows, show "מוצגות 50 מתוך X שורות" footer

    e. **Action buttons**: Cancel (same pattern) and Execute button:
       - Disabled when `executing || ensemblePreview.preview.ensembles.length === 0`
       - Button text: `אשר ייבוא (${ensemblePreview.preview.ensembles.length} הרכבים)`
       - Executing text: "מבצע ייבוא..." with spinning icon

14. **Existing preview blocks -- add guards:**
    - The conservatory preview block already checks `activeTab === 'conservatory'` -- no change needed
    - The teacher/student preview block (line 1068) already checks `activeTab !== 'conservatory'` -- update to `activeTab !== 'conservatory' && activeTab !== 'ensembles'`

15. **Results State -- Ensembles (NEW BLOCK):**
    Add `{importState === 'results' && activeTab === 'ensembles' && ensembleResults && (` block BEFORE the existing conservatory results block. This renders:

    a. **Success header** in green gradient card with CheckCircleIcon + "ייבוא הרכבים הושלם"
    b. **Stats grid (5 columns)** following the existing teacher/student results pattern (lines 1284-1306):
       - Total: `ensembleResults.totalRows`, label "סה"כ שורות"
       - Created: `ensembleResults.createdCount` in blue, label "נוצרו"
       - Updated: `ensembleResults.updatedCount` in green, label "עודכנו"
       - No Change: `ensembleResults.noChangeCount` in gray, label "ללא שינוי"
       - Errors: `ensembleResults.errorCount` in red, label "שגיאות"

    c. **Skipped details** (if `ensembleResults.skipped.length > 0`):
       - Amber card with WarningIcon header "הרכבים שדולגו"
       - List each: `שורה ${s.row}: ${s.name} - ${s.reason}`

    d. **Error details** (if `ensembleResults.errors.length > 0`):
       - Red card following existing error pattern (lines 1309-1328)
       - List each: `שורה ${e.row}: ${e.name} - ${e.error}`

    e. **Reset button**: "ייבוא קובץ נוסף" (same as existing)

16. **Existing results blocks -- add guards:**
    - The conservatory results block already checks `activeTab === 'conservatory'` -- no change needed
    - The teacher/student results block (line 1276) checks `activeTab !== 'conservatory'` -- update to `activeTab !== 'conservatory' && activeTab !== 'ensembles'`

**IMPORTANT NOTES:**
- Do NOT create new files. All changes go into existing ImportData.tsx and apiService.js.
- The ensemble preview is row-based (like teacher/student) NOT field-diff-based (like conservatory), but uses its OWN rendering block because the data shape (ensembles[] with conductorMatch/orchestraMatch) is different from the matched/notFound/errors shape used by teacher/student.
- The `executeImport` method in apiService.js is already shared across all import types -- no changes needed to it.
- The existing `getTeacherMatchBadge` function is for STUDENT import's teacher matching. Create a SEPARATE `getConductorMatchBadge` for ensemble conductor matching (same visual pattern, different labels).
  </action>
  <verify>
Run TypeScript compilation check:
```bash
cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && npx tsc --noEmit 2>&1 | head -40
```
Verify no TypeScript errors in modified files. Check that `MusicNotesIcon` import resolves. Verify the `previewEnsembleImport` method exists in apiService.js. Grep for the 4 tab values in ImportTab type:
```bash
grep -n "ImportTab" /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/ImportData.tsx
```
  </verify>
  <done>
ImportData.tsx compiles without errors. Four tabs visible in the import page. The ensemble tab has its own file guide, row-level preview with conductor badges and orchestra match status, conductor/orchestra summary cards, warnings display, and results with created/updated/skipped/error counts. apiService.js has previewEnsembleImport method following the same pattern as existing preview methods.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify ensemble import UI flow</name>
  <what-built>
4th "הרכבים" tab on the import page with:
- MusicNotesIcon tab button
- Ensemble-specific file guide explaining Ministry ensemble Excel format
- Row-level preview table showing each ensemble's name, type, conductor match badge, orchestra match status, schedule, and reporting hours
- Conductor summary cards (resolved/unresolved/ambiguous)
- Orchestra match summary cards (new/updated/no-change)
- Warnings for unrecognized ensemble names
- Results display with created/updated/no-change/skipped/error counts
- Skipped and error detail sections
  </what-built>
  <how-to-verify>
1. Navigate to the import page in the frontend app
2. Verify 4 tabs appear: "ייבוא מורים", "ייבוא תלמידים", "פרטי קונסרבטוריון", "הרכבים"
3. Click the ensemble tab -- should show upload zone with ensemble file guide
4. Verify subtitle changes to "ייבוא הרכבים מקובץ Excel"
5. Upload a Ministry ensemble Excel file (.xlsx) containing the "הרכבי ביצוע" sheet
6. Verify preview appears with:
   a. Summary stat cards (total, new, updated, no-change)
   b. Conductor summary cards (resolved in green, unresolved in red, ambiguous in amber)
   c. Warnings section (if any unrecognized names exist)
   d. Table with ensemble rows showing name, type badge, conductor badge, participants, schedule, hours
7. Verify conductor badges: green for resolved (shows teacher name), red for unresolved, amber for ambiguous (shows candidate count)
8. Verify orchestra match badges: blue "הרכב חדש" for new, green "עדכון" for existing-updated, gray "ללא שינוי" for no-change
9. Click execute -- verify results screen shows created/updated/no-change/error counts
10. If there are skipped ensembles, verify they appear in an amber details section
11. Switch between all 4 tabs -- verify state resets properly (no stale data from other tabs)
12. Verify teacher, student, and conservatory import tabs still work correctly (no regressions)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- TypeScript compilation passes with no errors in ImportData.tsx
- Import page renders 4 tabs without console errors
- Ensemble tab upload calls POST /api/import/ensembles/preview
- Preview table renders all ensemble rows from backend response
- Conductor badges display correctly for resolved/unresolved/ambiguous/none
- Orchestra match badges display correctly for new/existing-updated/existing-no-change
- Execute calls POST /api/import/execute/:importLogId (existing endpoint)
- Results display createdCount, updatedCount, noChangeCount, skippedCount, errorCount
- Teacher, student, and conservatory tabs function identically to before (no regression)
</verification>

<success_criteria>
1. Import page has 4 functional tabs (teachers, students, conservatory, ensembles)
2. Ensemble upload produces a row-level preview with conductor match badges and orchestra match status
3. Conductor summary and orchestra match summary cards display correctly
4. Warnings for unrecognized ensemble names are visible in the preview
5. Execute produces results with created/updated/no-change/skipped/error breakdown
6. No TypeScript errors, no console errors, no regressions in existing tabs
</success_criteria>

<output>
After completion, create `.planning/phases/25-ensemble-import-frontend/25-01-SUMMARY.md`
</output>
