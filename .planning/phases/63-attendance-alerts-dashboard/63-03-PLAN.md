---
phase: 63-attendance-alerts-dashboard
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/RehearsalCalendar.tsx
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Rehearsals.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Past rehearsal cards are visually dimmed/faded compared to future rehearsals"
    - "Switching between month/week/day views animates smoothly with framer-motion"
    - "Month view shows heat map coloring on busy days (more rehearsals = stronger color intensity)"
    - "Rehearsal cards show minimal info: orchestra name + time + room (not overloaded)"
    - "Hover on cards produces a subtle lift effect, click has a scale micro-interaction"
    - "Clicking a rehearsal card opens a slide-in side panel with full details and edit/delete actions"
    - "Drag-and-drop snaps to 15-minute grid intervals in day view"
    - "Drop zone shows real-time red conflict highlight when a drop would create a scheduling conflict"
  artifacts:
    - path: "src/components/RehearsalCalendar.tsx"
      provides: "Animated calendar with framer-motion transitions, past dimming, heat map month view, 15-min snap"
    - path: "src/pages/Rehearsals.tsx"
      provides: "Side panel state management and rendering for rehearsal detail on click"
  key_links:
    - from: "src/components/RehearsalCalendar.tsx"
      to: "framer-motion"
      via: "AnimatePresence + motion.div for view transitions and card animations"
      pattern: "motion\\.div|AnimatePresence"
    - from: "src/pages/Rehearsals.tsx"
      to: "src/components/RehearsalCalendar.tsx"
      via: "onRehearsalClick callback opening side panel"
      pattern: "onRehearsalClick"
---

<objective>
Redesign the rehearsal calendar to feel interactive and alive (matching the room schedule page feel), with smooth animations, visual hierarchy between past/future events, heat map month view, slide-in side panel for details, and improved drag-and-drop with 15-minute snap grid and conflict feedback.

Purpose: The current calendar is flat and static. User feedback says it needs the interactive feel of the room schedule page with animations, micro-interactions, and better visual hierarchy.
Output: Animated RehearsalCalendar.tsx with framer-motion, side panel integration in Rehearsals.tsx
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-attendance-alerts-dashboard/63-CONTEXT.md

@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/RehearsalCalendar.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Rehearsals.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/RoomSchedule.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/utils/rehearsalUtils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add framer-motion animations, past/future dimming, and heat map month view</name>
  <files>
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/RehearsalCalendar.tsx
  </files>
  <action>
Modify RehearsalCalendar.tsx to add visual polish and animations. framer-motion is already installed.

1. **View transition animations:** Wrap the view content (MonthView/WeekView/DayView) in `AnimatePresence mode="wait"`. Each view gets a `motion.div` wrapper with `initial={{ opacity: 0, x: direction * 30 }}`, `animate={{ opacity: 1, x: 0 }}`, `exit={{ opacity: 0, x: -direction * 30 }}` where `direction` is +1 for next, -1 for previous navigation. Use a `key` based on `${viewMode}-${currentDate.toISOString()}` so navigation and view switches both animate. Transition duration 0.25s with easeInOut.

2. **Past rehearsal dimming:** In `RehearsalCard`, the existing `isPast` variable is already computed. Add `opacity-50` class when `isPast` is true. For the compact/minimal variants too. This makes past rehearsals visually recede.

3. **Card micro-interactions:** Wrap each `RehearsalCard` outer div with framer-motion. Add `whileHover={{ y: -2, boxShadow: '0 4px 12px rgba(0,0,0,0.15)' }}` and `whileTap={{ scale: 0.97 }}` for hover lift and click feedback. Keep `transition={{ duration: 0.15 }}`.

4. **Month view heat map coloring:** In `MonthView`, compute a density score per day: `const density = day.rehearsals.length`. Apply background color intensity based on density:
   - 0 rehearsals: no extra bg (default white/gray-50)
   - 1 rehearsal: `bg-blue-50`
   - 2 rehearsals: `bg-blue-100`
   - 3+ rehearsals: `bg-blue-200`
   This gives a heat map feel where busy days are visually distinct.

5. **15-minute snap grid in day view:** Update `handleDropOnHourSlot` to calculate the drop position relative to the hour slot. Use `e.nativeEvent.offsetY` divided by `HOUR_HEIGHT` to determine the fractional hour, then snap to nearest 15-minute interval: `Math.round((offsetFraction * 60) / 15) * 15`. Update the new start time accordingly instead of always snapping to the hour boundary.

6. **Conflict drop feedback:** Add state `const [dropConflict, setDropConflict] = useState<string | null>(null)`. In `handleDragOver`, check if dropping the dragged rehearsal at the target slot would create a conflict by checking `conflictMap` or computing overlap with rehearsals already in that slot. If conflict, set `dropConflict` to the slot key and apply `bg-red-100 border-red-400` instead of `bg-primary/10`. In `handleDragLeave` and after drop, clear `dropConflict`.

IMPORTANT: Keep the existing card content structure (orchestra name + time + room) which already matches the "minimal info" requirement from the user. Do NOT add more fields to cards.
  </action>
  <verify>
    cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && npx tsc --noEmit --skipLibCheck 2>&1 | head -30
  </verify>
  <done>
    Calendar has framer-motion view transitions, past rehearsals are dimmed, cards have hover/tap micro-interactions, month view shows heat map density coloring, day view drag snaps to 15-min intervals, and conflict zones show red feedback on drag-over.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add slide-in side panel for rehearsal details on card click</name>
  <files>
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Rehearsals.tsx
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/RehearsalCalendar.tsx
  </files>
  <action>
Add a slide-in side panel to the Rehearsals page that opens when a rehearsal card is clicked (replacing the current navigate-to-detail behavior).

1. **In Rehearsals.tsx:** Add state `const [sidePanelRehearsal, setSidePanelRehearsal] = useState<Rehearsal | null>(null)`. Wire `onRehearsalClick` prop on `RehearsalCalendar` to `setSidePanelRehearsal` instead of navigating.

2. **Side panel component (inline in Rehearsals.tsx):** Render a `motion.div` fixed to the left side (RTL layout) of the screen:
   - Container: `fixed top-0 left-0 h-full w-[400px] bg-white shadow-2xl z-50 overflow-y-auto`
   - Animation: `initial={{ x: '-100%' }}`, `animate={{ x: 0 }}`, `exit={{ x: '-100%' }}`, transition 0.3s ease
   - Backdrop: semi-transparent overlay `fixed inset-0 bg-black/30 z-40` that closes panel on click
   - Content shows: orchestra name (large), date formatted nicely, time range, location with MapPin icon, conductor name, type badge (orchestra/ensemble), attendance summary if past rehearsal (using calculateAttendanceStats)
   - Action buttons at bottom: "צפה בפרטים" (navigate to detail), "ערוך" (opens edit form), "מחק" (opens delete confirmation), "סמן נוכחות" (opens attendance manager — only for past rehearsals)
   - Close button (X) at top-right of panel

3. **Wrap side panel in AnimatePresence** so it animates in/out smoothly.

4. Keep existing `onNavigateToRehearsal`, `onViewDetails`, `onEditRehearsal`, `onDeleteRehearsal` handlers in Rehearsals.tsx for the action buttons inside the side panel. The side panel just adds an intermediary view before those actions.

NOTE: The side panel replaces the modal-based interaction. When user clicks a card, they see the side panel with details + action buttons. This matches the user's request for "click card opens a slide-in side panel with full details and actions".
  </action>
  <verify>
    cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && npx tsc --noEmit --skipLibCheck 2>&1 | head -30
  </verify>
  <done>
    Clicking a rehearsal card in any calendar view opens a slide-in side panel from the left showing full rehearsal details with edit/delete/attendance action buttons. Panel animates in/out with framer-motion. Backdrop click or X button closes panel.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors
2. Calendar view transitions animate on month/week/day switch and on prev/next navigation
3. Past rehearsal cards appear dimmed (opacity-50)
4. Month view days show blue heat map intensity based on rehearsal count
5. Card hover produces slight lift, card tap produces scale feedback
6. Clicking a card opens slide-in side panel with details and actions
7. Day view drag-and-drop snaps to 15-minute intervals
8. Dragging over a conflicting time slot shows red warning instead of blue highlight
</verification>

<success_criteria>
Calendar feels interactive and alive with smooth animations. Past/future visual hierarchy is clear. Side panel provides quick access to rehearsal actions without navigating away. Drag-and-drop is precise with 15-min snap and conflict feedback.
</success_criteria>

<output>
After completion, create `.planning/phases/63-attendance-alerts-dashboard/63-03-SUMMARY.md`
</output>
