---
phase: 63-attendance-alerts-dashboard
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/AttendanceDashboard.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Monthly trends displayed as a proper Recharts line/area chart with hover tooltips"
    - "Summary cards have icons with colored backgrounds, better typography, and visual weight"
    - "Per-orchestra table has sorting capability (click column headers to sort)"
    - "Flagged students have action buttons: View Profile and Dismiss Flag"
    - "Dismiss Flag is client-side only (temporary, resets on page reload) until backend endpoint exists"
    - "Quick filter buttons for time range: This Week, This Month, This Semester"
    - "Recent alerts section shows newly flagged students and orchestras dropping below 70%"
    - "Clicking a flagged student expands an inline drill-down showing their full attendance history"
  artifacts:
    - path: "src/pages/AttendanceDashboard.tsx"
      provides: "Polished dashboard with Recharts, sorting, filtering, actions, and drill-down"
  key_links:
    - from: "src/pages/AttendanceDashboard.tsx"
      to: "recharts"
      via: "AreaChart or LineChart for monthly trends"
      pattern: "AreaChart|LineChart|ResponsiveContainer"
    - from: "src/pages/AttendanceDashboard.tsx"
      to: "apiService.attendanceAlerts"
      via: "getDashboard and getStudentSummary API calls"
      pattern: "attendanceAlerts\\."
---

<objective>
Polish the attendance dashboard from basic placeholder quality to a production-ready analytics page with proper Recharts visualization, sortable tables, time range filtering, flagged student actions with inline drill-down, and recent alerts section.

Purpose: Current dashboard is too basic with Tailwind bar placeholders for charts, no sorting, no filtering, no drill-down, and missing alert notifications. User feedback says it needs significant polish.
Output: Production-quality AttendanceDashboard.tsx with all improvements
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-attendance-alerts-dashboard/63-CONTEXT.md

@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/AttendanceDashboard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace trend bars with Recharts, polish summary cards, and add sortable orchestra table</name>
  <files>
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/AttendanceDashboard.tsx
  </files>
  <action>
Upgrade the dashboard visuals. Recharts is already installed (`recharts: ^2.15.0` in package.json).

1. **Recharts monthly trends:** Replace the Tailwind bar chart section with a proper Recharts `ResponsiveContainer` + `AreaChart`. Import from recharts: `ResponsiveContainer, AreaChart, Area, XAxis, YAxis, CartesianGrid, Tooltip`. Data is the `monthlyTrends` array with `month` (label) and `attendanceRate` fields. Configure:
   - `ResponsiveContainer width="100%" height={300}`
   - `AreaChart data={monthlyTrends}` with `dir="ltr"` on the container div (charts render LTR even in RTL pages)
   - `XAxis dataKey="label"` with `tick={{ fontSize: 12 }}`
   - `YAxis domain={[0, 100]}` with `tick={{ fontSize: 12 }}` and `unit="%"`
   - `Tooltip` with custom formatter showing `${value}%`
   - `Area type="monotone" dataKey="attendanceRate"` with `stroke="#3b82f6" fill="#93c5fd" fillOpacity={0.3}`
   - `CartesianGrid strokeDasharray="3 3"`
   Add a gradient fill for visual polish: define a `<defs><linearGradient>` from blue-400 opacity 0.4 to blue-400 opacity 0.05.

2. **Summary cards polish:** Enhance the 4 summary cards:
   - Add subtle gradient backgrounds instead of plain white: e.g., `bg-gradient-to-br from-white to-blue-50` for orchestras card
   - Increase icon container size: `p-3 rounded-xl` instead of `p-2 rounded-lg`
   - Add a subtle bottom border accent in the card's theme color: `border-b-4 border-blue-500` for orchestras, `border-indigo-500` for students, color-based for rate, `border-red-500` for flagged
   - Value text: `text-4xl font-extrabold` instead of `text-3xl font-bold`
   - Label text: `text-sm font-medium text-gray-600` (add font-medium)
   - For attendance rate card, add a small trend indicator: up/down arrow if previous month data is available (compare last two monthlyTrends entries). If no comparison data, skip.

3. **Sortable orchestra table:** Add `sortField` and `sortDirection` state. Columns: orchestraName, conductorName, totalRehearsals, attendanceRate, flaggedCount. Clicking a column header toggles sort direction. Add a small CaretUp/CaretDown icon next to the active sort column header. Default sort: attendanceRate ascending (worst first, as currently). Apply sorting in the render instead of the hardcoded sort. Make column headers `cursor-pointer hover:bg-gray-100 select-none` with the sort icon.
  </action>
  <verify>
    cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && npx tsc --noEmit --skipLibCheck 2>&1 | head -30
  </verify>
  <done>
    Monthly trends show as Recharts area chart with hover tooltips. Summary cards have gradient backgrounds, colored bottom borders, and larger typography. Orchestra table headers are clickable for sorting with direction indicators.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add time range filtering and recent alerts section</name>
  <files>
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/AttendanceDashboard.tsx
  </files>
  <action>
Add filtering and alerts to the dashboard.

1. **Time range quick filters:** Add a filter bar below the page title with preset buttons: "השבוע" (this week), "החודש" (this month), "הסמסטר" (this semester). Add state `dateRange: 'week' | 'month' | 'semester'` defaulting to `'month'`. Style active button with `bg-primary text-white` and inactive with `bg-gray-100 text-gray-700 hover:bg-gray-200`. When filter changes, pass `startDate` and `endDate` query params to the `getDashboard` API call. Compute dates:
   - week: today minus 7 days
   - month: first day of current month
   - semester: September 1 of current school year (if current month >= September, use current year; else previous year)
   Note: The backend `getDashboard` endpoint may not support date filtering yet. If the API doesn't accept these params, still add the UI buttons but apply client-side filtering on `monthlyTrends` by filtering the array to only show trends within the selected range. The per-orchestra and flagged data will remain unfiltered (acceptable for now).

2. **Recent alerts section:** Add a new card between summary cards and orchestra table. Title: "התראות אחרונות". Show:
   - Count of newly flagged students (from `flaggedStudents` where flag value is at or just over threshold — compute by checking `flag.value === flag.threshold` or `flag.value === flag.threshold + 1` for consecutive, or `Math.abs(flag.value - flag.threshold) < 5` for rate-based)
   - If any orchestra has attendance rate below 70%, show it as an alert: "תזמורת {name} ירדה מתחת ל-70% נוכחות"
   - Style each alert as a small card with WarningIcon, colored left border (red for critical, amber for warning)
   - If no alerts, show "אין התראות חדשות" with a green CheckCircle
  </action>
  <verify>
    cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && npx tsc --noEmit --skipLibCheck 2>&1 | head -30
  </verify>
  <done>
    Dashboard has time range filter buttons (week/month/semester) that filter the trends view. Recent alerts section highlights newly flagged students and low-attendance orchestras with warning styling.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add flagged student actions with inline drill-down</name>
  <files>
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/AttendanceDashboard.tsx
  </files>
  <action>
Add action buttons and drill-down capability to the flagged students table.

1. **Flagged student action buttons:** In the flagged students table, add a new column "פעולות" with two buttons per row:
   - "צפה בפרופיל" (View Profile): blue text button with UserIcon, navigates to `/students/${student.studentId}`
   - "בטל סימון" (Dismiss Flag): gray text button with XCircleIcon. On click, remove the student from the local `flaggedStudents` array state as a CLIENT-SIDE ONLY dismissal. This is intentionally temporary — the dismissal resets on page reload. A persistent dismiss would require a backend endpoint (future work). Add an inline confirmation before dismissing: replace the row buttons briefly with "בטוח?" + "כן" (confirm) / "לא" (cancel). On confirm, filter the student out of the local state.

   NOTE ON DISMISS PERSISTENCE: This dismiss is explicitly client-side temporary. Do NOT attempt to call a backend endpoint — none exists. Document this in a code comment: `// TODO: Persist dismiss via backend endpoint when available. Currently client-side only (resets on reload).`

2. **Inline drill-down for flagged students:** Add `expandedStudentId` state. When a flagged student row is clicked (not on the action buttons — use `e.stopPropagation()` on action buttons), toggle expansion. Below the clicked row, render an expanded section with:
   - Fetch `apiService.attendanceAlerts.getStudentSummary(studentId)` on expand
   - Show attendance rate as a colored progress bar (green >80%, amber 60-80%, red <60%), consecutive absences count, and the `recentHistory` as a list of colored status dots with dates
   - Use `AnimatePresence` + `motion.div` for smooth expand/collapse animation
   - Show a loading spinner while fetching
   - Cache results in a `Map` ref so re-expanding doesn't re-fetch
  </action>
  <verify>
    cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && npx tsc --noEmit --skipLibCheck 2>&1 | head -30
  </verify>
  <done>
    Flagged students have View Profile (navigates to student page) and Dismiss Flag (client-side removal with inline confirmation) action buttons. Clicking a flagged student row expands inline drill-down with attendance rate progress bar, consecutive absences, and color-coded history dots. Drill-down results are cached.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors
2. Monthly trend chart renders as Recharts AreaChart with tooltips on hover
3. Summary cards have gradient backgrounds, colored borders, larger numbers
4. Orchestra table columns are sortable by clicking headers
5. Time range buttons filter the view (week/month/semester)
6. Recent alerts section shows warnings for new flags and low-attendance orchestras
7. Flagged students have View Profile and Dismiss action buttons
8. Dismiss Flag removes student from list (client-side, resets on reload)
9. Clicking a flagged student row expands drill-down with attendance details
</verification>

<success_criteria>
Dashboard looks production-quality with proper data visualization, interactive tables, actionable flagged student list, and at-a-glance alerts. No more placeholder Tailwind bars. Dismiss flag works as temporary client-side action.
</success_criteria>

<output>
After completion, create `.planning/phases/63-attendance-alerts-dashboard/63-05-SUMMARY.md`
</output>
