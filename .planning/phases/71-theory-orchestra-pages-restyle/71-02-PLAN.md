---
phase: 71-theory-orchestra-pages-restyle
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Orchestras.tsx
  - src/components/OrchestraCard.tsx
autonomous: true

must_haves:
  truths:
    - "Orchestras page shows 4 GlassStatCard stat boxes with green-blue gradient matching Students/Teachers"
    - "Orchestras page uses GlassSelect for all filter dropdowns (type, conductor, location, status)"
    - "Orchestras page uses SearchInput for search field"
    - "Orchestras page buttons use HeroUI Button with consistent color/variant patterns"
    - "Orchestra data cards have clean visual styling matching analytics KPI card aesthetic"
  artifacts:
    - path: "src/pages/Orchestras.tsx"
      provides: "Restyled Orchestras page with GlassStatCard, GlassSelect, HeroButton"
      contains: "GlassStatCard"
    - path: "src/components/OrchestraCard.tsx"
      provides: "Restyled orchestra card with consistent shadows/typography"
      contains: "hover:shadow-lg"
  key_links:
    - from: "src/pages/Orchestras.tsx"
      to: "src/components/ui/GlassStatCard.tsx"
      via: "import"
      pattern: "import.*GlassStatCard"
    - from: "src/pages/Orchestras.tsx"
      to: "src/components/ui/GlassSelect.tsx"
      via: "import"
      pattern: "import.*GlassSelect"
---

<objective>
Restyle the Orchestras page header area (add stat cards, replace filter dropdowns, update buttons) and the OrchestraCard component to match the Students/Teachers page visual pattern.

Purpose: Visual consistency across all entity pages using the established restyle pattern from Phase 70.
Output: Updated Orchestras.tsx and OrchestraCard.tsx with consistent design system components.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/71-theory-orchestra-pages-restyle/71-RESEARCH.md

Reference pages (read these to understand the established pattern):
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Teachers.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Orchestras.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/OrchestraCard.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/ui/GlassStatCard.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/ui/GlassSelect.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle Orchestras page header, add stat cards, replace filters, and update buttons</name>
  <files>src/pages/Orchestras.tsx</files>
  <action>
Read the current Orchestras.tsx and the Teachers.tsx reference page.

**Import changes:**
- Add: `import { GlassStatCard } from '../components/ui/GlassStatCard'`
- Add: `import { GlassSelect } from '../components/ui/GlassSelect'`
- Add: `import { Button as HeroButton } from '@heroui/react'`
- (SearchInput is already imported — verify and keep)

**Page header (h1):**
- Restyle the title from current styling to `text-2xl font-extrabold text-slate-900 dark:text-white`
- Add subtitle `text-sm text-slate-500 dark:text-slate-400 mt-0.5` with appropriate text (e.g., "ניהול הרכבים ותזמורות")

**Stat cards section (NEW — add after header, before view toggle):**
The Orchestras page currently has a `stats` object computed but no stat card display. Add a GlassStatCard row visible in ALL view modes (dashboard, grid, table):
```tsx
<div className="grid grid-cols-2 lg:grid-cols-4 gap-3">
  {[
    { value: stats.totalOrchestras, label: 'סה״כ הרכבים' },
    { value: stats.activeOrchestras, label: 'הרכבים פעילים' },
    { value: stats.totalMembers, label: 'סה״כ חברים' },
    { value: stats.orchestrasReady, label: 'מוכנות מלאה' },
  ].map((s) => (
    <GlassStatCard key={s.label} value={s.value} label={s.label} size="sm" />
  ))}
</div>
```
Verify the actual `stats` object property names by reading the file — use whatever stats are already computed. If only some stats exist, pick the 4 most useful ones.

**Filter bar (replace raw selects with GlassSelect):**
The filters section currently uses raw `<select>` elements. Replace each:

1. Type filter → `<GlassSelect value={typeFilter || '__all__'} onValueChange={(v) => setTypeFilter(v === '__all__' ? '' : v)} placeholder="כל הסוגים" options={[{ value: '__all__', label: 'כל הסוגים' }, ...VALID_ORCHESTRA_TYPES.map(t => ({ value: t, label: t }))]} />`

2. Conductor filter → `<GlassSelect value={conductorFilter || '__all__'} onValueChange={(v) => setConductorFilter(v === '__all__' ? '' : v)} placeholder="כל המנצחים" options={[{ value: '__all__', label: 'כל המנצחים' }, ...teachers.map(t => ({ value: t._id, label: t.displayName || `${t.personalInfo?.firstName} ${t.personalInfo?.lastName}` }))]} />`

3. Location filter → `<GlassSelect value={locationFilter || '__all__'} onValueChange={(v) => setLocationFilter(v === '__all__' ? '' : v)} placeholder="כל המיקומים" options={[{ value: '__all__', label: 'כל המיקומים' }, ...VALID_LOCATIONS.map(l => ({ value: l, label: l }))]} />`

4. "Active only" checkbox → Convert to `<GlassSelect value={activeFilter || '__all__'} onValueChange={(v) => setActiveFilter(v === '__all__' ? '' : v)} placeholder="כל הסטטוסים" options={[{ value: '__all__', label: 'כל הסטטוסים' }, { value: 'active', label: 'פעילים בלבד' }, { value: 'inactive', label: 'לא פעילים' }]} />`
   - Update the filter logic accordingly: instead of boolean `activeOnly`, use `activeFilter === 'active'` for filtering, and `activeFilter === 'inactive'` for the inverse

Use actual state variable names from the file — the names above are illustrative. Read the file first to get the exact state variables and filter handler patterns.

Use the `flex items-center gap-3 flex-wrap px-1` container pattern matching Teachers page.

**Action buttons:**
- "New Orchestra" / "הרכב חדש": `<HeroButton color="primary" variant="solid" size="sm" onPress={...} startContent={<PlusIcon size={14} weight="bold" />} className="font-bold">הרכב חדש</HeroButton>`
- "Ensemble Summary" / "סיכום הרכבים": `<HeroButton color="default" variant="bordered" size="sm" onPress={...} startContent={<TableIcon size={14} />}>סיכום הרכבים</HeroButton>`

**View mode toggle:**
- Keep the existing 3-mode toggle (dashboard/grid/table) — do NOT convert to HeroButton
- Optionally restyle toggle buttons with consistent slate/primary palette if they use gray-* classes

**Result count display:**
Add `<span className="text-xs font-medium text-slate-400">` showing filtered count in the filter bar area.

**IMPORTANT:**
- Do NOT modify the OrchestraManagementDashboard component or its rendering
- Do NOT modify the table view logic
- Only restyle the header/stat cards/filters/buttons section of the page
- Make stat cards visible in ALL view modes (not conditional on grid/table)
  </action>
  <verify>
- `grep -c "GlassStatCard" src/pages/Orchestras.tsx` returns at least 1
- `grep -c "GlassSelect" src/pages/Orchestras.tsx` returns at least 1
- `grep -c "HeroButton" src/pages/Orchestras.tsx` returns at least 1
- No raw `<select` elements remain in the filter section (may still exist in other parts — check context)
- No TypeScript errors
  </verify>
  <done>Orchestras page has GlassStatCard row (4 cards, always visible), GlassSelect for all filters, SearchInput for search, and HeroButton for action buttons — visually matching Students/Teachers pages.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle OrchestraCard to match clean card aesthetic</name>
  <files>src/components/OrchestraCard.tsx</files>
  <action>
Read the current OrchestraCard.tsx to understand its structure (247 lines per research).

**Card container restyle:**
- Change from `bg-white rounded border border-gray-200 shadow-sm hover:shadow-md` (or similar) to:
  `bg-white dark:bg-neutral-900 rounded-xl border border-neutral-200 dark:border-neutral-700 shadow-sm hover:shadow-lg transition-all duration-200 hover:scale-[1.02] hover:-translate-y-1`

**Typography updates:**
- Replace all `text-gray-*` with `text-slate-*` equivalents (gray-900→slate-900, gray-700→slate-700, gray-600→slate-600, gray-500→slate-500, gray-400→slate-400)
- Replace all `border-gray-*` with `border-neutral-*` equivalents
- Replace all `bg-gray-*` with `bg-neutral-*` equivalents (for section backgrounds within the card)
- Ensure headings use `font-semibold` or `font-bold` consistently

**Spacing:**
- Ensure consistent internal padding (p-4 throughout)
- Consistent gap between content sections

**Action buttons within card:**
- Replace raw `<button>` elements for edit/delete with HeroButton:
  - Edit: `<HeroButton color="default" variant="light" size="sm" isIconOnly>`
  - Delete: `<HeroButton color="danger" variant="light" size="sm" isIconOnly>`
- Add `import { Button as HeroButton } from '@heroui/react'` if using HeroButton

**Badge styling:**
- Keep existing type/subType/status badges but ensure they use consistent styling
- Replace any `bg-gray-100 text-gray-800` badge patterns with `bg-neutral-100 text-neutral-800`

**Group hover effects:**
- Keep existing group hover logic for edit/delete buttons but update colors from gray to neutral/slate

**IMPORTANT:** Keep ALL existing data display fields (location, members, rehearsals, schedule, ministry hours, instruments, attendance), click handlers, navigation, and conditional rendering logic intact. Only change visual styling classes and button components.
  </action>
  <verify>
- `grep -c "text-gray-" src/components/OrchestraCard.tsx` returns 0 (all migrated to slate)
- `grep -c "border-gray-" src/components/OrchestraCard.tsx` returns 0 (all migrated to neutral)
- `grep "hover:shadow-lg" src/components/OrchestraCard.tsx` returns a match
- `grep "rounded-xl" src/components/OrchestraCard.tsx` returns a match
- No TypeScript errors in the file
  </verify>
  <done>OrchestraCard has clean card aesthetic — rounded-xl corners, subtle shadow with hover elevation, slate typography, neutral borders, consistent padding. All data and handlers preserved.</done>
</task>

</tasks>

<verification>
1. Orchestras page opens without errors in browser
2. 4 GlassStatCard boxes visible at top in ALL view modes (dashboard, grid, table)
3. Filters use GlassSelect with glass morphism styling (no raw select elements)
4. SearchInput still works for orchestra search
5. Action buttons use HeroUI Button styling
6. Orchestra cards in grid view show clean elevated hover effect
7. Dashboard view mode still renders OrchestraManagementDashboard without issues
8. Table view mode still works without issues
</verification>

<success_criteria>
Orchestras page header area (stat cards, filters, search, buttons) is visually indistinguishable from the Students/Teachers pages. OrchestraCard has clean, modern card styling with consistent shadows, typography, and hover effects. Dashboard view mode is unaffected.
</success_criteria>

<output>
After completion, create `.planning/phases/71-theory-orchestra-pages-restyle/71-02-SUMMARY.md`
</output>
