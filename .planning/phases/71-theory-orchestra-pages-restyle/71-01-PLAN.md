---
phase: 71-theory-orchestra-pages-restyle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/TheoryLessons.tsx
  - src/components/TheoryLessonCard.tsx
autonomous: true

must_haves:
  truths:
    - "Theory Lessons page shows 4 GlassStatCard stat boxes with green-blue gradient matching Students/Teachers"
    - "Theory Lessons page uses GlassSelect for category filter dropdown"
    - "Theory Lessons page uses SearchInput for search field"
    - "Theory Lessons page buttons use HeroUI Button with consistent color/variant patterns"
    - "Theory Lesson data cards have clean visual styling matching analytics KPI card aesthetic"
  artifacts:
    - path: "src/pages/TheoryLessons.tsx"
      provides: "Restyled Theory Lessons page with GlassStatCard, GlassSelect, SearchInput, HeroButton"
      contains: "GlassStatCard"
    - path: "src/components/TheoryLessonCard.tsx"
      provides: "Restyled theory lesson card with consistent shadows/typography"
      contains: "hover:shadow-lg"
  key_links:
    - from: "src/pages/TheoryLessons.tsx"
      to: "src/components/ui/GlassStatCard.tsx"
      via: "import"
      pattern: "import.*GlassStatCard"
    - from: "src/pages/TheoryLessons.tsx"
      to: "src/components/ui/GlassSelect.tsx"
      via: "import"
      pattern: "import.*GlassSelect"
---

<objective>
Restyle the Theory Lessons page header area (stat cards, filters, search, buttons) and the TheoryLessonCard component to match the Students/Teachers page visual pattern.

Purpose: Visual consistency across all entity pages using the established restyle pattern from Phase 70.
Output: Updated TheoryLessons.tsx and TheoryLessonCard.tsx with consistent design system components.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/71-theory-orchestra-pages-restyle/71-RESEARCH.md

Reference pages (read these to understand the established pattern):
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Teachers.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/TheoryLessons.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/TheoryLessonCard.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/ui/GlassStatCard.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/ui/GlassSelect.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/ui/SearchInput.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle Theory Lessons page header, stat cards, filters, and buttons</name>
  <files>src/pages/TheoryLessons.tsx</files>
  <action>
Read the current TheoryLessons.tsx and the Teachers.tsx reference page to understand both the current state and the target pattern.

**Import changes:**
- Remove: `import StatsCard from '../components/ui/StatsCard'` (or similar old stat card import)
- Remove: `import { Card } from '../components/ui/Card'` if only used for filter wrapper
- Add: `import { GlassStatCard } from '../components/ui/GlassStatCard'`
- Add: `import { GlassSelect } from '../components/ui/GlassSelect'`
- Add: `import { SearchInput } from '../components/ui/SearchInput'` (if not already imported)
- Add: `import { Button as HeroButton } from '@heroui/react'`

**Page header (h1):**
- Change from `text-3xl font-bold text-gray-900` (or similar) to `text-2xl font-extrabold text-slate-900 dark:text-white`
- Add subtitle with `text-sm text-slate-500 dark:text-slate-400 mt-0.5`

**Stat cards section:**
Replace the 4 old `StatsCard` instances with `GlassStatCard` using this exact pattern:
```tsx
<div className="grid grid-cols-2 lg:grid-cols-4 gap-3">
  {[
    { value: stats.totalLessons, label: 'סה״כ שיעורים' },
    { value: stats.upcomingLessons, label: 'שיעורים קרובים' },
    { value: stats.totalStudents, label: 'סה״כ תלמידים' },
    { value: `${stats.averageAttendance}%`, label: 'נוכחות ממוצעת' },
  ].map((s) => (
    <GlassStatCard key={s.label} value={s.value} label={s.label} size="sm" />
  ))}
</div>
```
Use the actual Hebrew labels from the existing page (verify by reading the file). Keep the same stat values.

**Filter bar:**
- Remove the Card wrapper around the filter section
- Use `<div className="flex items-center gap-3 flex-wrap px-1">` as the container
- Replace raw `<input type="text">` search with `<SearchInput>` component: `value={searchQuery}`, `onChange={(e) => setSearchQuery(e.target.value)}`, `onClear={() => setSearchQuery('')}`, `placeholder="חיפוש שיעורים..."`
- Replace raw `<select>` for category filter with `<GlassSelect>`:
  - `value={categoryFilter || '__all__'}`
  - `onValueChange={(v) => setCategoryFilter(v === '__all__' ? '' : v)}`
  - `placeholder="כל הקטגוריות"` (or existing Hebrew)
  - `options={[{ value: '__all__', label: 'כל הקטגוריות' }, ...categoryOptions]}` (map existing categories)
- Keep date input as-is but style consistently with `className="h-9 rounded-md border border-neutral-200 bg-white/70 backdrop-blur-sm px-3 text-sm"` or similar to visually match GlassSelect height
- Replace raw "clear filters" button with `<HeroButton color="default" variant="flat" size="sm">`

**Action buttons:**
- "New Lesson" / "שיעור חדש": `<HeroButton color="primary" variant="solid" size="sm" onPress={...} startContent={<PlusIcon size={14} weight="bold" />} className="font-bold">שיעור חדש</HeroButton>`
- "Delete Lessons" (if present): `<HeroButton color="danger" variant="flat" size="sm" ...>`
- "Bulk Edit" (if present): `<HeroButton color="primary" variant="flat" size="sm" ...>`
- Tab toggle buttons (lessons/bulk tabs): Keep as styled buttons, NOT HeroButtons. Use consistent slate/primary palette.

**Result count display:**
Add `<span className="text-xs font-medium text-slate-400">` showing filtered count, matching Teachers pattern.

**IMPORTANT:** Do NOT modify the data display section (card grid, date grouping), pagination logic, or the BulkTheoryUpdateTab component. Only restyle the header area.
  </action>
  <verify>
- `grep -c "GlassStatCard" src/pages/TheoryLessons.tsx` returns at least 1
- `grep -c "GlassSelect" src/pages/TheoryLessons.tsx` returns at least 1
- `grep -c "SearchInput" src/pages/TheoryLessons.tsx` returns at least 1
- `grep -c "HeroButton" src/pages/TheoryLessons.tsx` returns at least 1
- `grep -c "StatsCard" src/pages/TheoryLessons.tsx` returns 0 (old component removed)
- No TypeScript errors: `npx tsc --noEmit --project tsconfig.json 2>&1 | grep -c "error"` returns 0 (or run the project's type check command)
  </verify>
  <done>Theory Lessons page header area uses GlassStatCard (4 cards), GlassSelect (category filter), SearchInput, and HeroButton — visually matching the Students/Teachers pages. Old StatsCard import removed.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle TheoryLessonCard to match clean card aesthetic</name>
  <files>src/components/TheoryLessonCard.tsx</files>
  <action>
Read the current TheoryLessonCard.tsx to understand its structure.

**Card container restyle:**
- Change from `bg-white rounded border shadow-sm hover:shadow-md` (or similar) to:
  `bg-white dark:bg-neutral-900 rounded-xl border border-neutral-200 dark:border-neutral-700 shadow-sm hover:shadow-lg transition-all duration-200 hover:scale-[1.02] hover:-translate-y-1`
- This matches the Teachers grid card hover effect pattern

**Typography updates:**
- Replace all `text-gray-*` with `text-slate-*` equivalents (gray-900→slate-900, gray-600→slate-600, gray-500→slate-500, gray-400→slate-400)
- Ensure headings use `font-semibold` or `font-bold` consistently

**Spacing:**
- Ensure consistent internal padding (p-4 throughout)
- Consistent gap between sections

**Action buttons within card:**
- Replace raw `<button>` elements with `<HeroButton>` or at minimum use consistent button styling:
  - View: `<HeroButton color="primary" variant="light" size="sm" isIconOnly>`
  - Edit: `<HeroButton color="default" variant="light" size="sm" isIconOnly>`
  - Delete: `<HeroButton color="danger" variant="light" size="sm" isIconOnly>`
  - Attendance: `<HeroButton color="success" variant="light" size="sm" isIconOnly>`
- If HeroButton `isIconOnly` doesn't match the current icon button layout, use `variant="light"` with `startContent` and no children text instead
- Add `import { Button as HeroButton } from '@heroui/react'` if using HeroButton in card

**Badge styling:**
- Keep existing category/status badges but ensure they use `text-xs font-medium` consistently
- Use semantic colors: status badges should use success/warning/danger palette

**IMPORTANT:** Keep ALL existing data display fields, click handlers, navigation, and conditional rendering logic intact. Only change visual styling classes and button components.
  </action>
  <verify>
- `grep -c "text-gray-" src/components/TheoryLessonCard.tsx` returns 0 (all migrated to slate)
- `grep "hover:shadow-lg" src/components/TheoryLessonCard.tsx` returns a match
- `grep "rounded-xl" src/components/TheoryLessonCard.tsx` returns a match
- No TypeScript errors in the file
  </verify>
  <done>TheoryLessonCard has clean card aesthetic — rounded-xl corners, subtle shadow with hover elevation, slate typography, consistent padding. All data and handlers preserved.</done>
</task>

</tasks>

<verification>
1. Theory Lessons page opens without errors in browser
2. 4 GlassStatCard boxes visible at top with green-blue gradient
3. Search uses SearchInput component (with clear button functionality)
4. Category dropdown uses GlassSelect with glass morphism styling
5. Action buttons use HeroUI Button styling
6. Theory lesson cards show clean elevated hover effect
7. No raw `<select>` or `StatsCard` references remain
</verification>

<success_criteria>
Theory Lessons page header area (stat cards, filters, search, buttons) is visually indistinguishable from the Students/Teachers pages. TheoryLessonCard has clean, modern card styling with consistent shadows, typography, and hover effects.
</success_criteria>

<output>
After completion, create `.planning/phases/71-theory-orchestra-pages-restyle/71-01-SUMMARY.md`
</output>
