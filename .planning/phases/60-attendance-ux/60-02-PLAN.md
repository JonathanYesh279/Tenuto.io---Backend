---
phase: 60-attendance-ux
plan: 02
type: execute
wave: 2
depends_on: ["60-01"]
files_modified:
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/utils/rehearsalUtils.ts
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/AttendanceManager.tsx
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/RehearsalDetails.tsx
autonomous: true

must_haves:
  truths:
    - "Conductor sees all orchestra members in a toggle list and can tap to cycle each student through statuses (not marked -> present -> late -> absent)"
    - "Students with >80% attendance rate show a green hint icon and frequent absentees (<50% with 3+ rehearsals) show a warning badge"
    - "Changes auto-save after 1.5 seconds of idle time with a visible save confirmation indicator"
    - "Mark all present and Mark all absent buttons update every student in the list at once"
    - "Each student row has a notes field where the conductor can record absence reasons or late arrival details"
  artifacts:
    - path: "/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/AttendanceManager.tsx"
      provides: "Rewritten attendance UI with 4-status cycling, auto-save, notes, smart suggestions"
      min_lines: 200
    - path: "/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/utils/rehearsalUtils.ts"
      provides: "Updated AttendanceUpdate type with records array format"
      contains: "AttendanceRecord"
    - path: "/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js"
      provides: "Updated updateAttendance to send records format and new getMemberAttendanceRates"
      contains: "member-attendance-rates"
  key_links:
    - from: "/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/AttendanceManager.tsx"
      to: "/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js"
      via: "orchestraService.getMemberAttendanceRates and rehearsalService.updateAttendance"
      pattern: "getMemberAttendanceRates|updateAttendance"
    - from: "/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/RehearsalDetails.tsx"
      to: "/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/AttendanceManager.tsx"
      via: "component import and props"
      pattern: "AttendanceManager"
    - from: "/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js"
      to: "api/orchestra/:id/member-attendance-rates"
      via: "HTTP GET request"
      pattern: "member-attendance-rates"
---

<objective>
Rewrite the frontend attendance UI to support 4-status cycling (unmarked -> present -> late -> absent), per-student notes, auto-save with debounce, batch operations, and smart attendance suggestions based on historical rates.

Purpose: Conductors need to take attendance quickly. The current UI has only present/absent, no late status, no notes, no auto-save, and no smart suggestions. This plan delivers all 5 AUX requirements.

Output: Rewritten AttendanceManager component, updated types, updated API service methods, and simplified RehearsalDetails page (attendance logic extracted to component).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-attendance-data-layer/59-01-SUMMARY.md
@.planning/phases/60-attendance-ux/60-01-SUMMARY.md
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/AttendanceManager.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/RehearsalDetails.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/utils/rehearsalUtils.ts
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js
@api/rehearsal/rehearsal.validation.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update types and API service for new attendance format</name>
  <files>
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/utils/rehearsalUtils.ts
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js
  </files>
  <action>
**rehearsalUtils.ts — Update types:**

1. Add `AttendanceStatus` type: `'unmarked' | 'present' | 'late' | 'absent'`

2. Add `AttendanceRecord` interface:
```typescript
export interface AttendanceRecord {
  studentId: string
  status: 'הגיע/ה' | 'לא הגיע/ה' | 'איחור'  // Backend Hebrew values
  notes: string
}
```

3. Add `AttendanceRecordInput` interface (UI-facing with English status):
```typescript
export interface AttendanceRecordInput {
  studentId: string
  status: AttendanceStatus  // English for UI state
  notes: string
}
```

4. Add `MemberAttendanceRate` interface:
```typescript
export interface MemberAttendanceRate {
  studentId: string
  totalRehearsals: number
  attended: number
  late: number
  attendanceRate: number
  suggestion: 'likelyPresent' | 'frequentAbsent' | null
}
```

5. Update the existing `Rehearsal` interface attendance field to include `late: string[]` (the cache now has late array per 59-01).

6. Keep the old `AttendanceUpdate` interface for backward compatibility but mark it as `@deprecated`.

7. Add a status mapping helper function:
```typescript
export const STATUS_MAP = {
  present: 'הגיע/ה',
  absent: 'לא הגיע/ה',
  late: 'איחור',
} as const

export const REVERSE_STATUS_MAP = {
  'הגיע/ה': 'present',
  'לא הגיע/ה': 'absent',
  'איחור': 'late',
} as const
```

8. Add `STATUS_CYCLE` array for tap-to-cycle: `['unmarked', 'present', 'late', 'absent'] as const`

**apiService.js — Update rehearsalService.updateAttendance:**

Find the `updateAttendance` method in the rehearsalService object (around line 2942). Update it to:
1. Accept `records: Array<{ studentId, status, notes }>` (new format)
2. Send `{ records }` to `PUT /rehearsal/${rehearsalId}/attendance`
3. The status values must be Hebrew (backend format): 'הגיע/ה', 'לא הגיע/ה', 'איחור'

**apiService.js — Add orchestraService.getMemberAttendanceRates:**

Add a new method to the orchestraService object:
```javascript
async getMemberAttendanceRates(orchestraId) {
  try {
    const rates = await apiClient.get(`/orchestra/${orchestraId}/member-attendance-rates`);
    return rates;
  } catch (error) {
    console.error('Error fetching member attendance rates:', error);
    throw error;
  }
}
```

IMPORTANT: Do NOT remove the old updateAttendance format in the orchestraService section (around line 3082) — it may still be used by other callers. Only update the rehearsalService.updateAttendance (around line 2942).
  </action>
  <verify>
Grep for `AttendanceRecord` in rehearsalUtils.ts — type must exist.
Grep for `STATUS_MAP` in rehearsalUtils.ts — helper must exist.
Grep for `member-attendance-rates` in apiService.js — new method must exist.
Grep for `records` in the rehearsalService.updateAttendance function in apiService.js — new format used.
  </verify>
  <done>
Types updated with 4-status model and per-student records. API service sends records format to backend. New getMemberAttendanceRates method fetches batch attendance rates.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite AttendanceManager with 4-status cycling, auto-save, notes, and smart suggestions</name>
  <files>
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/AttendanceManager.tsx
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/RehearsalDetails.tsx
  </files>
  <action>
**AttendanceManager.tsx — Complete rewrite:**

The component receives props: `{ rehearsal, orchestraId, onSaved, onClose }`. It is self-contained — it fetches member rates, manages state, and auto-saves.

**State model:**
- `attendanceMap: Map<string, { status: AttendanceStatus, notes: string }>` — keyed by studentId
- `memberRates: MemberAttendanceRate[]` — fetched from backend on mount
- `saveState: 'idle' | 'saving' | 'saved' | 'error'` — for save indicator
- `searchQuery: string` — for member filtering
- `expandedNotes: Set<string>` — which students have notes field expanded

**Initialization (useEffect on mount):**
1. Fetch member attendance rates via `orchestraService.getMemberAttendanceRates(orchestraId)`
2. Initialize attendanceMap from existing `rehearsal.attendance` cache:
   - present array -> status 'present'
   - absent array -> status 'absent'
   - late array -> status 'late'
   - All other members -> status 'unmarked'
3. Load existing notes from activity_attendance if available (from the rehearsal data, or default to '')

**Status cycling (tap handler):**
When a student row is tapped/clicked, cycle through `STATUS_CYCLE`: unmarked -> present -> late -> absent -> unmarked.
Use the `STATUS_CYCLE` array from rehearsalUtils.ts. Find current index, advance by 1, wrap around with modulo.

**Per-student notes (AUX-05):**
- Each row has a small note icon button. Clicking it toggles the `expandedNotes` set for that studentId.
- When expanded, show a text input below the student row.
- Notes are stored in the attendanceMap alongside status.
- Notes are sent to backend as part of the records array.

**Auto-save with debounce (AUX-03):**
- Use a `useRef` for a debounce timer.
- On every change to `attendanceMap`, reset the timer to 1500ms.
- When the timer fires, call the save function.
- Save function:
  1. Set saveState to 'saving'
  2. Build records array from attendanceMap: only include entries where status !== 'unmarked'
  3. Map English status to Hebrew using STATUS_MAP
  4. Call `rehearsalService.updateAttendance(rehearsal._id, records)` where records is `{ records: [...] }`
  5. On success: set saveState to 'saved', then after 2s set back to 'idle'
  6. On error: set saveState to 'error', show error message
- Show a save indicator in the header area:
  - 'saving': spinning icon + "שומר..." text
  - 'saved': green checkmark + "נשמר" text
  - 'error': red warning + "שגיאה בשמירה" text
  - 'idle': nothing
- Do NOT auto-save on the very first render (use a `hasInteracted` ref that becomes true after first user action)

**Batch operations (AUX-04):**
- "סמן הכל כנוכח" (Mark all present) button: sets every member to 'present' status
- "סמן הכל כנעדר" (Mark all absent) button: sets every member to 'absent' status
- These trigger the auto-save debounce normally

**Smart suggestions (AUX-02):**
- Use memberRates from the backend to show indicators next to each student:
  - suggestion === 'likelyPresent': small green dot or upward trend icon with tooltip "נוכחות גבוהה (X%)"
  - suggestion === 'frequentAbsent': small orange/amber warning triangle with tooltip "נוכחות נמוכה (X%)"
  - null: no indicator
- Display the attendance rate percentage as small text next to the suggestion indicator
- Handle case where memberRates request fails (just don't show suggestions, log error)

**Visual design:**
- Keep the existing modal overlay pattern (fixed inset-0 with bg-black bg-opacity-50)
- Header: title "ניהול נוכחות", orchestra name, date/time, save indicator
- Stats bar: 4 columns showing present/late/absent/unmarked counts
- Quick actions row: Mark all present, Mark all absent buttons
- Search input for filtering members
- Member list: each row shows:
  - Status color background (green=present, yellow/amber=late, red=absent, gray=unmarked)
  - Student name + class + primary instrument
  - Smart suggestion indicator (if available)
  - Tap anywhere on the row to cycle status
  - Note icon button on the right
  - Expandable notes input below the row
- No manual save button — auto-save handles everything
- Close button (X) in header

**Status colors:**
- unmarked: `bg-gray-50 border-gray-200 text-gray-600`
- present: `bg-green-50 border-green-300 text-green-800`
- late: `bg-amber-50 border-amber-300 text-amber-800`
- absent: `bg-red-50 border-red-300 text-red-800`

**Status labels (Hebrew):**
- unmarked: "לא סומן"
- present: "נוכח/ת"
- late: "איחור"
- absent: "נעדר/ת"

**Import the following from rehearsalUtils.ts:** `STATUS_MAP, STATUS_CYCLE, AttendanceStatus, MemberAttendanceRate, type Rehearsal`
**Import from apiService.js:** `rehearsalService, orchestraService`
**Import icons from @phosphor-icons/react:** CheckIcon, XIcon, ClockIcon, WarningIcon (or WarningCircle), NotePencilIcon (or NoteBlank), CheckCircleIcon, SpinnerIcon (or CircleNotch), UsersIcon, MagnifyingGlassIcon, TrendUpIcon (for likely present), CalendarIcon

**RehearsalDetails.tsx — Simplify attendance section:**

1. Remove all inline attendance state management from RehearsalDetails.tsx:
   - Remove `attendanceState`, `attendanceSearchQuery`, `attendanceLoading`, `hasAttendanceChanges`, `attendanceError`, `attendanceSuccess`, `showAttendanceModal` state variables
   - Remove `handleAttendanceChange`, `handleQuickMarkAll`, `handleAttendanceReset`, `markAllPresent`, `markAllAbsent`, `clearAllAttendance`, `handleSaveAttendance` functions
   - Remove the useEffect that tracks attendance changes
   - Remove the inline attendance modal JSX

2. Instead, render the new AttendanceManager component:
   - Show a "ניהול נוכחות" button in the rehearsal details page
   - When clicked, render `<AttendanceManager rehearsal={rehearsal} orchestraId={rehearsal.groupId} onSaved={() => loadRehearsalDetails()} onClose={() => setShowAttendanceModal(false)} />`
   - Keep the `showAttendanceModal` boolean state, but now it just controls showing/hiding the AttendanceManager component

3. Keep the attendance summary display in the details page (showing present/absent/late counts from rehearsal.attendance cache), but update it to also show late count.
  </action>
  <verify>
Run `cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && npx tsc --noEmit 2>&1 | head -30` to check for TypeScript errors.
Grep for `STATUS_CYCLE` in AttendanceManager.tsx — cycling logic must exist.
Grep for `debounce\|setTimeout.*1500\|autoSave\|timer` in AttendanceManager.tsx — auto-save must exist.
Grep for `likelyPresent\|frequentAbsent\|suggestion` in AttendanceManager.tsx — smart suggestions must exist.
Grep for `notes` in AttendanceManager.tsx — notes field must exist.
Grep for `handleAttendanceChange\|markAllPresent\|attendanceState` in RehearsalDetails.tsx — inline attendance logic must be REMOVED (should NOT match).
  </verify>
  <done>
AttendanceManager renders a toggle list with 4-status cycling (unmarked->present->late->absent), smart suggestion indicators from member rates, auto-save with 1.5s debounce and visible save confirmation, batch mark-all buttons, and per-student notes fields. RehearsalDetails uses the new component instead of inline attendance management.
  </done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- AttendanceManager has 4-status cycling, auto-save, notes, batch operations, and smart suggestions
- RehearsalDetails no longer has inline attendance logic — delegates to AttendanceManager
- API calls use new records format `{ records: [{ studentId, status, notes }] }` with Hebrew status values
- Smart suggestions fetched from GET /orchestra/:id/member-attendance-rates
- Save indicator shows saving/saved/error states visually
</verification>

<success_criteria>
1. Tapping a student row cycles through unmarked -> present -> late -> absent -> unmarked
2. Students with >80% attendance show green hint, <50% show warning badge
3. Changes auto-save after 1.5s idle with visible "saving" -> "saved" indicator
4. "Mark all present" and "Mark all absent" buttons work for all members
5. Each student row has expandable notes field for absence reasons
6. RehearsalDetails page delegates all attendance logic to AttendanceManager component
</success_criteria>

<output>
After completion, create `.planning/phases/60-attendance-ux/60-02-SUMMARY.md`
</output>
