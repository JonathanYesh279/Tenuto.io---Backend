---
phase: 37-room-schedule-ux-fixes-conflict-prevention
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/seed-dev-data.js
  - src/components/room-schedule/ActivityCell.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Seed data generates zero room-time conflicts across teachers, rehearsals, and theory lessons"
    - "Activity type accent borders are prominent and immediately noticeable"
  artifacts:
    - path: "scripts/seed-dev-data.js"
      provides: "Conflict-free room-time scheduling via occupancy tracking"
      contains: "roomOccupancy"
    - path: "src/components/room-schedule/ActivityCell.tsx"
      provides: "Wider accent borders with background tint for type identification"
      contains: "border-r-[6px]"
  key_links:
    - from: "scripts/seed-dev-data.js"
      to: "LOCATIONS array"
      via: "pickAvailableRoom helper"
      pattern: "pickAvailableRoom"
---

<objective>
Fix seed data room-time conflicts and make accent borders more prominent.

Purpose: Seed data conflicts make it impossible to test the room schedule meaningfully. Subtle accent borders reduce the visual distinction between activity types.
Output: Conflict-free seed script and visually stronger accent borders.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-room-schedule-ux-fixes-conflict-prevention/37-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add room-occupancy tracking to seed script</name>
  <files>scripts/seed-dev-data.js</files>
  <action>
Add a global room occupancy tracker to prevent room-time collisions across all three generators (teachers, rehearsals, theory lessons).

1. Create a `roomOccupancy` Map at module level (key: `${room}::${dayIndex}`, value: array of `{start, end}` minute ranges).

2. Create a `isRoomAvailable(room, dayIndex, startMin, endMin)` helper that checks if a room+day+time range overlaps any existing occupancy entry for that key. Use the same overlap logic: `startMin < existingEnd && endMin > existingStart`.

3. Create a `pickAvailableRoom(dayIndex, startMin, endMin)` helper that:
   - Shuffles a copy of LOCATIONS
   - Returns the first room where `isRoomAvailable` returns true
   - If no room is available, returns null (caller should skip this entry)
   - When a room is found, records the occupancy: pushes `{start: startMin, end: endMin}` into the `roomOccupancy` Map entry

4. In `generateTeachers` (around line 294): Replace `const location = pick(LOCATIONS)` with:
   ```js
   const dayIndex = DAY_INDICES[day];
   const room = pickAvailableRoom(dayIndex, startTotalMin, endTotalMin);
   if (!room) continue; // skip this time block if no room available
   const location = room;
   ```
   Note: `startTotalMin` and `endTotalMin` already exist in scope at this point.

5. In `generateRehearsals` (around line 658): Replace `location: pick(LOCATIONS)` with conflict-aware selection:
   ```js
   const startMin = timeToMinutes(start.str);
   const endMin = timeToMinutes(endTime);
   const room = pickAvailableRoom(dayIndex, startMin, endMin);
   if (!room) continue; // skip this rehearsal
   ```
   Then use `location: room` in the rehearsal object.

6. In `generateTheoryLessons` (around line 713): Same pattern:
   ```js
   const startMin = timeToMinutes(start.str);
   const endMin = timeToMinutes(endTime);
   const room = pickAvailableRoom(dayIndex, startMin, endMin);
   if (!room) continue; // skip this theory lesson
   ```
   Then use `location: room` in the theory lesson object.

7. In `generateOrchestras` (line 608): Orchestra `location` is a default/home room, not a scheduled event. Leave as `pick(LOCATIONS)` -- this is fine because orchestra.location is not a time-bound booking.

8. Remove the dead log at line 1096: `console.log(\`      Conflicts:      ${result.conflicts}\`)` -- `result` does not have a `conflicts` property (seedData return at line 1019 does not include it).

9. Clear the `roomOccupancy` Map at the start of `seedData()` function (line 794) so re-runs start fresh: `roomOccupancy.clear()`.
  </action>
  <verify>
Run `node scripts/seed-dev-data.js --clean` and verify it completes without errors. The output should show 0 in any conflict-related logging. Grep the script to confirm no `pick(LOCATIONS)` calls remain in generateTeachers, generateRehearsals, or generateTheoryLessons.
  </verify>
  <done>Seed script generates room schedules with zero room-time collisions. All three time-bound generators use pickAvailableRoom. Dead conflicts log line removed.</done>
</task>

<task type="auto">
  <name>Task 2: Make accent borders more prominent with background tint</name>
  <files>src/components/room-schedule/ActivityCell.tsx</files>
  <action>
In the frontend repo at `/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend`:

1. In `ACTIVITY_COLORS` constant, update each `borderAccent` from `border-r-4` to `border-r-[6px]` for a wider accent:
   - timeBlock: `'border-r-[6px] border-r-blue-600'`
   - rehearsal: `'border-r-[6px] border-r-purple-600'`
   - theory: `'border-r-[6px] border-r-orange-600'`

2. Add a `bgTint` field to each ACTIVITY_COLORS entry for a subtle colored background that reinforces the type distinction:
   - timeBlock: `bgTint: 'bg-blue-50/60'`  (very light blue tint, 60% opacity)
   - rehearsal: `bgTint: 'bg-purple-50/60'`
   - theory: `bgTint: 'bg-orange-50/60'`

3. In the card div's className (around line 83-91), add `colors.bgTint` alongside the existing `colors.bg`. Actually, replace `colors.bg` with `colors.bgTint` for the card background so it's lighter/subtler in the cell but the accent border is the dominant visual. Wait -- the existing `colors.bg` (e.g., `bg-blue-100`) is already the cell background. The accent border at 4px was not prominent enough. Instead of adding bgTint:

   Simply increase the border width to `border-r-[6px]` and also add a matching left border for RTL visibility: add a `borderAccentLeft` field:
   - timeBlock: add `borderAccentLeft: 'border-l-2 border-l-blue-400'`
   - rehearsal: add `borderAccentLeft: 'border-l-2 border-l-purple-400'`
   - theory: add `borderAccentLeft: 'border-l-2 border-l-orange-400'`

   Apply both `colors.borderAccent` and `colors.borderAccentLeft` in the className.

This gives a 6px colored right border (the main accent in RTL) plus a subtle 2px left border, making the type identification much more prominent without changing the existing bg colors.
  </action>
  <verify>
Run TypeScript check: `cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && npx tsc --noEmit --skipLibCheck 2>&1 | grep -i 'ActivityCell\|error' | head -20`. Should show no errors in ActivityCell.tsx.
  </verify>
  <done>Accent borders are 6px wide on the right and have a subtle 2px colored left border, making activity type identification much more prominent.</done>
</task>

</tasks>

<verification>
- Seed script: `node scripts/seed-dev-data.js --clean` runs successfully
- No `pick(LOCATIONS)` in generateTeachers, generateRehearsals, or generateTheoryLessons
- ActivityCell.tsx has `border-r-[6px]` and `border-l-2` in ACTIVITY_COLORS
- TypeScript passes for ActivityCell.tsx
</verification>

<success_criteria>
- Seed data is conflict-free when generated
- Accent borders are visually prominent at 6px right + 2px left
- User can re-seed DB and see clean room schedule
</success_criteria>

<output>
After completion, create `.planning/phases/37-room-schedule-ux-fixes-conflict-prevention/37-05-SUMMARY.md`
</output>
