---
phase: 37-room-schedule-ux-fixes-conflict-prevention
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/room-schedule/ActivityCell.tsx
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/room-schedule/RoomGrid.tsx
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/room-schedule/FilterBar.tsx
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Backend/scripts/seed-dev-data.js
autonomous: true

must_haves:
  truths:
    - "Each activity cell displays three lines: teacher name with prefix, student/group name with prefix, and time range"
    - "Grid cells are significantly larger (120px+ width, 80px+ height) making content readable without hovering"
    - "Each activity type has a thick 4px right accent border in a strong color distinguishing type at a glance"
    - "Conflict indicator is reduced to a subtle red border (not prominent ring/icon)"
    - "Activity type filter toggles have clear visual on/off states with distinct active vs inactive appearance"
    - "Seed data contains zero intentional conflicts"
  artifacts:
    - path: "src/components/room-schedule/ActivityCell.tsx"
      provides: "3-line cell content with prefix labels and accent borders"
      contains: "borderAccent"
    - path: "src/components/room-schedule/RoomGrid.tsx"
      provides: "Larger grid cells (120px+ columns, 80px+ rows)"
      contains: "minmax(120px"
    - path: "src/components/room-schedule/FilterBar.tsx"
      provides: "Improved toggle on/off visual states"
      contains: "line-through"
    - path: "scripts/seed-dev-data.js"
      provides: "Seed script without conflict generation"
  key_links:
    - from: "ActivityCell.tsx"
      to: "ACTIVITY_COLORS constant"
      via: "borderAccent field per activity type"
      pattern: "borderAccent.*border-r-4"
    - from: "RoomGrid.tsx"
      to: "gridTemplateColumns style"
      via: "CSS grid template"
      pattern: "minmax\\(120px"
---

<objective>
Improve cell readability and visual clarity across the room schedule grid.

Purpose: Users currently need to hover over cells to see full activity details. Larger cells with 3-line content, accent borders, and better filter states make the grid immediately scannable. Seed data cleanup removes artificial conflicts so dev/test data looks like production.

Output: Updated ActivityCell, RoomGrid, FilterBar components with improved readability. Clean seed script.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-room-schedule-ux-fixes-conflict-prevention/37-CONTEXT.md
@.planning/phases/37-room-schedule-ux-fixes-conflict-prevention/37-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Larger cells with 3-line content and accent borders</name>
  <files>
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/room-schedule/ActivityCell.tsx
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/room-schedule/RoomGrid.tsx
  </files>
  <action>
    **ActivityCell.tsx changes:**

    1. Add `borderAccent` field to the ACTIVITY_COLORS constant for each type:
       - timeBlock: `'border-r-4 border-r-blue-600'`
       - rehearsal: `'border-r-4 border-r-purple-600'`
       - theory: `'border-r-4 border-r-orange-600'`

    2. Update the cell content to show 3 lines with Hebrew prefix labels:
       - Line 1: `<div className="font-medium truncate leading-tight text-[11px]">מורה: {activity.teacherName}</div>`
       - Line 2: `<div className="truncate leading-tight text-[10px] opacity-80">תלמיד: {activity.label}</div>`
       - Line 3: `<div className="truncate leading-tight text-[9px] opacity-60">{activity.startTime}-{activity.endTime}</div>`

    3. Apply the accent border class in the card's className: add `colors.borderAccent` to the cn() call.

    4. Reduce the conflict indicator from `CONFLICT_BORDER = 'border-2 border-red-500 ring-2 ring-red-200'` to a subtle safety-net style: `CONFLICT_BORDER = 'border border-red-400'`. Remove the WarningCircle icon from the conflict indicator (conflicts should be prevented, not displayed prominently). Keep the hasConflict check but make it subtle.

    5. In the tooltip, keep all the detail info since it still provides value for quick reference.

    **RoomGrid.tsx changes:**

    1. Update the gridTemplateColumns in both the loading skeleton and the main grid from:
       `'120px repeat(24, minmax(80px, 1fr))'`
       to:
       `'140px repeat(24, minmax(120px, 1fr))'`
       (140px for the room name column to give more space, 120px minimum for each time slot column)

    2. Update BASE_ROW_HEIGHT from 60 to 80 (per user decision: 80px+ row height).

    3. Update STACKED_ITEM_HEIGHT from 32 to 40 to proportionally fit taller rows.

    4. In the skeleton rows, update h-16 to h-20 to match the new row height.
  </action>
  <verify>
    Run the frontend dev server (`npm run dev` in the frontend directory). Navigate to /room-schedule. Confirm:
    - Grid columns are wider (120px+ per time slot)
    - Each activity cell shows 3 lines: "מורה: [name]", "תלמיד: [label]", "[startTime]-[endTime]"
    - Each cell has a thick colored right border matching its type (blue for timeBlock, purple for rehearsal, orange for theory)
    - Row height is at least 80px
    - No TypeScript errors in the build
  </verify>
  <done>
    Activity cells display 3 readable lines with prefix labels and accent borders. Grid cells are 120px+ wide and 80px+ tall. Conflict indicator is a subtle red border, not prominent.
  </done>
</task>

<task type="auto">
  <name>Task 2: Filter toggle visual states and seed cleanup</name>
  <files>
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/room-schedule/FilterBar.tsx
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Backend/scripts/seed-dev-data.js
  </files>
  <action>
    **FilterBar.tsx changes:**

    1. Improve the activity type toggle button visual states. Currently, inactive buttons use `'bg-gray-50 text-gray-400 border-gray-200'` which is too subtle. Update to:
       - Active state: keep current colored backgrounds + add a small filled circle or checkmark indicator. Add `font-medium` and `shadow-sm` for more prominence. Example: `'${btn.activeBg} ${btn.activeText} border-transparent font-medium shadow-sm'`
       - Inactive state: use strikethrough text + more muted appearance: `'bg-white text-gray-400 border-gray-300 line-through'`
       This makes it immediately obvious which types are showing vs hidden.

    2. Verify that SummaryBar already uses filtered data. Looking at RoomSchedule.tsx, the `stats` useMemo (line 315-348) computes from `filteredRooms` which IS filtered by activity type toggles. This means summary bar already reflects filtered view -- no changes needed to SummaryBar.tsx. Just confirm this by reading the code flow: filteredRooms filters by `filters.activityTypes` -> stats computes from filteredRooms -> SummaryBar receives stats. This satisfies the user decision "update summary bar stats to reflect only visible (filtered) activities".

    **seed-dev-data.js changes:**

    3. Remove the intentional conflict generation from the seed script:
       - Comment out or delete lines 1107-1119 (step 9: "Create intentional conflicts") including the `generateConflicts()` call, the `insertMany` calls for conflictRehearsals and conflictTheoryLessons, and the console.log about conflict count.
       - Delete the `generateConflicts` function (starting at line 732). It is no longer needed.
       - This produces zero intentional conflicts in seed data, matching realistic production data per user decision.
  </action>
  <verify>
    1. In the frontend: navigate to /room-schedule, click activity type toggle buttons. Inactive buttons should show line-through text and muted appearance. Active buttons should be bold with colored background.
    2. Verify that toggling activity types also updates the SummaryBar stats (occupied/free counts change when filtering).
    3. In the backend: run `node scripts/seed-dev-data.js` and verify no "Creating intentional conflicts" message appears in output, and the room schedule grid shows no red-bordered conflict cells.
  </verify>
  <done>
    Activity type toggles have clear on/off visual states with line-through for inactive. Summary bar reflects filtered data (already worked, confirmed). Seed script produces zero intentional conflicts.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes in the frontend directory (no TypeScript errors)
- Room schedule grid is visually scannable with larger cells and 3-line content
- Filter toggles clearly communicate on/off state
- Seed data has no intentional conflicts
</verification>

<success_criteria>
1. Each cell shows teacher name (with prefix), student name (with prefix), and time range without needing to hover
2. Grid cells are 120px+ wide, 80px+ tall
3. Activity types have distinct 4px right accent borders (blue/purple/orange)
4. Conflict indicator is subtle (thin red border, no icon)
5. Filter toggles use line-through for inactive state
6. Summary bar updates when filters change (already works -- just verify)
7. Seed script generateConflicts function removed, zero conflicts in seeded data
</success_criteria>

<output>
After completion, create `.planning/phases/37-room-schedule-ux-fixes-conflict-prevention/37-01-SUMMARY.md`
</output>
