---
phase: 33-read-only-room-grid-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/RoomSchedule.tsx
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/room-schedule/DaySelector.tsx
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/room-schedule/RoomGrid.tsx
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/App.tsx
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/Sidebar.tsx
autonomous: true

must_haves:
  truths:
    - "Admin can navigate to /room-schedule from the sidebar and the page loads"
    - "Admin sees a grid with rooms as rows and 30-minute time slots (08:00-20:00) as columns in RTL layout"
    - "Admin can switch between weekdays (Sunday-Friday) using day tabs and the grid refetches data for that day"
    - "Each occupied cell displays teacher name and student/group label, spanning the correct number of columns based on duration"
  artifacts:
    - path: "src/services/apiService.js"
      provides: "roomScheduleService with getRoomSchedule(day) and moveActivity(data) methods"
      contains: "roomScheduleService"
    - path: "src/pages/RoomSchedule.tsx"
      provides: "Main page component with data fetching, day selection state, loading/error states"
      min_lines: 50
    - path: "src/components/room-schedule/DaySelector.tsx"
      provides: "Radix Tabs wrapper for 6 Hebrew weekday tabs"
      min_lines: 20
    - path: "src/components/room-schedule/RoomGrid.tsx"
      provides: "CSS grid rendering rooms x time slots with activity placement"
      min_lines: 80
    - path: "src/App.tsx"
      provides: "Route registration at /room-schedule with admin-only ProtectedRoute"
      contains: "room-schedule"
    - path: "src/components/Sidebar.tsx"
      provides: "Navigation entry for room schedule page"
      contains: "room-schedule"
  key_links:
    - from: "src/pages/RoomSchedule.tsx"
      to: "/api/room-schedule"
      via: "roomScheduleService.getRoomSchedule(selectedDay)"
      pattern: "roomScheduleService\\.getRoomSchedule"
    - from: "src/pages/RoomSchedule.tsx"
      to: "src/components/room-schedule/RoomGrid.tsx"
      via: "props: rooms array from API response"
      pattern: "<RoomGrid"
    - from: "src/App.tsx"
      to: "src/pages/RoomSchedule.tsx"
      via: "lazy import and Route path"
      pattern: "RoomSchedule"
---

<objective>
Build the room schedule page skeleton: API service, page component with day selector, and the CSS grid layout rendering rooms as rows and 30-minute time slots as columns. Activities appear as positioned cells spanning the correct columns.

Purpose: Establishes the core visual grid that all subsequent plans (color coding, conflict indicators, summary stats) build upon. Without the grid layout and data pipeline, nothing else can render.

Output: A navigable `/room-schedule` page showing rooms x time-slots with basic activity cells, day switching, and loading states.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-read-only-room-grid-ui/33-RESEARCH.md
@.planning/phases/32-room-schedule-api-conflict-detection/32-01-SUMMARY.md

IMPORTANT: All files in this plan are in the FRONTEND repo at:
/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/

The backend API is already complete (Phase 32). This plan only creates frontend files.
</context>

<tasks>

<task type="auto">
  <name>Task 1: API service extension, TypeScript types, and route/sidebar registration</name>
  <files>
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/App.tsx
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/Sidebar.tsx
  </files>
  <action>
    1. **apiService.js** -- Add a new `roomScheduleService` export after the `adminAuditService` section (around line 5589). Follow the exact pattern of `hoursSummaryService`:

    ```javascript
    // ==================== Room Schedule Service ====================
    /**
     * Room Schedule Service
     * Room-based schedule grid with conflict detection (admin only)
     * Backend endpoints: /api/room-schedule/*
     */
    export const roomScheduleService = {
      async getRoomSchedule(day) {
        try {
          const response = await apiClient.get('/room-schedule', { day });
          return response;
        } catch (error) {
          console.error('Error fetching room schedule:', error);
          throw error;
        }
      },
      async moveActivity(moveData) {
        try {
          const response = await apiClient.put('/room-schedule/move', moveData);
          return response;
        } catch (error) {
          console.error('Error moving activity:', error);
          throw error;
        }
      },
    };
    ```

    Also add `roomSchedule: roomScheduleService` to the default export object (around line 5610, after `attendance: attendanceService`).

    2. **App.tsx** -- Add lazy import for RoomSchedule page:
    ```typescript
    const RoomSchedule = lazyWithRetry(() => import('./pages/RoomSchedule'), 'RoomSchedule')
    ```
    Place it near the other admin page imports (after `AuditTrail`).

    Add route BEFORE the `/settings` route (around line 396) following the exact pattern of `/ministry-reports`:
    ```tsx
    <Route
      path="/room-schedule"
      element={
        <ProtectedRoute allowedRoles={['admin']}>
          <Layout>
            <Suspense fallback={<PageLoadingFallback message="..." />}>
              <RoomSchedule />
            </Suspense>
          </Layout>
        </ProtectedRoute>
      }
    />
    ```
    Use Hebrew loading message: "טוען לוח חדרים..."

    3. **Sidebar.tsx** -- Add navigation entry to `adminNavigation` array. Insert AFTER the 'חזרות' (rehearsals) entry and BEFORE 'בגרויות' (bagruts):
    ```typescript
    { name: 'לוח חדרים', href: '/room-schedule', Icon: CalendarIcon, category: 'management', roles: ['admin'] },
    ```
    Note: `CalendarIcon` is already imported from `@phosphor-icons/react`. If there is no suitable distinct icon already imported, use `GridNineIcon` or `SquaresFourIcon` from Phosphor -- import it at the top. Prefer an icon that suggests a grid/table layout rather than reusing `CalendarIcon` (which is already used for 'חזרות'). Check what's available and pick an appropriate one.
  </action>
  <verify>
    - `grep -n "roomScheduleService" /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js` shows the service definition and default export entry
    - `grep -n "room-schedule" /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/App.tsx` shows the lazy import and route
    - `grep -n "room-schedule" /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/Sidebar.tsx` shows the nav entry
  </verify>
  <done>
    roomScheduleService exported from apiService.js with getRoomSchedule and moveActivity methods. Route registered at /room-schedule with admin-only ProtectedRoute. Sidebar shows 'לוח חדרים' link in management category.
  </done>
</task>

<task type="auto">
  <name>Task 2: RoomSchedule page, DaySelector, and RoomGrid components</name>
  <files>
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/RoomSchedule.tsx
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/room-schedule/DaySelector.tsx
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/room-schedule/RoomGrid.tsx
  </files>
  <action>
    Create the `components/room-schedule/` directory first.

    1. **DaySelector.tsx** -- Day tab selector using existing Radix Tabs wrapper (`@/components/ui/tabs`):
    - Import `Tabs, TabsList, TabsTrigger` from `@/components/ui/tabs`
    - Hebrew day names array: `['ראשון', 'שני', 'שלישי', 'רביעי', 'חמישי', 'שישי']`
    - Props: `{ selectedDay: number; onDayChange: (day: number) => void; disabled?: boolean }`
    - Render 6 `TabsTrigger` buttons (value = day index 0-5), controlled via `selectedDay`
    - Use `Tabs` with `value={String(selectedDay)}` and `onValueChange={(v) => onDayChange(Number(v))}`

    2. **RoomGrid.tsx** -- The core CSS grid component:
    - Props: `{ rooms: RoomScheduleRoom[]; loading: boolean }` where `RoomScheduleRoom` is `{ room: string; activities: Activity[]; hasConflicts: boolean }`
    - Activity type: `{ id: string; source: 'timeBlock' | 'rehearsal' | 'theory'; room: string; day: number; startTime: string; endTime: string; teacherName: string; teacherId: string; label: string; activityType: string; hasConflict: boolean; conflictGroupId: string | null }`
    - Define types inline (or in the file) -- no separate types file needed for now.
    - Constants:
      - `GRID_START_HOUR = 8`, `GRID_END_HOUR = 20`, `SLOT_DURATION = 30`
      - `TIME_SLOTS`: Generate array of 24 strings `['08:00', '08:30', ..., '19:30']`
    - Helper function `timeToMinutes(time: string): number` -- splits on `:` and returns `hours * 60 + minutes`
    - Helper function `getActivityGridPlacement(startTime, endTime)` returns `{ startCol, endCol, span }`:
      - `startCol = Math.floor((startMinutes - gridStartMinutes) / SLOT_DURATION) + 2` (offset by 1 for room header column, 1-based grid)
      - `endCol = Math.ceil((endMinutes - gridStartMinutes) / SLOT_DURATION) + 2`
      - Clamp to grid boundaries (08:00-20:00)
    - Layout structure:
      - Outer `div` with `overflow-x-auto` and `max-h-[calc(100vh-280px)] overflow-y-auto`
      - Inner `div` with CSS grid: `gridTemplateColumns: '120px repeat(24, minmax(80px, 1fr))'`
      - **Header row:** First cell = empty (room header column), then 24 time slot labels styled as column headers with `text-xs text-center font-medium text-muted-foreground`. Make the header row sticky at top: `position: sticky; top: 0; z-index: 20; bg-white`
      - **Room rows:** For each room in `rooms[]`:
        - Room name cell: `position: sticky; right: 0; z-index: 10` (RTL sticky header), `bg-gray-50 font-medium text-sm px-3 py-2 border-b border-l flex items-center`
        - Activity cells: For each activity in `room.activities`, compute grid placement with `getActivityGridPlacement` and render a div with `style={{ gridColumn: \`${startCol} / ${endCol}\` }}`. Display `teacherName` on first line, `label` on second line (truncated). Basic styling for now: `bg-blue-50 border border-blue-200 rounded px-1 py-0.5 text-xs overflow-hidden` (color coding will be refined in Plan 33-02).
        - Empty cells fill remaining slots (just borders, no content)
      - Each room row uses a sub-grid or relative positioning within its row. The simplest approach: each room gets its own grid row. Activities are absolutely positioned or use `gridColumn` within the main grid. Use a consistent approach -- activities placed with `gridRow` matching their room index + 2 (header is row 1) and `gridColumn` from the placement function.
    - Handle loading state: show skeleton rows (gray pulsing rectangles)
    - Handle empty state: if `rooms.length === 0` and not loading, show a centered message "אין פעילויות להצגה ביום זה" (No activities to show for this day)

    3. **RoomSchedule.tsx** -- Main page component:
    - Import `useState, useEffect` from React
    - Import `roomScheduleService` from `@/services/apiService`
    - Import `DaySelector` from `@/components/room-schedule/DaySelector`
    - Import `RoomGrid` from `@/components/room-schedule/RoomGrid`
    - Import `toast` from `react-hot-toast`
    - State: `selectedDay` (default: `Math.min(new Date().getDay(), 5)` -- current day capped at Friday; if Saturday=6 then 0=Sunday), `schedule` (API response or null), `loading` (boolean)
    - `useEffect` on `selectedDay` change: call `loadSchedule()`
    - `loadSchedule` async function: `setLoading(true)`, try `roomScheduleService.getRoomSchedule(selectedDay)`, set schedule, catch shows `toast.error('שגיאה בטעינת לוח החדרים')`, finally `setLoading(false)`
    - Page layout:
      - Page title: `<h1>` with "לוח חדרים" (Room Schedule)
      - `DaySelector` component passing `selectedDay` and `onDayChange`
      - `RoomGrid` component passing `rooms={schedule?.rooms || []}` and `loading`
    - Use the existing `ListPageHero` pattern if appropriate, or a simple header div with the title and day selector in a flex row.
  </action>
  <verify>
    - All three files exist: `ls /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/RoomSchedule.tsx /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/room-schedule/DaySelector.tsx /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/room-schedule/RoomGrid.tsx`
    - `cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && npx tsc --noEmit --pretty 2>&1 | head -30` -- no TypeScript errors in these files
    - `grep -c "timeToMinutes\|getActivityGridPlacement\|TIME_SLOTS\|GRID_START" /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/room-schedule/RoomGrid.tsx` shows all helpers defined
  </verify>
  <done>
    RoomSchedule page loads, fetches data for the current weekday via roomScheduleService, renders DaySelector with 6 Hebrew day tabs, and RoomGrid displays rooms as rows with activities positioned across 24 time-slot columns. Day switching triggers API refetch. Loading and empty states handled.
  </done>
</task>

</tasks>

<verification>
- Navigate to `/room-schedule` in the browser (admin user) -- page loads without errors
- Day tabs display Hebrew weekday names and clicking a tab changes the displayed data
- Grid shows room names on the right (RTL) and time slots as column headers
- Activities appear as small cards at the correct column positions based on their start/end times
- Browser console shows no uncaught errors
- Non-admin users are redirected (ProtectedRoute with admin-only)
</verification>

<success_criteria>
- Room schedule page accessible at /room-schedule with sidebar navigation
- CSS grid renders rooms x time-slots matrix correctly in RTL
- Day selector switches between Sunday-Friday
- Activity cells span correct columns based on start/end time
- Loading spinner shown during API fetch
- Empty day shows appropriate message
</success_criteria>

<output>
After completion, create `.planning/phases/33-read-only-room-grid-ui/33-01-SUMMARY.md`
</output>
