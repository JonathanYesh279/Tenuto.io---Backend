---
phase: 33-read-only-room-grid-ui
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/room-schedule/ActivityCell.tsx
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/room-schedule/RoomGrid.tsx
autonomous: true

must_haves:
  truths:
    - "Private lesson cells are styled with blue background, rehearsal cells with purple, theory cells with orange"
    - "Cells with hasConflict=true show a red border or warning indicator distinct from normal cells"
    - "Each cell shows teacher name on first line and student/group label on second line"
    - "Conflicting activities in the same room and time are stacked vertically, not overlapping"
    - "Hovering an activity cell shows a tooltip with full details (activity type, exact times, teacher, label)"
  artifacts:
    - path: "src/components/room-schedule/ActivityCell.tsx"
      provides: "Color-coded activity card with conflict indicator and tooltip"
      min_lines: 40
    - path: "src/components/room-schedule/RoomGrid.tsx"
      provides: "Updated grid with ActivityCell rendering and conflict stacking"
      contains: "ActivityCell"
  key_links:
    - from: "src/components/room-schedule/RoomGrid.tsx"
      to: "src/components/room-schedule/ActivityCell.tsx"
      via: "renders ActivityCell for each activity in a room"
      pattern: "<ActivityCell"
    - from: "src/components/room-schedule/ActivityCell.tsx"
      to: "activity.source field"
      via: "ACTIVITY_COLORS map keyed by source type"
      pattern: "ACTIVITY_COLORS"
---

<objective>
Extract activity rendering into a dedicated ActivityCell component with color coding by activity type (blue=private, purple=rehearsal, orange=theory), conflict indicators (red border), and tooltip on hover. Update RoomGrid to handle overlapping activities in the same room/time by stacking them vertically.

Purpose: Satisfies GRID-03 (cell content: teacher + student/group) and GRID-04 (color coding by activity type + conflict indicator). Without this plan, all cells look identical and conflicts are invisible.

Output: Visually distinct activity cells with color coding and conflict indicators in the grid.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-read-only-room-grid-ui/33-RESEARCH.md
@.planning/phases/33-read-only-room-grid-ui/33-01-SUMMARY.md

IMPORTANT: All files are in the FRONTEND repo at:
/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/
</context>

<tasks>

<task type="auto">
  <name>Task 1: ActivityCell component with color coding and conflict indicator</name>
  <files>
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/room-schedule/ActivityCell.tsx
  </files>
  <action>
    Create `ActivityCell.tsx` in the `room-schedule` directory:

    1. **Color map** -- Define `ACTIVITY_COLORS` constant matching the existing `SimpleWeeklyGrid.tsx` color conventions:
    ```typescript
    const ACTIVITY_COLORS = {
      timeBlock: {
        bg: 'bg-blue-100',
        border: 'border-blue-300',
        text: 'text-blue-900',
        label: 'שיעור פרטי',
      },
      rehearsal: {
        bg: 'bg-purple-100',
        border: 'border-purple-300',
        text: 'text-purple-900',
        label: 'חזרה',
      },
      theory: {
        bg: 'bg-orange-100',
        border: 'border-orange-300',
        text: 'text-orange-900',
        label: 'תאוריה',
      },
    } as const
    ```

    2. **Conflict style** -- Separate constant for conflict overlay:
    ```typescript
    const CONFLICT_BORDER = 'border-2 border-red-500 ring-2 ring-red-200'
    ```

    3. **Props interface:**
    ```typescript
    interface ActivityCellProps {
      activity: {
        id: string
        source: 'timeBlock' | 'rehearsal' | 'theory'
        startTime: string
        endTime: string
        teacherName: string
        label: string
        activityType: string
        hasConflict: boolean
        conflictGroupId: string | null
      }
    }
    ```

    4. **Rendering:**
    - Look up colors from `ACTIVITY_COLORS[activity.source]` (fallback to timeBlock if source is unknown)
    - Apply `CONFLICT_BORDER` classes when `activity.hasConflict` is true, REPLACING the normal border class
    - Layout: a small card with rounded corners, padding `px-1.5 py-1`, `text-xs`, `overflow-hidden`, `h-full`, `cursor-default`
    - Content:
      - First line: `teacherName` truncated with `truncate` class, `font-medium`
      - Second line: `label` truncated with `truncate` class, `text-[10px] opacity-80`
    - If `hasConflict`, also show a small warning icon or a "!" indicator in the top-left corner. Use a small red dot or the WarningCircle icon from Phosphor (import `WarningCircleIcon` from `@phosphor-icons/react`, size 12, absolute positioned top-right).

    5. **Tooltip** -- Wrap the cell in Radix Tooltip (`@/components/ui/tooltip`) to show full details on hover:
    - Check if the project has a Tooltip wrapper at `components/ui/tooltip.tsx`. If yes, import `Tooltip, TooltipContent, TooltipProvider, TooltipTrigger` from `@/components/ui/tooltip`.
    - Tooltip content: activity type label (Hebrew), teacher name, label, start-end time, and if conflict: "התנגשות!" (Conflict!) in red text.
    - Use `TooltipProvider` with `delayDuration={300}` to avoid tooltip spam.

    6. Use `clsx` (imported from `clsx`) or `cn` (from `@/lib/utils`) for conditional class merging.
  </action>
  <verify>
    - File exists: `ls /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/room-schedule/ActivityCell.tsx`
    - Contains color map: `grep -c "ACTIVITY_COLORS\|CONFLICT_BORDER\|hasConflict" /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/room-schedule/ActivityCell.tsx`
    - `cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && npx tsc --noEmit --pretty 2>&1 | grep -i "ActivityCell" | head -5` -- no TS errors
  </verify>
  <done>
    ActivityCell component renders color-coded cards (blue/purple/orange by source type) with red conflict border, teacher name + label content, and Radix tooltip with full activity details on hover.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update RoomGrid to use ActivityCell and handle conflict stacking</name>
  <files>
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/room-schedule/RoomGrid.tsx
  </files>
  <action>
    Modify `RoomGrid.tsx` (created in Plan 33-01) to integrate ActivityCell and handle conflicting activities stacking:

    1. **Import ActivityCell** from `./ActivityCell`

    2. **Replace inline activity rendering** with `<ActivityCell activity={activity} />`. Each activity div should still use `style={{ gridColumn: ... }}` for horizontal positioning.

    3. **Conflict stacking logic:**
    - For each room row, group activities by overlapping time ranges. Two activities overlap if their time ranges intersect (startA < endB AND startB < endA). Use the `conflictGroupId` from the API: activities with the same non-null `conflictGroupId` are in the same conflict group.
    - For non-conflicting activities: render in the normal row at standard height.
    - For conflicting activities (sharing a conflictGroupId):
      - Increase the row height for that room row to accommodate stacking.
      - Render conflicting activities stacked vertically within the same cell area. The simplest approach:
        - Group activities by `conflictGroupId`
        - For groups with >1 activity, render them in a flex column within a wrapper div positioned at the correct grid columns
        - Each stacked activity gets reduced height (e.g., `h-8` instead of full row height)
    - Alternative simpler approach if CSS sub-grid is complex: Use `grid-auto-rows: minmax(60px, auto)` so rows expand. Within each room row, render a relatively positioned container. Place activities with absolute positioning, and for conflicts, offset each subsequent conflicting activity vertically by a fixed amount (e.g., 28px per stacked item). Add `min-height` to the row based on max stack count.
    - Choose whichever approach produces cleaner code. The key requirement: conflicting activities must BOTH be visible, not one hiding behind the other.

    4. **Row height for conflict rooms:**
    - Compute max overlap depth per room: for each room, find the maximum number of overlapping activities at any point in time. If the API provides `hasConflicts` on the room level, rooms with `hasConflicts: true` should get a taller row.
    - Simple calculation: group by `conflictGroupId`, find the largest group size. Row min-height = `60px * maxGroupSize` (or 60px for no conflicts).

    5. **Grid cell borders for empty slots:**
    - Render light border lines for each 30-minute slot to show the grid structure even when cells are empty
    - Use a background grid approach: for each room row, render 24 thin border-right divs in the background spanning each column. Style with `border-l border-gray-100` (very subtle).

    6. **Ensure RTL correctness:**
    - The overall page is RTL. Room headers should be on the RIGHT side (first column in RTL grid flows right-to-left).
    - Time progresses from right (08:00) to left (19:30) in RTL.
    - `gridColumn` values are correct as-is because CSS grid handles RTL column ordering.
  </action>
  <verify>
    - `grep -c "ActivityCell\|conflictGroupId\|hasConflicts" /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/room-schedule/RoomGrid.tsx` shows ActivityCell import and conflict handling
    - `cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && npx tsc --noEmit --pretty 2>&1 | grep -i "RoomGrid\|ActivityCell" | head -5` -- no TS errors
  </verify>
  <done>
    RoomGrid renders ActivityCell components with color coding by activity type. Conflicting activities in the same room/time are stacked vertically (both visible, not overlapping). Grid cell borders show the 30-minute slot structure. Room rows expand height to accommodate conflict stacks.
  </done>
</task>

</tasks>

<verification>
- Activities show distinct colors: blue cells for private lessons, purple for rehearsals, orange for theory
- Cells with `hasConflict: true` have a red border and warning indicator
- Hovering an activity shows a tooltip with full details
- When two activities conflict in the same room, both are visible (stacked, not overlapping)
- Grid cell borders show the 30-minute slot structure even in empty areas
</verification>

<success_criteria>
- Three distinct activity type colors visible in the grid
- Conflict cells have red border clearly distinguishable from normal cells
- Tooltip appears on hover with activity type, teacher, label, and times
- No activities hidden behind others -- conflict stacking works
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/33-read-only-room-grid-ui/33-02-SUMMARY.md`
</output>
