---
phase: 64-v1.9-bug-fixes-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/rehearsal/RehearsalAttendance.tsx"
  - "api/rehearsal/rehearsal.service.js"
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Attendance history dots in RehearsalAttendance show correct colors based on backend recentHistory data"
    - "No console.log statements exist in rehearsal bulk delete handlers"
  artifacts:
    - path: "/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/rehearsal/RehearsalAttendance.tsx"
      provides: "Corrected field name reading recentHistory instead of recentSessions"
      contains: "recentHistory"
    - path: "api/rehearsal/rehearsal.service.js"
      provides: "Clean bulk delete handlers without console.log"
  key_links:
    - from: "RehearsalAttendance.tsx fetchHistoryDots"
      to: "GET /api/attendance-alerts/student/:id/summary"
      via: "apiService.attendanceAlerts.getStudentSummary"
      pattern: "recentHistory"
---

<objective>
Close remaining v1.9 gaps: fix attendance history dots field name mismatch and remove console.log from bulk delete handlers.

Purpose: Two of the four gaps from the v1.9 milestone audit are already fixed in the codebase (conflict error preservation was fixed in commit 7798df8, snap indicator lines were implemented in RehearsalCalendar.tsx). This plan addresses the two remaining gaps.
Output: Working attendance history dots + clean bulk delete logging
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-conflict-detection-engine/58-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix attendance history dots field name mismatch</name>
  <files>/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/rehearsal/RehearsalAttendance.tsx</files>
  <action>
In RehearsalAttendance.tsx, the fetchHistoryDots function reads the wrong field name from the backend response. The backend GET /api/attendance-alerts/student/:id/summary returns `recentHistory` (see attendanceAlert.service.js line 423), but the frontend reads `recentSessions`.

Fix two lines:

1. Line ~154: Change `recentSessions: res?.recentSessions || res?.data?.recentSessions || []` to `recentSessions: res?.recentHistory || res?.data?.recentHistory || []`

2. Line ~164: The variable name `recentSessions` on the local result object can stay as-is (it is an internal variable name, not a field from the API). Only the property access on `res` needs to change to `recentHistory`.

Verify that the other two consumers of this API (AttendanceDashboard.tsx line 237, AttendanceTab.tsx line 156) already use `recentHistory` correctly -- do NOT change those files.
  </action>
  <verify>
grep -n "recentSessions" /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/rehearsal/RehearsalAttendance.tsx
Expected: Only the local variable name `recentSessions` should appear (in the destructuring/mapping), NOT as a property access on `res` or `res.data`.

grep -n "recentHistory" /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/rehearsal/RehearsalAttendance.tsx
Expected: Should now appear where we read from the API response.
  </verify>
  <done>RehearsalAttendance.tsx reads `recentHistory` from the backend response instead of `recentSessions`. The history dots will now receive actual attendance data and display correct colors (green=present, red=absent, yellow=late).</done>
</task>

<task type="auto">
  <name>Task 2: Remove console.log from rehearsal bulk delete handlers</name>
  <files>api/rehearsal/rehearsal.service.js</files>
  <action>
Remove console.log statements from the two bulk delete functions in rehearsal.service.js:

1. Line ~644 in bulkDeleteRehearsalsByOrchestra: Remove `console.log(User ${userId} deleted ${deletedCount} rehearsals for orchestra ${orchestraId})` -- this is a success log in a service function. The controller does not need it.

2. Line ~763 in bulkDeleteRehearsalsByDateRange: Remove `console.log(User ${userId} deleted ${deletedCount} rehearsals for orchestra ${orchestraId} in date range ${startDate} to ${endDate})` -- same pattern.

Keep all `console.error` statements (error logging is appropriate). Only remove `console.log` statements in these two specific functions.

Do NOT touch console.log statements in other functions (addRehearsal, bulkCreateRehearsals, etc.) -- those are out of scope for this gap. The roadmap gap specifically targets bulk delete handlers.
  </action>
  <verify>
grep -n "console.log" api/rehearsal/rehearsal.service.js | grep -i "delet"
Expected: No output (no console.log in delete functions).

grep -n "console.error" api/rehearsal/rehearsal.service.js | grep -i "delet"
Expected: console.error statements still present (error logging preserved).
  </verify>
  <done>No console.log statements remain in bulkDeleteRehearsalsByOrchestra or bulkDeleteRehearsalsByDateRange. Error logging via console.error is preserved.</done>
</task>

</tasks>

<verification>
## Already-Resolved Gaps (no action needed)

1. **Conflict error preservation (Gap 1):** Fixed in commit 7798df8. Both addRehearsal (line 198) and updateRehearsal (line 289) now have `if (err.code === 'CONFLICT') throw err;` before the generic error wrapper, matching the BULK_CONFLICT pattern.

2. **Snap indicator lines (Gap 3):** Implemented in RehearsalCalendar.tsx lines 451-458. Dashed grid lines at 25%/50%/75% render within each hour slot when isDropTarget is true during drag-over.

## Gap Closure Verification

3. **Field name fix:** After Task 1, run the app and navigate to a rehearsal attendance page. History dots next to each student name should show colored dots (green/red/yellow) instead of empty/gray placeholder dots.

4. **Console.log cleanup:** After Task 2, run `grep -c "console.log" api/rehearsal/rehearsal.service.js` and confirm the count decreased by 2 from the bulk delete handlers.
</verification>

<success_criteria>
- RehearsalAttendance.tsx reads `recentHistory` from API response (not `recentSessions`)
- No console.log in bulkDeleteRehearsalsByOrchestra or bulkDeleteRehearsalsByDateRange
- All 4 roadmap success criteria for Phase 64 are satisfied (2 already fixed, 2 fixed by this plan)
</success_criteria>

<output>
After completion, create `.planning/phases/64-v1.9-bug-fixes-polish/64-01-SUMMARY.md`
</output>
