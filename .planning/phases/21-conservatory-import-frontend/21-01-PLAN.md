---
phase: 21-conservatory-import-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/ImportData.tsx
autonomous: false

must_haves:
  truths:
    - "Import page shows a third tab labeled 'פרטי קונסרבטוריון' alongside teachers and students tabs"
    - "Clicking the conservatory tab resets state and shows the upload step with conservatory-specific file guide"
    - "Uploading an .xlsx file calls the conservatory preview API and displays a side-by-side diff table"
    - "Changed fields are visually highlighted (amber background, red strikethrough for old value, green for new value)"
    - "Unchanged fields are shown with muted styling"
    - "Execute button shows changed field count, is disabled when changedCount is 0"
    - "After execute, results step shows a simple success message with count of updated fields"
    - "Page subtitle updates dynamically based on active tab"
  artifacts:
    - path: "/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js"
      provides: "previewConservatoryImport method in importService"
      contains: "previewConservatoryImport"
    - path: "/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/ImportData.tsx"
      provides: "Conservatory import tab with diff preview and results rendering"
      contains: "conservatory"
  key_links:
    - from: "ImportData.tsx handleFile"
      to: "importService.previewConservatoryImport"
      via: "activeTab === 'conservatory' branch"
      pattern: "previewConservatoryImport"
    - from: "ImportData.tsx handleExecute"
      to: "importService.executeImport"
      via: "existing executeImport with conservatory result handling"
      pattern: "executeImport.*importLogId"
    - from: "importService.previewConservatoryImport"
      to: "POST /api/import/conservatory/preview"
      via: "fetch with FormData"
      pattern: "import/conservatory/preview"
---

<objective>
Add conservatory profile import as a third tab on the existing import page, with a side-by-side diff preview showing current vs imported values for each field, and a simplified results view showing updated field count.

Purpose: Enables admins to import conservatory profile data from Ministry of Education Excel forms through the same familiar import interface used for teachers and students.
Output: Modified ImportData.tsx with 3-tab layout and diff preview; apiService.js with previewConservatoryImport method.
</objective>

<execution_context>
@/home/yona279/.claude/get-shit-done/workflows/execute-plan.md
@/home/yona279/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-conservatory-import-frontend/21-RESEARCH.md
@.planning/phases/20-conservatory-excel-parser-api/20-01-SUMMARY.md

# Source files to modify
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/ImportData.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add conservatory API method and extend ImportData with third tab, diff preview, and results</name>
  <files>
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/ImportData.tsx
  </files>
  <action>
**apiService.js changes:**

Add `previewConservatoryImport(file)` method to the `importService` object (after `previewStudentImport`, before `executeImport`). Follow the exact same pattern as `previewTeacherImport` (lines 5072-5101):
- Create FormData, append file
- POST to `${apiClient.baseURL}/import/conservatory/preview`
- Authorization header with `apiClient.getStoredToken()`
- Return parsed JSON response
- Console log and error handling matching existing style

**ImportData.tsx changes:**

1. **Type updates (top of file):**
   - Change `ImportTab` type from `'teachers' | 'students'` to `'teachers' | 'students' | 'conservatory'`
   - Add `ConservatoryPreviewField` interface: `{ field: string, label: string, currentValue: string | null, importedValue: string | null, changed: boolean }`
   - Add `ConservatoryPreviewData` interface: `{ importLogId: string, preview: { fields: ConservatoryPreviewField[], changedCount: number, unchangedCount: number, warnings: any[] } }`
   - Add `ConservatoryImportResult` interface: `{ success: boolean, updatedFields: number }`

2. **Import `BuildingsIcon` from `@phosphor-icons/react`** (already used in other pages like TenantListPage.tsx -- import it alongside existing icons)

3. **Add `ConservatoryFileGuide` component** (before the default export, following the pattern of the existing student file guide component around line 431-565):
   - A card with the title "קובץ פרטי קונסרבטוריון"
   - A blue info banner explaining this is the Ministry of Education conservatory form
   - Text: "יש להעלות את קובץ ה-Excel של פרטי הקונסרבטוריון כפי שהתקבל ממשרד החינוך. הקובץ מכיל גיליון אחד עם פרטי המוסד בפורמט טופס (לא טבלה)."
   - A 2-column grid listing the fields that will be parsed: שם קונסרבטוריון, קוד קונסרבטוריון, שם בעלות/רשות, אשכול חברתי, סטטוס, יחידה מקדמת, מספר עוסק (ח.פ.), שלב קונסרבטוריון, מנהל/ת, סמל ישוב, טלפון משרד/נייד, רשות גדולה/קטנה, דוא"ל, מחלקה עיקרית, כתובת, סטטוס פיקוח, מחוז, מקדם עיר מעורבת

4. **Add state for conservatory-specific data:**
   - `conservatoryPreview: ConservatoryPreviewData | null` (separate from `previewData` because the shape is completely different)
   - `conservatoryResults: ConservatoryImportResult | null`
   - Update `resetState()` to also clear `conservatoryPreview` and `conservatoryResults`

5. **Tab button (around line 720-743):**
   - Add a third tab button after the students button following the exact same pattern
   - Icon: `<BuildingsIcon size={16} weight="regular" />`
   - Label: `פרטי קונסרבטוריון`
   - onClick: `handleTabChange('conservatory')`
   - Same conditional className pattern as existing tabs

6. **Page subtitle (line 714-715):**
   - Change the static subtitle to be dynamic based on `activeTab`:
     - teachers: "ייבוא מורים מקובץ Excel"
     - students: "ייבוא תלמידים מקובץ Excel"
     - conservatory: "ייבוא פרטי קונסרבטוריון מקובץ Excel"

7. **Upload step file guide (around line 756-757):**
   - Existing code shows student guide when `activeTab === 'students'` and implicitly shows teacher guide otherwise
   - Add condition for `activeTab === 'conservatory'` to show `<ConservatoryFileGuide />`
   - The teacher file guide (TeacherFileStructureGuide) should only show when `activeTab === 'teachers'`

8. **handleFile callback (lines 600-624):**
   - Add conservatory branch: when `activeTab === 'conservatory'`, call `importService.previewConservatoryImport(file)`
   - Store result in `conservatoryPreview` state (NOT `previewData`) because the shapes are incompatible
   - Still set `importState` to `'preview'`

9. **Preview step rendering (lines ~845-1041):**
   - The existing `{importState === 'preview' && previewData && (` block handles teacher/student previews
   - Add a SEPARATE block: `{importState === 'preview' && activeTab === 'conservatory' && conservatoryPreview && (`
   - This block renders:
     a. **Summary stat cards** (2 cards instead of 4): changedCount (amber/orange gradient) and unchangedCount (gray gradient)
     b. **Warnings** section if conservatoryPreview.preview.warnings.length > 0 (reuse existing warning pattern from line 951-968)
     c. **Diff table** in a `rounded-3xl shadow-sm bg-white overflow-hidden` card:
        - Header: "השוואת נתונים"
        - Table with 3 columns: "שדה" | "ערך נוכחי" | "ערך מיובא"
        - Each row from `conservatoryPreview.preview.fields`:
          - Row className: `f.changed ? 'bg-amber-50/60' : 'hover:bg-gray-50/50'`
          - Field label in `font-medium text-gray-700`
          - Current value: if changed, `text-red-500 line-through`; otherwise `text-gray-500`
          - Imported value: if changed, `text-green-700 font-medium`; otherwise `text-gray-500`
          - Empty values show `---` in `text-gray-300`
     d. **Action buttons**: Cancel button (same as existing) and Execute button:
        - Disabled when `executing || conservatoryPreview.preview.changedCount === 0`
        - Button text when not executing: `עדכן ${conservatoryPreview.preview.changedCount} שדות`
        - Button text when executing: "מעדכן נתונים..." with spinning icon
   - The existing preview block should add a guard: only render when `activeTab !== 'conservatory'` (to prevent crashes from accessing `previewData.preview.matched` when conservatory is active)

10. **handleExecute modification (lines 648-668):**
    - The existing `handleExecute` calls `importService.executeImport(previewData.importLogId)`
    - When `activeTab === 'conservatory'`: use `conservatoryPreview.importLogId` instead of `previewData.importLogId`
    - Store the result in `conservatoryResults` state
    - Show a different toast: `toast.success('הנתונים עודכנו בהצלחה: ${result.updatedFields} שדות עודכנו')`
    - Set `importState` to `'results'`

11. **Results step rendering (lines 1043-1110):**
    - Add a SEPARATE results block for conservatory: `{importState === 'results' && activeTab === 'conservatory' && conservatoryResults && (`
    - Show a simple success card with green gradient (matching existing style):
      - CheckCircleIcon with "הייבוא הושלם" title
      - Single stat: `${conservatoryResults.updatedFields} שדות עודכנו`
      - Text: "פרטי הקונסרבטוריון עודכנו בהצלחה"
    - "ייבוא קובץ נוסף" reset button (same as existing)
    - The existing results block should add guard: only render when `activeTab !== 'conservatory'`

12. **allPreviewRows guard (line 698-704):**
    - The `allPreviewRows` computed variable accesses `previewData.preview.matched` etc. which crashes when `activeTab === 'conservatory'`
    - Guard it: only compute when `activeTab !== 'conservatory'` and `previewData` exists; otherwise empty array

**IMPORTANT: Do NOT create new files.** All changes go into the existing ImportData.tsx and apiService.js. The conservatory tab is an extension of the existing component, not a separate page.
  </action>
  <verify>
Run TypeScript compilation check:
```bash
cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && npx tsc --noEmit 2>&1 | head -30
```
Verify no TypeScript errors in modified files. Check that `BuildingsIcon` import resolves. Verify the `previewConservatoryImport` method exists in apiService.js.
  </verify>
  <done>
ImportData.tsx compiles without errors. Three tabs visible in the import page. The conservatory tab has its own file guide, diff preview with field-level comparison, and simplified results display. apiService.js has previewConservatoryImport method following the same pattern as existing preview methods.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify conservatory import UI flow</name>
  <files>
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/ImportData.tsx
  </files>
  <action>
Human verification of the conservatory import UI flow.

What was built:
- Third tab "פרטי קונסרבטוריון" with BuildingsIcon on the import page
- Conservatory-specific file upload guide explaining Ministry form format
- Side-by-side diff table showing current vs imported field values
- Changed fields highlighted (amber bg, red strikethrough old value, green new value)
- Execute button with changed field count
- Simplified results showing updated field count

How to verify:
1. Navigate to the import page in the frontend app
2. Verify 3 tabs appear: "ייבוא מורים", "ייבוא תלמידים", "פרטי קונסרבטוריון"
3. Click the conservatory tab -- should show upload zone with conservatory file guide
4. Verify subtitle changes to "ייבוא פרטי קונסרבטוריון מקובץ Excel"
5. Upload a Ministry conservatory Excel file (.xlsx)
6. Verify diff table appears with field labels, current values, and imported values
7. Verify changed fields have amber background, old values in red strikethrough, new values in green
8. Verify execute button shows count of changed fields
9. Click execute -- verify success message with updated field count
10. Switch between tabs -- verify state resets properly (no stale data from other tabs)
11. Verify teacher and student import tabs still work correctly (no regressions)
  </action>
  <verify>User visually confirms all 11 verification steps pass</verify>
  <done>User approves the conservatory import UI flow or describes issues to fix</done>
</task>

</tasks>

<verification>
- TypeScript compilation passes with no errors in ImportData.tsx
- Import page renders 3 tabs without console errors
- Conservatory tab upload calls POST /api/import/conservatory/preview
- Diff table renders all fields from backend response
- Execute calls POST /api/import/execute/:importLogId (existing endpoint)
- Teacher and student tabs function identically to before (no regression)
</verification>

<success_criteria>
1. Import page has 3 functional tabs (teachers, students, conservatory)
2. Conservatory upload produces a field-level diff preview (not a row table)
3. Changed fields are visually distinct from unchanged fields
4. Execute updates tenant settings and shows success with field count
5. No TypeScript errors, no console errors, no regressions in existing tabs
</success_criteria>

<output>
After completion, create `.planning/phases/21-conservatory-import-frontend/21-01-SUMMARY.md`
</output>
