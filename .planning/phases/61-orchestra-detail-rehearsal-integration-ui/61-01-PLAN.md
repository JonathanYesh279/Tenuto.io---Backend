---
phase: 61-orchestra-detail-rehearsal-integration-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/features/orchestras/details/types/index.ts
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/features/orchestras/details/components/tabs/ScheduleTab.tsx
autonomous: true

must_haves:
  truths:
    - "Orchestra detail ScheduleTab lists all linked rehearsals with correct date, time, room, and attendance status including late count"
    - "Deleted rehearsals do not appear in the orchestra rehearsal list (backend enforced via Phase 57 transactional sync)"
    - "Conductor can navigate from orchestra detail to a specific rehearsal's attendance view using in-app navigation"
  artifacts:
    - path: "src/features/orchestras/details/types/index.ts"
      provides: "Updated OrchestraRehearsal type with late array in attendance"
      contains: "late"
    - path: "src/features/orchestras/details/components/tabs/ScheduleTab.tsx"
      provides: "Rehearsal list with 3-status attendance display and in-app navigation"
      min_lines: 100
  key_links:
    - from: "src/features/orchestras/details/components/tabs/ScheduleTab.tsx"
      to: "/rehearsal/orchestra/:orchestraId"
      via: "apiService.rehearsals.getOrchestraRehearsals"
      pattern: "getOrchestraRehearsals"
    - from: "src/features/orchestras/details/components/tabs/ScheduleTab.tsx"
      to: "/rehearsals/:rehearsalId"
      via: "useNavigate from react-router-dom"
      pattern: "navigate.*rehearsals"
---

<objective>
Update the orchestra detail page ScheduleTab to accurately display linked rehearsals with the new 3-status attendance model (present/absent/late), use in-app navigation instead of window.open, and add a direct link to the attendance view for each rehearsal.

Purpose: The ScheduleTab was built before Phases 57-60 upgraded the attendance data model. It still reads old `present/absent` arrays, ignores the `late` array, uses `window.open` for navigation, and has no direct path to the conductor attendance view. This plan aligns the UI with the new backend.

Output: Updated ScheduleTab with accurate attendance stats and proper navigation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/features/orchestras/details/types/index.ts
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/features/orchestras/details/components/tabs/ScheduleTab.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/features/orchestras/details/components/OrchestraDetailsPage.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js (lines 2795-2880 for rehearsalService)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update OrchestraRehearsal type and ScheduleTab attendance logic</name>
  <files>
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/features/orchestras/details/types/index.ts
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/features/orchestras/details/components/tabs/ScheduleTab.tsx
  </files>
  <action>
    1. In `types/index.ts`, update the `OrchestraRehearsal` interface:
       - Add `late: string[]` to the `attendance` object (alongside existing `present` and `absent`)
       - Add optional `dayOfWeek?: number` and `type?: string` fields (returned by backend)

    2. In `ScheduleTab.tsx`, fix `getAttendanceStats`:
       - Count `late` array: `const late = rehearsal.attendance.late?.length || 0`
       - **Late counts as present** for the percentage calculation (per decision [59-02]):
         `const presentCount = present + late` — use this for the percentage
       - Return `{ present, absent, late, total }` where `total = orchestra?.memberIds?.length || 0`
       - Display attendance as: "נוכחות: {present + late} מתוך {total}" with late shown separately: "({late} באיחור)" when late > 0

    3. Fix `getAttendancePercentage`:
       - `return Math.round(((stats.present + stats.late) / stats.total) * 100)`

    4. Replace `window.open('/rehearsals/${rehearsal._id}', '_blank')` with `navigate('/rehearsals/${rehearsal._id}')` (in-app navigation, not new tab):
       - Import `useNavigate` from react-router-dom (already used in OrchestraDetailsPage but NOT in ScheduleTab)
       - Add `const navigate = useNavigate()` inside the component
       - Replace BOTH window.open calls (the eye button and the attendance button)

    5. Add a direct "Take Attendance" button that navigates to `/conductor/attendance/${rehearsal._id}` for past rehearsals (this is the route for the RehearsalAttendance component from Phase 60):
       - Show this button for past rehearsals (not upcoming)
       - Use the existing UserCircleCheckIcon but navigate to `/conductor/attendance/${rehearsal._id}` instead of `/rehearsals/${rehearsal._id}`
       - The "view details" EyeIcon button should still navigate to `/rehearsals/${rehearsal._id}`

    6. Fix the "Add Rehearsal" button: currently opens `/rehearsals` in a new tab. Change to navigate to `/rehearsals` (in-app). Consider passing orchestraId as state or query param so the rehearsal form could pre-select this orchestra: `navigate('/rehearsals', { state: { preselectedOrchestraId: orchestraId } })`.

    7. Update the attendance display section to show the late indicator with a yellow/amber color (consistent with the 3-status color scheme from Phase 60):
       - Present+Late bar stays green/yellow/red based on total percentage
       - Add a small text indicator when late > 0: "({late} באיחור)" in amber text
  </action>
  <verify>
    - TypeScript compiles: `cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && npx tsc --noEmit --pretty 2>&1 | grep -E "error|ScheduleTab|types/index" | head -20`
    - No `window.open` calls remain in ScheduleTab: `grep -n "window.open" src/features/orchestras/details/components/tabs/ScheduleTab.tsx`
    - `late` field present in OrchestraRehearsal type: `grep "late" src/features/orchestras/details/types/index.ts`
    - `navigate` used for all navigation: `grep -n "navigate" src/features/orchestras/details/components/tabs/ScheduleTab.tsx`
  </verify>
  <done>
    - OrchestraRehearsal type includes late array
    - Attendance stats count late as present for percentage, display late count separately
    - All navigation uses React Router navigate() instead of window.open
    - Past rehearsals show "Take Attendance" button linking to conductor attendance view
    - "Add Rehearsal" button uses in-app navigation
  </done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes with no errors in modified files
2. No `window.open` calls remain in ScheduleTab.tsx
3. OrchestraRehearsal type includes `late` in attendance object
4. Late count is factored into attendance percentage (late counts as present)
5. Navigation to rehearsal details uses `/rehearsals/:id` route
6. Navigation to attendance uses `/conductor/attendance/:id` route
</verification>

<success_criteria>
- Orchestra detail ScheduleTab correctly displays attendance with 3 statuses (present, absent, late) with late counted as present in percentages
- All navigation is in-app via React Router (no window.open)
- Past rehearsals have a direct "Take Attendance" link to the conductor attendance view
- No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/61-orchestra-detail-rehearsal-integration-ui/61-01-SUMMARY.md`
</output>
