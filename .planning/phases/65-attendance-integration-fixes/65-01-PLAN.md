---
phase: 65-attendance-integration-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/attendance-alerts/attendanceAlert.service.js
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/rehearsal/RehearsalAttendance.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Attendance alerts dashboard returns real data for orchestras with recorded attendance"
    - "Conductor can save attendance from RehearsalAttendance page without validation errors"
    - "Flagged students appear correctly when absence thresholds are exceeded"
    - "Student attendance summary returns accurate recent history with correct statuses"
  artifacts:
    - path: "api/attendance-alerts/attendanceAlert.service.js"
      provides: "Attendance alert queries using sessionId field"
      contains: "sessionId"
    - path: "/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/rehearsal/RehearsalAttendance.tsx"
      provides: "Correct attendance save payload with records array and Hebrew statuses"
      contains: "records"
  key_links:
    - from: "api/attendance-alerts/attendanceAlert.service.js"
      to: "activity_attendance collection"
      via: "sessionId field query"
      pattern: "sessionId.*\\$in.*rehearsalIds"
    - from: "RehearsalAttendance.tsx"
      to: "apiService.rehearsals.updateAttendance"
      via: "records array with Hebrew statuses"
      pattern: "records.*studentId.*status"
---

<objective>
Fix two critical integration bugs that break attendance save and alerts/dashboard functionality.

Purpose: Without these fixes, (1) attendance alerts dashboard always returns empty data because queries use wrong field name, and (2) conductor attendance save from RehearsalAttendance page fails validation because data shape and status values are wrong.

Output: Working attendance save flow and functioning attendance alerts dashboard.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@api/attendance-alerts/attendanceAlert.service.js
@api/rehearsal/rehearsal.service.js (lines 859-978 — updateAttendance writes sessionId)
@api/rehearsal/rehearsal.validation.js (lines 91-99 — attendanceSchema expects records array with Hebrew statuses)
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/rehearsal/RehearsalAttendance.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js (lines 2951-2967 — rehearsals.updateAttendance sends { records })
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix sessionId/activityId mismatch in attendanceAlert.service.js</name>
  <files>api/attendance-alerts/attendanceAlert.service.js</files>
  <action>
Replace all `activityId` references with `sessionId` in attendanceAlert.service.js. The `rehearsal.service.js` writes attendance records to `activity_attendance` collection with field `sessionId: rehearsalId` (line 924), but `attendanceAlert.service.js` queries using `activityId` which never matches anything.

Specific changes:
1. Line 78: Change `activityId: { $in: rehearsalIds }` to `sessionId: { $in: rehearsalIds }`
2. Line 241: Change `activityId: { $in: rehearsalIds }` to `sessionId: { $in: rehearsalIds }`
3. Line 244: Change `records.map(r => r.activityId)` to `records.map(r => r.sessionId)` (for counting unique rehearsals)
4. Line 311: Change `record.activityId` to `record.sessionId` (monthly trend session tracking)
5. Line 382: Change `activityName: r.activityId` to `activityName: r.sessionId` (recent history mapping)

Do NOT change the comments on line 74 — update them to say "via rehearsal sessionIds" instead of "via rehearsal activityIds".
  </action>
  <verify>
Run: `grep -n 'activityId' api/attendance-alerts/attendanceAlert.service.js` — should return zero matches.
Run: `grep -n 'sessionId' api/attendance-alerts/attendanceAlert.service.js` — should show the corrected field references.
  </verify>
  <done>All queries in attendanceAlert.service.js use `sessionId` to match what rehearsal.service.js writes to activity_attendance. Dashboard and flagging queries will now find matching records.</done>
</task>

<task type="auto">
  <name>Task 2: Fix RehearsalAttendance.tsx data shape and status mapping</name>
  <files>/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/rehearsal/RehearsalAttendance.tsx</files>
  <action>
Fix the `saveAttendance` function (lines 275-303) to send data in the format expected by `apiService.rehearsals.updateAttendance(rehearsalId, records)`.

The API service method at apiService.js line 2957 takes `(rehearsalId, records)` and sends `{ records }` to the backend. The backend validation (rehearsal.validation.js line 91-99) expects `records` to be an array of `{studentId, status, notes}` where status is one of `'הגיע/ה'`, `'לא הגיע/ה'`, `'איחור'`.

Currently, the component sends `attendanceData = { rehearsalId, attendanceList: [...] }` as the second argument, meaning the API sends `{ records: { rehearsalId, attendanceList: [...] } }` — an object instead of an array, with English status values.

**Fix the saveAttendance function to:**

1. Add a status mapping constant at the top of the file (near the other constants around line 24):
```typescript
const STATUS_TO_HEBREW: Record<string, string> = {
  present: 'הגיע/ה',
  absent: 'לא הגיע/ה',
  late: 'איחור',
}
```

2. Replace the saveAttendance function body to build `records` as a flat array:
```typescript
const currentMembers = membersRef.current
const records = currentMembers
  .filter((member) => member.status !== 'not_marked')
  .map((member) => ({
    studentId: member.id,
    status: STATUS_TO_HEBREW[member.status] || member.status,
    notes: member.notes || '',
  }))

await apiService.rehearsals.updateAttendance(rehearsal.id, records)
```

This sends `records` (a plain array) as the second argument. The API service wraps it as `{ records }` which matches the backend schema exactly.

3. Also update the `loadRehearsalAttendance` function (around line 211) to add a reverse mapping when reading attendance records back. Add a reverse mapping constant:
```typescript
const HEBREW_TO_STATUS: Record<string, AttendanceStatus> = {
  'הגיע/ה': 'present',
  'לא הגיע/ה': 'absent',
  'איחור': 'late',
}
```

In the section where `attendance.records` are read (line 211), map the Hebrew status back:
```typescript
attendanceMap.set(rec.studentId, {
  status: HEBREW_TO_STATUS[rec.status] || (rec.status as AttendanceStatus) || 'not_marked'
})
```
  </action>
  <verify>
1. Search for `attendanceList` in the file — should return zero matches (removed).
2. Search for `STATUS_TO_HEBREW` — should find the mapping constant.
3. Search for `HEBREW_TO_STATUS` — should find the reverse mapping constant.
4. Verify no TypeScript errors: Run `cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && npx tsc --noEmit --pretty 2>&1 | head -30` (some pre-existing errors may appear but no new errors in RehearsalAttendance.tsx).
  </verify>
  <done>RehearsalAttendance.tsx sends attendance records as a flat array with Hebrew status values matching backend validation schema. Reading back attendance records correctly maps Hebrew statuses to English display values.</done>
</task>

</tasks>

<verification>
1. Backend: `grep -rn 'activityId' api/attendance-alerts/attendanceAlert.service.js` returns no matches
2. Backend: `grep -rn 'sessionId' api/attendance-alerts/attendanceAlert.service.js` shows corrected queries
3. Frontend: `grep -n 'attendanceList' src/components/rehearsal/RehearsalAttendance.tsx` returns no matches
4. Frontend: `grep -n 'STATUS_TO_HEBREW\|HEBREW_TO_STATUS' src/components/rehearsal/RehearsalAttendance.tsx` shows both mappings
5. Backend server starts without errors: `node -e "import('./api/attendance-alerts/attendanceAlert.service.js').then(() => console.log('OK')).catch(e => console.error(e))" --input-type=module`
</verification>

<success_criteria>
- attendanceAlert.service.js queries activity_attendance using `sessionId` (matching what rehearsal.service.js writes)
- RehearsalAttendance.tsx sends `records` as flat array of `{studentId, status, notes}` with Hebrew status values
- Both status mappings (English<->Hebrew) exist for bidirectional conversion
- No new TypeScript compilation errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/65-attendance-integration-fixes/65-01-SUMMARY.md`
</output>
