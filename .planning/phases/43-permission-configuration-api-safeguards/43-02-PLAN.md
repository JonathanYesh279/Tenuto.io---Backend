---
phase: 43-permission-configuration-api-safeguards
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - api/teacher/teacher.route.js
  - api/teacher/teacher.controller.js
  - api/teacher/teacher.service.js
autonomous: true

must_haves:
  truths:
    - "Admin can PUT /api/teacher/:id/roles with a roles array and the teacher's roles are updated"
    - "When roles include 'רכז/ת מחלקתי', coordinatorDepartments is accepted and saved"
    - "When roles do NOT include 'רכז/ת מחלקתי', coordinatorDepartments is cleared to []"
    - "Attempting to remove the last admin role from a tenant returns 400 and the role is not changed"
    - "Roles array is validated against TEACHER_ROLES"
  artifacts:
    - path: "api/teacher/teacher.route.js"
      provides: "New PUT /:id/roles route"
      contains: "requirePermission('roles', 'assign')"
    - path: "api/teacher/teacher.controller.js"
      provides: "updateTeacherRoles handler"
      contains: "updateTeacherRoles"
    - path: "api/teacher/teacher.service.js"
      provides: "updateTeacherRoles service function with last-admin safeguard"
      contains: "updateTeacherRoles"
  key_links:
    - from: "api/teacher/teacher.service.js"
      to: "teacher collection"
      via: "countDocuments for last-admin check + updateOne for role assignment"
      pattern: "countDocuments.*ADMIN_TIER_ROLES"
    - from: "api/teacher/teacher.route.js"
      to: "api/teacher/teacher.controller.js"
      via: "router.put('/:id/roles')"
      pattern: "put.*/:id/roles"
---

<objective>
Add a dedicated role assignment endpoint to the teacher API with SAFE-01 (last-admin prevention), enabling admins to assign multiple roles and coordinator departments to staff.

Purpose: SAFE-01 (last-admin safeguard), success criterion 5 (role assignment with coordinatorDepartments)
Output: New PUT /api/teacher/:id/roles endpoint with last-admin protection
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@api/teacher/teacher.route.js
@api/teacher/teacher.controller.js
@api/teacher/teacher.service.js
@api/teacher/teacher.validation.js
@config/permissions.js
@config/constants.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add updateTeacherRoles to teacher service with last-admin safeguard</name>
  <files>api/teacher/teacher.service.js</files>
  <action>
Add `updateTeacherRoles` to the teacherService export object and implement the function:

`async function updateTeacherRoles(teacherId, { roles, coordinatorDepartments }, options = {})`

1. **Validate roles**: Import `TEACHER_ROLES, ADMIN_TIER_ROLES, INSTRUMENT_DEPARTMENTS` from constants.js. Check every role in the `roles` array is in TEACHER_ROLES. If not, throw with descriptive error.

2. **Validate coordinatorDepartments**: If `roles` includes `'רכז/ת מחלקתי'` and `coordinatorDepartments` is provided, validate each department is in INSTRUMENT_DEPARTMENTS. If `roles` does NOT include `'רכז/ת מחלקתי'`, force `coordinatorDepartments = []`.

3. **Load existing teacher**: Find teacher by `_id` and `tenantId` from `options.context`. If not found, throw `'Teacher not found'`.

4. **SAFE-01 -- Last admin prevention**: Check if the teacher currently has an admin-tier role (any role in ADMIN_TIER_ROLES). If yes, and the NEW roles do NOT include any admin-tier role (meaning we are removing admin access):
   - Count how many OTHER active teachers in this tenant have at least one admin-tier role: `countDocuments({ tenantId, isActive: true, roles: { $in: ADMIN_TIER_ROLES }, _id: { $ne: teacherObjectId } })`.
   - If count === 0, throw an error: `"Cannot remove admin role: this is the last admin for this tenant"`. Use a custom error with `code: 'LAST_ADMIN'`.

5. **Update**: `updateOne({ _id: teacherObjectId }, { $set: { roles, coordinatorDepartments, updatedAt: new Date() } })`.

6. **Return**: The updated roles and coordinatorDepartments.

Use `requireTenantId(options.context?.tenantId)` for tenant guard. Use ObjectId.createFromHexString for the teacher ID.
  </action>
  <verify>`grep -n 'updateTeacherRoles' api/teacher/teacher.service.js` shows the function exists and is exported. `grep -n 'LAST_ADMIN' api/teacher/teacher.service.js` confirms the safeguard error code.</verify>
  <done>updateTeacherRoles function handles role assignment with SAFE-01 (last-admin check via countDocuments) and coordinator department validation</done>
</task>

<task type="auto">
  <name>Task 2: Add roles route and controller handler</name>
  <files>api/teacher/teacher.controller.js, api/teacher/teacher.route.js</files>
  <action>
**teacher.controller.js**: Add `updateTeacherRoles` to the exported controller object and implement:

```
async function updateTeacherRoles(req, res, next) {
  try {
    const { id } = req.params;
    const { roles, coordinatorDepartments } = req.body;

    if (!Array.isArray(roles) || roles.length === 0) {
      return res.status(400).json({
        success: false,
        error: 'roles must be a non-empty array',
        code: 'INVALID_ROLES',
      });
    }

    const result = await teacherService.updateTeacherRoles(id, { roles, coordinatorDepartments }, { context: req.context });
    res.json({ success: true, data: result });
  } catch (err) {
    if (err.code === 'LAST_ADMIN') {
      return res.status(400).json({
        success: false,
        error: err.message,
        code: 'LAST_ADMIN',
      });
    }
    next(err);
  }
}
```

**teacher.route.js**: Add the new route BEFORE the `/:id` GET route (to avoid param conflicts):
```
router.put('/:id/roles', requirePermission('roles', 'assign'), teacherController.updateTeacherRoles)
```

Place it after the existing `router.put('/:id', ...)` line. The `/roles` suffix will not conflict with the parameterized `:id` route because Express matches more specific paths first when the path segment is a literal.

Actually, since Express matches routes in order and `/:id/roles` is more specific than `/:id`, place `/:id/roles` ABOVE `/:id` GET and PUT routes. But checking the existing file: `/:id` GET is at line 23. The new route `PUT /:id/roles` is distinct from `PUT /:id` (different path). Place the new route right after `router.put('/:id', ...)` on line 28. Express will match `PUT /:id/roles` before `PUT /:id` because of the longer path.

Import note: `requirePermission` is already imported in teacher.route.js.
  </action>
  <verify>
1. `grep -n 'roles' api/teacher/teacher.route.js` shows the new route
2. `grep -n 'updateTeacherRoles' api/teacher/teacher.controller.js` shows the handler
3. `node -e "import('./api/teacher/teacher.route.js').then(() => console.log('OK')).catch(e => console.error(e.message))"` -- no import errors
  </verify>
  <done>PUT /api/teacher/:id/roles endpoint is live, gated by requirePermission('roles', 'assign') (admin-only via locked domain), handles last-admin errors with 400 status</done>
</task>

</tasks>

<verification>
1. PUT /api/teacher/:id/roles with `{ roles: ['מורה', 'רכז/ת מחלקתי'], coordinatorDepartments: ['כלי קשת'] }` updates teacher
2. PUT /api/teacher/:id/roles removing admin role from last admin returns 400 with LAST_ADMIN code
3. PUT /api/teacher/:id/roles with invalid role name returns validation error
4. PUT /api/teacher/:id/roles without רכז/ת מחלקתי clears coordinatorDepartments to []
5. Non-admin teachers get 403 (locked 'roles' domain)
</verification>

<success_criteria>
- Role assignment endpoint works with multi-role support
- coordinatorDepartments accepted only when department coordinator role is included
- Last-admin safeguard prevents removing the only admin from a tenant
- Endpoint is admin-only via requirePermission('roles', 'assign')
</success_criteria>

<output>
After completion, create `.planning/phases/43-permission-configuration-api-safeguards/43-02-SUMMARY.md`
</output>
