---
phase: 82-profile-page-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/profile/CredentialsTab.tsx
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Profile.tsx
autonomous: true

must_haves:
  truths:
    - "User can navigate to a Credentials tab on the Profile page"
    - "User can enter current password, new password, and confirm password"
    - "Password strength indicator shows visual feedback as user types"
    - "Submitting the form calls POST /auth/change-password with current and new password"
    - "After successful password change, tokens are refreshed in localStorage and auth state is updated"
    - "Validation errors (mismatch, too short, wrong current password) show Hebrew error messages"
  artifacts:
    - path: "src/services/apiService.js"
      provides: "changePassword method in auth namespace"
      contains: "changePassword"
    - path: "src/components/profile/CredentialsTab.tsx"
      provides: "Password change form with strength indicator"
      min_lines: 80
    - path: "src/pages/Profile.tsx"
      provides: "Credentials tab registered in getTabsByRole"
      contains: "credentials"
  key_links:
    - from: "src/components/profile/CredentialsTab.tsx"
      to: "src/services/apiService.js"
      via: "apiService.auth.changePassword()"
      pattern: "apiService\\.auth\\.changePassword"
    - from: "src/components/profile/CredentialsTab.tsx"
      to: "src/services/authContext.jsx"
      via: "checkAuthStatus(true) after token refresh"
      pattern: "checkAuthStatus"
    - from: "src/pages/Profile.tsx"
      to: "src/components/profile/CredentialsTab.tsx"
      via: "dynamic import in getTabsByRole"
      pattern: "CredentialsTab"
---

<objective>
Add a Credentials tab to the Profile page with a password change form that uses the existing backend POST /auth/change-password endpoint.

Purpose: Allow users to change their password from within the app without admin intervention. The backend endpoint already exists and is fully functional (verifies current password, hashes new, returns fresh tokens). This plan adds the frontend API method and the Credentials tab UI.

Output: Working password change flow accessible from Profile page Credentials tab.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-profile-page-redesign/82-RESEARCH.md

# Key source files
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js (auth section ~line 580-610)
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Profile.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/ForcePasswordChange.tsx (existing password UI patterns)
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/authContext.jsx (checkAuthStatus, useAuth)
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Backend/api/auth/auth.route.js (POST /auth/change-password at line 42)
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Backend/api/auth/auth.service.js (changePassword function at lines 360-446)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add changePassword method to apiService + create CredentialsTab component</name>
  <files>
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/profile/CredentialsTab.tsx
  </files>
  <action>
    **apiService.js** -- Add `changePassword` method to the `auth` object (near the existing `forcePasswordChange` method around line 589):
    ```js
    async changePassword(currentPassword, newPassword) {
      try {
        const response = await apiClient.post('/auth/change-password', { currentPassword, newPassword });
        if (response.accessToken) {
          localStorage.setItem('token', response.accessToken);
          apiClient.setToken(response.accessToken);
        }
        if (response.refreshToken) {
          localStorage.setItem('refreshToken', response.refreshToken);
        }
        return response;
      } catch (error) {
        throw error;
      }
    },
    ```

    **CredentialsTab.tsx** -- Create new component with:
    1. Three password fields: current password, new password, confirm password. All with `dir="ltr"` and `style={{ textAlign: 'left' }}` for RTL compat (same pattern as ForcePasswordChange.tsx).
    2. Toggle show/hide for each field using EyeIcon/EyeSlashIcon from @phosphor-icons/react.
    3. Password strength indicator below new password field:
       - Calculate strength based on: length >= 6 (weak), length >= 8 + has number (medium), length >= 10 + number + uppercase + special char (strong).
       - Visual: 3-segment bar with color (red=weak, amber=medium, green=strong) + Hebrew label (חלשה/בינונית/חזקה).
       - Use Tailwind classes only (bg-red-500, bg-amber-500, bg-emerald-500) -- these are domain-specific severity colors, not design token migration targets per decision [67-03].
    4. Client-side validation before submit:
       - New password min 6 chars: "הסיסמה חייבת להכיל לפחות 6 תווים"
       - Passwords must match: "הסיסמאות אינן תואמות"
       - New must differ from current: "הסיסמה החדשה חייבת להיות שונה מהסיסמה הנוכחית"
    5. Submit calls `apiService.auth.changePassword(currentPassword, newPassword)`.
    6. On success: call `checkAuthStatus(true)` from useAuth() to refresh auth state, show success message (green banner with CheckCircleIcon), clear all fields.
    7. On error: show error in red banner. Map known backend errors:
       - "Current password is incorrect" -> "הסיסמה הנוכחית שגויה"
       - Default: "שגיאה בשינוי הסיסמה. נסה שוב."
    8. Use HeroUI Button for submit (color="primary", variant="solid").
    9. Wrap form in a card with `bg-card rounded-card border border-border p-6 shadow-1`.
    10. Add a LockKeyIcon header with title "שינוי סיסמה" and subtitle "עדכן את הסיסמה שלך".
    11. Import useAuth from '../../services/authContext.jsx'.
    12. Import apiService from '../../services/apiService'.
  </action>
  <verify>
    - File exists: `ls /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/profile/CredentialsTab.tsx`
    - Grep for changePassword in apiService: `grep -n "changePassword" /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js`
    - No TypeScript errors: `cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && npx tsc --noEmit --skipLibCheck 2>&1 | head -20`
  </verify>
  <done>
    changePassword method exists in apiService.auth namespace. CredentialsTab.tsx renders a password change form with current/new/confirm fields, show/hide toggles, strength indicator, client-side validation, and proper token refresh after success.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register CredentialsTab in Profile page tabs</name>
  <files>
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Profile.tsx
  </files>
  <action>
    1. Add import at top of Profile.tsx:
       `import CredentialsTab from '../components/profile/CredentialsTab'`
       `import { LockKeyIcon } from '@phosphor-icons/react'` (if not already imported -- check existing imports, may need to add only LockKeyIcon)

    2. In the `getTabsByRole()` function, add the Credentials tab AFTER the base "general" tab (before role-specific tabs), so ALL users see it:
       ```ts
       tabs.push({
         id: 'credentials',
         label: 'סיסמה',
         icon: LockKeyIcon,
         component: CredentialsTab,
       })
       ```
       Insert this right after the baseTabs spread (`const tabs = [...baseTabs]`) and before the `if (isTeacher())` block.

    3. Verify the existing tab rendering logic in Profile.tsx handles this without changes -- the component mapping at line 458 (`<tab.component ... />`) already renders any component from the tabs array.
  </action>
  <verify>
    - Grep for CredentialsTab in Profile.tsx: `grep -n "CredentialsTab\|credentials" /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Profile.tsx`
    - No TypeScript errors: `cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && npx tsc --noEmit --skipLibCheck 2>&1 | head -20`
    - Dev server check: `cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && npx vite build --mode development 2>&1 | tail -5`
  </verify>
  <done>
    Profile page shows a "סיסמה" tab with lock icon for all users. Clicking it renders the CredentialsTab with password change form. Existing tabs (general, students, schedule, etc.) are unaffected.
  </done>
</task>

</tasks>

<verification>
1. `grep -n "changePassword" /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js` -- method exists
2. `ls /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/profile/CredentialsTab.tsx` -- file exists
3. `grep -n "credentials" /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Profile.tsx` -- tab registered
4. `cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && npx tsc --noEmit --skipLibCheck 2>&1 | head -20` -- no type errors
</verification>

<success_criteria>
- changePassword method in apiService calls POST /auth/change-password
- CredentialsTab renders password change form with strength indicator
- Profile page includes Credentials tab for all users
- Client-side validation catches mismatched/short passwords with Hebrew messages
- Successful change refreshes tokens and auth state
- No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/82-profile-page-redesign/82-01-SUMMARY.md`
</output>
