---
phase: 82-profile-page-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Profile.tsx
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/profile/ProfileSidebar.tsx
autonomous: true

must_haves:
  truths:
    - "Profile page uses 3-column grid layout on desktop (profile card right, stats/widgets center-left)"
    - "Profile sidebar card shows avatar, name, role chips, quick contact popovers (phone, email, WhatsApp)"
    - "GlassStatCard row shows role-appropriate statistics (admin sees conservatory totals)"
    - "Admin profile shows conservatory-wide summary stats (total teachers, students, orchestras)"
    - "Layout collapses to single-column on mobile"
    - "All components use design system tokens (rounded-card, border-border, shadow-1, text-foreground, text-muted-foreground)"
  artifacts:
    - path: "src/components/profile/ProfileSidebar.tsx"
      provides: "Profile card with avatar, badges, contact popovers"
      min_lines: 80
    - path: "src/pages/Profile.tsx"
      provides: "3-column layout with ProfileSidebar + stats + tabs"
      contains: "lg:grid-cols-3"
  key_links:
    - from: "src/pages/Profile.tsx"
      to: "src/components/profile/ProfileSidebar.tsx"
      via: "import and render in grid"
      pattern: "ProfileSidebar"
    - from: "src/pages/Profile.tsx"
      to: "src/components/ui/GlassStatCard.tsx"
      via: "role-aware stat cards"
      pattern: "GlassStatCard"
    - from: "src/components/profile/ProfileSidebar.tsx"
      to: "@heroui/react"
      via: "User, Chip, Button, Popover components"
      pattern: "Popover"
---

<objective>
Restructure the Profile page into a 3-column dashboard layout with a profile sidebar card (matching the StudentDashboardView pattern), role-aware admin stats, and quick contact Popovers.

Purpose: Transform the Profile page from a single-column layout into a rich dashboard matching the Student Details UI pattern established in Phase 80. Admin users see conservatory-wide totals, not just personal teacher stats.

Output: Visually rich Profile dashboard with sidebar card, role-aware stats, and consistent design system usage.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-profile-page-redesign/82-RESEARCH.md

# Layout pattern sources
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/features/students/details/components/dashboard/StudentDashboardView.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/features/students/details/components/dashboard/ProfileCard.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Profile.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/profile/GeneralInfoTab.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/ui/GlassStatCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ProfileSidebar component with contact Popovers</name>
  <files>
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/profile/ProfileSidebar.tsx
  </files>
  <action>
    Create ProfileSidebar.tsx matching the student ProfileCard pattern but adapted for the teacher/admin profile context. This card sits in the right column (RTL) of the 3-column grid.

    **Structure:**
    1. Card container: `bg-card rounded-card border border-border overflow-hidden shadow-1 h-full flex flex-col`
    2. Gradient header band: same as current Profile.tsx header (`background: 'linear-gradient(135deg, #6ec49d 0%, #4db8a4 50%, #3aa89e 100%)'`) with curved SVG bottom edge (exact path from ProfileCard.tsx: `M0,200 C480,40 960,40 1440,200 L1440,200 L0,200 Z`). Height: h-28 (matching ProfileCard).
    3. Avatar overlapping gradient: HeroUI `<User>` with `avatarProps={{ radius: 'full', size: 'lg', showFallback: true, name: displayName, style: { backgroundColor: avatarColor }, classNames: { base: 'w-14 h-14 text-lg text-white ring-3 ring-card' } }}`. Position with `-mt-14` like ProfileCard.
    4. Display name as h2 (text-xl font-bold text-foreground).
    5. Role chips row: map user.roles to HeroUI `<Chip color="primary" variant="flat" size="sm">` with Hebrew labels (same mapping as GeneralInfoTab.tsx lines 330-385: מורה, מנצח, מורה תיאוריה, מנהל).
    6. Status chip: `<Chip color="success" variant="flat" size="sm">פעיל</Chip>` if user.isActive.
    7. Instrument display: if user.professionalInfo?.instrument, show with MusicNoteIcon.

    **Quick Contact Popovers** (matching ProfileCard.tsx pattern):
    8. Icon row: `<div className="flex items-center justify-center gap-1.5">`
    9. Phone Popover: `<Popover placement="bottom">` with `<PopoverTrigger>` wrapping `<Button isIconOnly variant="flat" size="sm">` with PhoneIcon. Content shows phone number with "התקשר" link (`tel:`) and WhatsApp link (`https://wa.me/972${cleaned}`). Use ChatCircleDotsIcon for WhatsApp button per decision [80-01].
    10. Email Popover: Same pattern with EnvelopeIcon. Content shows email with `mailto:` link.
    11. If no phone/email available, show "לא צוין" in popover content.

    **Props interface:**
    ```ts
    interface ProfileSidebarProps {
      user: any
    }
    ```

    **Imports:** User, Chip, Button, Popover, PopoverTrigger, PopoverContent from @heroui/react. PhoneIcon, EnvelopeIcon, ChatCircleDotsIcon, MusicNoteIcon from @phosphor-icons/react. getDisplayName from utils/nameUtils. getAvatarColorHex from utils/avatarColorHash.

    **WhatsApp link helper** (from ProfileCard.tsx):
    ```ts
    const getWhatsAppLink = (phoneNum: string) => {
      const cleaned = phoneNum.replace(/^0/, '').replace(/[-\s]/g, '')
      return `https://wa.me/972${cleaned}`
    }
    ```
  </action>
  <verify>
    - File exists: `ls /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/profile/ProfileSidebar.tsx`
    - Contains Popover: `grep -c "Popover" /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/profile/ProfileSidebar.tsx`
    - No TypeScript errors: `cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && npx tsc --noEmit --skipLibCheck 2>&1 | head -20`
  </verify>
  <done>
    ProfileSidebar.tsx renders a card with gradient header, overlapping avatar, role chips, instrument badge, and quick contact Popovers for phone/email/WhatsApp.
  </done>
</task>

<task type="auto">
  <name>Task 2: Restructure Profile.tsx to 3-column layout with role-aware admin stats</name>
  <files>
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Profile.tsx
  </files>
  <action>
    Refactor Profile.tsx layout from the current structure (header card + stats row + tabs card) into a 3-column dashboard layout matching StudentDashboardView.

    **Layout restructure:**
    1. Remove the existing full-width header card (lines 356-411 -- the gradient/avatar/name block). This is now handled by ProfileSidebar.
    2. Import ProfileSidebar: `import ProfileSidebar from '../components/profile/ProfileSidebar'`
    3. New top-level structure:
    ```tsx
    <div className="space-y-5" dir="rtl">
      {/* Row 1: 3-column dashboard grid */}
      <div className="grid grid-cols-1 lg:grid-cols-3 gap-4">
        {/* Right column (RTL first): Profile sidebar card */}
        <ProfileSidebar user={user} />

        {/* Center + Left columns: Stats + role widgets */}
        <div className="lg:col-span-2 flex flex-col gap-4">
          {/* Stat cards row */}
          <div className="grid grid-cols-2 lg:grid-cols-4 gap-3">
            {statCards.map((card) => (
              <GlassStatCard key={card.label} value={card.value} label={card.label} loading={card.loading} size="sm" />
            ))}
          </div>

          {/* Role info summary card (optional quick-glance widget) */}
          {/* ... see below */}
        </div>
      </div>

      {/* Row 2: Tabs + Content (full-width, unchanged structure) */}
      <div className="bg-card rounded-card border border-border shadow-1 overflow-hidden">
        <Tabs ...> ... </Tabs>
      </div>
    </div>
    ```

    **Admin-specific conservatory stats:**
    4. Modify `loadProfileStatistics` to detect admin role and fetch conservatory-wide counts:
       - If `isAdmin()`: fetch from existing list endpoints (same pattern as Dashboard.tsx):
         - `apiService.teachers.getTeachers()` -> total teachers count
         - `apiService.students.getStudents()` -> total students count
         - `apiService.orchestras.getOrchestras()` -> total orchestras count
       - Use Promise.allSettled for resilience (same as existing code).
    5. Modify `getStatCards()` to return admin-specific cards when `isAdmin()`:
       - "סה״כ מורים" (total teachers)
       - "סה״כ תלמידים" (total students)
       - "סה״כ תזמורות" (total orchestras)
       - "שעות שבועיות" (weekly hours -- kept from personal stats)
       For non-admin, keep existing cards (students, active students, weekly hours, orchestras/theory).
    6. Add admin stats to the ProfileStatistics interface:
       ```ts
       interface ProfileStatistics {
         studentsCount: number
         orchestrasCount: number
         theoryLessonsCount: number
         weeklyHours: number
         totalRehearsals: number
         activeStudents: number
         // Admin conservatory-wide stats
         totalTeachers?: number
         totalStudents?: number
         totalOrchestras?: number
       }
       ```

    **Role info widget** (center column, below stats):
    7. Add a small role information card below the stat cards in the center/left column area:
       ```tsx
       <div className="bg-primary/5 rounded-card border border-primary/20 p-4">
         <div className="flex items-center gap-2 mb-2">
           <ShieldCheckIcon className="w-4 h-4 text-primary" />
           <span className="text-sm font-semibold text-foreground">מידע תפקיד</span>
         </div>
         <div className="flex flex-wrap gap-2">
           {/* Role chips, instrument, status -- compact summary */}
         </div>
       </div>
       ```
       Show: role chips + instrument + user ID (condensed from GeneralInfoTab's role card). This gives at-a-glance role info without needing to open GeneralInfoTab.

    **Cleanup:**
    8. Remove the p-6 wrapper padding from the outer div (Layout.tsx already provides p-6 per decision [68-01]).
    9. Keep all existing tab logic, tab components, and role-based tab visibility completely unchanged.
    10. Ensure the LockKeyIcon import is present (needed by Plan 01's credentials tab -- if Plan 01 runs first, it will already be there; if not, add it).
  </action>
  <verify>
    - 3-column grid: `grep "lg:grid-cols-3" /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Profile.tsx`
    - ProfileSidebar import: `grep "ProfileSidebar" /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Profile.tsx`
    - Admin stats: `grep "totalTeachers\|totalStudents\|totalOrchestras" /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Profile.tsx`
    - No TypeScript errors: `cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && npx tsc --noEmit --skipLibCheck 2>&1 | head -20`
    - Build check: `cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && npx vite build --mode development 2>&1 | tail -5`
  </verify>
  <done>
    Profile page renders a 3-column grid with ProfileSidebar on right, stats + role widget in center-left, and full-width tabs below. Admin users see conservatory-wide totals (teachers, students, orchestras). Layout collapses to single-column on mobile. No hardcoded gray-*/blue-* classes -- all design system tokens.
  </done>
</task>

</tasks>

<verification>
1. `grep "lg:grid-cols-3" /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Profile.tsx` -- 3-column layout
2. `ls /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/profile/ProfileSidebar.tsx` -- sidebar exists
3. `grep "Popover" /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/profile/ProfileSidebar.tsx` -- contact popovers
4. `grep "totalTeachers" /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Profile.tsx` -- admin stats
5. `cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && npx tsc --noEmit --skipLibCheck 2>&1 | head -20` -- no type errors
6. No hardcoded color classes: `grep -E "gray-[0-9]|blue-[0-9]" /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Profile.tsx /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/profile/ProfileSidebar.tsx` -- should return nothing
</verification>

<success_criteria>
- Profile page uses 3-column grid (ProfileSidebar | stats+widgets | spans 2 cols) on desktop
- ProfileSidebar has gradient, avatar, role chips, contact Popovers matching student ProfileCard
- Admin user sees conservatory-wide stats (total teachers, students, orchestras)
- Non-admin user sees personal stats (students, active students, weekly hours, orchestras/theory)
- Role info widget shows compact role/instrument summary below stats
- Mobile layout collapses to single column
- Zero hardcoded gray-*/blue-* classes in modified files
- All existing tabs and tab components work unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/82-profile-page-redesign/82-02-SUMMARY.md`
</output>
