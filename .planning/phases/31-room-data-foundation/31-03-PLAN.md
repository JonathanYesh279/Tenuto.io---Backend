---
phase: 31-room-data-foundation
plan: 03
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - scripts/seed-dev-data.js

autonomous: true

must_haves:
  truths:
    - "Running the seed script produces rooms in tenant.settings.rooms[]"
    - "Running the seed script produces time blocks with location field (not room field) referencing room names"
    - "Running the seed script produces rehearsals in the rehearsal collection with room-referenced locations"
    - "Running the seed script produces theory lessons in the theory_lesson collection with room-referenced locations"
    - "Seed data contains at least 10 intentional room/time conflicts for Phase 32 testing"
    - "Seed produces 30+ teachers and 200+ time blocks"
  artifacts:
    - path: "scripts/seed-dev-data.js"
      provides: "Extended seed script with rooms, rehearsals, theory lessons, and intentional conflicts"
      min_lines: 600
  key_links:
    - from: "scripts/seed-dev-data.js"
      to: "tenant.settings.rooms"
      via: "$set on tenant document after creation"
      pattern: "settings\\.rooms"
    - from: "scripts/seed-dev-data.js"
      to: "rehearsal collection"
      via: "insertMany for seeded rehearsals"
      pattern: "insertMany.*rehearsal|rehearsal.*insertMany"
    - from: "scripts/seed-dev-data.js"
      to: "theory_lesson collection"
      via: "insertMany for seeded theory lessons"
      pattern: "insertMany.*theory|theory.*insertMany"
---

<objective>
Extend the existing dev seed script to create rooms in tenant settings, generate rehearsals and theory lessons, populate time blocks with room-referenced locations, and create intentional scheduling conflicts for grid development testing.

Purpose: Provides realistic test data for Phases 32-35 (room schedule grid). The seed must produce enough volume (30+ teachers, 200+ time blocks) and variety (all three data sources, intentional conflicts) to validate conflict detection and grid rendering.
Output: Enhanced seed-dev-data.js that populates all data needed for room schedule development.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-room-data-foundation/31-RESEARCH.md
@.planning/phases/31-room-data-foundation/31-01-SUMMARY.md
@scripts/seed-dev-data.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend seed script with rooms, rehearsals, theory lessons, and conflicts</name>
  <files>
    scripts/seed-dev-data.js
  </files>
  <action>
Read the entire existing `scripts/seed-dev-data.js` first to understand all current patterns, then make these modifications:

1. **Fix timeBlock field name**: Change `room: pick(LOCATIONS)` (around line 224) to `location: pick(LOCATIONS)`. This fixes the `room` vs `location` inconsistency identified in research. The time-block service reads `location`, not `room`.

2. **Create rooms in tenant.settings**: After the tenant is inserted (around the tenant creation section), update the tenant document to add `settings.rooms[]`:
   ```javascript
   const rooms = LOCATIONS.map(name => ({
     _id: new ObjectId(),
     name,
     isActive: true,
     createdAt: new Date(),
   }));
   await tenantCollection.updateOne(
     { _id: tenantDoc._id },
     { $set: { 'settings.rooms': rooms } }
   );
   ```
   Use the existing `LOCATIONS` array as the source. This ensures seed time blocks reference valid room names.

3. **Generate rehearsals**: After orchestras are created, generate rehearsals for each orchestra:
   - For each orchestra, create 1-2 rehearsals per week (different days)
   - Each rehearsal needs: `{ tenantId, groupId: orchestra._id.toString(), type: pick(['תזמורת', 'הרכב']), date, dayOfWeek, startTime, endTime, location: pick(LOCATIONS), attendance: { present: [], absent: [] }, notes: '', schoolYearId, isActive: true }`
   - Generate dates within the school year range (September to June)
   - Use the existing `generateTime()` helper for start times; end time = start + 1-2 hours
   - Target: ~100-150 rehearsals total (2-3 per orchestra, 50 orchestras)
   - Insert via `db.collection('rehearsal').insertMany(rehearsals)`
   - Log count: `Seeded N rehearsals`

4. **Generate theory lessons**: Create theory lessons:
   - Define THEORY_CATEGORIES array (use the existing `VALID_THEORY_CATEGORIES` values from theory.validation.js or hardcode the list): `['תלמידים חדשים ב-ד', 'מתחילים', 'מתחילים ב', 'מתקדמים א', 'מתקדמים ב', 'מגמה']` (subset is fine)
   - For each category, create 2-4 lessons across different days of the week
   - Each theory lesson needs: `{ tenantId, category, teacherId: pick(teacherIds).toString(), date, dayOfWeek, startTime, endTime, location: pick(LOCATIONS), studentIds: pickN(studentIds, randInt(5, 15)).map(id => id.toString()), notes: '', schoolYearId, isActive: true }`
   - Use `pickN` helper: `function pickN(arr, n) { return arr.sort(() => Math.random() - 0.5).slice(0, n); }` (or shuffle and take first N)
   - Target: ~30-50 theory lessons total
   - Insert via `db.collection('theory_lesson').insertMany(theoryLessons)`
   - Log count: `Seeded N theory lessons`

5. **Create intentional conflicts** (CRITICAL for Phase 32): After generating all data, deliberately create 10-15 scheduling conflicts:
   - **Same-room conflicts (5-7)**: Pick a room and a time slot that's already occupied by a time block. Create a rehearsal or theory lesson in the SAME room at the SAME day/time. Add `notes: 'INTENTIONAL_CONFLICT:same_room'` for easy identification.
   - **Cross-source conflicts (3-4)**: One time block and one rehearsal in same room at same time. One rehearsal and one theory lesson in same room at same time.
   - **Teacher double-booking (2-3)**: Same teacher assigned to two activities at the same time in different rooms.
   - **Implementation approach**: After all random data is inserted, pick specific existing records, read their day/time/location, and create a conflicting record in a different collection but same room+time.
   - Log each conflict: `Created conflict: [type] in room [name] on day [N] at [time]`
   - Final log: `Created N intentional conflicts for Phase 32 testing`

6. **Update the --clean/--drop-only logic**: Add `rehearsal` and `theory_lesson` to the collections that get dropped during cleanup. Currently the script drops `teacher`, `student`, `orchestra`, `school_year` -- add `rehearsal` and `theory_lesson` to the list.

7. **Update the script header comment** to reflect new data created (rehearsals, theory lessons, rooms, conflicts).

8. **Ensure time slot realism**: Time blocks should use 30-minute aligned slots (08:00, 08:30, 09:00, etc.) for compatibility with the grid's 30-minute slot system. Theory lessons and rehearsals should also use 30-minute aligned start/end times. Modify `generateTime()` if needed to ensure minute values are only 0 or 30 (not 15 or 45).
  </action>
  <verify>
    Run `node scripts/seed-dev-data.js --drop-only` to clean existing data, then `node scripts/seed-dev-data.js` to seed fresh data. Verify output logs show:
    - Rooms seeded in tenant settings (28 rooms)
    - Teachers created (130+)
    - Time blocks use `location` field (not `room`)
    - Rehearsals created (100+)
    - Theory lessons created (30+)
    - Intentional conflicts created (10+)

    Then verify in MongoDB:
    ```bash
    mongosh --eval "
      use('Tenuto-DB');
      print('Rooms:', db.tenant.findOne({slug:'dev-conservatory'}).settings.rooms.length);
      print('Rehearsals:', db.rehearsal.countDocuments());
      print('Theory lessons:', db.theory_lesson.countDocuments());
      print('TimeBlocks with location:', db.teacher.aggregate([{$unwind:'$teaching.timeBlocks'},{$match:{'teaching.timeBlocks.location':{$exists:true}}},{$count:'n'}]).toArray());
      print('Conflicts:', db.rehearsal.countDocuments({notes:/INTENTIONAL_CONFLICT/}) + db.theory_lesson.countDocuments({notes:/INTENTIONAL_CONFLICT/}));
    "
    ```
  </verify>
  <done>
    Seed script produces: 28 rooms in tenant.settings.rooms[], 130+ teachers with 200+ time blocks (using `location` not `room`), 100+ rehearsals, 30+ theory lessons, 10+ intentional conflicts labeled with INTENTIONAL_CONFLICT in notes. All time slots use 30-minute alignment. --clean flag drops rehearsal and theory_lesson collections. Script is idempotent with --clean.
  </done>
</task>

</tasks>

<verification>
1. `node scripts/seed-dev-data.js --clean` runs without errors and produces all expected data
2. Tenant document has `settings.rooms` array with 28 entries
3. No teacher time blocks use `room` field -- all use `location`
4. Rehearsal collection has 100+ documents with valid locations
5. Theory lesson collection has 30+ documents with valid locations
6. At least 10 documents have `INTENTIONAL_CONFLICT` in notes
7. All time slots use 30-minute alignment (minutes are 00 or 30)
8. Login with `admin@tenuto-dev.com` / `Admin123` still works after re-seeding
</verification>

<success_criteria>
- Running `node scripts/seed-dev-data.js --clean` from scratch produces a complete test dataset
- Dataset is sufficient for Phase 32 conflict detection (10+ intentional conflicts across all 3 data sources)
- Dataset is sufficient for Phase 33 grid rendering (30+ teachers, 200+ time blocks, multiple rooms populated)
- All location references use the `location` field name consistently
- Room names in time blocks, rehearsals, and theory lessons all match entries in tenant.settings.rooms[].name
</success_criteria>

<output>
After completion, create `.planning/phases/31-room-data-foundation/31-03-SUMMARY.md`
</output>
