---
phase: 35-polish-week-overview
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/tailwind.config.js
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/index.css
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/room-schedule/ScheduleToolbar.tsx
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/RoomSchedule.tsx
autonomous: true

must_haves:
  truths:
    - "Admin sees a toolbar above the room schedule grid with Print, Export PDF, and Day/Week view toggle buttons"
    - "Admin clicks Print and the browser print dialog opens showing only the grid content (no sidebar, header, or interactive controls)"
    - "Admin clicks Export PDF and downloads a PDF file containing a table of the day's room schedule with room, time, teacher, student/group columns"
    - "Printed layout shows the grid at full width without scroll clipping, with sidebar and header hidden"
  artifacts:
    - path: "src/components/room-schedule/ScheduleToolbar.tsx"
      provides: "Toolbar component with print, export, and view mode buttons"
      min_lines: 40
    - path: "tailwind.config.js"
      provides: "print screen variant for Tailwind print: classes"
      contains: "print.*raw.*print"
    - path: "src/index.css"
      provides: "Enhanced print media rules for grid overflow and content expansion"
      contains: "@media print"
    - path: "src/pages/RoomSchedule.tsx"
      provides: "ScheduleToolbar integration with print and PDF handlers"
      contains: "ScheduleToolbar"
  key_links:
    - from: "src/components/room-schedule/ScheduleToolbar.tsx"
      to: "src/pages/RoomSchedule.tsx"
      via: "props: onPrint, onExportPDF, viewMode, onViewModeChange"
      pattern: "ScheduleToolbar"
    - from: "src/pages/RoomSchedule.tsx"
      to: "window.print()"
      via: "handlePrint callback"
      pattern: "window\\.print"
    - from: "src/pages/RoomSchedule.tsx"
      to: "jsPDF"
      via: "handleExportPDF callback generating PDF with autoTable"
      pattern: "jsPDF|autoTable"
---

<objective>
Add a schedule toolbar with print/export functionality and Tailwind print styling to the room schedule page.

Purpose: Fulfills PLSH-01 (admin can print or export a day's room schedule as a clean printable layout). Also prepares the view mode toggle UI that Plan 35-02 will wire to the week overview.
Output: ScheduleToolbar.tsx component, enhanced print CSS, PDF export function, toolbar integrated into RoomSchedule.tsx.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-polish-week-overview/35-RESEARCH.md

@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/RoomSchedule.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/room-schedule/utils.ts
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/room-schedule/ActivityCell.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/room-schedule/RoomGrid.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/room-schedule/SummaryBar.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/tailwind.config.js
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/index.css
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/Layout.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/quickActionsService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tailwind print variant, print CSS, and ScheduleToolbar component</name>
  <files>
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/tailwind.config.js
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/index.css
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/room-schedule/ScheduleToolbar.tsx
  </files>
  <action>
    1. **tailwind.config.js** -- Add `'print': { raw: 'print' }` to `theme.extend.screens` alongside the existing `'xs': '475px'` entry. This enables Tailwind `print:hidden`, `print:block`, `print:overflow-visible` etc. classes throughout the project.

    2. **src/index.css** -- Extend the existing `@media print` block (currently at ~line 331) with additional rules for room schedule printing:
       - `.overflow-x-auto, .overflow-y-auto { overflow: visible !important; max-height: none !important; }` -- Prevents grid clipping during print
       - `body { -webkit-print-color-adjust: exact !important; print-color-adjust: exact !important; }` -- Preserves activity cell background colors in print
       - Do NOT duplicate the existing `.no-print`, `main` margin, or shadow rules -- they already exist.

    3. **src/components/room-schedule/ScheduleToolbar.tsx** -- Create new component:
       - Props interface: `{ viewMode: 'day' | 'week'; onViewModeChange: (mode: 'day' | 'week') => void; onPrint: () => void; onExportPDF: () => void }`
       - Uses Phosphor icons: `Printer`, `FilePdf`, `CalendarBlank` (day), `Calendar` (week)
       - View mode toggle: two adjacent buttons in a segmented control (border group). Active state: `bg-blue-50 text-blue-700 font-medium`. Inactive: `bg-white text-gray-600 hover:bg-gray-50`.
       - Print button: `הדפסה` label with Printer icon
       - Export PDF button: `ייצוא PDF` label with FilePdf icon
       - The entire toolbar has `print:hidden` class so it does not appear in print output
       - Use `cn()` from `@/lib/utils` for conditional classes
       - Hebrew labels: day button = "יום", week button = "שבוע"
  </action>
  <verify>
    - Run `cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && npx tsc --noEmit` -- no TypeScript errors
    - Verify `tailwind.config.js` has `'print': { raw: 'print' }` in screens
    - Verify `ScheduleToolbar.tsx` exports default function with correct props interface
  </verify>
  <done>
    ScheduleToolbar component exists with print/export/view-mode buttons. Tailwind print variant configured. Print CSS rules expanded for grid overflow handling.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate toolbar into RoomSchedule with print and PDF export handlers</name>
  <files>
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/RoomSchedule.tsx
  </files>
  <action>
    Update `RoomSchedule.tsx` to add ScheduleToolbar and wire up print/PDF export:

    1. **Add imports:**
       - `import ScheduleToolbar from '@/components/room-schedule/ScheduleToolbar'`
       - `import { DAY_NAMES } from '@/components/room-schedule/utils'`
       - `import jsPDF from 'jspdf'` and `import 'jspdf-autotable'` (same pattern as quickActionsService.ts)
       - Add the jsPDF module declaration: `declare module 'jspdf' { interface jsPDF { autoTable: (options: any) => jsPDF } }`

    2. **Add viewMode state:**
       - `const [viewMode, setViewMode] = useState<'day' | 'week'>('day')`
       - This state is declared now but the week view rendering is deferred to Plan 35-02. When `viewMode === 'week'`, show a placeholder div with text "Week view coming soon" (will be replaced in 35-02).

    3. **Add handlePrint callback:**
       - Simply calls `window.print()`. The existing `no-print` classes on sidebar/header in Layout.tsx plus the `print:hidden` on ScheduleToolbar and FilterBar handle visibility.

    4. **Add handleExportPDF callback:**
       - Creates a landscape jsPDF document
       - Title: `לוח חדרים - יום ${DAY_NAMES[selectedDay]}` right-aligned at top
       - Date line: `תאריך: ${new Date().toLocaleDateString('he-IL')}` right-aligned
       - Stats summary line: `חדרים: ${stats.totalRooms} | תפוסות: ${stats.occupiedSlots} | פנויות: ${stats.freeSlots} | התנגשויות: ${stats.conflictCount}`
       - Table via `doc.autoTable()` with columns: `['חדר', 'התחלה', 'סיום', 'מורה', 'תלמיד/קבוצה', 'סוג', 'התנגשות']`
       - Table body: flatten `filteredRooms` -- for each room, map activities to row arrays: `[room.room, activity.startTime, activity.endTime, activity.teacherName, activity.label, activityTypeLabel, activity.hasConflict ? 'כן' : '']`
       - Activity type label mapping: `{ timeBlock: 'שיעור פרטי', rehearsal: 'חזרה', theory: 'תאוריה' }`
       - autoTable styles: `{ fontSize: 8, cellPadding: 2, halign: 'right' }`, headStyles: `{ fillColor: [63, 126, 223], halign: 'right' }`
       - Column widths: room 30, start/end 18 each, conflict 18
       - Save as: `doc.save(\`room-schedule-${DAY_NAMES[selectedDay]}.pdf\`)`
       - Wrap in try/catch, show `toast.error('שגיאה בייצוא PDF')` on failure

    5. **Add print:hidden to interactive elements:**
       - Add `print:hidden` class to the FilterBar wrapper div
       - Add `print:hidden` class to the DaySelector wrapper (or the entire header row)
       - The SummaryBar should remain visible in print (useful context)
       - The RoomGrid container div in RoomSchedule gets `print:overflow-visible print:max-h-none` classes to prevent clipping

    6. **Insert ScheduleToolbar in JSX:**
       - Place between the page header (h1 + DaySelector) and FilterBar
       - Pass: `viewMode={viewMode}`, `onViewModeChange={setViewMode}`, `onPrint={handlePrint}`, `onExportPDF={handleExportPDF}`

    7. **Conditional rendering for view mode:**
       - Wrap the existing day view content (FilterBar, SummaryBar, DndContext+RoomGrid, UnassignedRow, CreateLessonDialog) in `{viewMode === 'day' && (...)}`
       - Add `{viewMode === 'week' && (<div className="text-center text-gray-500 py-12">Week view placeholder</div>)}` -- this will be replaced by Plan 35-02

    IMPORTANT: The DaySelector should remain visible in both view modes (user needs to see which day is selected even in week view, and it will be hidden in week view by Plan 35-02). Keep it in the header row, outside the viewMode conditional.
  </action>
  <verify>
    - Run `cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && npx tsc --noEmit` -- no TypeScript errors
    - Run `cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && npm run build` -- build succeeds
    - Verify RoomSchedule.tsx imports ScheduleToolbar and jsPDF
    - Verify `handlePrint` calls `window.print()`
    - Verify `handleExportPDF` creates jsPDF, calls autoTable, calls doc.save
    - Grep for `print:hidden` in RoomSchedule.tsx -- should appear on FilterBar wrapper and toolbar
  </verify>
  <done>
    Admin sees toolbar with Print, Export PDF, and Day/Week toggle. Clicking Print opens browser print dialog with sidebar/header hidden and grid at full width. Clicking Export PDF downloads a PDF table of the current day's activities. View mode toggle switches between day content and week placeholder.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes with no errors
2. Frontend build succeeds
3. ScheduleToolbar.tsx exists with correct interface and Phosphor icons
4. tailwind.config.js includes `print: { raw: 'print' }` screen variant
5. RoomSchedule.tsx integrates toolbar with print/PDF/viewMode handlers
6. Print CSS rules prevent grid clipping and preserve background colors
7. jsPDF export produces a valid PDF with Hebrew column headers and right-aligned text
</verification>

<success_criteria>
- Admin navigates to Room Schedule page and sees the toolbar with three button groups (Day/Week toggle, Print, Export PDF)
- Clicking Print opens the native browser print dialog; print preview shows only the grid and summary bar, no sidebar/header/filters
- Clicking Export PDF downloads a `room-schedule-{dayName}.pdf` file with a table of all activities grouped by room
- The PDF contains Hebrew text for column headers, teacher names, and activity labels
- Toggling to Week view shows a placeholder (to be implemented in Plan 35-02)
- Day view retains all existing functionality (filters, drag-and-drop, create dialog)
</success_criteria>

<output>
After completion, create `.planning/phases/35-polish-week-overview/35-01-SUMMARY.md`
</output>
