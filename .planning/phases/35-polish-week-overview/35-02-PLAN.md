---
phase: 35-polish-week-overview
plan: 02
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/room-schedule/utils.ts
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/room-schedule/WeekOverview.tsx
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/room-schedule/WeekMiniGrid.tsx
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/RoomSchedule.tsx
autonomous: true

must_haves:
  truths:
    - "Admin can toggle to week view and see all 6 weekdays side by side in a compact grid"
    - "Each room appears as a row in the week overview with 6 day columns showing colored mini-blocks for activities"
    - "Each room row shows a utilization percentage bar (green <30%, yellow <70%, red >=70%) computed from occupied slots across all 6 days"
    - "Hovering a mini-block in week view shows a tooltip with teacher name, label, and time range"
    - "Week data is cached and invalidated when the admin creates/moves activities in day mode"
    - "Switching back to day view from week view preserves the previously selected day"
  artifacts:
    - path: "src/components/room-schedule/WeekOverview.tsx"
      provides: "Compact 6-day grid with per-room utilization bars"
      min_lines: 60
    - path: "src/components/room-schedule/WeekMiniGrid.tsx"
      provides: "Compact activity blocks for one room-day cell with tooltips"
      min_lines: 30
    - path: "src/components/room-schedule/utils.ts"
      provides: "GRID_END_HOUR export and computeRoomUtilization helper"
      contains: "computeRoomUtilization"
    - path: "src/pages/RoomSchedule.tsx"
      provides: "Week data fetching, cache invalidation, WeekOverview rendering"
      contains: "WeekOverview"
  key_links:
    - from: "src/pages/RoomSchedule.tsx"
      to: "roomScheduleService.getRoomSchedule"
      via: "Promise.all fetching 6 days when viewMode switches to week"
      pattern: "Promise\\.all"
    - from: "src/components/room-schedule/WeekOverview.tsx"
      to: "src/components/room-schedule/WeekMiniGrid.tsx"
      via: "renders WeekMiniGrid for each room-day cell"
      pattern: "WeekMiniGrid"
    - from: "src/components/room-schedule/WeekOverview.tsx"
      to: "src/components/room-schedule/utils.ts"
      via: "imports computeRoomUtilization, DAY_NAMES, timeToMinutes, GRID_START_HOUR, SLOT_DURATION, TOTAL_SLOTS, GRID_END_HOUR"
      pattern: "computeRoomUtilization"
    - from: "src/pages/RoomSchedule.tsx"
      to: "weekData state"
      via: "setWeekData(null) when loadSchedule succeeds (cache invalidation)"
      pattern: "setWeekData\\(null\\)"
---

<objective>
Add a compact week overview with per-room utilization indicators to the room schedule page.

Purpose: Fulfills PLSH-02 (compact week overview showing all 6 weekdays side by side) and PLSH-03 (per-room utilization percentage). Replaces the week view placeholder from Plan 35-01 with a fully functional week overview component.
Output: WeekOverview.tsx, WeekMiniGrid.tsx, utilization helpers in utils.ts, week data loading integrated into RoomSchedule.tsx.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-polish-week-overview/35-RESEARCH.md
@.planning/phases/35-polish-week-overview/35-01-SUMMARY.md

@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/RoomSchedule.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/room-schedule/utils.ts
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/room-schedule/ActivityCell.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/room-schedule/RoomGrid.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Utilization helpers, WeekMiniGrid, and WeekOverview components</name>
  <files>
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/room-schedule/utils.ts
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/room-schedule/WeekMiniGrid.tsx
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/room-schedule/WeekOverview.tsx
  </files>
  <action>
    1. **utils.ts** -- Add exports needed by week view:
       - Export `GRID_END_HOUR` (already defined as `20` but verify it is exported; currently it IS exported)
       - Add `computeRoomUtilization` function:
         ```
         function computeRoomUtilization(roomName: string, weekData: any[], totalSlotsPerDay: number): number
         ```
         - Iterates over each day in weekData (6 entries)
         - Finds the room by name in each day's `rooms` array
         - Uses `Set<number>` to count unique occupied slot indices per day (same algorithm as SummaryBar stats)
         - Returns `Math.round((totalOccupied / (totalSlotsPerDay * 6)) * 100)` or 0 if no slots
       - The function signature accepts `any[]` for weekData to avoid coupling utils.ts to the RoomScheduleResponse type defined in RoomSchedule.tsx. The caller in WeekOverview will pass the typed array.

    2. **WeekMiniGrid.tsx** -- Create compact mini-grid component for one room-day cell:
       - Props: `{ activities: Array<{ source: 'timeBlock' | 'rehearsal' | 'theory'; startTime: string; endTime: string; teacherName: string; label: string; hasConflict: boolean }> }`
       - Renders a `div` with `relative` positioning, fixed height `h-10`, border-b border-l
       - Each activity renders as an absolutely-positioned colored rectangle:
         - Position: `right: startOffset%` (RTL -- time flows left-to-right visually, so right=0% is 08:00)
         - Width: `(duration / totalGridMinutes) * 100` percent, minimum 2% to be visible
         - Use `inset-inline-start` instead of `right` for proper RTL logical direction: the start edge in RTL is the right edge, so `insetInlineStart: \`${leftPct}%\`` positions from the right
         - Color from `ACTIVITY_COLORS[source].bg` (import from ActivityCell.tsx)
         - If `hasConflict`, add `ring-1 ring-red-400`
       - Each block wrapped in `TooltipProvider > Tooltip > TooltipTrigger + TooltipContent`:
         - Tooltip shows: `{teacherName} - {label}` and `{startTime}-{endTime}` on second line
         - Import Tooltip components from `@/components/ui/tooltip`
         - `delayDuration={200}` on TooltipProvider
       - If activities array is empty, render an empty div (no special empty state needed -- the cell is just blank)

    3. **WeekOverview.tsx** -- Create the compact 6-day overview component:
       - Props interface:
         ```typescript
         interface WeekOverviewProps {
           weekData: RoomScheduleResponse[] | null  // 6 entries, index = day number 0-5
           tenantRooms: Array<{ name: string; isActive: boolean }>
           loading: boolean
         }
         ```
       - NOTE: Import `RoomScheduleResponse` -- since it is currently defined inside RoomSchedule.tsx, you need to either: (a) extract it to a types file, or (b) define a compatible local interface. **Choose option (b)**: define a local `RoomScheduleDay` interface with only the fields needed (`rooms: Array<{ room: string; activities: Array<...> }>`) to avoid circular imports. The page will pass its data which structurally matches.
       - **allRooms memo:** Collect unique room names from all 6 days + active tenant rooms, sorted by Hebrew locale
       - **utilization memo:** For each room, call `computeRoomUtilization(roomName, weekData, TOTAL_SLOTS)` from utils.ts. Store in a `Map<string, number>`
       - **Loading state:** If `loading` is true, show a centered spinner or skeleton grid (simple `animate-pulse` div)
       - **Null state:** If `weekData` is null, show nothing (parent handles this)
       - **Grid layout:**
         - CSS grid with `gridTemplateColumns: '120px repeat(6, 1fr) 80px'`
         - 8 columns: room name (120px) + 6 day columns (flexible) + utilization bar (80px)
         - Header row: "חדר" label, then 6 day names from `DAY_NAMES`, then "ניצולת" (utilization)
         - Header cells: `bg-gray-50 border-b border-l p-2 text-sm font-medium text-gray-500`
         - Day name headers centered
       - **Room rows:** For each room in allRooms:
         - Room name cell: sticky right-0 z-10 bg-white, `text-sm font-medium`, truncated text
         - 6 day cells: each renders `<WeekMiniGrid activities={room?.activities || []} />`
           - Find the room in `weekData[dayIdx].rooms` by name match
         - Utilization cell: progress bar + percentage text
           - Progress bar: `bg-gray-200 rounded-full h-2` container with inner colored div
           - Color: green-400 if <30%, yellow-400 if <70%, red-400 if >=70%
           - Width: `{pct}%` of container
           - Text: `{pct}%` in `text-xs text-gray-500 w-8 text-left`
       - Wrap in `overflow-x-auto rounded-lg border border-gray-200`
       - Use `cn()` for conditional classes
  </action>
  <verify>
    - Run `cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && npx tsc --noEmit` -- no TypeScript errors
    - Verify utils.ts exports `computeRoomUtilization`
    - Verify WeekOverview.tsx and WeekMiniGrid.tsx exist with correct component signatures
    - Verify ACTIVITY_COLORS is imported from ActivityCell in WeekMiniGrid
  </verify>
  <done>
    WeekOverview and WeekMiniGrid components exist with utilization calculation. Compact colored blocks render per activity with tooltips. Utilization progress bars show green/yellow/red based on percentage thresholds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire week data loading, cache invalidation, and replace week placeholder in RoomSchedule</name>
  <files>
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/RoomSchedule.tsx
  </files>
  <action>
    Update RoomSchedule.tsx to load week data and render WeekOverview:

    1. **Add imports:**
       - `import WeekOverview from '@/components/room-schedule/WeekOverview'`

    2. **Add week data state:**
       - `const [weekData, setWeekData] = useState<RoomScheduleResponse[] | null>(null)`
       - `const [weekLoading, setWeekLoading] = useState(false)`

    3. **Add loadWeekData function:**
       ```typescript
       const loadWeekData = useCallback(async () => {
         setWeekLoading(true)
         try {
           const days = await Promise.all(
             [0, 1, 2, 3, 4, 5].map(day => roomScheduleService.getRoomSchedule(day))
           )
           setWeekData(days)
         } catch (err) {
           console.error('Error loading week data:', err)
           toast.error('שגיאה בטעינת מבט שבועי')
         } finally {
           setWeekLoading(false)
         }
       }, [])
       ```

    4. **Auto-fetch week data on view mode change:**
       ```typescript
       useEffect(() => {
         if (viewMode === 'week' && weekData === null) {
           loadWeekData()
         }
       }, [viewMode, weekData, loadWeekData])
       ```
       - Only fetches when switching to week AND weekData is null (not cached)
       - This prevents re-fetching when toggling back and forth if no edits occurred

    5. **Cache invalidation on day-mode edits:**
       - In `loadSchedule()` callback, after `setSchedule(result)` succeeds, add `setWeekData(null)` to invalidate the week cache. This ensures that when the admin creates/moves a lesson in day view and then switches to week view, the week data is refreshed.
       - This is the simplest invalidation strategy: any successful day data reload clears the week cache.

    6. **Hide DaySelector in week view:**
       - The DaySelector is not useful in week view (all 6 days are shown). Wrap it in `{viewMode === 'day' && (...)}`

    7. **Replace week view placeholder with WeekOverview:**
       - Remove the placeholder div that was added in 35-01
       - Replace with:
         ```tsx
         {viewMode === 'week' && (
           <WeekOverview
             weekData={weekData}
             tenantRooms={tenantRooms}
             loading={weekLoading}
           />
         )}
         ```

    8. **PDF export in week mode:**
       - The existing `handleExportPDF` from Plan 35-01 exports day data. When `viewMode === 'week'`, the PDF export should still export the current selected day's data (the day view export). This is fine because PLSH-01 says "print a day's schedule." No change needed to the PDF handler -- it always uses `filteredRooms` which is day-mode data. If the user clicks Export PDF while in week mode, it exports the last-selected day. This is acceptable behavior.
       - The Print button in week mode should print the week overview. Since `window.print()` prints whatever is visible, and in week mode the WeekOverview is visible (not the day grid), this works automatically with no code change.

    IMPORTANT: Preserve all existing day-view functionality. The DndContext, FilterBar, SummaryBar, RoomGrid, UnassignedRow, and CreateLessonDialog must continue to work exactly as before when viewMode === 'day'.
  </action>
  <verify>
    - Run `cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && npx tsc --noEmit` -- no TypeScript errors
    - Run `cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && npm run build` -- build succeeds
    - Verify RoomSchedule.tsx imports WeekOverview
    - Verify `Promise.all` is used to fetch 6 days
    - Verify `setWeekData(null)` appears in the loadSchedule success path
    - Verify the week placeholder from 35-01 is replaced with `<WeekOverview ...>`
    - Grep for `viewMode === 'day'` and `viewMode === 'week'` to confirm conditional rendering
  </verify>
  <done>
    Admin toggles to week view and sees a compact 6-day grid with colored mini-blocks per activity, tooltips on hover, and a utilization percentage bar per room (green/yellow/red). Week data is fetched via 6 parallel API calls and cached. Cache is invalidated when day-mode edits occur. Switching between day and week preserves the selected day. All day-mode functionality (drag-drop, create, filter) continues to work.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes with no errors
2. Frontend build succeeds
3. WeekOverview renders 6 day columns with room rows and mini activity blocks
4. WeekMiniGrid shows colored rectangles matching activity type colors from ActivityCell
5. Tooltips display teacher name, label, and time on hover
6. Utilization bars show correct color (green <30%, yellow <70%, red >=70%) and percentage
7. Week data loads via Promise.all of 6 API calls when switching to week mode
8. Week data cache invalidates when loadSchedule completes after a day-mode edit
9. DaySelector hidden in week mode, visible in day mode
10. All existing day-mode features (drag-drop, create, filter, summary) unaffected
</verification>

<success_criteria>
- Admin clicks "שבוע" (Week) toggle and sees a compact grid: room names on the right, 6 day columns, utilization bar on the left
- Each day-room cell shows colored mini-blocks proportional to activity duration
- Hovering a mini-block shows teacher name, label, and time range
- Utilization column shows a colored progress bar with percentage per room
- Rooms with high utilization (>=70%) show red bars; low utilization (<30%) shows green
- Admin creates/moves a lesson in day view, switches to week view, and sees updated data
- Admin can print the week overview via the Print button (week grid prints cleanly)
</success_criteria>

<output>
After completion, create `.planning/phases/35-polish-week-overview/35-02-SUMMARY.md`
</output>
