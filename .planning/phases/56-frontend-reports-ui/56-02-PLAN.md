---
phase: 56-frontend-reports-ui
plan: 02
type: execute
wave: 2
depends_on: ["56-01"]
files_modified:
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/ReportViewer.tsx
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/reports/ReportFiltersBar.tsx
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/reports/ExportButtons.tsx
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/reports/TeacherHoursChart.tsx
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/reports/MinistryReadinessGauge.tsx
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/reports/YearComparisonToggle.tsx
autonomous: true

must_haves:
  truths:
    - "Report viewer page has a filters bar at the top with school year selector and report-specific parameter controls"
    - "Report viewer page has Excel and PDF export buttons that download the report file"
    - "Teacher Hours Summary report shows a bar chart visualization above the table"
    - "Ministry Readiness Audit report shows a circular gauge/progress indicator for completion percentage"
    - "Reports that support year comparison show a toggle; when enabled, comparison data appears side-by-side"
  artifacts:
    - path: "src/pages/ReportViewer.tsx"
      provides: "Full report viewer shell with filters, content, and export"
      min_lines: 80
    - path: "src/components/reports/ReportFiltersBar.tsx"
      provides: "Shared filter bar with school year and report-specific params"
      min_lines: 30
    - path: "src/components/reports/ExportButtons.tsx"
      provides: "Excel and PDF download buttons"
      min_lines: 20
    - path: "src/components/reports/TeacherHoursChart.tsx"
      provides: "Recharts bar chart for teacher hours breakdown"
      min_lines: 40
    - path: "src/components/reports/MinistryReadinessGauge.tsx"
      provides: "Circular progress gauge for Ministry completion percentage"
      min_lines: 30
    - path: "src/components/reports/YearComparisonToggle.tsx"
      provides: "Toggle + comparison year selector + side-by-side rendering"
      min_lines: 30
  key_links:
    - from: "src/components/reports/ExportButtons.tsx"
      to: "/api/reports/:reportId/export/excel"
      via: "reportsService.exportExcel()"
      pattern: "reportsService\\.exportExcel"
    - from: "src/components/reports/ExportButtons.tsx"
      to: "/api/reports/:reportId/export/pdf"
      via: "reportsService.exportPdf()"
      pattern: "reportsService\\.exportPdf"
    - from: "src/components/reports/YearComparisonToggle.tsx"
      to: "/api/reports/:reportId"
      via: "comparisonYearId query param"
      pattern: "comparisonYearId"
---

<objective>
Enhance the report viewer with filters bar, export buttons, custom renderers for key reports, and year comparison toggle.

Purpose: Completes the reports experience — users can filter, export, see enhanced visualizations for specific reports, and compare data across school years.
Output: Full-featured ReportViewerShell with all FEND-02 through FEND-05 requirements met.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-frontend-reports-ui/56-01-SUMMARY.md

@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/ReportViewer.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/reports/DefaultTableRenderer.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/ui/Pagination.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/ui/progress.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Backend/api/reports/report.contract.js
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Backend/api/reports/report.dashboard.js
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Backend/api/reports/generators/teacher-hours-summary.generator.js
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Backend/api/reports/generators/ministry-readiness-audit.generator.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: ReportViewerShell with filters bar and export buttons</name>
  <files>
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/ReportViewer.tsx
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/reports/ReportFiltersBar.tsx
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/reports/ExportButtons.tsx
  </files>
  <action>
    **1. Create ReportFiltersBar.tsx:**
    Shared horizontal filter bar rendered at the top of the report viewer. Props:
    ```typescript
    interface ReportFiltersBarProps {
      reportParams?: Record<string, { type: string; label?: string; required?: boolean; allowedValues?: string[] }>
      currentFilters: Record<string, string>
      onFilterChange: (key: string, value: string) => void
      schoolYearId: string
      onSchoolYearChange: (yearId: string) => void
    }
    ```
    - School year selector: use the `useSchoolYear()` hook to get available school years. Render a `<select>` dropdown with all school years, current selected year highlighted.
    - Report-specific params: iterate over `reportParams` (from the report's `metadata.params` or registry info). For each param:
      - If `allowedValues` exists: render a `<select>` dropdown with those values.
      - If `type === 'string'`: render a text input.
      - If `type === 'boolean'`: render a toggle/checkbox.
      - Label from `param.label` or param key.
    - Layout: flex row with gap, items vertically centered, bg-muted/30 rounded-lg padding.
    - All labels in Hebrew.

    **2. Create ExportButtons.tsx:**
    Component renders Excel and PDF download buttons. Props:
    ```typescript
    interface ExportButtonsProps {
      reportId: string
      currentFilters: Record<string, string>
      exports: string[]  // from metadata.exports, e.g. ['excel', 'pdf']
    }
    ```
    - Only show buttons for formats listed in `exports` array.
    - Excel button: green-tinted, DownloadIcon, text "ייצוא Excel". On click: call `reportsService.exportExcel(reportId, currentFilters)`, show loading spinner during download, toast success/error.
    - PDF button: red-tinted, FileTextIcon, text "ייצוא PDF". On click: call `reportsService.exportPdf(reportId, currentFilters)`, same loading/toast pattern.
    - Both buttons disabled while download is in progress.
    - Use `react-hot-toast` for notifications (already in project).

    **3. Enhance ReportViewer.tsx (created in Plan 01):**
    Upgrade the basic viewer from Plan 01 into the full ReportViewerShell:
    - Layout (top to bottom):
      1. Header bar: back link "חזרה לדוחות" (navigate to /reports), report title from `metadata.title`, ExportButtons on the left side.
      2. ReportFiltersBar below header.
      3. Content area: renders either a custom renderer (Task 2) or DefaultTableRenderer based on reportId.
      4. Custom renderer lookup: create a `CUSTOM_RENDERERS` map: `{ 'teacher-hours-summary': TeacherHoursChart, 'ministry-readiness-audit': MinistryReadinessGauge }`. If reportId is in the map, render the custom renderer ABOVE the DefaultTableRenderer (not instead of — both show). Otherwise just show DefaultTableRenderer.
    - Filter state management: maintain a `filters` state object. When ReportFiltersBar changes a filter, update state and refetch report data.
    - Include `schoolYearId` from useSchoolYear() in all API calls.
    - Query params sync: update URL query params when filters change (optional, nice-to-have).
  </action>
  <verify>
    - `ls /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/reports/ReportFiltersBar.tsx /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/reports/ExportButtons.tsx` confirms files exist
    - `grep "exportExcel\|exportPdf" /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/reports/ExportButtons.tsx` confirms export wiring
    - `grep "CUSTOM_RENDERERS" /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/ReportViewer.tsx` confirms renderer lookup
    - `grep "ReportFiltersBar" /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/ReportViewer.tsx` confirms filter bar integration
  </verify>
  <done>
    - Report viewer shows filter bar with school year selector and report-specific param controls
    - Excel and PDF export buttons appear when report supports those formats
    - Clicking export triggers file download via reportsService
    - Filter changes cause report data to refetch
    - Custom renderer mapping is wired (renderers built in Task 2)
  </done>
</task>

<task type="auto">
  <name>Task 2: Custom renderers and year comparison toggle</name>
  <files>
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/reports/TeacherHoursChart.tsx
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/reports/MinistryReadinessGauge.tsx
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/reports/YearComparisonToggle.tsx
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/ReportViewer.tsx
  </files>
  <action>
    **1. Create TeacherHoursChart.tsx:**
    Custom renderer for `teacher-hours-summary` report. Props: `{ rows, columns }` (same data the table receives).
    - Use `recharts` (already in package.json) to render a horizontal `BarChart` showing each teacher's hours breakdown.
    - X-axis: teacher name (from the row's teacher name column).
    - Stacked bars with 4 segments: individual lessons, orchestra conducting, theory teaching, management (use column keys from the report: likely `individualLessons`, `orchestraConducting`, `theoryTeaching`, `management`). Read the actual column keys from the teacher-hours-summary generator to get exact field names.
    - Color scheme: use the project's entity color system — distinct colors for each category.
    - Legend below the chart with Hebrew labels.
    - Responsive: use `ResponsiveContainer` from recharts with height={Math.max(300, rows.length * 35)} to scale with data.
    - Show top 15 teachers only if more than 15 rows (add note "מציג 15 מורים מובילים").
    - Card wrapper with title "התפלגות שעות לפי מורה".

    **2. Create MinistryReadinessGauge.tsx:**
    Custom renderer for `ministry-readiness-audit` report. Props: `{ rows, summary }`.
    - Central visual: a large circular progress indicator showing the overall completion percentage. Use SVG for the circular gauge (not a library — simple SVG circle with stroke-dasharray/dashoffset trick). Colors: green (80%+), amber (50-79%), red (<50%).
    - Completion percentage displayed large and centered inside the circle.
    - Below the gauge: summary.items rendered as stat cards (total teachers, total students, missing items count, etc).
    - If rows contain missing items, show a warning section: "פריטים חסרים" with the list.
    - Card wrapper with title "מוכנות לדיווח למשרד".
    - Fallback: if summary doesn't have a completion percentage, compute from rows or show "לא זמין".

    **3. Create YearComparisonToggle.tsx:**
    Component enables year-over-year comparison for reports that support it. Props:
    ```typescript
    interface YearComparisonToggleProps {
      enabled: boolean
      onToggle: (enabled: boolean) => void
      comparisonYearId: string | null
      onComparisonYearChange: (yearId: string) => void
    }
    ```
    - Toggle switch (use the existing Switch component from ui/switch.tsx) with label "השוואה לשנה קודמת".
    - When enabled: show a school year dropdown for selecting the comparison year (populated from useSchoolYear() hook, excluding the current year).
    - Visual indicator that comparison mode is active (subtle blue border/background on the toggle area).

    **4. Wire year comparison into ReportViewer.tsx:**
    - Add `comparisonEnabled` and `comparisonYearId` state to ReportViewer.
    - Render `<YearComparisonToggle>` in the filters area (next to ReportFiltersBar).
    - When comparison is enabled and a comparison year is selected, add `comparisonYearId` to the API request params.
    - When the response includes `comparisonRows`, render a second DefaultTableRenderer below the main one with a "נתוני השוואה — {comparison year name}" heading, or render comparison rows inline with a different background color.
    - Only show the toggle for reports that support comparison. Check: the backend year-over-year-comparison generator is the main one, but the toggle should be shown whenever `comparisonRows` is a field in the response or the report's params include `comparisonYearId`. Simplest: always show the toggle and let the backend handle it — if the generator doesn't support comparison, it will ignore the param and no comparisonRows will be in the response.
  </action>
  <verify>
    - `ls /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/reports/TeacherHoursChart.tsx /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/reports/MinistryReadinessGauge.tsx /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/reports/YearComparisonToggle.tsx` confirms files exist
    - `grep "BarChart\|recharts" /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/reports/TeacherHoursChart.tsx` confirms chart usage
    - `grep "stroke-dasharray\|circle\|gauge" /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/reports/MinistryReadinessGauge.tsx` confirms gauge SVG
    - `grep "comparisonYearId" /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/ReportViewer.tsx` confirms comparison wiring
    - Build check: `cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && npx tsc --noEmit --pretty 2>&1 | head -30`
  </verify>
  <done>
    - Teacher Hours Summary report shows a stacked bar chart above the table
    - Ministry Readiness Audit report shows a circular completion gauge above the table
    - Year comparison toggle appears on report viewer; when enabled and a comparison year is selected, comparison data shows alongside current data
    - TypeScript compilation passes for all new components
  </done>
</task>

</tasks>

<verification>
- Navigate to /reports/teacher-hours-summary — see bar chart visualization above the table
- Navigate to /reports/ministry-readiness-audit — see circular gauge above the table
- Filter bar shows school year selector and updates data on change
- Export Excel button downloads an .xlsx file
- Export PDF button downloads a .pdf file
- Year comparison toggle shows comparison data when enabled
- TypeScript compilation passes
</verification>

<success_criteria>
1. Report viewer has a complete shell: header with back link + title + export, filters bar, content area
2. Excel and PDF export buttons trigger file downloads
3. TeacherHoursChart renders a recharts bar chart for teacher-hours-summary report
4. MinistryReadinessGauge renders a circular SVG gauge for ministry-readiness-audit report
5. Year comparison toggle enables side-by-side data display with comparison year selector
6. All text is in Hebrew, UI matches existing design system
</success_criteria>

<output>
After completion, create `.planning/phases/56-frontend-reports-ui/56-02-SUMMARY.md`
</output>
