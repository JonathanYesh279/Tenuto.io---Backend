---
phase: 76-attendance-management-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/App.tsx
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/Sidebar.tsx
autonomous: true
must_haves:
  truths:
    - "apiService exports attendanceAlertService with getDashboard, getFlaggedStudents, getStudentSummary methods"
    - "/attendance route is registered in App.tsx with ProtectedRoute admin-only guard"
    - "Sidebar admin navigation links 'נוכחות' to /attendance instead of /teachers"
  artifacts:
    - path: "src/services/apiService.js"
      provides: "attendanceAlertService export with 3 API methods"
      contains: "attendanceAlertService"
    - path: "src/App.tsx"
      provides: "/attendance route with lazy import and ProtectedRoute"
      contains: "AttendanceManagement"
    - path: "src/components/Sidebar.tsx"
      provides: "Admin attendance link pointing to /attendance"
      contains: "href: '/attendance'"
  key_links:
    - from: "src/services/apiService.js"
      to: "/api/attendance-alerts/dashboard"
      via: "apiClient.get"
      pattern: "attendance-alerts/dashboard"
    - from: "src/App.tsx"
      to: "src/pages/AttendanceManagement.tsx"
      via: "lazyWithRetry import"
      pattern: "AttendanceManagement"
    - from: "src/components/Sidebar.tsx"
      to: "/attendance"
      via: "href property"
      pattern: "href.*attendance"
---

<objective>
Wire API service methods for attendance-alerts and register the /attendance route with sidebar navigation.

Purpose: Establish the infrastructure (API client methods, routing, navigation) before the page component is built in Plan 02.
Output: attendanceAlertService in apiService.js, /attendance route in App.tsx, sidebar link fix.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-attendance-management-page/76-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add attendanceAlertService to apiService.js</name>
  <files>/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js</files>
  <action>
Add a new `attendanceAlertService` export to apiService.js, placed after the existing `attendanceService` export (around line 5110). Follow the same pattern as other service exports (e.g., `hoursSummaryService`).

Methods to add:

1. `getDashboard(options = {})` - calls `GET /attendance-alerts/dashboard` with optional `startDate` and `endDate` query params. Build params object, pass to `apiClient.get('/attendance-alerts/dashboard', params)`.

2. `getFlaggedStudents(orchestraId)` - calls `GET /attendance-alerts/orchestra/${orchestraId}/flagged`. Returns array of flagged student objects.

3. `getStudentSummary(studentId)` - calls `GET /attendance-alerts/student/${studentId}/summary`. Returns student attendance summary.

4. `getSettings()` - calls `GET /attendance-alerts/settings`. Returns alert threshold settings.

Follow the existing error handling pattern used by other services (the apiClient already handles auth headers and error responses). Do NOT add try/catch within these methods -- the apiClient layer handles that.

Also add `attendanceAlertService` to the default export object at the bottom of the file if one exists, or ensure it is a named export accessible via `import { attendanceAlertService } from '../services/apiService'`.
  </action>
  <verify>
Grep apiService.js for "attendanceAlertService" -- should find the export declaration and all 4 methods. Verify it follows the same pattern as nearby service exports (e.g., `hoursSummaryService`).
  </verify>
  <done>attendanceAlertService is exported from apiService.js with getDashboard, getFlaggedStudents, getStudentSummary, and getSettings methods, all calling the correct /attendance-alerts/* endpoints.</done>
</task>

<task type="auto">
  <name>Task 2: Register /attendance route in App.tsx and fix Sidebar navigation</name>
  <files>/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/App.tsx, /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/Sidebar.tsx</files>
  <action>
**App.tsx changes:**

1. Add lazy import at the top with the other page imports (around line 20-40):
   `const AttendanceManagement = lazyWithRetry(() => import('./pages/AttendanceManagement'), 'AttendanceManagement')`

2. Add the route in the admin routes section (near the other admin-only routes like /reports, /settings). Place it as a ProtectedRoute with admin-only access:
   ```
   <Route
     path="/attendance"
     element={
       <ProtectedRoute allowedRoles={['admin']}>
         <Layout>
           <Suspense fallback={<PageLoadingFallback message="טוען ניהול נוכחות..." />}>
             <AttendanceManagement />
           </Suspense>
         </Layout>
       </ProtectedRoute>
     }
   />
   ```
   Follow the exact same pattern used for other routes (Rehearsals, Reports, etc.).

**Sidebar.tsx changes:**

1. In the `adminNavigation` array (around line 58), change the attendance item's href from `'/teachers'` to `'/attendance'`:
   `{ name: 'נוכחות', href: '/attendance', Icon: UserCircleCheckIcon, category: 'operations', roles: ['admin'] }`

2. In the `getNavigation` function (around line 193-216), update the default admin attendance href from `'/teachers'` to `'/attendance'`. The smart-redirect logic for admin users who also have teacher/conductor roles should remain -- those still redirect to profile tabs. Only change the default fallback (line ~202) from `'/teachers'` to `'/attendance'`.
  </action>
  <verify>
1. Grep App.tsx for "AttendanceManagement" -- should find lazy import and route element.
2. Grep Sidebar.tsx for "/attendance" -- should find the new href.
3. Grep Sidebar.tsx for "/teachers" in the attendance context -- should NOT find it (replaced).
  </verify>
  <done>/attendance route is registered with lazy import and admin-only ProtectedRoute. Sidebar admin navigation links "נוכחות" to /attendance (default). Smart-redirect for dual-role admins still goes to profile tabs.</done>
</task>

</tasks>

<verification>
- `grep -n "attendanceAlertService" src/services/apiService.js` shows export with methods
- `grep -n "AttendanceManagement" src/App.tsx` shows lazy import and route
- `grep -n "'/attendance'" src/components/Sidebar.tsx` shows updated navigation
- No TypeScript compilation errors from the changes (the page file won't exist yet, but the lazy import handles that gracefully at build time)
</verification>

<success_criteria>
- attendanceAlertService exported with 4 methods calling correct endpoints
- /attendance route registered with admin-only ProtectedRoute
- Sidebar "נוכחות" links to /attendance for admin users
- No regressions to existing routes or navigation
</success_criteria>

<output>
After completion, create `.planning/phases/76-attendance-management-page/76-01-SUMMARY.md`
</output>
