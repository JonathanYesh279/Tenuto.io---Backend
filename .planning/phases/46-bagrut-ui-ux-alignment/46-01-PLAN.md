---
phase: 46-bagrut-ui-ux-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Bagruts.tsx
autonomous: true

must_haves:
  truths:
    - "Bagrut list page uses SearchInput component instead of inline <input> search"
    - "Bagrut list page uses FilterPanel (horizontal variant) instead of inline <select> dropdowns"
    - "New grade filter (כיתה) allows filtering bagruts by student class/grade level"
    - "New age filter allows filtering bagruts by student age range"
    - "All filter values persist in URL search params and survive page refresh"
    - "EmptyState component replaces custom inline empty markup"
    - "ErrorState component replaces custom inline error markup"
    - "TableSkeleton replaces CircleNotch spinner for loading state"
  artifacts:
    - path: "src/pages/Bagruts.tsx"
      provides: "Modernized bagrut list page with FilterPanel, SearchInput, EmptyState, ErrorState, TableSkeleton, and URL param persistence"
      min_lines: 500
  key_links:
    - from: "src/pages/Bagruts.tsx"
      to: "src/components/filters/FilterPanel.tsx"
      via: "import and render with horizontal variant"
      pattern: "FilterPanel.*variant.*horizontal"
    - from: "src/pages/Bagruts.tsx"
      to: "src/components/ui/SearchInput.tsx"
      via: "import and render"
      pattern: "SearchInput"
    - from: "src/pages/Bagruts.tsx"
      to: "src/components/feedback/Skeleton.tsx"
      via: "import TableSkeleton"
      pattern: "TableSkeleton"
    - from: "src/pages/Bagruts.tsx"
      to: "src/components/feedback/EmptyState.tsx"
      via: "import EmptyState"
      pattern: "EmptyState"
    - from: "src/pages/Bagruts.tsx"
      to: "src/components/feedback/ErrorState.tsx"
      via: "import ErrorState"
      pattern: "ErrorState"
---

<objective>
Modernize the Bagrut list page (Bagruts.tsx) to use the same component patterns as Students/Teachers pages: SearchInput, FilterPanel, EmptyState, ErrorState, TableSkeleton, and useSearchParams for URL persistence. Add new grade (כיתה) and age filters.

Purpose: Align Bagrut pages with the modern UI patterns established in other list pages for consistent UX across the application.
Output: Updated Bagruts.tsx with modern component usage and new filter capabilities.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Reference these files for the modern patterns to follow:
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Students.tsx (uses SearchInput, TableSkeleton, EmptyState, ErrorState, useSearchParams)
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/filters/FilterPanel.tsx (FilterPanel with horizontal variant)
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/ui/SearchInput.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/feedback/Skeleton.tsx (TableSkeleton)
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/feedback/EmptyState.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/feedback/ErrorState.tsx

The file being modified:
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Bagruts.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace loading, error, and empty states with modern components</name>
  <files>/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Bagruts.tsx</files>
  <action>
In Bagruts.tsx, make the following changes:

1. **Add imports** (replace existing ones where needed):
   - `import { useSearchParams } from 'react-router-dom'` (add useSearchParams)
   - `import { SearchInput } from '../components/ui/SearchInput'`
   - `import { TableSkeleton } from '../components/feedback/Skeleton'`
   - `import { EmptyState } from '../components/feedback/EmptyState'`
   - `import { ErrorState } from '../components/feedback/ErrorState'`
   - `import FilterPanel from '../components/filters/FilterPanel'` with types `FilterGroup, FilterState`
   - Remove MagnifyingGlassIcon from phosphor imports (SearchInput handles its own icon)
   - Remove WarningCircleIcon from phosphor imports (ErrorState handles its own icon)

2. **Replace loading state** (lines ~424-433 area):
   Replace the CircleNotch spinner div with:
   ```tsx
   if (loading || loadingAdditionalData) {
     return <TableSkeleton rows={8} cols={6} />
   }
   ```

3. **Replace error state** (lines ~435-452 area):
   Replace the inline error div with:
   ```tsx
   if (error) {
     return <ErrorState message={error} onRetry={() => { clearError(); loadData() }} />
   }
   ```

4. **Replace empty state** (lines ~621-638 area):
   Replace the inline empty div with EmptyState component:
   ```tsx
   {filteredBagruts.length === 0 && !loading && !loadingAdditionalData && (
     <EmptyState
       title={isTeacherRole && totalBagruts === 0
         ? 'אין תלמידים עם תהליכי בגרות פעילים'
         : 'לא נמצאו בגרויות התואמות לחיפוש'}
       description={isTeacherRole && totalBagruts === 0
         ? 'כאשר תלמידיך יירשמו לבגרויות, הן יופיעו כאן'
         : undefined}
       icon={<FileTextIcon size={48} weight="regular" />}
     />
   )}
   ```
  </action>
  <verify>
    File compiles without TypeScript errors: `cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && npx tsc --noEmit src/pages/Bagruts.tsx 2>&1 | head -20`
  </verify>
  <done>Loading state shows TableSkeleton rows, error state uses ErrorState component with retry, empty state uses EmptyState component with contextual messaging</done>
</task>

<task type="auto">
  <name>Task 2: Replace inline search and filters with SearchInput, FilterPanel, and useSearchParams</name>
  <files>/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Bagruts.tsx</files>
  <action>
In the same Bagruts.tsx file, continue with these changes:

1. **Add useSearchParams state initialization** (following Students.tsx pattern):
   ```tsx
   const [searchParams, setSearchParams] = useSearchParams()
   ```
   Initialize filter state from URL params:
   ```tsx
   const [searchTerm, setSearchTerm] = useState(searchParams.get('search') || '')
   const [filters, setFilters] = useState<FilterState>({
     status: searchParams.get('status') || '',
     teacherId: searchParams.get('teacher') || '',
     conservatory: searchParams.get('conservatory') || '',
     grade: searchParams.get('grade') || '',
     age: searchParams.get('age') ? JSON.parse(searchParams.get('age')!) : { min: '', max: '' }
   })
   ```

2. **Add URL sync effect** (following Students.tsx pattern):
   ```tsx
   useEffect(() => {
     const params = new URLSearchParams()
     if (searchTerm) params.set('search', searchTerm)
     if (filters.status) params.set('status', filters.status)
     if (filters.teacherId) params.set('teacher', filters.teacherId)
     if (filters.conservatory) params.set('conservatory', filters.conservatory)
     if (filters.grade) params.set('grade', filters.grade)
     if (filters.age?.min || filters.age?.max) params.set('age', JSON.stringify(filters.age))
     if (viewMode !== 'table') params.set('view', viewMode)
     const newSearch = params.toString()
     const currentSearch = searchParams.toString()
     if (newSearch !== currentSearch) {
       setSearchParams(params, { replace: true })
     }
   }, [searchTerm, filters, viewMode])
   ```

3. **Replace inline search input** (lines ~511-523) with SearchInput component:
   ```tsx
   <SearchInput
     value={searchTerm}
     onChange={setSearchTerm}
     onClear={() => setSearchTerm('')}
     placeholder="חיפוש לפי שם תלמיד, מורה או קונסרבטוריון..."
     className="flex-1"
   />
   ```

4. **Replace inline select dropdowns** (lines ~526-559) with FilterPanel using horizontal variant:
   Build the filterGroups array dynamically:
   ```tsx
   const filterGroups: FilterGroup[] = [
     {
       key: 'status',
       label: 'סטטוס',
       type: 'select',
       options: [
         { value: 'completed', label: 'הושלם' },
         { value: 'pending', label: 'בתהליך' }
       ]
     },
     {
       key: 'teacherId',
       label: 'מורה',
       type: 'select',
       options: teachers.map(t => ({
         value: t._id,
         label: getDisplayName(t.personalInfo)
       }))
     },
     {
       key: 'conservatory',
       label: 'קונסרבטוריון',
       type: 'select',
       options: [...new Set(bagruts.map(b => b.conservatoryName).filter(Boolean))].map(name => ({
         value: name,
         label: name
       }))
     },
     {
       key: 'grade',
       label: 'כיתה',
       type: 'select',
       options: getUniqueGrades().map(g => ({ value: g, label: g }))
     },
     {
       key: 'age',
       label: 'גיל',
       type: 'range',
       min: 10,
       max: 25
     }
   ]
   ```

   Add helper function to extract unique grades from students:
   ```tsx
   const getUniqueGrades = () => {
     const grades = new Set<string>()
     bagrutSource.forEach(bagrut => {
       const student = students.find(s => s._id === bagrut.studentId)
       const grade = student?.personalInfo?.class || student?.academicInfo?.class
       if (grade) grades.add(String(grade))
     })
     return [...grades].sort()
   }
   ```

   Replace the Card containing inline filters with:
   ```tsx
   <div className="flex flex-col gap-4">
     <div className="flex gap-4">
       <SearchInput
         value={searchTerm}
         onChange={setSearchTerm}
         onClear={() => setSearchTerm('')}
         placeholder="חיפוש לפי שם תלמיד, מורה או קונסרבטוריון..."
         className="flex-1"
       />
     </div>
     <FilterPanel
       filters={filterGroups}
       values={filters}
       onChange={(newValues) => setFilters(newValues as typeof filters)}
       onReset={() => setFilters({ status: '', teacherId: '', conservatory: '', grade: '', age: { min: '', max: '' } })}
       variant="horizontal"
     />
   </div>
   ```

5. **Update filtering logic** (the `filteredBagruts` computation) to include grade and age filters:
   Add these to the filter chain:
   ```tsx
   const matchesGrade = !filters.grade || (() => {
     const student = students.find(s => s._id === bagrut.studentId)
     const studentGrade = student?.personalInfo?.class || student?.academicInfo?.class
     return String(studentGrade) === filters.grade
   })()

   const matchesAge = (!filters.age?.min && !filters.age?.max) || (() => {
     const student = students.find(s => s._id === bagrut.studentId)
     const birthDate = student?.personalInfo?.birthDate || student?.personalInfo?.dateOfBirth
     if (!birthDate) return false
     const age = Math.floor((Date.now() - new Date(birthDate).getTime()) / (365.25 * 24 * 60 * 60 * 1000))
     const minAge = filters.age?.min ? Number(filters.age.min) : 0
     const maxAge = filters.age?.max ? Number(filters.age.max) : 999
     return age >= minAge && age <= maxAge
   })()
   ```
   Add `&& matchesGrade && matchesAge` to the return statement.

6. **Remove the `isCompleted` filter from state** since it duplicated the status filter. The old `filters.isCompleted` logic is now handled by the `status` filter.

7. **Remove the old Card import** if no longer used elsewhere in the file (it was only used for the filter wrapper). Keep it if StatsCard or other components still use it.
  </action>
  <verify>
    File compiles: `cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && npx tsc --noEmit src/pages/Bagruts.tsx 2>&1 | head -20`
    Dev server runs: `cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && npx vite build --mode development 2>&1 | tail -5`
  </verify>
  <done>SearchInput replaces inline search field, FilterPanel (horizontal) replaces inline select dropdowns, grade and age filters are available and functional, all filter values persist in URL search params via useSearchParams</done>
</task>

</tasks>

<verification>
1. Open `/bagruts` page — should show TableSkeleton during load, then data
2. Apply status filter — URL updates with `?status=completed`
3. Type in search — URL updates with `?search=...`
4. Select grade filter — only bagruts for students in that class show
5. Set age range — only bagruts for students in that age range show
6. Refresh page — all filters persist from URL params
7. Clear all filters — reset button works, URL clears
8. Empty result set shows EmptyState component (not inline div)
9. If API error, ErrorState component shows with retry button
</verification>

<success_criteria>
- Bagruts.tsx uses SearchInput, FilterPanel (horizontal), TableSkeleton, EmptyState, ErrorState
- Grade and age filters work correctly with client-side student data
- URL search params persist all filter values across page refresh
- No TypeScript compilation errors
- Visual consistency with Students/Teachers pages
</success_criteria>

<output>
After completion, create `.planning/phases/46-bagrut-ui-ux-alignment/46-01-SUMMARY.md`
</output>
