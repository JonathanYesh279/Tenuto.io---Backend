---
phase: 69-css-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/components.css
  - src/styles/orchestra-enrollment.css
  - src/styles/teacher-management.css
  - src/index.css
autonomous: true

must_haves:
  truths:
    - "Dead CSS files (components.css, orchestra-enrollment.css, teacher-management.css) no longer exist on disk"
    - "index.css no longer imports components.css"
    - "Legacy .btn, .input, .card blocks are removed from index.css"
    - "Scrollbar utilities (.scrollbar-hide, .custom-scrollbar) and animation utilities (.animate-slide-down, .animate-fade-in, .animate-zoom-in) remain in index.css"
    - "The app builds without errors after deletion"
  artifacts:
    - path: "src/styles/components.css"
      provides: "DELETED"
    - path: "src/styles/orchestra-enrollment.css"
      provides: "DELETED"
    - path: "src/styles/teacher-management.css"
      provides: "DELETED"
    - path: "src/index.css"
      provides: "Clean index.css with dead code removed, active utilities and imports preserved"
  key_links:
    - from: "src/index.css"
      to: "src/styles/tab-navigation-fix.css"
      via: "@import"
      pattern: "@import.*tab-navigation-fix"
    - from: "src/index.css"
      to: "src/styles/teacher-modal-fixes.css"
      via: "@import"
      pattern: "@import.*teacher-modal-fixes"
    - from: "src/index.css"
      to: "src/styles/simple-weekly-grid.css"
      via: "@import"
      pattern: "@import.*simple-weekly-grid"
---

<objective>
Delete all dead CSS files and remove dead code blocks from index.css, reducing ~1,125 lines of unused CSS.

Purpose: Eliminate dead code that was superseded by CVA components (Phase 67) and the token system (Phase 66). The files themselves mark themselves as deprecated and targeted for Phase 69 removal.

Output: 3 CSS files deleted, index.css cleaned of legacy .btn/.input/.card blocks and the components.css import.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-css-cleanup/69-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete 3 dead CSS files and remove components.css import from index.css</name>
  <files>
    src/styles/components.css
    src/styles/orchestra-enrollment.css
    src/styles/teacher-management.css
    src/index.css
  </files>
  <action>
All work in frontend repo: /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src

1. Delete these 3 files entirely (confirmed zero TSX class references by research grep):
   - src/styles/components.css (575 lines, all legacy .btn-*/.card-*/.badge-*/.modal-* classes superseded by CVA components)
   - src/styles/orchestra-enrollment.css (343 lines, not imported anywhere, zero class refs)
   - src/styles/teacher-management.css (157 lines, not imported anywhere, zero class refs)

2. In src/index.css, remove the `@import './styles/components.css'` line. Do NOT remove or reorder the other 3 imports (tab-navigation-fix.css, teacher-modal-fixes.css, simple-weekly-grid.css) -- those are active.

3. In src/index.css, remove the legacy base class blocks (~50 lines):
   - The `.btn` block (marked DEPRECATED)
   - The `.input` block (marked DEPRECATED)
   - The `.card` block (marked DEPRECATED)
   Keep the scrollbar utilities (.scrollbar-hide, .custom-scrollbar) -- actively used by 18 occurrences across 12 files.
   Keep the animation utilities (.animate-slide-down, .animate-fade-in, .animate-zoom-in) -- actively used by 5 components.

4. Do NOT touch tailwind.config.js addComponents block -- out of scope per research recommendation.

IMPORTANT: Do NOT reorder remaining @import statements in index.css. CSS cascade order matters.
  </action>
  <verify>
Run from frontend repo root:
- `ls src/styles/components.css src/styles/orchestra-enrollment.css src/styles/teacher-management.css 2>&1` should show "No such file" for all 3
- `grep -c "components.css" src/index.css` should return 0
- `grep -c "\.btn\b" src/index.css` should return 0 (no legacy .btn block)
- `grep -c "scrollbar-hide" src/index.css` should return >= 1 (scrollbar utilities preserved)
- `grep -c "animate-slide-down" src/index.css` should return >= 1 (animation utilities preserved)
- `grep -c "tab-navigation-fix\|teacher-modal-fixes\|simple-weekly-grid" src/index.css` should return 3 (active imports preserved)
- `npx tsc --noEmit` or `npm run build` completes without errors
  </verify>
  <done>
3 dead CSS files deleted (1,075 lines removed). Legacy .btn/.input/.card blocks removed from index.css (~50 more lines). components.css import removed. Active imports and utilities preserved. App builds cleanly.
  </done>
</task>

</tasks>

<verification>
- No dead CSS files remain in src/styles/
- index.css still imports exactly 3 active CSS files (tab-navigation-fix, teacher-modal-fixes, simple-weekly-grid)
- Scrollbar and animation utilities intact
- App builds without errors
</verification>

<success_criteria>
~1,125 lines of dead CSS removed. Zero functional impact (all deleted code had zero TSX references). Build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/69-css-cleanup/69-01-SUMMARY.md`
</output>
