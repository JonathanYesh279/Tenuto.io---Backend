---
phase: 62-rehearsal-calendar-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/RehearsalCalendar.tsx
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Rehearsals.tsx
autonomous: true

must_haves:
  truths:
    - "Calendar displays a day view showing hourly time slots with rehearsals positioned at their actual times"
    - "User can drag a rehearsal card from one day/time to another and it calls the update API to reschedule"
    - "Clicking an empty time slot in week or day view opens the create form pre-filled with the clicked date and time"
    - "Drag-and-drop reschedule shows conflict error if the new time/room/teacher conflicts"
  artifacts:
    - path: "src/components/RehearsalCalendar.tsx"
      provides: "Day view mode, drag-and-drop handlers, empty slot click handlers"
      min_lines: 200
    - path: "src/pages/Rehearsals.tsx"
      provides: "Day mode toggle, onSlotClick -> create form with pre-filled date/time, onDrop -> update API call"
      min_lines: 50
  key_links:
    - from: "RehearsalCalendar.tsx DayView onDrop"
      to: "Rehearsals.tsx handleDragReschedule"
      via: "callback prop"
      pattern: "onReschedule"
    - from: "RehearsalCalendar.tsx onSlotClick"
      to: "Rehearsals.tsx setShowCreateForm with pre-filled date/time"
      via: "callback prop"
      pattern: "onEmptySlotClick"
---

<objective>
Add day view mode, drag-and-drop rescheduling, and quick-add from empty time slots to the rehearsal calendar.

Purpose: Users need to manage rehearsals visually on an interactive calendar with intuitive drag-to-reschedule and click-to-create workflows (RCAL-01, RCAL-03).
Output: Enhanced RehearsalCalendar with 3 view modes and interactive scheduling.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/RehearsalCalendar.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Rehearsals.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/RehearsalForm.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/utils/rehearsalUtils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DayView component and drag-and-drop to RehearsalCalendar</name>
  <files>
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/RehearsalCalendar.tsx
  </files>
  <action>
Add a DayView component inside RehearsalCalendar.tsx that shows a single day with hourly time slots from 07:00 to 22:00 (vertical layout, each hour row ~60px). Rehearsal cards are positioned absolutely within the day column based on their startTime/endTime, spanning the correct height. The existing RehearsalCard component should be reused.

Update the RehearsalCalendarProps interface:
- Add `viewMode: 'week' | 'month' | 'day'` (replace the existing `'week' | 'month'`)
- Add `onEmptySlotClick?: (date: Date, time: string) => void` callback — fires when user clicks empty space in a time slot
- Add `onReschedule?: (rehearsalId: string, newDate: string, newStartTime: string, newEndTime: string) => void` callback — fires when a drag-drop completes

Implement HTML5 drag-and-drop on rehearsal cards in week and day views:
- Set `draggable={true}` on RehearsalCard wrapper in week/day views (NOT month view — too compact)
- `onDragStart`: store rehearsal._id and original time info in dataTransfer
- On day cells in WeekView and hour slots in DayView: `onDragOver` (preventDefault to allow drop), `onDrop` handler that:
  - Reads rehearsal ID from dataTransfer
  - Calculates new date from the target cell's date
  - For DayView: calculates new startTime from the target hour slot, preserves original duration to compute endTime
  - For WeekView: changes date only (keeps same time), since week cells don't have granular time slots
  - Calls `onReschedule` with the computed values

For empty slot click in WeekView: add onClick to each day cell that checks `e.target === e.currentTarget` (only fires when clicking empty space, not on a rehearsal card). Call `onEmptySlotClick(day.date, '19:00')` with default time.

For empty slot click in DayView: each hour slot row is clickable. When clicked on empty space, call `onEmptySlotClick(currentDate, hourString)` where hourString is the slot's hour (e.g., '14:00').

Update the calendarData computation to handle `viewMode === 'day'` — return a single DayData object for the current date.

Update the header to show day name + date for day view. Add a calendar view toggle section that includes 'day' option.

RTL note: the calendar is Hebrew/RTL. Ensure drag feedback respects RTL layout.
  </action>
  <verify>
Build the frontend with `cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && npx tsc --noEmit` to check for TypeScript errors.
  </verify>
  <done>
DayView shows hourly slots 07-22 with rehearsals positioned at correct times. Rehearsal cards are draggable in week and day views. Empty slot clicks fire onEmptySlotClick with date and time. Drag-drop fires onReschedule with new date/time.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire calendar interactions in Rehearsals page</name>
  <files>
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Rehearsals.tsx
  </files>
  <action>
Update the Rehearsals page to support the new calendar interactions:

1. Update `calendarView` state to include 'day': `useState<'week' | 'month' | 'day'>('month')`

2. Add a `handleDragReschedule` async function that:
   - Accepts `(rehearsalId: string, newDate: string, newStartTime: string, newEndTime: string)`
   - Finds the rehearsal in the local state to get its groupId and other required fields
   - Calls `rehearsalService.updateRehearsal(rehearsalId, { ...existingFields, date: newDate, startTime: newStartTime, endTime: newEndTime })`
   - On success: calls `loadData()` to refresh
   - On conflict error (409 or error.code === 'CONFLICT'): shows error message in the existing error state with details like "התנגשות בלוח הזמנים" plus conflict details if available
   - On other error: shows generic error

3. Add a `handleEmptySlotClick` function that:
   - Accepts `(date: Date, time: string)`
   - Pre-fills the single form initial data: sets `singleForm` or creates initialData-like state with the date (formatted as YYYY-MM-DD) and startTime (the clicked time), endTime (startTime + 2 hours, capped at 23:00)
   - Opens the create form: `setShowCreateForm(true)`
   - Important: need a new state `const [preFilledData, setPreFilledData] = useState<Partial<RehearsalFormData> | undefined>(undefined)` that is passed to RehearsalForm's `initialData` prop ONLY when opening from slot click. Clear it when form closes.

4. Update the calendar view toggle in the filters section to include 'יום' (day) button alongside 'שבוע' and 'חודש'.

5. Pass the new props to RehearsalCalendar:
   ```
   onEmptySlotClick={handleEmptySlotClick}
   onReschedule={handleDragReschedule}
   ```

6. Update RehearsalForm usage: when `showCreateForm` is true with `preFilledData`, pass `preFilledData` as a prop that sets the initial date/time fields but does NOT make the form an "edit" form (no initialData prop that disables orchestra selection). This means adding a new prop to RehearsalForm like `defaultValues?: Partial<RehearsalFormData>` that pre-fills fields without triggering edit mode. Alternatively, pass `initialData` but ensure RehearsalForm treats it as "pre-fill" not "edit" — the simplest approach is to check if `initialData` has a `groupId` to determine edit vs pre-fill mode. Since pre-filled data from slot click won't have groupId, this works naturally.

7. When the create form closes, clear preFilledData: `setPreFilledData(undefined)`.
  </action>
  <verify>
Build the frontend with `cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && npx tsc --noEmit` to check for TypeScript errors.
  </verify>
  <done>
Day view toggle works. Clicking empty slot opens create form pre-filled with clicked date/time. Dragging a rehearsal to a new slot calls updateRehearsal API and refreshes on success, shows conflict error on 409.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes without errors
2. Calendar renders in day, week, and month modes
3. Day view shows hourly time slots with rehearsals at correct positions
4. Dragging a rehearsal in week view changes its date
5. Dragging a rehearsal in day view changes its time
6. Clicking empty space in week/day view opens create form with pre-filled date/time
7. Drag-to-conflicting-slot shows error message
</verification>

<success_criteria>
- Three calendar view modes (day/week/month) all render correctly
- Drag-and-drop rescheduling calls PUT /api/rehearsals/:id with new date/time
- Empty slot click opens creation form with pre-filled date and time
- Conflict errors from rescheduling are surfaced to the user
</success_criteria>

<output>
After completion, create `.planning/phases/62-rehearsal-calendar-ux/62-01-SUMMARY.md`
</output>
