---
phase: 72-theory-lesson-entity-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - api/theory/theory.controller.js
  - services/permissionService.js
  - api/export/ministry-mappers.js
  - validate-api-schemas.js
autonomous: true

must_haves:
  truths:
    - "Theory controller does not have redundant inline role checks (RBAC middleware handles authorization)"
    - "Permission service maps 'תאוריה' role to theory permissions (not old 'מורה תאוריה' key)"
    - "Ministry export mapper correctly identifies theory teachers with both old and new role strings"
    - "validate-api-schemas.js VALID_RULES matches current TEACHER_ROLES"
  artifacts:
    - path: "api/theory/theory.controller.js"
      provides: "Theory lesson CRUD controller"
      contains: "req.context"
    - path: "services/permissionService.js"
      provides: "Role-to-permission mapping"
      contains: "תאוריה"
    - path: "api/export/ministry-mappers.js"
      provides: "Ministry report export logic"
      contains: "תאוריה"
    - path: "validate-api-schemas.js"
      provides: "Test utility with valid roles"
      contains: "תאוריה"
  key_links:
    - from: "middleware/tenant.middleware.js"
      to: "services/permissionService.js"
      via: "ROLE_RENAME_MAP normalizes before permission lookup"
      pattern: "resolveEffectivePermissions"
    - from: "api/theory/theory.route.js"
      to: "api/theory/theory.controller.js"
      via: "requirePermission middleware gates access before controller"
      pattern: "requirePermission.*theory"
---

<objective>
Fix backend authorization and legacy role references in theory controller, permission service, ministry export, and test utility.

Purpose: The theory controller has redundant inline auth checks using the old "מורה תאוריה" role string that bypass RBAC middleware normalization. The permission service key doesn't match the normalized role name. Ministry export mappers miss new-format teachers.

Output: Clean authorization flow — RBAC middleware handles all auth, permission service uses normalized key, ministry mapper handles both role strings for raw DB data.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-theory-lesson-entity-fixes/72-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove redundant inline auth from theory controller + fix permissionService key</name>
  <files>
    api/theory/theory.controller.js
    services/permissionService.js
  </files>
  <action>
    In theory.controller.js:
    - Line 791 (bulkDeleteTheoryLessonsByDate): Remove the entire `if (!isAdmin && !req.loggedinUser?.roles?.includes('מורה תאוריה') ...)` block.
      WHY safe to remove: The route `router.delete('/bulk-delete-by-date', requirePermission('theory', 'delete'), ...)` already enforces authorization. The inline check is redundant and uses the old role string.
      Add a comment where the check was: `// Authorization handled by requirePermission('theory', 'delete') middleware on route`

    - Line 849 (bulkDeleteTheoryLessonsByCategory): Same removal — remove the `if (!isAdmin && ...roles?.includes('מורה תאוריה')...)` block. Add same comment.

    - Lines 904-905 (bulkDeleteTheoryLessonsByTeacher): Same removal — remove the `if (!isAdmin && !(req.loggedinUser?.roles?.includes('מורה תאוריה') || ...))` block. Add same comment.

    IMPORTANT: Do NOT remove any non-auth logic from these functions. Only remove the role-check `if` blocks that return 403. Keep all other logic (parameter validation, DB operations, responses).

    In permissionService.js:
    - Line 145: Change the role key from `'מורה תאוריה'` to `'תאוריה'`.
      WHY safe: `tenant.middleware.js` normalizes roles via `ROLE_RENAME_MAP` BEFORE calling `resolveEffectivePermissions()`. By the time permission lookup happens, the role is already `'תאוריה'`. The old key `'מורה תאוריה'` would never be looked up post-normalization.
      Add a comment above: `// Normalized role name (legacy 'מורה תאוריה' normalized by middleware before lookup)`
  </action>
  <verify>
    grep -n "מורה תאוריה" api/theory/theory.controller.js
    # Should return 0 lines

    grep -n "מורה תאוריה" services/permissionService.js
    # Should return 0 lines

    grep -n "תאוריה" services/permissionService.js | head -5
    # Should show the new key 'תאוריה'

    # Run existing tests to verify nothing breaks:
    cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Backend && npm test 2>&1 | tail -20
  </verify>
  <done>Theory controller has zero inline role checks — all auth handled by requirePermission() RBAC middleware on routes. Permission service uses normalized 'תאוריה' key.</done>
</task>

<task type="auto">
  <name>Task 2: Fix ministry export mapper and validate-api-schemas test utility</name>
  <files>
    api/export/ministry-mappers.js
    validate-api-schemas.js
  </files>
  <action>
    In ministry-mappers.js (around line 188):
    - The current check is: `theory: subjects.includes('תאוריה') || roles.includes('מורה תאוריה')`
    - Change to: `theory: subjects.includes('תאוריה') || roles.includes('תאוריה') || roles.includes('מורה תאוריה')`
    - Add comment: `// Check both normalized and legacy role names (export reads raw DB data, not middleware-normalized)`
    WHY both strings: Unlike controller code that uses `req.context.userRoles` (normalized), the export mapper receives raw teacher documents from DB queries. Legacy teachers may have either role string.

    In validate-api-schemas.js (line 20):
    - Current: `const VALID_RULES = ['מורה', 'מנצח', 'מדריך הרכב', 'מנהל', 'מורה תאוריה', 'מגמה']`
    - Update to match current `TEACHER_ROLES` from `config/constants.js`. Read the constants file to get the exact current list.
    - Better yet: Import `TEACHER_ROLES` from `config/constants.js` instead of hardcoding, if the file structure allows it. If not practical, update the hardcoded array to match the current constant values exactly.
    - The key change: replace `'מורה תאוריה'` with `'תאוריה'`, and add any other roles that are in `TEACHER_ROLES` but missing from this array.
  </action>
  <verify>
    grep -n "מורה תאוריה" api/export/ministry-mappers.js
    # Should show only the backward-compat check (alongside 'תאוריה')

    grep -n "VALID_RULES\|TEACHER_ROLES" validate-api-schemas.js
    # Should show updated roles matching constants.js

    # Run tests:
    cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Backend && npm test 2>&1 | tail -20
  </verify>
  <done>Ministry export mapper identifies theory teachers with both normalized and legacy role strings. validate-api-schemas.js VALID_RULES matches current TEACHER_ROLES from config/constants.js.</done>
</task>

</tasks>

<verification>
1. grep -rn "מורה תאוריה" api/ services/ --include="*.js" — should only appear in ministry-mappers.js (backward-compat) and config/constants.js (ROLE_RENAME_MAP definition)
2. npm test — all existing tests pass
3. Theory controller functions contain only business logic, no inline role checks
</verification>

<success_criteria>
- Theory controller has zero redundant inline auth checks
- Permission service uses normalized 'תאוריה' key for permission mapping
- Ministry export correctly identifies theory teachers with both old and new role strings
- validate-api-schemas.js reflects current TEACHER_ROLES
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/72-theory-lesson-entity-fixes/72-02-SUMMARY.md`
</output>
