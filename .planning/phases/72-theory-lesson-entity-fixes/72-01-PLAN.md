---
phase: 72-theory-lesson-entity-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/TheoryLessonForm.tsx
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/BulkTheoryUpdateTab.tsx
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/TeacherForm.tsx
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Profile.tsx
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/TeacherCard.tsx
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/teachers/RoleDistributionPanel.tsx
autonomous: true

must_haves:
  truths:
    - "Theory lesson form teacher dropdown shows teachers with role 'תאוריה' (and legacy 'מורה תאוריה')"
    - "Bulk theory update tab filters teachers correctly by 'תאוריה' role"
    - "TeacherForm instrument requirement logic uses normalized 'תאוריה' role"
    - "Profile.tsx theory teacher tab check uses 'תאוריה' (no dead 'theory_teacher' string)"
    - "TeacherCard and RoleDistributionPanel retain backward-compat color entries for legacy display"
  artifacts:
    - path: "src/components/TheoryLessonForm.tsx"
      provides: "Theory teacher dropdown filter"
      contains: "תאוריה"
    - path: "src/components/BulkTheoryUpdateTab.tsx"
      provides: "Bulk theory teacher filter"
      contains: "תאוריה"
    - path: "src/components/TeacherForm.tsx"
      provides: "Instrument requirement validation"
      contains: "תאוריה"
    - path: "src/pages/Profile.tsx"
      provides: "Theory teacher tab visibility"
      contains: "תאוריה"
  key_links:
    - from: "src/components/TheoryLessonForm.tsx"
      to: "teacher API response"
      via: "roles filter in teacher dropdown"
      pattern: "roles.*includes.*תאוריה"
    - from: "src/components/BulkTheoryUpdateTab.tsx"
      to: "teacher API response"
      via: "roles filter for teacher selection"
      pattern: "roles.*includes.*תאוריה"
---

<objective>
Fix the critical teacher dropdown bug in theory lesson forms and normalize all frontend "מורה תאוריה" references to "תאוריה".

Purpose: Theory lesson forms show empty teacher dropdowns because the frontend filters for the old role string "מורה תאוריה" but new teachers are created with the normalized "תאוריה" role (since v1.7 RBAC migration). This blocks creating/editing theory lessons.

Output: All frontend role filters use "תאוריה" with backward-compat fallback for legacy DB records. Display-only color maps retain both entries.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-theory-lesson-entity-fixes/72-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix theory teacher dropdown filters (TheoryLessonForm, BulkTheoryUpdateTab)</name>
  <files>
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/TheoryLessonForm.tsx
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/BulkTheoryUpdateTab.tsx
  </files>
  <action>
    In TheoryLessonForm.tsx (around lines 169-171):
    - Change the teacher filter from `teacher.roles.includes('מורה תאוריה')` to `teacher.roles.some(r => r === 'תאוריה' || r === 'מורה תאוריה')`.
    - Update the comment on line 169 to: `// Filter teachers to show only those with "תאוריה" role (check legacy "מורה תאוריה" for pre-RBAC DB records)`

    In BulkTheoryUpdateTab.tsx (around line 112):
    - Change the teacher filter from `teacher.roles.includes('מורה תאוריה')` to `teacher.roles.some(r => r === 'תאוריה' || r === 'מורה תאוריה')`.
    - Add comment: `// Check both normalized and legacy role names for backward compatibility`

    WHY both strings: The API returns raw DB roles. Legacy teachers have "מורה תאוריה", new teachers have "תאוריה". Must match both until a DB migration normalizes all records.
  </action>
  <verify>
    grep -n "תאוריה" /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/TheoryLessonForm.tsx | head -5
    grep -n "תאוריה" /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/BulkTheoryUpdateTab.tsx | head -5
    # Both files should show 'תאוריה' as the primary check
  </verify>
  <done>TheoryLessonForm teacher dropdown matches teachers with either 'תאוריה' or legacy 'מורה תאוריה' role. BulkTheoryUpdateTab teacher filter does the same.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize remaining frontend "מורה תאוריה" references (TeacherForm, Profile, display maps)</name>
  <files>
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/TeacherForm.tsx
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Profile.tsx
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/TeacherCard.tsx
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/teachers/RoleDistributionPanel.tsx
  </files>
  <action>
    In TeacherForm.tsx:
    - Line 151: Change `formData.roles.includes('מורה תאוריה')` to `formData.roles.includes('תאוריה')`.
      Note: TeacherForm creates NEW teachers using VALID_ROLES from validationUtils.ts which already has 'תאוריה'. So formData.roles will always have the normalized name. No backward-compat needed here.
    - Line 153: Update the Hebrew message string — change `'מורה תאוריה'` to `'תאוריה'` in the validation message text.
    - Line 449: Change `formData.roles.includes('מורה תאוריה')` to `formData.roles.includes('תאוריה')`.
    - Line 457: Change `formData.roles.includes('מורה תאוריה')` to `formData.roles.includes('תאוריה')`.

    In Profile.tsx:
    - Line 180-181: Replace the `isTheoryTeacher` function body. Remove the dead `'theory_teacher'` check. Change to:
      ```
      return user?.roles?.includes('תאוריה') || user?.roles?.includes('מורה תאוריה')
      ```
      Keep backward-compat for legacy because Profile reads from the user's raw DB data.
    - Lines 261-264: In the role display switch/case, update:
      - Remove `case 'theory_teacher': return 'מורה תיאוריה'` (dead code, English role doesn't exist).
      - Keep `case 'מורה תיאוריה': return 'מורה תיאוריה'` if it exists for display.
      - Add `case 'תאוריה': return 'תאוריה'` if not already present.

    In TeacherCard.tsx:
    - Lines 69, 94: KEEP the `'מורה תאוריה'` entries in the color maps as backward-compat (these are display-only, coloring role badges for teachers with the legacy role string in DB).
    - Line 76: KEEP `'מורה תאוריה'` in the roleOrder array (it's display ordering, both old and new should be listed).
    - Add a comment above the backward-compat entries: `// Legacy role name — kept for backward compatibility with pre-RBAC DB records`

    In RoleDistributionPanel.tsx:
    - Line 17: KEEP the `'מורה תאוריה': '#10b981'` entry as backward-compat for display.
    - Add a `'תאוריה': '#10b981'` entry with the same color so normalized roles also display correctly.
    - Add comment: `// Both normalized and legacy role names for backward compatibility`
  </action>
  <verify>
    # Check no functional (non-display) references to old role remain:
    grep -n "מורה תאוריה" /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/TeacherForm.tsx
    # Should return 0 lines (or only in comments)

    grep -n "theory_teacher" /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Profile.tsx
    # Should return 0 lines

    # Verify TypeScript compiles:
    cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && npx tsc --noEmit --skipLibCheck 2>&1 | head -20
  </verify>
  <done>
    TeacherForm uses 'תאוריה' for all instrument requirement logic. Profile.tsx uses 'תאוריה' with legacy fallback, no dead 'theory_teacher' code. TeacherCard and RoleDistributionPanel have backward-compat color entries for both role strings. Zero functional (non-display) references to "מורה תאוריה" remain in frontend src/ except backward-compat color maps and comments.
  </done>
</task>

</tasks>

<verification>
1. grep -rn "מורה תאוריה" /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/ — only backward-compat color map entries and comments should remain
2. grep -rn "theory_teacher" /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/ — should return 0 results
3. TypeScript compilation passes without errors
</verification>

<success_criteria>
- Theory lesson form teacher dropdown will show teachers with role "תאוריה" (and legacy "מורה תאוריה")
- BulkTheoryUpdateTab teacher filter matches both role strings
- TeacherForm instrument validation uses normalized "תאוריה"
- No dead "theory_teacher" English role references remain
- Backward-compat display entries retained in color maps
</success_criteria>

<output>
After completion, create `.planning/phases/72-theory-lesson-entity-fixes/72-01-SUMMARY.md`
</output>
