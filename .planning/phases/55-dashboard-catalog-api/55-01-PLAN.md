---
phase: 55-dashboard-catalog-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/reports/report.dashboard.js
  - api/reports/report.controller.js
  - api/reports/report.route.js
autonomous: true
must_haves:
  truths:
    - "GET /api/reports/dashboard returns KPI cards with current values and trend deltas"
    - "Dashboard response includes alerts for anomalies (teachers without students, stale imports, data quality)"
    - "Each KPI card has a drillTo field mapping to a valid generator reportId"
  artifacts:
    - path: "api/reports/report.dashboard.js"
      provides: "Dashboard KPI computation, trend calculation, and alert generation"
      min_lines: 100
      exports: ["buildDashboard"]
    - path: "api/reports/report.controller.js"
      provides: "getDashboard controller method"
      contains: "getDashboard"
    - path: "api/reports/report.route.js"
      provides: "GET /dashboard route"
      contains: "/dashboard"
  key_links:
    - from: "api/reports/report.route.js"
      to: "api/reports/report.controller.js"
      via: "reportController.getDashboard"
      pattern: "reportController\\.getDashboard"
    - from: "api/reports/report.controller.js"
      to: "api/reports/report.dashboard.js"
      via: "buildDashboard import"
      pattern: "buildDashboard"
    - from: "api/reports/report.dashboard.js"
      to: "MongoDB collections"
      via: "getCollection for student, teacher, orchestra, hours_summary, import_log"
      pattern: "getCollection"
---

<objective>
Create the dashboard KPI endpoint that returns metric cards with trend indicators, drillTo navigation, and anomaly alerts.

Purpose: Administrators need an at-a-glance overview of conservatory health with trend data and actionable alerts before drilling into detailed reports.
Output: `GET /api/reports/dashboard` endpoint returning `{ kpis: [...], alerts: [...] }`
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@api/reports/report.controller.js
@api/reports/report.route.js
@api/reports/report.scope.js
@api/reports/report.orchestrator.js
@api/reports/report.registry.js
@api/reports/generators/data-quality.generator.js
@api/reports/generators/year-over-year-comparison.generator.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard service with KPI computation, trends, and alerts</name>
  <files>api/reports/report.dashboard.js</files>
  <action>
Create `report.dashboard.js` exporting `buildDashboard(scope, schoolYearId, { services })`.

IMPORTANT: The `scope` object from `buildReportScope` contains `{ type, tenantId }` only — it does NOT carry `schoolYearId`. Therefore `schoolYearId` is passed as a separate second argument (same pattern as how `params.schoolYearId` is separate in the orchestrator).

**KPI Cards** (6 cards, each with `{ id, label, value, unit, trend, drillTo }`):

1. **activeStudents** — count of `student` docs with `{ tenantId, isActive: true }`. Label: `תלמידים פעילים`. Unit: `count`. drillTo: `student-enrollment`.
2. **activeTeachers** — count of `teacher` docs with `{ tenantId, isActive: true }`. Label: `מורים פעילים`. Unit: `count`. drillTo: `teacher-roster`.
3. **totalWeeklyHours** — sum of `totals.totalWeeklyHours` from `hours_summary` docs with `{ tenantId, schoolYearId }`. Label: `שעות שבועיות`. Unit: `hours`. drillTo: `teacher-hours-summary`.
4. **orchestraCount** — count of `orchestra` docs with `{ tenantId, schoolYearId }`. Label: `הרכבים`. Unit: `count`. drillTo: `orchestra-participation`.
5. **assignmentRate** — percentage of active students that have at least one entry in `teacherAssignments`. Label: `אחוז שיבוץ`. Unit: `percentage`. drillTo: `student-teacher-assignments`.
6. **dataQualityScore** — 100 minus (number of high-severity anomalies), floored at 0. Count anomalies: students without assignments + teachers without students (same logic as data-quality generator but just counts). Label: `ציון איכות נתונים`. Unit: `score`. drillTo: `data-quality`.

**drillTo validation:** After defining the VALID_DRILL_TARGETS array (`['student-enrollment', 'teacher-roster', 'teacher-hours-summary', 'orchestra-participation', 'student-teacher-assignments', 'data-quality', 'import-history']`), add a module-level comment documenting that these must stay in sync with registered generator IDs. The runtime validation is not needed since the mapping is static (hardcoded KPI definitions), but the comment ensures future maintainers update both places.

**Trend Calculation:**
- Accept `schoolYearId` (current) as second argument. Query the `school_year` collection for the current tenant to find the previous year (sort by `createdAt` desc, pick the one after the current). If no previous year, trend is `null`.
- For each KPI, compute the same metric for the previous year. Trend object: `{ delta, direction }` where `delta` = current - previous as a percentage change (rounded to 1 decimal), and `direction` = `'up'` | `'down'` | `'stable'` (stable if |delta| < 1%).

**Alerts** (array of `{ id, type, severity, message, drillTo }`):
- `idle-teachers`: If any teacher has no assigned students (count > 0). severity: `warning`. message: `{count} מורים ללא תלמידים`. drillTo: `data-quality`.
- `unassigned-students`: If any student has no teacherAssignments (count > 0). severity: `warning`. message: `{count} תלמידים ללא שיבוץ`. drillTo: `data-quality`.
- `stale-imports`: Query `import_log` for tenant, find the most recent import by `createdAt`. If older than 30 days or no imports exist, severity: `info`. message: `לא בוצע ייבוא נתונים מעל 30 יום`. drillTo: `import-history`.
- `low-data-quality`: If dataQualityScore < 80. severity: `warning`. message: `ציון איכות נתונים נמוך: {score}`. drillTo: `data-quality`.

Only include alerts where the condition is true (non-empty array).

Use `getCollection` from services (same pattern as generators). For 'own' scope, return empty KPIs and no alerts (dashboard is admin-only but handle gracefully).

Queries should be efficient: use `countDocuments` where possible, aggregate only when needed. Run independent queries in parallel with `Promise.all`.
  </action>
  <verify>
1. `node -e "import('./api/reports/report.dashboard.js').then(m => console.log(Object.keys(m)))"` prints `['buildDashboard']`
2. Confirm function signature accepts 3 args: `(scope, schoolYearId, { services })`
3. `grep -c 'drillTo' api/reports/report.dashboard.js` returns 10+ (6 KPIs + 4 alerts)
4. `grep 'VALID_DRILL_TARGETS\|sync with.*generator' api/reports/report.dashboard.js` shows sync documentation
  </verify>
  <done>
`report.dashboard.js` exports `buildDashboard(scope, schoolYearId, { services })` that returns `{ kpis: Array<6>, alerts: Array<0-4> }` with trend data. schoolYearId is a separate argument (not embedded in scope). All drillTo values are documented as requiring sync with generator IDs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire dashboard endpoint into controller and routes</name>
  <files>api/reports/report.controller.js, api/reports/report.route.js</files>
  <action>
**report.controller.js:**
- Add import: `import { buildDashboard } from './report.dashboard.js';`
- Add import: `import { buildReportScope } from './report.scope.js';`
- Add import: `import { getCollection } from '../../services/mongoDB.service.js';`
- Add `getDashboard` to the exported controller object: `export const reportController = { getRegistry, getReport, getDashboard };`
- Implement `getDashboard(req, res, next)`:
  ```
  async function getDashboard(req, res, next) {
    try {
      const scope = buildReportScope(req.context);
      const schoolYearId = req.query.schoolYearId || req.context.schoolYearId;
      const { kpis, alerts } = await buildDashboard(scope, schoolYearId, {
        services: { getCollection },
      });
      res.json({ kpis, alerts });
    } catch (err) {
      next(err);
    }
  }
  ```
  Note: `schoolYearId` is extracted from `req.query` (override) or `req.context` (default from middleware), then passed separately to `buildDashboard` since `buildReportScope` does not include it in the scope object.

**report.route.js:**
- Add `router.get('/dashboard', requirePermission('reports', 'view'), reportController.getDashboard);` BEFORE the `/registry` route (specific routes before parameterized ones).
- Route order should be: `/dashboard`, `/registry`, `/:reportId/export/excel`, `/:reportId/export/pdf`, `/:reportId`.

Do NOT modify the existing `getRegistry` or `getReport` methods.
  </action>
  <verify>
1. `grep -n 'getDashboard\|dashboard' api/reports/report.controller.js` shows import, export, and function definition
2. `grep -n 'dashboard' api/reports/report.route.js` shows route registered before /registry
3. `node -e "import('./api/reports/report.route.js').then(() => console.log('OK'))"` loads without error
4. `node -e "import('./api/reports/report.controller.js').then(m => console.log(Object.keys(m.reportController)))"` includes `getDashboard`
  </verify>
  <done>
`GET /api/reports/dashboard` route exists before `/registry`, protected by `requirePermission('reports', 'view')`. Controller builds scope via `buildReportScope`, extracts `schoolYearId` from query/context, and passes both to `buildDashboard`. Error handling via `next(err)`.
  </done>
</task>

</tasks>

<verification>
1. `GET /api/reports/dashboard` returns 200 with `{ kpis: [...], alerts: [...] }` for an admin user
2. Each KPI has `id`, `label`, `value`, `unit`, `trend` (or null), `drillTo` fields
3. Each alert has `id`, `type`, `severity`, `message`, `drillTo` fields
4. All `drillTo` values are valid generator IDs: `student-enrollment`, `teacher-roster`, `teacher-hours-summary`, `orchestra-participation`, `student-teacher-assignments`, `data-quality`, `import-history`
5. Route order: `/dashboard` before `/registry` before `/:reportId` to avoid catch-all conflict
6. `schoolYearId` flows correctly: query param -> context fallback -> buildDashboard second arg -> collection queries
</verification>

<success_criteria>
Dashboard endpoint returns KPI cards with current values, trend deltas from comparison period, drillTo report IDs, and anomaly alerts. schoolYearId is passed as a separate argument to buildDashboard (not via scope). All drillTo fields map to existing generator IDs with documented sync requirement.
</success_criteria>

<output>
After completion, create `.planning/phases/55-dashboard-catalog-api/55-01-SUMMARY.md`
</output>
