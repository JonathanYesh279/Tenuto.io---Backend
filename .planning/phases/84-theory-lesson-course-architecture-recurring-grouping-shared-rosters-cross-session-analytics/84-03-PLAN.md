---
phase: 84-theory-lesson-course-architecture
plan: 03
type: execute
wave: 3
depends_on: ["84-02"]
files_modified:
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/theory/TheoryGroupManager.tsx
autonomous: false

must_haves:
  truths:
    - "TheoryGroupManager fetches and displays real course data from the API"
    - "Creating a course via the UI creates a theory_course entity in the database"
    - "Adding/removing students from a course in the UI updates the course roster"
    - "Course attendance analytics are displayed per-student with real data"
  artifacts:
    - path: "/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js"
      provides: "theoryCourse API methods: getCourses, getCourseById, createCourse, updateCourse, deleteCourse, addStudentToCourse, removeStudentFromCourse, getCourseAnalytics"
      contains: "theoryCourse"
    - path: "/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/theory/TheoryGroupManager.tsx"
      provides: "Real data integration replacing mock data"
      contains: "apiService"
  key_links:
    - from: "/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/theory/TheoryGroupManager.tsx"
      to: "/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js"
      via: "apiService.getCourses / apiService.createCourse calls"
      pattern: "apiService\\.(getCourses|createCourse|getCourseAnalytics)"
    - from: "/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js"
      to: "/api/theory/courses"
      via: "HTTP requests to course endpoints"
      pattern: "theory/courses"
---

<objective>
Wire the frontend TheoryGroupManager component to real course API endpoints, replacing mock/client-side-grouped data with actual theory_course entities.

Purpose: Complete the full-stack course feature by connecting the existing UI shell to the new backend API, enabling real course management, roster editing, and attendance analytics viewing.

Output: Updated apiService with course methods, TheoryGroupManager connected to real data.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-theory-lesson-course-architecture-recurring-grouping-shared-rosters-cross-session-analytics/84-RESEARCH.md
@.planning/phases/84-theory-lesson-course-architecture-recurring-grouping-shared-rosters-cross-session-analytics/84-01-SUMMARY.md
@.planning/phases/84-theory-lesson-course-architecture-recurring-grouping-shared-rosters-cross-session-analytics/84-02-SUMMARY.md
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/theory/TheoryGroupManager.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add theory course API methods to apiService.js</name>
  <files>/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js</files>
  <action>
Add a new section of methods to apiService.js near the existing theory methods (around line 2150). Add these methods:

1. **getCourses(filters = {})** — GET `/api/theory/courses` with query params from filters (category, teacherId, schoolYearId, isActive). Return response.data (array of courses).

2. **getCourseById(courseId)** — GET `/api/theory/courses/${courseId}`. Return response.data.

3. **createCourse(courseData)** — POST `/api/theory/courses` with courseData body. Return response.data.

4. **updateCourse(courseId, updateData)** — PUT `/api/theory/courses/${courseId}` with updateData body. Return response.data.

5. **deleteCourse(courseId)** — DELETE `/api/theory/courses/${courseId}`. Return response.data.

6. **addStudentToCourse(courseId, studentId)** — POST `/api/theory/courses/${courseId}/student/${studentId}`. Return response.data.

7. **removeStudentFromCourse(courseId, studentId)** — DELETE `/api/theory/courses/${courseId}/student/${studentId}`. Return response.data.

8. **getCourseAnalytics(courseId)** — GET `/api/theory/courses/${courseId}/analytics`. Return response.data.

Follow the existing apiService pattern: use `this.apiClient.get/post/put/delete`, wrap in try/catch, log errors with `console.error('Error [operation]:', error)`, and re-throw.

Also update the existing `getTheoryGroups` method (around line 2155) to use the new `getCourses` endpoint instead of client-side grouping. Replace the body with:
```javascript
async getTheoryGroups(filters = {}) {
  try {
    // Use real course API if available, fall back to client-side grouping
    const courses = await this.getCourses(filters);
    if (courses && courses.length > 0) {
      return courses.map(course => ({
        id: course._id,
        name: `${course.category}`,
        category: course.category,
        level: 'intermediate',
        teacherId: course.teacherId,
        teacherName: course.teacherName || '',
        capacity: 15,
        schedule: {
          dayOfWeek: course.dayOfWeek,
          startTime: course.startTime,
          endTime: course.endTime
        },
        location: course.location,
        isActive: course.isActive,
        studentIds: course.studentIds || [],
        enrolledStudents: (course.studentIds || []).length
      }));
    }
    // Fallback: no courses exist yet, return empty
    return [];
  } catch (error) {
    console.error('Error getting theory groups:', error);
    throw error;
  }
}
```
  </action>
  <verify>
Search apiService.js for "getCourses", "createCourse", "getCourseAnalytics" — all should be found. Verify no syntax errors by checking the file loads without issues in the frontend dev server.
  </verify>
  <done>
apiService.js has 8 new course methods + updated getTheoryGroups that reads from real course API. All methods follow existing apiService patterns with proper error handling.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire TheoryGroupManager to real course API data</name>
  <files>/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/theory/TheoryGroupManager.tsx</files>
  <action>
Refactor TheoryGroupManager.tsx to use real API data instead of mock data. This is a 937-line component so focus on the data layer changes, not UI restructuring.

Key changes:

1. **Replace mock data loading** — Find the `useEffect` that loads groups (likely sets mock data). Replace with `apiService.getCourses()` call. Map the API response to the `TheoryGroup` interface shape:
   - `id` = course._id
   - `name` = course.category
   - `category` = course.category
   - `teacherId` = course.teacherId
   - `teacherName` = fetch from teacher list or leave empty (fill in via teacher map pattern from Phase 80)
   - `schedule` = { dayOfWeek: VALID_DAYS_OF_WEEK[course.dayOfWeek], startTime, endTime }
   - `location` = course.location
   - `enrolledStudents` = map course.studentIds to GroupStudent objects (fetch student details via apiService.getStudents)
   - `isActive` = course.isActive
   - `waitingList` = [] (not implemented yet)
   - `curriculum` = [] (not implemented yet)

2. **Replace mock create** — Find the create/add group handler. Replace with `apiService.createCourse(courseData)` call. After success, refetch courses.

3. **Replace mock student add/remove** — Find handlers that add/remove students from groups. Replace with `apiService.addStudentToCourse(courseId, studentId)` and `apiService.removeStudentFromCourse(courseId, studentId)`. After success, refetch the specific course.

4. **Replace mock delete** — Find the delete handler. Replace with `apiService.deleteCourse(courseId)`. After success, refetch courses.

5. **Wire attendance data** — If the component shows attendance stats per student, replace mock attendance with `apiService.getCourseAnalytics(courseId)` call. Map the response's `studentStats` array to the component's expected format.

6. **Keep UI structure intact** — Do NOT change the JSX layout, styling, or component structure. Only change the data fetching and mutation handlers.

7. **Handle loading/error states** — Keep existing loading/error state management. Set `loading: true` before API calls, `loading: false` after, catch errors and set error state.

8. **Add teacher name enrichment** — After fetching courses, fetch teachers via `apiService.getTeachers()` to build a `teacherMap: Record<string, { firstName, lastName }>`. Use it to populate `teacherName` on each course. Follow the teacher map pattern from Phase 80 (fetch once, pass to children).

IMPORTANT: The TheoryGroup interface has fields like `level`, `capacity`, `curriculum`, `requirements`, `waitingList`, `grades` that don't exist on the backend course entity. Keep these fields in the interface with default/empty values for now — they are UI aspirational fields that may be added to the backend later. Do NOT remove them from the interface or the UI rendering.
  </action>
  <verify>
Run the frontend dev server (`npm run dev` in the frontend repo) and navigate to the theory group management page. Verify no console errors on load. If no courses exist yet, the page should show an empty state (not mock data).
  </verify>
  <done>
TheoryGroupManager fetches real course data from /api/theory/courses, creates courses via the API, manages student rosters through API calls, and displays attendance analytics from the course analytics endpoint. Mock data is completely removed. Empty state shows when no courses exist.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full-stack theory course feature: backend course entity with CRUD + roster + analytics, enhanced bulk create, and frontend TheoryGroupManager wired to real API</what-built>
  <how-to-verify>
    1. Start the backend server (`npm run dev` in backend repo)
    2. Run the migration script: `node scripts/migrate-theory-courses.js` — verify it reports indexes created and lessons updated
    3. Start the frontend dev server (`npm run dev` in frontend repo)
    4. Navigate to the Theory page and open the group/course management view
    5. Verify the page loads without errors (empty state if no courses exist)
    6. Create a new course: select a category, teacher, day, time, location, date range
    7. Verify the course appears in the list after creation
    8. Add a student to the course — verify the student appears in the course roster
    9. Remove the student — verify they are removed
    10. If the course has lessons with attendance data, check the analytics view
    11. Delete the course — verify it disappears and lessons are unlinked (not deleted)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Frontend loads TheoryGroupManager without console errors
- Creating a course via UI results in a document in theory_course collection
- Course list shows real data (category, teacher name, schedule, student count)
- Student add/remove updates both the course and future lesson documents
- Course analytics show per-student attendance rates from activity_attendance data
- Deleting a course unlinks lessons (sets courseId: null) but does not delete them
</verification>

<success_criteria>
- TheoryGroupManager displays real course data from the API (zero mock data)
- Full CRUD cycle works: create, read, update, delete courses
- Student roster management works: add/remove students updates course and future lessons
- Course attendance analytics display per-student stats computed from activity_attendance
- Existing theory lesson features (standalone lessons, attendance marking) are completely unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/84-theory-lesson-course-architecture-recurring-grouping-shared-rosters-cross-session-analytics/84-03-SUMMARY.md`
</output>
