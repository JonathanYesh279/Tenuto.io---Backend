---
phase: 66-design-system-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/tailwind.config.js
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/index.css
autonomous: true

must_haves:
  truths:
    - "bg-primary and bg-primary-500 resolve to the same indigo color family"
    - "neutral-50 through neutral-900 Tailwind classes are available and map to existing --neutral-* CSS vars"
    - "bg-success, bg-warning, bg-info and their -foreground variants are usable Tailwind classes backed by CSS vars"
  artifacts:
    - path: "/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/tailwind.config.js"
      provides: "Unified indigo primary palette + neutral color scale + semantic status colors"
      contains: "eef2ff"
    - path: "/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/index.css"
      provides: "CSS custom properties for semantic status colors (success, warning, info)"
      contains: "--success"
  key_links:
    - from: "tailwind.config.js primary.500"
      to: "index.css --primary"
      via: "Both resolve to indigo #6366f1"
      pattern: "6366f1"
    - from: "tailwind.config.js neutral scale"
      to: "index.css --neutral-* vars"
      via: "CSS var references in color config"
      pattern: "var\\(--neutral"
    - from: "tailwind.config.js success/warning/info"
      to: "index.css --success/--warning/--info vars"
      via: "CSS var references in color config"
      pattern: "var\\(--success"
---

<objective>
Resolve the primary color collision, wire the neutral scale into Tailwind, and add semantic status tokens.

Purpose: The primary color collision (bg-primary = indigo #6366f1 via CSS var, bg-primary-500 = #4F46E5 hardcoded blue-ish hex) is the root blocker for all design system work. The neutral scale exists as CSS vars but has no Tailwind classes. Semantic status colors (success, warning, info) need CSS var backing for theming.

Output: Updated tailwind.config.js with unified indigo primary palette, neutral Tailwind scale, and semantic status colors. Updated index.css with success/warning/info CSS custom properties.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-design-system-foundation/66-RESEARCH.md
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/tailwind.config.js
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unify primary palette to indigo + wire neutral scale</name>
  <files>
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/tailwind.config.js
  </files>
  <action>
In `tailwind.config.js`, make two changes to `theme.extend.colors`:

**1. Replace the hardcoded primary hex palette with Tailwind's indigo palette values:**

Replace the current `primary` object (lines ~75-89) with:
```javascript
primary: {
  50: '#eef2ff',   // indigo-50
  100: '#e0e7ff',  // indigo-100
  200: '#c7d2fe',  // indigo-200
  300: '#a5b4fc',  // indigo-300
  400: '#818cf8',  // indigo-400
  500: '#6366f1',  // indigo-500 — matches --primary CSS var
  600: '#4f46e5',  // indigo-600
  700: '#4338ca',  // indigo-700
  800: '#3730a3',  // indigo-800
  900: '#312e81',  // indigo-900
  950: '#1e1b4b',  // indigo-950
  DEFAULT: "hsl(var(--primary))",
  foreground: "hsl(var(--primary-foreground))",
},
```

Update the comment above the primary block to say: `// Unified indigo primary palette (CLR-01 — collision resolved)` and remove the old NOTE comment about "hardcoded blue hex values" and "DO NOT remove — migration to Phase 22-03".

**2. Add the neutral color scale referencing existing CSS vars:**

Add a `neutral` key to `theme.extend.colors`:
```javascript
neutral: {
  50: 'var(--neutral-50)',
  100: 'var(--neutral-100)',
  200: 'var(--neutral-200)',
  300: 'var(--neutral-300)',
  400: 'var(--neutral-400)',
  500: 'var(--neutral-500)',
  600: 'var(--neutral-600)',
  700: 'var(--neutral-700)',
  800: 'var(--neutral-800)',
  900: 'var(--neutral-900)',
},
```

Place this after the `gray` palette block. The CSS vars are already defined in index.css :root as `hsl()` values, so no `hsl()` wrapper is needed in the config.

**Important:** Do NOT change any existing :root CSS variable values. Do NOT remove any existing color entries beyond replacing the primary hex values. Do NOT modify any component files.
  </action>
  <verify>
Run from the frontend directory:
```bash
cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend
npx tailwindcss --content "./src/**/*.tsx" --output /tmp/tw-test.css 2>&1 | head -5
```
Then grep the generated CSS or the config to confirm:
- `grep '6366f1' tailwind.config.js` returns the primary-500 line
- `grep 'neutral' tailwind.config.js` returns 10 neutral scale entries
- `grep 'eef2ff' tailwind.config.js` returns primary-50 (indigo, not old blue #eff6ff)
  </verify>
  <done>
- primary-500 = #6366f1 (indigo), matching --primary CSS var (239 84% 67% = #6366f1)
- primary-50 through primary-950 are all indigo family hex values
- neutral-50 through neutral-900 Tailwind classes exist, referencing --neutral-* CSS vars
- No existing :root CSS values were changed
  </done>
</task>

<task type="auto">
  <name>Task 2: Add semantic status CSS vars and wire into Tailwind</name>
  <files>
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/index.css
    /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/tailwind.config.js
  </files>
  <action>
**1. In `src/index.css`, add semantic status CSS custom properties to the existing `:root` block** (after the shadow scale, before the closing `}`):

```css
    /* Semantic status tokens (CLR-03) */
    --success: 160 84% 39%;           /* emerald-600 #059669 */
    --success-foreground: 0 0% 100%;  /* white */
    --warning: 38 92% 50%;            /* amber-500 #f59e0b */
    --warning-foreground: 0 0% 100%;  /* white */
    --info: 199 89% 48%;              /* sky-500 #0ea5e9 */
    --info-foreground: 0 0% 100%;     /* white */
```

These use the same HSL-channels format as the existing `--primary`, `--destructive`, etc. so they work with `hsl(var(--success))`.

Also add corresponding entries to the `.dark` block:
```css
    --success: 160 84% 39%;
    --success-foreground: 0 0% 100%;
    --warning: 38 92% 50%;
    --warning-foreground: 0 0% 100%;
    --info: 199 89% 48%;
    --info-foreground: 0 0% 100%;
```

**2. In `tailwind.config.js`, add semantic status color entries** to `theme.extend.colors` (after the `destructive` block):

```javascript
success: {
  ...existing success object...,
  DEFAULT: "hsl(var(--success))",
  foreground: "hsl(var(--success-foreground))",
},
warning: {
  DEFAULT: "hsl(var(--warning))",
  foreground: "hsl(var(--warning-foreground))",
},
info: {
  DEFAULT: "hsl(var(--info))",
  foreground: "hsl(var(--info-foreground))",
},
```

For `success`: the existing success object already has 50-950 hex values. Add `DEFAULT` and `foreground` keys to it (merge, do not replace the existing hex shades).

For `warning`: create a new block with DEFAULT + foreground only (no hex shade scale needed yet — the existing `orange` palette covers that use case).

For `info`: create a new block with DEFAULT + foreground only.

**Important:** Do NOT change any existing :root CSS variable values. Only ADD new variables. Do NOT modify any component files.
  </action>
  <verify>
- `grep '\-\-success:' /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/index.css` returns the new CSS var
- `grep '\-\-warning:' /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/index.css` returns the new CSS var
- `grep '\-\-info:' /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/index.css` returns the new CSS var
- `grep 'hsl(var(--success))' /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/tailwind.config.js` returns the Tailwind config entry
- `grep 'hsl(var(--warning))' /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/tailwind.config.js` returns the Tailwind config entry
- `grep 'hsl(var(--info))' /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/tailwind.config.js` returns the Tailwind config entry
  </verify>
  <done>
- --success, --warning, --info CSS vars defined in :root with HSL channel format
- bg-success, bg-warning, bg-info Tailwind classes available via hsl(var(--*)) wiring
- text-success-foreground, text-warning-foreground, text-info-foreground classes available
- Existing success hex scale (50-950) preserved alongside new DEFAULT/foreground
- No existing :root values changed, only new variables added
  </done>
</task>

</tasks>

<verification>
1. Open tailwind.config.js and confirm primary-500 = #6366f1 (indigo, not old #4F46E5 blue)
2. Confirm neutral-50 through neutral-900 entries exist referencing CSS vars
3. Confirm success, warning, info color entries exist with DEFAULT + foreground
4. Open index.css and confirm --success, --warning, --info CSS vars exist in :root
5. Confirm NO existing :root variable values were changed (only additions)
6. Run `npx tailwindcss --content "./src/**/*.tsx" --output /tmp/tw-test.css` from frontend dir to verify config parses without errors
</verification>

<success_criteria>
- bg-primary and bg-primary-500 resolve to the same indigo family (#6366f1 and its shades)
- neutral-50 through neutral-900 Tailwind utility classes work and map to --neutral-* CSS vars
- bg-success, bg-warning, bg-info (and -foreground variants) are usable Tailwind classes
- Zero visual breakage: no existing :root values changed, only additions and hex value updates in tailwind.config.js
</success_criteria>

<output>
After completion, create `.planning/phases/66-design-system-foundation/66-01-SUMMARY.md`
</output>
