---
phase: 36-seed-teacher-schedule-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/seed-dev-data.js
autonomous: true

must_haves:
  truths:
    - "Running `node scripts/seed-dev-data.js --clean` produces teachers whose time blocks contain populated assignedLessons arrays with back-to-back student lessons"
    - "Each student's teacherAssignments[0] has day, time, duration, timeBlockId, lessonId, and scheduleInfo that match the corresponding teacher assignedLesson entry"
    - "Lesson start/end times within each block are sequential (no gaps, no overlaps) and fall within the block's startTime-endTime range"
    - "The room schedule grid displays individual private lesson activities (not empty blocks) across all weekdays after seeding"
  artifacts:
    - path: "scripts/seed-dev-data.js"
      provides: "Single-command seed script producing complete bidirectional schedule data"
      contains: "assignedLessons"
  key_links:
    - from: "scripts/seed-dev-data.js (teacher.teaching.timeBlocks[].assignedLessons[])"
      to: "scripts/seed-dev-data.js (student.teacherAssignments[])"
      via: "shared lessonId, timeBlockId, day, time, location"
      pattern: "lessonId.*toHexString"
    - from: "teacher.teaching.timeBlocks[].assignedLessons[].lessonStartTime"
      to: "api/room-schedule/room-schedule.service.js getTimeBlockActivities"
      via: "aggregation pipeline reads assignedLessons for grid display"
      pattern: "lesson\\.lessonStartTime"
---

<objective>
Integrate bidirectional schedule seeding into seed-dev-data.js so that a single `node scripts/seed-dev-data.js --clean` produces teachers with populated assignedLessons and students with aligned teacherAssignments.

Purpose: Currently the seed script creates empty assignedLessons arrays and random student times, meaning the room schedule grid shows blank time-block-level activities instead of individual lessons. This makes the grid useless for development and testing.

Output: Enhanced seed-dev-data.js that produces complete schedule data in one command.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@scripts/seed-dev-data.js
@scripts/seed-schedules.js
@api/room-schedule/room-schedule.service.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate schedule enrichment into seed-dev-data.js</name>
  <files>scripts/seed-dev-data.js</files>
  <action>
Add a post-processing step after student creation (step 5, before orchestras) that enriches teacher time blocks with proper fields and packs student lessons back-to-back. Port the logic from seed-schedules.js phases A, B, and C directly into seed-dev-data.js as inline functions. Do NOT import from seed-schedules.js (keep it as a standalone utility).

Specifically:

1. Add helper functions from seed-schedules.js:
   - `minutesToTime(totalMin)` and `timeToMinutes(timeStr)` (simple time conversion)
   - `pickDuration()` with distribution: 30 min (50%), 45 min (40%), 60 min (10%)

2. Replace the current `generateTeachers()` time block generation to include proper fields on each block:
   - Add `totalDuration` (calculated from start/end in minutes)
   - Add `isActive: true`
   - Add `recurring: { isRecurring: true, excludeDates: [] }`
   - Add `notes: null`
   - Add `createdAt: new Date()` and `updatedAt: new Date()`
   - Use morning (08-10 start, 3-5 hour span) / afternoon (13-15 start, 3-5 hour span) pattern from seed-schedules.js for more realistic windows (replace current random `randInt(8,15)` start with the morning/afternoon split)

3. Replace the current `generateStudents()` function to produce properly-aligned assignments:
   - Group students by teacher (keep existing distribution: ~basePerTeacher per teacher with remainder)
   - For each teacher, calculate block capacity in minutes, then distribute students proportionally across blocks using cursor-based back-to-back packing from seed-schedules.js
   - Generate a shared `lessonId` (ObjectId) for each lesson, used in both the student's teacherAssignment AND the teacher's assignedLessons
   - Each student's teacherAssignment must have: `day` matching block.day, `time` matching the cursor position (minutesToTime), `duration` from pickDuration(), `location` matching block.location, `timeBlockId` as block._id.toHexString(), `lessonId` as the shared lessonId.toHexString(), `scheduleSlotId`, and a complete `scheduleInfo` sub-object with `{ day, startTime, endTime, duration, location, notes: null }`
   - Handle overflow students: force 30-min slots into the block with the most remaining capacity (15-min overflow tolerance per seed-schedules.js pattern)
   - Return both the students array AND a `lessonRefs` Map (teacherId -> blockId -> lesson[]) for populating assignedLessons

4. After student insertMany (step 5), add step 5b: populate assignedLessons on teachers.
   - For each teacher, read their timeBlocks, look up the lessonRefs map, and set assignedLessons[] on each block
   - Each assignedLesson entry: `{ _id: lessonId, studentId: student._id.toHexString(), studentName: "firstName lastName", lessonStartTime, lessonEndTime, duration, notes: null, isActive: true, isRecurring: true, startDate: new Date('2024-09-01'), endDate: null, createdAt, updatedAt }`
   - Use bulkWrite with batches of 50 to update teacher docs with populated timeBlocks
   - Log: "Schedule enrichment: X lessons across Y teachers (Zms)"

5. Add a verification step (step 10b, after indexes) that checks:
   - Count of teachers with non-empty assignedLessons
   - Count of students with valid timeBlockId references
   - Count of time mismatches (lesson time outside block range, allowing 15-min tolerance)
   - Print pass/fail summary

Keep seed-schedules.js untouched as a standalone utility for re-enriching existing data.
  </action>
  <verify>
Run `node scripts/seed-dev-data.js --clean` in the project directory. Verify:
- Script completes without errors
- "Schedule enrichment" line shows ~1200 lessons across ~130 teachers
- Verification step shows 0 invalid block refs, 0 missing scheduleInfo, 0 time mismatches
- "All checks passed" printed at end
  </verify>
  <done>
Running `node scripts/seed-dev-data.js --clean` produces teachers with 2-4 time blocks each containing back-to-back assignedLessons, and students with teacherAssignments whose day/time/location/timeBlockId/lessonId all match the teacher side. Verification step confirms 0 issues.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify room schedule grid displays individual lessons</name>
  <files>scripts/seed-dev-data.js</files>
  <action>
After Task 1 completes successfully, verify the seeded data works with the room-schedule API by running a quick database query that simulates what getRoomSchedule does:

1. Query teachers collection for the dev tenant, filter by one specific day (e.g., 'ראשון'), and count how many assignedLessons entries exist across all time blocks for that day. The count should be significantly greater than 0 (expect 50-200+ per day depending on random distribution).

2. Run a spot-check: pick one teacher, verify their time block has:
   - assignedLessons array with at least 1 entry
   - Each entry has lessonStartTime >= block.startTime
   - Each entry has lessonEndTime <= block.endTime (+ 15 min tolerance)
   - Each entry has a valid studentId string

3. Cross-reference: for that teacher's first assignedLesson, find the corresponding student and verify:
   - student.teacherAssignments[0].teacherId matches the teacher._id
   - student.teacherAssignments[0].timeBlockId matches the block._id
   - student.teacherAssignments[0].lessonId matches the assignedLesson._id
   - student.teacherAssignments[0].time matches assignedLesson.lessonStartTime
   - student.teacherAssignments[0].day matches block.day

Add this cross-reference check to the existing verification step in seed-dev-data.js (from Task 1). Sample 10 random teacher-student pairs for the cross-reference to keep it fast.

If any mismatches found, fix the generation logic until all cross-references pass.
  </action>
  <verify>
Run `node scripts/seed-dev-data.js --clean` and confirm:
- Verification shows "X/10 cross-references valid" (should be 10/10)
- All verification checks pass
- Total seed time remains under 10 seconds
  </verify>
  <done>
Verification step confirms bidirectional consistency: 10/10 sampled teacher-student pairs have matching lessonId, timeBlockId, day, time, and location across both sides. Seed script runs in under 10 seconds total.
  </done>
</task>

</tasks>

<verification>
1. `node scripts/seed-dev-data.js --clean` completes without errors
2. Built-in verification reports 0 invalid block refs, 0 missing scheduleInfo, 0 time mismatches
3. Cross-reference sampling shows 10/10 matches
4. scripts/seed-schedules.js still exists unchanged (standalone utility preserved)
5. Data summary shows populated time blocks (not "Time blocks: 350, Lessons: 0")
</verification>

<success_criteria>
- Single command (`node scripts/seed-dev-data.js --clean`) produces complete bidirectional schedule data
- Teachers have 2-4 time blocks with back-to-back assignedLessons (not empty arrays)
- Students have teacherAssignments with day/time/location/timeBlockId/lessonId matching teacher side
- Verification step proves data integrity with 0 issues and 10/10 cross-references
- Room schedule grid will display individual private lessons (not empty blocks) when queried
</success_criteria>

<output>
After completion, create `.planning/phases/36-seed-teacher-schedule-data/36-01-SUMMARY.md`
</output>
