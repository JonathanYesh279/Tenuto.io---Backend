---
phase: 70-teachers-page-restyle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/auth/auth.service.js
  - api/teacher/invitation.service.js
  - api/teacher/teacher.service.js
  - test/setup.js
autonomous: true

must_haves:
  truths:
    - "Each successful login increments credentials.loginCount on the teacher document"
    - "Teacher list API response includes top-level loginCount and lastLogin fields"
    - "Teachers who never logged in have loginCount=0 and lastLogin=null"
  artifacts:
    - path: "api/auth/auth.service.js"
      provides: "loginCount increment on all 4 auth paths"
      contains: "credentials.loginCount"
    - path: "api/teacher/invitation.service.js"
      provides: "loginCount increment on invitation acceptance"
      contains: "credentials.loginCount"
    - path: "api/teacher/teacher.service.js"
      provides: "loginCount and lastLogin extraction in enrichment"
      contains: "loginCount"
  key_links:
    - from: "api/auth/auth.service.js"
      to: "teacher collection"
      via: "$inc credentials.loginCount in updateOne"
      pattern: "\\$inc.*credentials\\.loginCount"
    - from: "api/teacher/teacher.service.js"
      to: "teacher list API response"
      via: "_enrichWithStudentCounts extracts loginCount/lastLogin"
      pattern: "loginCount.*credentials"
---

<objective>
Add login count tracking to the auth flow and expose loginCount/lastLogin in the teacher list API.

Purpose: Backend data needed for the Teachers page login activity badges (blue badge showing how many times a teacher has logged in).
Output: All 5 auth paths increment `credentials.loginCount` via `$inc`. Teacher list enrichment extracts `loginCount` and `lastLogin` to top-level response fields.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/70-teachers-page-restyle/70-RESEARCH.md
@api/auth/auth.service.js
@api/teacher/invitation.service.js
@api/teacher/teacher.service.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add $inc loginCount to all auth paths</name>
  <files>api/auth/auth.service.js, api/teacher/invitation.service.js</files>
  <action>
Add `$inc: { 'credentials.loginCount': 1 }` as a sibling to `$set` in every updateOne call that sets `credentials.lastLogin`. There are 5 locations total:

1. **auth.service.js line ~116** (login function) — standard email/password login. The existing updateOne has `$set: { 'credentials.refreshToken', 'credentials.lastLogin', updatedAt }`. Add `$inc` block alongside `$set`.

2. **auth.service.js line ~412** (acceptInvitation function) — first-time invitation login. Same pattern.

3. **auth.service.js line ~501** (resetPassword function) — post-reset login. Same pattern.

4. **auth.service.js line ~723** (another auth path) — same pattern.

5. **invitation.service.js line ~96-105** — invitation acceptance path. Currently has `$set: { 'credentials.refreshToken', 'credentials.lastLogin', updatedAt }`. Add `$inc` block alongside `$set`.

MongoDB `$inc` on a non-existent field creates it with the increment value, so no migration is needed. Existing teachers without `loginCount` will get `1` on their first login.

Pattern for each location:
```javascript
// Change from:
{ $set: { 'credentials.lastLogin': new Date(), ... } }
// To:
{ $set: { 'credentials.lastLogin': new Date(), ... }, $inc: { 'credentials.loginCount': 1 } }
```
  </action>
  <verify>
Search for all occurrences of `credentials.lastLogin` in auth.service.js and invitation.service.js — each one must have a corresponding `credentials.loginCount` in a `$inc` block in the same updateOne call. Run: `grep -n 'credentials.loginCount' api/auth/auth.service.js api/teacher/invitation.service.js` — expect 5 matches.
  </verify>
  <done>All 5 auth paths that set credentials.lastLogin also increment credentials.loginCount via $inc.</done>
</task>

<task type="auto">
  <name>Task 2: Expose loginCount and lastLogin in teacher list API</name>
  <files>api/teacher/teacher.service.js</files>
  <action>
In the `_enrichWithStudentCounts` function (line ~945), modify the return map (line ~977-980) to extract `loginCount` and `lastLogin` from the teacher's `credentials` object to top-level fields:

```javascript
return teachers.map(t => ({
  ...t,
  studentCount: countMap.get(t._id.toString()) || 0,
  loginCount: t.credentials?.loginCount || 0,
  lastLogin: t.credentials?.lastLogin || null,
}));
```

This ensures:
- Frontend reads `teacher.loginCount` (not `teacher.credentials.loginCount`)
- Teachers who never logged in get `loginCount: 0` and `lastLogin: null`
- No sensitive credential data (password, refreshToken) needs to be exposed

Do NOT modify the find() query projection — that is out of scope. The enrichment layer handles the data shaping.
  </action>
  <verify>
Read the `_enrichWithStudentCounts` function and confirm the return map includes `loginCount` and `lastLogin` fields. Run the existing test suite: `npm test -- --grep teacher` to ensure no regressions. If no matching tests, verify with `grep -n 'loginCount\|lastLogin' api/teacher/teacher.service.js` — expect matches in the enrichment function.
  </verify>
  <done>Teacher list API response includes top-level `loginCount` (number, default 0) and `lastLogin` (Date or null) for every teacher.</done>
</task>

</tasks>

<verification>
1. `grep -rn 'credentials.loginCount' api/auth/ api/teacher/invitation.service.js` returns 5 matches (4 in auth.service.js, 1 in invitation.service.js)
2. `grep -n 'loginCount\|lastLogin' api/teacher/teacher.service.js` shows extraction in _enrichWithStudentCounts
3. `npm test` passes with no regressions
</verification>

<success_criteria>
- All 5 auth paths increment credentials.loginCount on successful login
- Teacher list enrichment extracts loginCount (default 0) and lastLogin (default null) to top-level
- No test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/70-teachers-page-restyle/70-01-SUMMARY.md`
</output>
