---
phase: 70-teachers-page-restyle
plan: 02
type: execute
wave: 2
depends_on: ["70-01"]
files_modified:
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Teachers.tsx
autonomous: false

must_haves:
  truths:
    - "Teachers page shows GlassStatCard row identical to Students page"
    - "Filters use GlassSelect dropdowns for instrument and role"
    - "Search uses SearchInput component"
    - "Table uses HeroUI Table with avatar, initials, and colored circle per teacher"
    - "Each teacher avatar shows a blue badge with login count (if > 0)"
    - "Pagination uses HeroUI Pagination with numbered pages"
    - "Grid view still works"
  artifacts:
    - path: "/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Teachers.tsx"
      provides: "Restyled Teachers page matching Students page pattern"
      min_lines: 500
  key_links:
    - from: "Teachers.tsx"
      to: "GlassStatCard"
      via: "import from components/ui/GlassStatCard"
      pattern: "import.*GlassStatCard"
    - from: "Teachers.tsx"
      to: "GlassSelect"
      via: "import from components/ui/GlassSelect"
      pattern: "import.*GlassSelect"
    - from: "Teachers.tsx"
      to: "HeroUI Table"
      via: "import from @heroui/react"
      pattern: "import.*Table.*from.*@heroui"
    - from: "Teachers.tsx"
      to: "HeroUI Badge"
      via: "import Badge from @heroui/react"
      pattern: "Badge.*content.*loginCount"
    - from: "Teachers.tsx"
      to: "/api/teacher"
      via: "apiService call fetching teacher list with loginCount"
      pattern: "loginCount"
---

<objective>
Restyle the Teachers page to match the Students page visual pattern exactly — GlassStatCard row, GlassSelect filters, HeroUI Table with colored avatars + login count badges, and HeroUI Pagination.

Purpose: Visual consistency across entity pages (v2.1 milestone). Teachers page is the first entity page to be restyled to match the Students page pattern established in v2.0.
Output: Fully restyled Teachers.tsx using all shared components and HeroUI primitives.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/70-teachers-page-restyle/70-RESEARCH.md
@.planning/phases/70-teachers-page-restyle/70-01-SUMMARY.md
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Teachers.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Students.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/ui/GlassStatCard.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/ui/GlassSelect.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/components/ui/SearchInput.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Teachers page to match Students page pattern</name>
  <files>/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Teachers.tsx</files>
  <action>
**READ Students.tsx FIRST** as the reference implementation. The Teachers page must mirror its structure exactly. Then rewrite Teachers.tsx with these changes:

**1. Data Loading — Load all teachers at once (match Students pattern):**
- Change the API call to load all teachers at once (limit=0 or large limit like 10000) instead of the current paginated load-more approach
- The response now includes `loginCount` (number) and `lastLogin` (Date|null) per teacher from Plan 70-01

**2. Stat Cards — Replace current stat boxes with GlassStatCard:**
- Import `GlassStatCard` from `components/ui/GlassStatCard`
- Use same grid layout as Students: `grid grid-cols-2 lg:grid-cols-4 gap-4`
- Cards: Total Teachers, Active Teachers, Instruments (unique specializations), Average Students per Teacher (or similar relevant stats from current page)
- Same green-blue gradient styling as Students (GlassStatCard handles this)

**3. Filter Bar — Replace plain select/input with GlassSelect + SearchInput:**
- Import `GlassSelect` from `components/ui/GlassSelect`
- Import `SearchInput` from `components/ui/SearchInput`
- Replace the instrument filter custom input with `GlassSelect` using instrument options list (from VALID_INSTRUMENTS or hardcoded list from current page)
- Replace the role filter plain select with `GlassSelect` with role options: מורה, ניצוח, מדריך הרכב, מנהל, תאוריה, מגמה, ליווי פסנתר, הלחנה
- Replace the search input with `SearchInput` component
- Keep the Add Teacher button and view toggle (table/grid) as they are
- Match the filter bar layout from Students (flex row, gap, responsive wrapping)

**4. Table — Replace custom Table with HeroUI Table:**
- Import `Table as HeroTable, TableHeader, TableColumn, TableBody, TableRow, TableCell, User, Pagination` from `@heroui/react`
- Import `Badge as HeroBadge` from `@heroui/react`
- Define columns: שם המורה, התמחות, תפקידים, מס' תלמידים, סטטוס, פעולות
- Implement `renderCell` callback matching the Students page pattern:
  - **name column:** HeroUI `User` component with colored avatar (use `getAvatarColor` hash function — copy from Students.tsx). Avatar shows initials, description shows email. If `loginCount > 0`, wrap `User` in `HeroBadge` with `content={loginCount}`, `color="primary"`, `size="sm"`, `shape="circle"`.
  - **specialization column:** Use `InstrumentBadge` for valid instruments, `--` for unset
  - **roles column:** Display roles text (keep current logic)
  - **studentCount column:** Display count number
  - **status column:** Use `StatusBadge` (keep current logic)
  - **actions column:** Keep current action buttons (edit, view, etc.)
- Table classNames should match Students exactly:
  ```
  classNames={{
    base: 'flex-1 min-h-0 animate-table-rows',
    wrapper: 'h-full',
    th: 'bg-default-100 text-default-600',
    thead: '[&>tr]:border-b-0',
    tr: 'transition-colors duration-150 hover:bg-primary/5',
    td: 'py-3',
  }}
  ```
- Row click navigates to teacher detail (keep current behavior)

**5. Pagination — Replace load-more with HeroUI Pagination:**
- Add client-side pagination state: `const [tablePage, setTablePage] = useState(1)`
- `const tableRowsPerPage = 20`
- Compute `tablePages = Math.ceil(filteredTeachers.length / tableRowsPerPage)`
- Slice `filteredTeachers` for current page
- Add `Pagination` as `bottomContent` of HeroTable (same pattern as Students)
- Reset page to 1 when filters change

**6. Grid View — Preserve existing grid/card view:**
- Keep the existing `TeacherCard` grid view working
- Apply same pagination to grid view
- View toggle (table/grid) stays as-is

**7. getAvatarColor helper — Copy from Students:**
```typescript
const AVATAR_COLORS: Array<'primary' | 'secondary' | 'success' | 'warning' | 'danger'> = [
  'primary', 'secondary', 'success', 'warning', 'danger'
]
function getAvatarColor(name: string): typeof AVATAR_COLORS[number] {
  let hash = 0
  for (let i = 0; i < name.length; i++) {
    hash = name.charCodeAt(i) + ((hash << 5) - hash)
  }
  return AVATAR_COLORS[Math.abs(hash) % AVATAR_COLORS.length]
}
```

**Important:** Do NOT create new shared components. Import ALL shared components from their existing paths. Read Students.tsx carefully and match its structure section by section.
  </action>
  <verify>
1. `grep -c 'GlassStatCard\|GlassSelect\|SearchInput' src/pages/Teachers.tsx` — all 3 present
2. `grep -c 'HeroTable\|HeroBadge\|Pagination\|User' src/pages/Teachers.tsx` — HeroUI components present
3. `grep 'loginCount' src/pages/Teachers.tsx` — login badge rendering present
4. `grep -c 'getAvatarColor' src/pages/Teachers.tsx` — avatar color function present
5. No TypeScript errors: `npx tsc --noEmit --pretty 2>&1 | grep -i 'Teachers.tsx'` returns empty
6. Dev server loads without errors: `npm run dev` starts cleanly
  </verify>
  <done>Teachers.tsx uses GlassStatCard, GlassSelect, SearchInput, HeroUI Table with User+Badge avatars, and HeroUI Pagination. No custom Table, no plain selects, no load-more.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Teachers page</name>
  <what-built>Complete Teachers page restyle — stat cards, glass filters, HeroUI table with colored avatars and blue login count badges, numbered pagination. Should be visually indistinguishable from Students page layout.</what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev` in frontend
    2. Navigate to Teachers page
    3. Verify stat cards row shows green-blue gradient GlassStatCards (same as Students)
    4. Verify filter bar has GlassSelect dropdowns for instrument and role (glass morphism style)
    5. Verify search field is SearchInput component (with clear button)
    6. Verify table is HeroUI Table — check for sticky header, hover row highlight
    7. Verify each teacher row shows colored avatar circle with initials (not a generic icon)
    8. Verify teachers with login count > 0 show a blue badge on their avatar
    9. Verify pagination at bottom shows numbered pages (not load-more button)
    10. Toggle to grid view — verify grid still works
    11. Compare side-by-side with Students page — layout pattern should match
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Teachers page uses identical component set as Students page (GlassStatCard, GlassSelect, SearchInput, HeroUI Table/Pagination/Badge/User)
2. Login count badge appears on teacher avatars (blue, shows number)
3. No duplicate/custom versions of shared components
4. Grid view still functional
5. All existing functionality preserved (add teacher, edit, view details, filters)
</verification>

<success_criteria>
- Teachers page visual layout is indistinguishable from Students page (stat cards, filters, table, pagination)
- Each teacher row shows colored avatar with initials + blue login count badge (when count > 0)
- All shared components imported from existing paths — no duplication
- Grid view still works
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/70-teachers-page-restyle/70-02-SUMMARY.md`
</output>
