---
phase: 45-super-admin-tenant-admin-management
plan: 02
type: execute
wave: 2
depends_on: ["45-01"]
files_modified:
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/super-admin/TenantAdminManagementPage.tsx
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/App.tsx
autonomous: true

must_haves:
  truths:
    - "Super admin can navigate to a dedicated tenant admin management page"
    - "Page shows a table of all tenant admins grouped/filterable by tenant"
    - "Each row shows admin name, email, tenant name, last login, password status"
    - "Super admin can edit admin name and email via an inline edit modal"
    - "Super admin can trigger password reset with confirmation dialog"
  artifacts:
    - path: "/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/super-admin/TenantAdminManagementPage.tsx"
      provides: "Tenant admin management page component"
      min_lines: 100
    - path: "/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js"
      provides: "API methods: getAllTenantAdmins, getTenantAdmins, updateTenantAdmin, resetTenantAdminPassword"
    - path: "/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/App.tsx"
      provides: "Route for /tenant-admins pointing to TenantAdminManagementPage"
  key_links:
    - from: "/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/super-admin/TenantAdminManagementPage.tsx"
      to: "/api/super-admin/tenant-admins"
      via: "superAdminService.getAllTenantAdmins()"
      pattern: "superAdminService\\.getAllTenantAdmins"
    - from: "/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/App.tsx"
      to: "TenantAdminManagementPage"
      via: "React Router route definition"
      pattern: "tenant-admins.*TenantAdminManagementPage"
---

<objective>
Build a frontend page for super admins to view and manage all tenant admin accounts from one place.

Purpose: Super admin needs a single view showing all admin accounts across tenants -- who has access, their last login, password change status -- and the ability to update details or reset passwords without impersonating each tenant.

Output: New TenantAdminManagementPage component, API service methods, and route registration.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/45-super-admin-tenant-admin-management/45-01-SUMMARY.md
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/super-admin/TenantListPage.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/super-admin/SuperAdminManagementPage.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: API service methods and route registration</name>
  <files>/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js, /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/App.tsx</files>
  <action>
In apiService.js, add four methods to the `superAdminService` object (in the "Tenant admin management" section, after the existing admin management section):

```js
// Tenant Admin Management
async getAllTenantAdmins() {
  const response = await apiClient.get('/super-admin/tenant-admins');
  return response;
},

async getTenantAdmins(tenantId) {
  const response = await apiClient.get(`/super-admin/tenants/${tenantId}/admins`);
  return response;
},

async updateTenantAdmin(tenantId, adminId, data) {
  const response = await apiClient.put(`/super-admin/tenants/${tenantId}/admins/${adminId}`, data);
  return response;
},

async resetTenantAdminPassword(tenantId, adminId) {
  const response = await apiClient.post(`/super-admin/tenants/${tenantId}/admins/${adminId}/reset-password`);
  return response;
},
```

In App.tsx:
1. Add lazy import: `const TenantAdminManagementPage = lazyWithRetry(() => import('./pages/super-admin/TenantAdminManagementPage'), 'TenantAdminManagementPage')`
2. Add route alongside existing super-admin routes (near line 574): `<Route path="/tenant-admins" element={createProtectedRoute(TenantAdminManagementPage, 'loading tenant admins...')} />`
3. Add `/tenant-admins` to the `SUPER_ADMIN_ALLOWED_PATHS` array (near line 164)
  </action>
  <verify>Grep for `getAllTenantAdmins` in apiService.js. Grep for `tenant-admins` in App.tsx. Both should return matches.</verify>
  <done>Four API methods exist in superAdminService. Route /tenant-admins is registered in App.tsx. Path is in SUPER_ADMIN_ALLOWED_PATHS.</done>
</task>

<task type="auto">
  <name>Task 2: TenantAdminManagementPage component</name>
  <files>/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/super-admin/TenantAdminManagementPage.tsx</files>
  <action>
Create a new page component following the patterns from TenantListPage.tsx and SuperAdminManagementPage.tsx. Use Hebrew labels consistent with the rest of the app. RTL layout via Tailwind.

Component structure:
1. **State**: admins array, loading, error, searchQuery, editingAdmin (for modal), resetConfirmAdmin (for reset dialog)
2. **Data loading**: useEffect calls `superAdminService.getAllTenantAdmins()`, extracts `res?.data || res` into state
3. **Search/filter**: Filter admins by search query matching firstName, lastName, email, or tenantName
4. **Table columns** (RTL, Hebrew headers):
   - Tenant name (tenantName) + slug in smaller text
   - Admin name (firstName + lastName)
   - Email (credentials.email)
   - Roles (badge chips, same tier coloring as settings UI: admin=red, coordinator=blue, teaching=green)
   - Last login (credentials.lastLogin formatted, or "never" if null)
   - Password status: badge showing "requires change" (yellow) or "active" (green) based on credentials.requiresPasswordChange
   - Actions: Edit button (pencil icon), Reset Password button (key/arrow-clockwise icon)
5. **Edit Modal** (using existing Modal component):
   - Fields: firstName, lastName, email (controlled inputs)
   - Save calls `superAdminService.updateTenantAdmin(admin.tenantId, admin._id, data)`
   - On success: toast + refresh list
   - On error: show error message (handle 409 email exists)
6. **Reset Password Confirmation**:
   - Use a simple confirm dialog (can use ConfirmDeleteDialog pattern or a Modal with warning text)
   - Text: "Are you sure you want to reset the password for {name}? They will need to set a new password on next login."
   - Confirm calls `superAdminService.resetTenantAdminPassword(admin.tenantId, admin._id)`
   - On success: toast + refresh list
7. **Header**: Title "ניהול מנהלי מוסדות" with a count badge, search input
8. **Empty state**: If no admins found after search, show message
9. **Loading state**: Spinner or skeleton consistent with other pages

Styling: Use Tailwind classes matching TenantListPage style. Card-based or table-based layout. Include hover states on rows. Use @phosphor-icons/react for icons (PencilSimple, ArrowCounterClockwise or ArrowsClockwise for reset, Shield for header, etc.).

Import toast from 'react-hot-toast' for success/error notifications.
  </action>
  <verify>File exists at the expected path. TypeScript compiles without errors (check via `cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && npx tsc --noEmit --skipLibCheck 2>&1 | head -20` or just verify the file is syntactically valid).</verify>
  <done>TenantAdminManagementPage.tsx exists with table view, edit modal, reset password confirmation, search, and proper Hebrew labels. Navigable at /tenant-admins route.</done>
</task>

</tasks>

<verification>
- Frontend builds without TypeScript errors
- /tenant-admins route renders TenantAdminManagementPage
- Page calls GET /super-admin/tenant-admins on mount
- Edit modal calls PUT /super-admin/tenants/:id/admins/:adminId
- Reset password calls POST /super-admin/tenants/:id/admins/:adminId/reset-password
</verification>

<success_criteria>
- Super admin can navigate to /tenant-admins and see all tenant admin accounts in a searchable table
- Each row shows tenant name, admin name, email, roles, last login, password status
- Clicking edit opens a modal to update name/email with save functionality
- Clicking reset password shows confirmation then resets to default password
- Toast notifications on success/error
</success_criteria>

<output>
After completion, create `.planning/phases/45-super-admin-tenant-admin-management/45-02-SUMMARY.md`
</output>
