---
phase: 38-single-lesson-reschedule-detail-modal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/room-schedule/room-schedule.service.js
  - api/room-schedule/room-schedule.controller.js
  - api/room-schedule/room-schedule.route.js
  - api/room-schedule/room-schedule.validation.js
autonomous: true

must_haves:
  truths:
    - "GET /api/room-schedule returns lessonId, studentId, duration, and blockId for each timeBlock lesson activity"
    - "PUT /api/room-schedule/reschedule-lesson atomically removes a lesson from its source block, creates a new block at the target, and assigns the lesson there"
    - "Reschedule endpoint returns 409 with conflict details when target room/time has an existing activity"
    - "Empty source blocks are auto-deleted after the last lesson is rescheduled away"
  artifacts:
    - path: "api/room-schedule/room-schedule.service.js"
      provides: "Enhanced activity data + rescheduleLesson function"
      contains: "rescheduleLesson"
    - path: "api/room-schedule/room-schedule.controller.js"
      provides: "rescheduleLesson request handler"
      contains: "rescheduleLesson"
    - path: "api/room-schedule/room-schedule.route.js"
      provides: "PUT /reschedule-lesson route"
      contains: "reschedule-lesson"
    - path: "api/room-schedule/room-schedule.validation.js"
      provides: "rescheduleLesson validation schema"
      contains: "validateRescheduleBody"
  key_links:
    - from: "api/room-schedule/room-schedule.service.js"
      to: "api/schedule/time-block.service.js"
      via: "import timeBlockService for removeLessonFromBlock, createTimeBlock, assignLessonToBlock, deleteTimeBlock"
      pattern: "timeBlockService\\."
    - from: "api/room-schedule/room-schedule.controller.js"
      to: "api/room-schedule/room-schedule.service.js"
      via: "roomScheduleService.rescheduleLesson"
      pattern: "roomScheduleService\\.rescheduleLesson"
---

<objective>
Enhance the room schedule API to emit full lesson metadata (lessonId, studentId, duration, blockId) for timeBlock activities and add a new PUT /api/room-schedule/reschedule-lesson endpoint that atomically moves a single lesson from one time block to a new block at a different room/day/time.

Purpose: The current moveActivity endpoint moves entire time blocks. Phase 38 needs per-lesson granularity -- moving one student's lesson without affecting other lessons in the same block. The enhanced activity data enables the frontend detail modal to display and operate on individual lessons.

Output: Enhanced room-schedule.service.js with 4 new activity fields and rescheduleLesson function; updated controller, route, and validation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/38-single-lesson-reschedule-detail-modal/38-RESEARCH.md

@api/room-schedule/room-schedule.service.js
@api/room-schedule/room-schedule.controller.js
@api/room-schedule/room-schedule.route.js
@api/room-schedule/room-schedule.validation.js
@api/schedule/time-block.service.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance activity data with lesson metadata fields</name>
  <files>api/room-schedule/room-schedule.service.js</files>
  <action>
In the `getTimeBlockActivities()` function, add 4 new fields to each lesson-level activity object (inside the `activeLessons.length > 0` branch, the `for` loop at line ~365):

1. `lessonId`: `lesson._id ? lesson._id.toString() : null` -- the actual MongoDB ObjectId of the lesson, NOT the index
2. `studentId`: `lesson.studentId || null` -- the student's ID from the lesson record
3. `duration`: `lesson.duration || null` -- lesson duration in minutes (30, 45, or 60)
4. `blockId`: `blockId` -- the raw block ObjectId string (already computed as variable `blockId` on line ~356)

Also add these fields to the empty-block activity (the `else` branch at line ~386), all set to `null` except `blockId` which should be `blockId`.

Do NOT change the `id` field format (`${blockId}_${i}` for lessons, `blockId` for empty blocks) -- the frontend depends on this pattern for DnD routing.

The fields are added alongside existing fields like `teacherName`, `teacherId`, `label`, etc. No structural changes to the aggregation pipeline or batch lookups.
  </action>
  <verify>
Start the server (`npm run dev`) and call `GET /api/room-schedule?day=0` (or any day with timeBlock activities). Verify the response JSON:
- Lesson activities (id containing `_`) now include `lessonId` (24-char hex string), `studentId` (string), `duration` (number), and `blockId` (24-char hex string)
- Empty block activities include `lessonId: null`, `studentId: null`, `duration: null`, `blockId: "<hex string>"`
- Rehearsal and theory activities are unchanged (no new fields)
  </verify>
  <done>Every timeBlock activity in the room schedule response includes lessonId, studentId, duration, and blockId fields. Lesson-level activities have non-null values; empty-block activities have null values except blockId.</done>
</task>

<task type="auto">
  <name>Task 2: Add rescheduleLesson endpoint (service + controller + route + validation)</name>
  <files>
    api/room-schedule/room-schedule.service.js
    api/room-schedule/room-schedule.controller.js
    api/room-schedule/room-schedule.route.js
    api/room-schedule/room-schedule.validation.js
  </files>
  <action>
**room-schedule.validation.js:** Add a `rescheduleBodySchema` Joi schema and export `validateRescheduleBody` function:
- `teacherId`: Joi.string().required() -- teacher who owns the source block
- `sourceBlockId`: Joi.string().required() -- the block to remove the lesson from
- `lessonId`: Joi.string().required() -- the specific lesson ObjectId to move
- `targetRoom`: Joi.string().required() -- destination room name
- `targetDay`: Joi.number().integer().min(0).max(5).required() -- numeric day (0=Sunday, 5=Friday)
- `targetStartTime`: Joi.string().pattern(TIME_PATTERN).required() -- "HH:MM"
- `targetEndTime`: Joi.string().pattern(TIME_PATTERN).required() -- "HH:MM"
- Custom validation: targetStartTime must be before targetEndTime (reuse existing pattern from validateMoveBody)

**room-schedule.service.js:** Add `rescheduleLesson` function to the service and export it via `roomScheduleService`:

```
async function rescheduleLesson(rescheduleData, options = {}) {
  const tenantId = requireTenantId(options.context?.tenantId);
  const { teacherId, sourceBlockId, lessonId, targetRoom, targetDay, targetStartTime, targetEndTime } = rescheduleData;

  // 1. Find source teacher, block, and lesson to get studentId and duration
  //    Query teacher by _id + tenantId, find the block by _id, find the lesson by _id
  //    Use .toString() for all ObjectId comparisons (per 37-07 decision)
  //    Throw NOT_FOUND if teacher/block/lesson not found

  // 2. Extract lesson details: studentId, duration
  //    Calculate lesson duration from targetStartTime/targetEndTime if lesson.duration is null

  // 3. Convert targetDay to Hebrew day name using DAY_NAMES[targetDay]

  // 4. Conflict pre-check at target room/time:
  //    Call getRoomSchedule(targetDay, options)
  //    Collect all activities, exclude source lesson (by lessonId or by blockId prefix)
  //    Filter activities in targetRoom, check doTimesOverlap with target times
  //    If conflicts found, throw with code 'CONFLICT' and conflictsWith array

  // 5. Import timeBlockService from '../schedule/time-block.service.js'
  //    Call timeBlockService.removeLessonFromBlock(teacherId, sourceBlockId, lessonId, options)

  // 6. Create target block:
  //    Call timeBlockService.createTimeBlock(teacherId, {
  //      day: hebrewDay,
  //      startTime: targetStartTime,
  //      endTime: targetEndTime,
  //      location: targetRoom,
  //    }, options)
  //    Extract newBlockId from result.timeBlock._id.toString()

  // 7. Assign lesson to new block:
  //    Call timeBlockService.assignLessonToBlock({
  //      teacherId,
  //      studentId,
  //      timeBlockId: newBlockId,
  //      startTime: targetStartTime,
  //      duration: lessonDuration,
  //    }, options)

  // 8. Cleanup: check if source block has remaining active lessons
  //    Re-read teacher doc, find source block, count active lessons
  //    If zero active lessons remain, call timeBlockService.deleteTimeBlock(teacherId, sourceBlockId, options)

  // 9. Return fresh schedule for targetDay
  return getRoomSchedule(targetDay, options);
}
```

Important implementation details:
- Import `timeBlockService` from `'../schedule/time-block.service.js'` at the top of the file
- Use `ObjectId.createFromHexString()` for all ID conversions (not `new ObjectId(string)`)
- Use `.toString()` on both sides of comparisons for ObjectId safety
- The conflict pre-check must exclude the source lesson being moved (it will overlap with itself in current position)
- Error codes: 'NOT_FOUND' for missing teacher/block/lesson, 'CONFLICT' for target conflicts
- Wrap timeBlockService errors: if createTimeBlock or assignLessonToBlock throws, log and re-throw with descriptive message

**room-schedule.controller.js:** Add `rescheduleLesson` handler:
- Validate body with `validateRescheduleBody`
- Call `roomScheduleService.rescheduleLesson(validated, { context: { ...req.context, schoolYearId } })`
- Return 200 with fresh schedule on success
- Handle error codes: CONFLICT -> 409, NOT_FOUND -> 404, TENANT_GUARD -> 403, else -> 500
- Export `rescheduleLesson` in the controller object

**room-schedule.route.js:** Add route:
- `router.put('/reschedule-lesson', requireAuth(['מנהל']), roomScheduleController.rescheduleLesson)`
- Import `validateRescheduleBody` in controller (not route)
  </action>
  <verify>
1. Server starts without errors (`npm run dev`)
2. Test validation: `curl -X PUT /api/room-schedule/reschedule-lesson` with invalid body returns 400 with descriptive error
3. Test not-found: PUT with valid-format but non-existent IDs returns 404
4. Test the full reschedule flow using seed data:
   - GET /api/room-schedule?day=0 to find a lesson activity with lessonId, blockId, teacherId
   - PUT /api/room-schedule/reschedule-lesson with that lesson's data + a new targetRoom/targetStartTime/targetEndTime
   - Verify response shows the lesson at the new location
   - GET /api/room-schedule?day=0 again to confirm persistence
  </verify>
  <done>PUT /api/room-schedule/reschedule-lesson endpoint accepts teacherId, sourceBlockId, lessonId, targetRoom, targetDay, targetStartTime, targetEndTime. It atomically removes the lesson from the source block, creates a new block at the target, assigns the lesson there, cleans up empty source blocks, and returns the fresh schedule. Conflicts return 409, not-found returns 404.</done>
</task>

</tasks>

<verification>
1. GET /api/room-schedule?day=N returns enhanced activity data with lessonId, studentId, duration, blockId for all timeBlock activities
2. PUT /api/room-schedule/reschedule-lesson successfully moves a single lesson to a new room/time
3. Reschedule returns 409 when target has a conflict
4. After rescheduling the last lesson from a block, the source block is deleted (empty block cleanup)
5. Existing moveActivity endpoint continues to work unchanged for block-level moves
</verification>

<success_criteria>
- All 6 phase success criteria fields (lessonId, studentId, duration, blockId) present in API response
- Reschedule endpoint handles same-day and cross-day moves
- Conflict detection at target works correctly
- Empty source block auto-cleanup works
- No regressions to existing GET schedule or PUT move endpoints
</success_criteria>

<output>
After completion, create `.planning/phases/38-single-lesson-reschedule-detail-modal/38-01-SUMMARY.md`
</output>
