---
phase: 85-theory-course-gap-closure
plan: 02
type: execute
wave: 2
depends_on: ["85-01"]
files_modified:
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/TheoryLessonDetails.tsx
autonomous: true

must_haves:
  truths:
    - "apiService.theory has autoGroupLessons and linkLessonsToCourse methods"
    - "TheoryLessonDetails fetches parent course when lesson has courseId"
    - "Course badge displayed on lesson detail page when courseId is non-null"
    - "Enrolled students list shows course roster (not lesson studentIds) when course exists"
    - "Lesson detail page still works normally when courseId is null"
  artifacts:
    - path: "/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js"
      provides: "autoGroupLessons and linkLessonsToCourse API methods"
    - path: "/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/TheoryLessonDetails.tsx"
      provides: "Course-aware lesson detail with conditional course fetch and roster inheritance"
      contains: "courseId"
  key_links:
    - from: "/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/TheoryLessonDetails.tsx"
      to: "apiService.js theoryService.getCourseById"
      via: "conditional fetch when courseId is non-null"
      pattern: "getCourseById.*courseId"
    - from: "/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js"
      to: "POST /api/theory/courses/auto-group"
      via: "autoGroupLessons method"
      pattern: "autoGroupLessons"
---

<objective>
Add auto-group and link-lessons API methods to the frontend apiService, and make TheoryLessonDetails.tsx course-aware with conditional course fetch, course badge, and roster inheritance.

Purpose: Close the gap between the course data layer (Phase 84) and the lesson detail UI -- when a lesson belongs to a course, the detail page shows course context and uses the course's canonical student roster.
Output: apiService extended with 2 new methods; TheoryLessonDetails shows course badge and inherits course roster.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-theory-lesson-course-architecture-recurring-grouping-shared-rosters-cross-session-analytics/84-03-SUMMARY.md
@.planning/phases/85-theory-course-gap-closure-auto-grouping-roster-inheritance-lesson-ui/85-RESEARCH.md
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/TheoryLessonDetails.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add autoGroupLessons and linkLessonsToCourse to apiService.js</name>
  <files>/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/services/apiService.js</files>
  <action>
Find the `theoryService` object in apiService.js (it already has getCourses, getCourseById, createCourse, updateCourse, deleteCourse, addStudentToCourse, removeStudentFromCourse, getCourseAnalytics from Phase 84-03).

Add two new methods to the theory service section:

1. **`autoGroupLessons({ schoolYearId, minLessonCount, dryRun })`**:
   - `POST /api/theory/courses/auto-group`
   - Body: `{ schoolYearId, minLessonCount, dryRun }`
   - Returns `response.data.data`
   - Follow the exact same pattern as other POST methods in theoryService (use `apiClient.post` or equivalent).

2. **`linkLessonsToCourse(courseId, lessonIds)`**:
   - `POST /api/theory/courses/${courseId}/link-lessons`
   - Body: `{ lessonIds }`
   - Returns `response.data.data`
   - Follow the same pattern as other course methods.

Place these methods near the other course-related methods in theoryService for readability.
  </action>
  <verify>
Search apiService.js for both methods: `grep -n 'autoGroupLessons\|linkLessonsToCourse' src/services/apiService.js` from the frontend repo — both should appear.
  </verify>
  <done>apiService.theory.autoGroupLessons and apiService.theory.linkLessonsToCourse exist and call the correct backend endpoints.</done>
</task>

<task type="auto">
  <name>Task 2: Add course awareness to TheoryLessonDetails.tsx</name>
  <files>/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/TheoryLessonDetails.tsx</files>
  <action>
This is a 1256-line file with zero course references. Make these targeted changes:

**1. Update TheoryLesson interface:**
Add `courseId?: string | null` to the TheoryLesson interface (around line 34-55).

**2. Add Course interface:**
Add a simple Course interface near the existing interfaces:
```typescript
interface Course {
  _id: string
  category: string
  teacherId: string
  dayOfWeek: number
  startTime: string
  endTime: string
  location: string
  studentIds: string[]
  lessonIds: string[]
  schoolYearId: string
  isActive: boolean
}
```

**3. Add course state:**
In the component function, add state for the parent course:
```typescript
const [course, setCourse] = useState<Course | null>(null)
```

**4. Add course fetch effect:**
After the existing effect that fetches the lesson, add a conditional course fetch:
```typescript
useEffect(() => {
  if (theoryLesson?.courseId) {
    theoryService.getCourseById(theoryLesson.courseId)
      .then((data: Course) => setCourse(data))
      .catch(() => setCourse(null)) // Non-fatal
  } else {
    setCourse(null)
  }
}, [theoryLesson?.courseId])
```
Use `theoryService` which is already imported at the top of the file.

**5. Add course badge in the header area:**
Find the lesson header section (where category, date, teacher info are displayed). Add a course badge ONLY when `course` is non-null:
```tsx
{course && (
  <Chip
    color="primary"
    variant="flat"
    size="sm"
    startContent={<BookOpenIcon size={14} />}
  >
    קורס: {course.category}
  </Chip>
)}
```
`Chip` is already imported from `@heroui/react` and `BookOpenIcon` is already imported from `@phosphor-icons/react`.

**6. Roster inheritance — modify enrolled students logic:**
Find where the component displays enrolled students (the section that maps over `studentIds` or students). Apply this logic:
- When `course` exists (non-null), use `course.studentIds` as the canonical enrolled student list instead of `theoryLesson.studentIds`.
- When `course` is null, continue using `theoryLesson.studentIds` as before (backward compatible).
- The variable holding the effective student IDs should be:
```typescript
const effectiveStudentIds = course?.studentIds ?? theoryLesson?.studentIds ?? []
```
Then use `effectiveStudentIds` wherever the enrolled student IDs are displayed or used for fetching student details.

**7. Add a small note when roster is inherited:**
When course is active and providing the roster, add a subtle text note below the enrolled students header:
```tsx
{course && (
  <span className="text-xs text-muted-foreground">
    רשימה מקורס הורה
  </span>
)}
```

IMPORTANT: Do NOT modify any other behavior. The attendance section should continue using the lesson's own attendance object (not the course). The edit/delete functionality stays unchanged. Only the enrolled students display and the header badge change.
  </action>
  <verify>
1. `grep -c 'courseId' src/pages/TheoryLessonDetails.tsx` from frontend repo — should be >= 3 (interface field, fetch effect, effectiveStudentIds).
2. `grep 'getCourseById' src/pages/TheoryLessonDetails.tsx` — should show the course fetch call.
3. `grep 'effectiveStudentIds' src/pages/TheoryLessonDetails.tsx` — should show the roster inheritance variable.
  </verify>
  <done>TheoryLessonDetails.tsx conditionally fetches parent course when courseId exists, shows course badge in header, uses course.studentIds for enrolled students when available, falls back to lesson.studentIds when no course.</done>
</task>

</tasks>

<verification>
- apiService.js contains autoGroupLessons and linkLessonsToCourse methods
- TheoryLessonDetails.tsx has courseId in interface, course state, conditional fetch, course badge, and effectiveStudentIds for roster inheritance
- Lessons without courseId continue to work exactly as before (no regression)
- Course badge only appears when course is non-null
</verification>

<success_criteria>
- Two new API methods callable from frontend
- Lesson detail page shows course context when courseId exists
- Enrolled students list uses course roster when available
- No regression for lessons without a course
- Non-fatal course fetch (page still works if course fetch fails)
</success_criteria>

<output>
After completion, create `.planning/phases/85-theory-course-gap-closure-auto-grouping-roster-inheritance-lesson-ui/85-02-SUMMARY.md`
</output>
