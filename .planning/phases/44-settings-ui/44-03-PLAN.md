---
phase: 44-settings-ui
plan: 03
type: execute
wave: 3
depends_on: ["44-01", "44-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All 6 UI requirements (UI-01 through UI-06) are visually verified"
    - "No regressions in existing Settings page functionality"
---

<objective>
Visual verification of the complete Settings UI for roles and permissions.

Purpose: Confirm all UI requirements work correctly in the browser before marking the phase complete.
Output: Verified UI ready for Phase 45 migration.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/44-settings-ui/44-01-SUMMARY.md
@.planning/phases/44-settings-ui/44-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Settings UI with staff role assignment table and permission matrix editor</what-built>
  <how-to-verify>
    1. Start the frontend dev server: `cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && npm run dev`
    2. Log in as a tenant admin (role: מנהל)
    3. Navigate to Settings (הגדרות) page
    4. **UI-01**: Verify two tabs appear: "הגדרות כלליות" and "תפקידים והרשאות"
    5. Verify general settings tab still shows rooms, general info, director, ministry info, profile, defaults — all unchanged
    6. Click "תפקידים והרשאות" tab
    7. **UI-01**: Verify staff table shows all teachers with name, role badges, and status
    8. **UI-02**: Click Edit on a teacher row — verify modal opens with role checkboxes grouped by tier
    9. **UI-03**: Check "רכז/ת מחלקתי" checkbox — verify department multi-select appears with instrument departments
    10. Uncheck it — verify departments disappear
    11. Save a role change — verify toast and table updates
    12. Try removing the last admin — verify error message appears
    13. **UI-04**: Scroll to permission matrix — verify role dropdown and domains-by-actions grid
    14. Select a non-admin role (e.g., מורה) — verify cells show scope badges
    15. Click a cell — verify scope cycles (empty -> own -> department -> all -> empty)
    16. **UI-05**: Click "Reset to Default" — verify grid refreshes to defaults with toast
    17. **UI-06**: Select a non-admin role — verify settings and roles rows are grayed out with lock icon
    18. Select an admin role (מנהל) — verify all cells are locked with info banner
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
All 6 UI requirements verified in browser by human tester.
</verification>

<success_criteria>
- Human confirms all UI-01 through UI-06 requirements work as expected
- No visual regressions in existing settings
</success_criteria>

<output>
After completion, create `.planning/phases/44-settings-ui/44-03-SUMMARY.md`
</output>
