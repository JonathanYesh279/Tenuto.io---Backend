---
phase: 06-testing-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.config.tenant-isolation.js
  - test/tenant-isolation/setup.js
  - test/tenant-isolation/fixtures/two-tenant-seed.js
  - test/tenant-isolation/helpers/test-app.js
  - test/tenant-isolation/helpers/token.js
  - package.json
autonomous: true
must_haves:
  truths:
    - "Tenant isolation tests have a dedicated vitest config that does NOT use test/setup.js global mocks"
    - "Two-tenant seed fixture creates Tenant A and Tenant B with teachers, students, orchestras, school years, and all 11 collection types"
    - "Test app helper builds a real Express app with the actual middleware chain (authenticateToken -> buildContext -> enforceTenant -> stripTenantId -> addSchoolYearToRequest)"
    - "npm run test:tenant-isolation executes the tenant isolation test suite"
  artifacts:
    - path: "vitest.config.tenant-isolation.js"
      provides: "Vitest config with no setupFiles, pool:forks, test env vars"
      contains: "test/tenant-isolation"
    - path: "test/tenant-isolation/setup.js"
      provides: "MMS lifecycle management — start, connect, seed, reset, teardown"
      exports: ["setupTenantIsolationDB", "teardownTenantIsolationDB", "resetCollections", "getTestDb"]
    - path: "test/tenant-isolation/fixtures/two-tenant-seed.js"
      provides: "Canonical seed data for 2 tenants across all 11 collections"
      exports: ["TENANT_A_ID", "TENANT_B_ID", "seedTwoTenants", "teacherA", "teacherB", "studentA", "studentB"]
    - path: "test/tenant-isolation/helpers/test-app.js"
      provides: "Express app builder with real middleware chain and all route modules"
      exports: ["createTestApp"]
    - path: "test/tenant-isolation/helpers/token.js"
      provides: "JWT minting helper for test tenants"
      exports: ["makeToken"]
    - path: "package.json"
      provides: "test:tenant-isolation and test:ci npm scripts"
      contains: "vitest.config.tenant-isolation.js"
  key_links:
    - from: "vitest.config.tenant-isolation.js"
      to: "test/tenant-isolation/**/*.test.js"
      via: "include glob pattern"
      pattern: "test/tenant-isolation"
    - from: "test/tenant-isolation/setup.js"
      to: "services/mongoDB.service.js"
      via: "dynamic import override with real MMS connection"
      pattern: "mongoDB.service"
    - from: "test/tenant-isolation/helpers/test-app.js"
      to: "middleware/tenant.middleware.js"
      via: "import of real enforceTenant, buildContext, stripTenantId"
      pattern: "enforceTenant"
---

<objective>
Create the test infrastructure for tenant isolation testing: a dedicated Vitest config, MongoDB Memory Server setup, two-tenant seed fixtures, Express app builder with real middleware, and JWT helpers.

Purpose: All subsequent test plans (read isolation, write isolation, allowlist, concurrent) depend on this shared infrastructure. Without it, tests would either use the global test/setup.js (which mocks out tenant guards) or duplicate boilerplate in every file.

Output: 6 files providing the complete tenant isolation test scaffold.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-testing-verification/06-RESEARCH.md

@vitest.config.phase2.js
@test/setup.js
@test/setup/mongodb-memory-server.js
@middleware/tenant.middleware.js
@middleware/auth.middleware.js
@utils/queryScoping.js
@config/crossTenantAllowlist.js
@server.js
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create vitest config, MMS setup, and seed fixtures</name>
  <files>
    vitest.config.tenant-isolation.js
    test/tenant-isolation/setup.js
    test/tenant-isolation/fixtures/two-tenant-seed.js
  </files>
  <action>
**vitest.config.tenant-isolation.js** — Create a new Vitest config following the vitest.config.phase2.js pattern:
- `include: ['test/tenant-isolation/**/*.test.js']`
- NO `setupFiles` (critical: must NOT use test/setup.js which mocks requireTenantId and buildScopedFilter)
- `pool: 'forks'` (process isolation between test files)
- `maxConcurrency: 3`, `maxWorkers: 2` (MMS is memory-heavy)
- `testTimeout: 30000`, `hookTimeout: 30000` (MMS startup can be slow)
- `env`: NODE_ENV=test, ACCESS_TOKEN_SECRET=test-access-secret, REFRESH_TOKEN_SECRET=test-refresh-secret, LOG_LEVEL=silent
- `resolve.alias`: `'@': path.resolve(import.meta.dirname, './')`
- `globals: true`

**test/tenant-isolation/setup.js** — MMS lifecycle manager (NOT a vitest setupFile; imported directly by each test):
- `setupTenantIsolationDB()`: Create MongoMemoryServer instance (version 6.0.0, wiredTiger, random port, downloadDir: './node_modules/.cache/mongodb-memory-server/mongodb-binaries'). Connect MongoClient. Store db reference. Create tenant isolation indexes (tenantId compound indexes on all 11 collections: student, teacher, orchestra, rehearsal, theory_lesson, bagrut, school_year, activity_attendance, hours_summary, import_log, ministry_report_snapshots). Return { db, client, uri }.
- `teardownTenantIsolationDB()`: Close client, stop server.
- `resetCollections()`: Drop all documents from all collections (like existing resetTestDatabase).
- `getTestDb()`: Return the db reference.
- `patchMongoDBService(db)`: Uses `vi.doMock` to override `../../services/mongoDB.service.js` so that `getCollection(name)` returns `db.collection(name)`, `getDB()` returns `db`, and `initializeMongoDB` is a no-op. CRITICAL: This must be called BEFORE any app modules are imported (ESM static imports resolve at load time). Document this prominently in comments.

**test/tenant-isolation/fixtures/two-tenant-seed.js** — Canonical two-tenant seed data:
- Export constants: `TENANT_A_ID = 'tenant-a-raanana'`, `TENANT_B_ID = 'tenant-b-rishon'`
- Export `tenantA` and `tenantB` documents (for tenant collection): `{ _id: new ObjectId(), slug, name, tenantId, isActive: true }`
- Export `teacherA` (admin in Tenant A): `{ _id: new ObjectId(), tenantId: TENANT_A_ID, personalInfo: { firstName: 'David', lastName: 'Cohen', email: 'david@raanana.edu' }, credentials: { email: 'david@raanana.edu', password: bcrypt.hashSync('testpass123', 10), tokenVersion: 0, isInvitationAccepted: true }, roles: ['מנהל'], isActive: true }`
- Export `teacherB` (admin in Tenant B): similar structure with TENANT_B_ID, Sarah Levi, sarah@rishon.edu
- Export `teacherTeacherA` (teacher role, NOT admin, in Tenant A): for testing non-admin access patterns
- Export `studentA` (in Tenant A): `{ _id: new ObjectId(), tenantId: TENANT_A_ID, personalInfo: { firstName: 'Yoav', lastName: 'Shapira' }, academicInfo: { instrumentProgress: [{ instrument: 'פסנתר', currentStage: 1, isPrimary: true }] }, teacherAssignments: [{ teacherId: teacherA._id.toString(), isActive: true }], isActive: true }`
- Export `studentB` (in Tenant B): similar with TENANT_B_ID
- Export `schoolYearA` and `schoolYearB`: `{ _id: new ObjectId(), tenantId, year: '2025-2026', name: 'תשפ"ו', isCurrent: true, startDate, endDate }`
- Export `orchestraA` and `orchestraB`: `{ _id: new ObjectId(), tenantId, name, type, conductorId: teacherX._id.toString(), memberIds: [studentX._id.toString()], isActive: true }`
- Export `rehearsalA` and `rehearsalB`: linked to their respective orchestra
- Export `theoryLessonA` and `theoryLessonB`: `{ _id, tenantId, name, teacherId, studentIds: [studentX._id.toString()], isActive: true }`
- Export `bagrutA` and `bagrutB`: `{ _id, tenantId, studentId: studentX._id.toString(), year: '2026', program: [] }`
- Export `hoursSummaryA` and `hoursSummaryB`: `{ _id, tenantId, teacherId, schoolYearId, totalHours: 10 }`
- Export `attendanceA` and `attendanceB`: `{ _id, tenantId, studentId, type: 'rehearsal', date, status: 'present' }`
- Export `seedTwoTenants(db)`: Insert all documents into their respective collections. Use insertMany where multiple docs exist. Return the full set of document references for test assertions.
- All ObjectId values must be created at module level (stable references across imports).
  </action>
  <verify>
Run `node -e "import('./test/tenant-isolation/fixtures/two-tenant-seed.js').then(m => console.log('TENANT_A:', m.TENANT_A_ID, 'TENANT_B:', m.TENANT_B_ID, 'Collections:', Object.keys(m).filter(k => k.startsWith('seed') || k.endsWith('A') || k.endsWith('B')).length))"` to confirm exports work.
Run `node -e "import('./vitest.config.tenant-isolation.js').then(m => { const c = m.default.test; console.log('include:', c.include, 'setupFiles:', c.setupFiles || 'NONE', 'pool:', c.pool) })"` to confirm config has no setupFiles.
  </verify>
  <done>
vitest.config.tenant-isolation.js exists with NO setupFiles, includes test/tenant-isolation/**/*.test.js. test/tenant-isolation/setup.js exports setupTenantIsolationDB, teardownTenantIsolationDB, resetCollections, getTestDb, patchMongoDBService. two-tenant-seed.js exports TENANT_A_ID, TENANT_B_ID, all fixture documents for 11 collections, and seedTwoTenants function.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create test app builder, token helper, and npm scripts</name>
  <files>
    test/tenant-isolation/helpers/test-app.js
    test/tenant-isolation/helpers/token.js
    package.json
  </files>
  <action>
**test/tenant-isolation/helpers/token.js** — JWT minting helper:
- Export `makeToken(teacher)`: Signs a JWT with `{ _id: teacher._id.toString(), tenantId: teacher.tenantId, roles: teacher.roles, email: teacher.credentials.email, firstName: teacher.personalInfo.firstName, lastName: teacher.personalInfo.lastName, version: teacher.credentials.tokenVersion || 0 }` using `process.env.ACCESS_TOKEN_SECRET` ('test-access-secret' in test env) with `{ expiresIn: '1h' }`.
- Import jsonwebtoken. This is a thin helper, NOT a mock.

**test/tenant-isolation/helpers/test-app.js** — Express app builder with REAL middleware chain:
- Export `createTestApp()`: Builds an Express app matching server.js middleware structure.
- Use the REAL imports (not mocked): `authenticateToken` from `middleware/auth.middleware.js`, `buildContext` from `middleware/tenant.middleware.js`, `enforceTenant` from `middleware/tenant.middleware.js`, `stripTenantId` from `middleware/tenant.middleware.js`, `addSchoolYearToRequest` from `middleware/school-year.middleware.js`.
- Mount all tenant-scoped routes with the same middleware chain as server.js:
  - `/api/student`: authenticateToken, buildContext, enforceTenant, stripTenantId, addSchoolYearToRequest, studentRoutes
  - `/api/teacher`: same chain, teacherRoutes
  - `/api/orchestra`: same chain, orchestraRoutes
  - `/api/rehearsal`: same chain, rehearsalRoutes
  - `/api/theory`: same chain, theoryRoutes
  - `/api/bagrut`: same chain, bagrutRoutes
  - `/api/school-year`: same chain, schoolYearRoutes
  - `/api/hours-summary`: same chain, hoursSummaryRoutes
  - `/api/attendance`: same chain, attendanceRoutes
  - `/api/analytics`: same chain, analyticsRoutes
  - `/api/schedule`: same chain, scheduleRoutes
- Mount exempt routes WITHOUT enforceTenant (to verify they work):
  - `/api/auth`: authRoutes
  - `/api/health`: healthRoutes
  - `/api/config`: inline handler returning config JSON
- Include `express.json()`, `cookieParser()` middleware.
- Include the `errorHandler` from `middleware/error.handler.js` at the end.
- CRITICAL: This file must be imported AFTER `patchMongoDBService(db)` has been called. Add a prominent comment: "// IMPORTANT: Import this file AFTER calling patchMongoDBService(db) from setup.js"
- Return the express app instance (NOT calling listen()).

**package.json** — Add npm scripts (merge into existing scripts):
- `"test:tenant-isolation": "vitest run --config vitest.config.tenant-isolation.js"`
- `"test:tenant-isolation:watch": "vitest --config vitest.config.tenant-isolation.js"`
- `"test:ci": "npm run test:tenant-isolation && node utils/validateAllowlist.js"`
- Do NOT modify any other scripts. Preserve all existing content.
  </action>
  <verify>
Run `node -e "import('./test/tenant-isolation/helpers/token.js').then(m => console.log('makeToken:', typeof m.makeToken))"` to confirm token helper exports.
Run `node -e "const pkg = JSON.parse(require('fs').readFileSync('package.json','utf8')); console.log('tenant-isolation:', pkg.scripts['test:tenant-isolation']); console.log('ci:', pkg.scripts['test:ci'])"` to confirm npm scripts.
  </verify>
  <done>
test-app.js exports createTestApp() that builds a real Express app with the actual middleware chain from server.js. token.js exports makeToken() that mints real JWTs with tenantId. package.json has test:tenant-isolation and test:ci scripts.
  </done>
</task>

</tasks>

<verification>
1. `vitest.config.tenant-isolation.js` has no `setupFiles` property (grep confirms absence)
2. `test/tenant-isolation/setup.js` does NOT import from `test/setup.js`
3. `test/tenant-isolation/fixtures/two-tenant-seed.js` covers all 11 tenant-scoped collections
4. `test/tenant-isolation/helpers/test-app.js` imports real middleware (not mocked)
5. `npm run test:tenant-isolation` script exists in package.json and references correct config
</verification>

<success_criteria>
All 6 files exist and are syntactically valid. The vitest config explicitly avoids the global setup mocks. The seed data covers all 11 tenant-scoped collections with distinct tenantId values. The test app uses the real middleware chain. npm scripts are registered.
</success_criteria>

<output>
After completion, create `.planning/phases/06-testing-verification/06-01-SUMMARY.md`
</output>
