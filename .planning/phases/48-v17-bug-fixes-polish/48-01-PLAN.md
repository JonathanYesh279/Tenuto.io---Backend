---
phase: 48-v17-bug-fixes-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Bagruts.tsx
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/super-admin/TenantAdminManagementPage.tsx
autonomous: true

must_haves:
  truths:
    - "Teacher-role users can load the bagrut list page without ReferenceError"
    - "No console.log debugging statements exist in Bagruts.tsx"
    - "Super admin sidebar includes navigation link to /tenant-admins"
    - "ROLE_COLORS in TenantAdminManagementPage.tsx uses current TEACHER_ROLES names"
  artifacts:
    - path: "src/pages/Bagruts.tsx"
      provides: "Null-safe teacherProfile handling"
      contains: "teacherProfile ? \\[teacherProfile\\] : \\[\\]"
    - path: "src/pages/super-admin/TenantAdminManagementPage.tsx"
      provides: "Updated ROLE_COLORS with current role names"
      contains: "רכז/ת כללי"
  key_links:
    - from: "Bagruts.tsx line 128"
      to: "setTeachers"
      via: "null-safe array wrapping"
      pattern: "teacherProfile \\?"
    - from: "TenantAdminManagementPage.tsx ROLE_COLORS"
      to: "TEACHER_ROLES in constants.js"
      via: "matching role name keys"
      pattern: "רכז/ת מחלקתי|ניצוח|מדריך הרכב|תאוריה"
---

<objective>
Fix remaining bugs and tech debt from v1.7 audit: teacher-role bagrut page crash, outdated role color mapping.

Purpose: Close out v1.7 milestone with all known issues resolved.
Output: Two patched frontend files.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Bagruts.tsx
@/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/super-admin/TenantAdminManagementPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix teacherProfile null crash and audit console statements in Bagruts.tsx</name>
  <files>/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/Bagruts.tsx</files>
  <action>
Fix the ReferenceError crash on line 128 where `setTeachers([teacherProfile])` is called without null-safety.

Line 128 currently reads:
```
setTeachers([teacherProfile])
```

Change to match the pattern already used on line 102:
```
setTeachers(teacherProfile ? [teacherProfile] : [])
```

This is the only code change needed. The teacherProfile variable comes from `apiService.teachers.getTeacher(user._id)` on line 96 inside a Promise.all. If the API returns null/undefined (e.g., teacher not found or permissions issue), wrapping null in an array causes downstream ReferenceErrors when the teachers array is iterated.

Also audit for any `console.log` debugging statements. Current state: only `console.error` calls exist (lines 147, 213, 239) which are proper error handlers -- these should be LEFT IN PLACE. If any `console.log` statements are found, remove them.

NOTE: Criterion #3 (sidebar nav link to /tenant-admins) is ALREADY SATISFIED -- the link exists in Sidebar.tsx line 98 in the superAdminNavigation array. No changes needed.
  </action>
  <verify>
Search Bagruts.tsx for the pattern `setTeachers([teacherProfile])` -- should NOT appear (should always have null check).
Search Bagruts.tsx for `console.log` -- should return zero matches.
Confirm `console.error` calls still exist (proper error handling).
  </verify>
  <done>Line 128 uses `teacherProfile ? [teacherProfile] : []` pattern. No console.log statements in file.</done>
</task>

<task type="auto">
  <name>Task 2: Update ROLE_COLORS to match current TEACHER_ROLES names</name>
  <files>/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/super-admin/TenantAdminManagementPage.tsx</files>
  <action>
Replace the ROLE_COLORS constant (lines 35-53) to use the current role names from TEACHER_ROLES in config/constants.js.

Current (incorrect) role names and their replacements:

| Old Name (wrong) | New Name (correct) | Tier |
|---|---|---|
| רכז מחלקה | רכז/ת מחלקתי | coordinator |
| רכז הרכבים | רכז/ת כללי | coordinator |
| מנצח | ניצוח | teaching |
| מלווה | ליווי פסנתר | teaching |
| מורה תאוריה | תאוריה | teaching |
| מורה לכלי הקשה | הלחנה | teaching |
| מורה להרכבים | מדריך הרכב | teaching |
| מורה לקאמה | מורה מגמה | teaching |

The updated ROLE_COLORS should be:
```typescript
const ROLE_COLORS: Record<string, string> = {
  // Admin tier
  'מנהל': 'bg-red-100 text-red-700 border-red-200',
  'סגן מנהל': 'bg-red-50 text-red-600 border-red-100',
  'מזכירות': 'bg-red-50 text-red-600 border-red-100',
  // Coordinator tier
  'רכז/ת כללי': 'bg-blue-100 text-blue-700 border-blue-200',
  'רכז/ת מחלקתי': 'bg-blue-50 text-blue-600 border-blue-100',
  // Teaching tier
  'מורה': 'bg-green-100 text-green-700 border-green-200',
  'ניצוח': 'bg-green-50 text-green-600 border-green-100',
  'מדריך הרכב': 'bg-green-50 text-green-600 border-green-100',
  'תאוריה': 'bg-green-50 text-green-600 border-green-100',
  'ליווי פסנתר': 'bg-green-50 text-green-600 border-green-100',
  'הלחנה': 'bg-green-50 text-green-600 border-green-100',
  'מורה מגמה': 'bg-green-50 text-green-600 border-green-100',
  // View-only
  'צפייה בלבד': 'bg-gray-100 text-gray-600 border-gray-200',
}
```

This maps exactly to the 13 roles in TEACHER_ROLES (config/constants.js lines 107-116). Keep the same tier-based color scheme (admin=red, coordinator=blue, teaching=green, view-only=gray).
  </action>
  <verify>
Grep TenantAdminManagementPage.tsx for old role names (`רכז מחלקה`, `מנצח`, `מלווה`, `מורה תאוריה`) -- should return zero matches.
Grep for new role names (`רכז/ת כללי`, `רכז/ת מחלקתי`, `ניצוח`, `מדריך הרכב`, `תאוריה`, `ליווי פסנתר`, `הלחנה`, `מורה מגמה`) -- all should appear.
Count entries in ROLE_COLORS -- should be 13 (matching TEACHER_ROLES length).
  </verify>
  <done>ROLE_COLORS contains all 13 current role names from TEACHER_ROLES with correct tier-based coloring. No old/renamed role names remain.</done>
</task>

</tasks>

<verification>
1. Grep Bagruts.tsx for unsafe `setTeachers([teacherProfile])` (without ternary) -- zero matches
2. Grep Bagruts.tsx for `console.log` -- zero matches
3. Grep TenantAdminManagementPage.tsx for old role names -- zero matches
4. Grep TenantAdminManagementPage.tsx for `רכז/ת מחלקתי` -- one match in ROLE_COLORS
5. Grep Sidebar.tsx for `tenant-admins` -- confirms link present (already satisfied)
</verification>

<success_criteria>
- Teacher-role users can load the bagrut list page without crash (null-safe teacherProfile)
- No console.log debugging statements in Bagruts.tsx
- Super admin sidebar has /tenant-admins link (pre-existing, verified)
- ROLE_COLORS uses all 13 current TEACHER_ROLES names
</success_criteria>

<output>
After completion, create `.planning/phases/48-v17-bug-fixes-polish/48-01-SUMMARY.md`
</output>
