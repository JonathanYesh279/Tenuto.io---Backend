---
phase: 08-fix-import-teacher-bugs-wrong-labels-missing-enums-failed-creation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/constants.js
  - api/teacher/teacher.validation.js
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/ImportData.tsx
  - /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/constants/enums.ts
autonomous: true

must_haves:
  truths:
    - "Frontend preview shows 'מורה חדש' for new teacher rows (not 'תלמיד חדש')"
    - "Teachers with degree 'תואר שלישי' or 'מוסמך בכיר' pass Joi validation without warnings"
    - "Teachers with management role 'תיאור תפקיד' or 'ריכוז אחר (פרט)' pass Joi validation without warnings"
    - "Import execution creates teacher documents for unmatched rows (createdCount > 0, not silently 0)"
    - "Frontend guide text lists all available degree and management role values"
  artifacts:
    - path: "config/constants.js"
      provides: "TEACHER_DEGREES with 6 values, MANAGEMENT_ROLES with 6 values"
      contains: "תואר שלישי"
    - path: "api/teacher/teacher.validation.js"
      provides: "teacherImportSchema with createdAt and updatedAt fields"
      contains: "createdAt"
    - path: "/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/ImportData.tsx"
      provides: "Conditional label for teacher vs student import preview"
      contains: "מורה חדש"
    - path: "/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/constants/enums.ts"
      provides: "DEGREES with 6 values, MANAGEMENT_ROLES with 6 values"
      contains: "תואר שלישי"
  key_links:
    - from: "config/constants.js"
      to: "api/teacher/teacher.validation.js"
      via: "spread import of TEACHER_DEGREES and MANAGEMENT_ROLES into Joi .valid()"
      pattern: "valid\\(\\.\\.\\.TEACHER_DEGREES\\)"
    - from: "api/import/import.service.js buildImportTeacherDocument"
      to: "api/teacher/teacher.validation.js validateTeacherImport"
      via: "rawDoc passed to validateTeacherImport which must accept createdAt/updatedAt"
      pattern: "validateTeacherImport\\(rawDoc\\)"
---

<objective>
Fix three import teacher bugs: (1) wrong "תלמיד חדש" label in preview for teachers, (2) missing enum values for degrees and management roles causing validation warnings, (3) Joi schema rejecting all import documents due to missing createdAt/updatedAt field definitions.

Purpose: Unblock teacher import feature — currently import execution creates 0 teachers silently, and preview shows incorrect labels and warnings for valid data.
Output: Working teacher import with correct labels, accepted enums, and successful document creation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@config/constants.js
@api/teacher/teacher.validation.js
@api/import/import.service.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix backend constants and Joi import schema (Bugs 2 and 3)</name>
  <files>config/constants.js, api/teacher/teacher.validation.js</files>
  <action>
  **Bug 2 — Add missing enum values to config/constants.js:**

  In `config/constants.js` line 94, replace:
  ```javascript
  export const TEACHER_DEGREES = ['תואר שני', 'תואר ראשון', 'מוסמך', 'בלתי מוסמך'];
  ```
  with (ordered by level descending):
  ```javascript
  export const TEACHER_DEGREES = ['תואר שלישי', 'תואר שני', 'תואר ראשון', 'מוסמך בכיר', 'מוסמך', 'בלתי מוסמך'];
  ```

  In `config/constants.js` line 96, replace:
  ```javascript
  export const MANAGEMENT_ROLES = ['ריכוז פדגוגי', 'ריכוז מחלקה', 'סגן מנהל', 'ריכוז אחר'];
  ```
  with:
  ```javascript
  export const MANAGEMENT_ROLES = ['ריכוז פדגוגי', 'ריכוז מחלקה', 'סגן מנהל', 'ריכוז אחר', 'ריכוז אחר (פרט)', 'תיאור תפקיד'];
  ```

  No other backend file changes needed for Bug 2 — the Joi schemas in `teacher.validation.js` already spread these constants via `...TEACHER_DEGREES` and `...MANAGEMENT_ROLES`, so they auto-inherit the new values.

  **Bug 3 — Add createdAt and updatedAt to teacherImportSchema:**

  In `api/teacher/teacher.validation.js`, in the `teacherImportSchema` Joi.object (around line 342-422), add these two fields AFTER the `isActive` field (line 421) and BEFORE the closing `});`:
  ```javascript
  isActive: Joi.boolean().default(true),
  createdAt: Joi.date().default(() => new Date()),
  updatedAt: Joi.date().default(() => new Date()),
  ```

  This is the root cause of Bug 3: `buildImportTeacherDocument()` in `import.service.js` adds `createdAt` and `updatedAt` to every document, but `validateTeacherImport()` calls Joi validate WITHOUT `allowUnknown: true`. Joi 17 rejects unknown keys by default, so 100% of import documents fail validation and hit the `continue` on line 1236, resulting in 0 created teachers.

  Do NOT change the `validateTeacherImport` function to use `allowUnknown: true` — adding the fields explicitly to the schema is the correct approach because it validates they are Date objects and provides defaults.
  </action>
  <verify>
  Run: `node -e "import('./api/teacher/teacher.validation.js').then(m => { const doc = { tenantId: 't1', personalInfo: { firstName: 'Test', lastName: 'User' }, credentials: { email: 'test@test.com', password: 'hashed' }, createdAt: new Date(), updatedAt: new Date() }; const { error, value } = m.validateTeacherImport(doc); console.log('Error:', error ? error.message : 'NONE'); console.log('createdAt present:', !!value?.createdAt); console.log('Degrees count:', m.TEACHER_CONSTANTS.TEACHER_DEGREES.length); })"`

  Expected: Error: NONE, createdAt present: true, Degrees count: 6
  </verify>
  <done>
  - TEACHER_DEGREES has 6 values including 'תואר שלישי' and 'מוסמך בכיר'
  - MANAGEMENT_ROLES has 6 values including 'ריכוז אחר (פרט)' and 'תיאור תפקיד'
  - teacherImportSchema accepts documents with createdAt and updatedAt fields
  - validateTeacherImport returns no error for a well-formed import document
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix frontend label and sync enum constants (Bugs 1 and 2)</name>
  <files>/mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/ImportData.tsx, /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/constants/enums.ts</files>
  <action>
  **Bug 1 — Fix wrong label in ImportData.tsx:**

  In `ImportData.tsx` around line 634, replace:
  ```tsx
  <span className="text-blue-600">תלמיד חדש - ייווצר ברשומה חדשה</span>
  ```
  with:
  ```tsx
  <span className="text-blue-600">{activeTab === 'teachers' ? 'מורה חדש' : 'תלמיד חדש'} - ייווצר ברשומה חדשה</span>
  ```

  The `activeTab` state variable is already available in scope (defined on line 239 as `useState<ImportTab>('teachers')`).

  **Bug 2 — Update frontend guide text in ImportData.tsx:**

  Around line 135, replace:
  ```tsx
  <span className="text-gray-500 flex-1 text-xs">תואר שני, תואר ראשון, מוסמך, בלתי מוסמך</span>
  ```
  with:
  ```tsx
  <span className="text-gray-500 flex-1 text-xs">תואר שלישי, תואר שני, תואר ראשון, מוסמך בכיר, מוסמך, בלתי מוסמך</span>
  ```

  Around line 171, replace:
  ```tsx
  <span className="text-gray-500 flex-1 text-xs">ריכוז פדגוגי, ריכוז מחלקה, סגן מנהל, ריכוז אחר</span>
  ```
  with:
  ```tsx
  <span className="text-gray-500 flex-1 text-xs">ריכוז פדגוגי, ריכוז מחלקה, סגן מנהל, ריכוז אחר, ריכוז אחר (פרט), תיאור תפקיד</span>
  ```

  **Bug 2 — Sync enums.ts with backend constants:**

  In `src/constants/enums.ts` line 11, replace:
  ```typescript
  export const DEGREES = ['תואר שני', 'תואר ראשון', 'מוסמך', 'בלתי מוסמך'] as const;
  ```
  with:
  ```typescript
  export const DEGREES = ['תואר שלישי', 'תואר שני', 'תואר ראשון', 'מוסמך בכיר', 'מוסמך', 'בלתי מוסמך'] as const;
  ```

  In `src/constants/enums.ts` line 15, replace:
  ```typescript
  export const MANAGEMENT_ROLES = ['ריכוז פדגוגי', 'ריכוז מחלקה', 'סגן מנהל', 'ריכוז אחר'] as const;
  ```
  with:
  ```typescript
  export const MANAGEMENT_ROLES = ['ריכוז פדגוגי', 'ריכוז מחלקה', 'סגן מנהל', 'ריכוז אחר', 'ריכוז אחר (פרט)', 'תיאור תפקיד'] as const;
  ```
  </action>
  <verify>
  1. Check frontend TypeScript compiles: `cd /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend && npx tsc --noEmit --skipLibCheck 2>&1 | head -20`
  2. Grep to confirm label fix: `grep -n 'מורה חדש' /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/pages/ImportData.tsx`
  3. Grep to confirm enums: `grep -n 'תואר שלישי' /mnt/c/Users/yona2/Documents/Tenuto.io/Tenuto.io-Frontend/src/constants/enums.ts`
  </verify>
  <done>
  - Import preview shows "מורה חדש" for teacher tab and "תלמיד חדש" for student tab
  - Guide text in ImportData.tsx lists all 6 degree values and all 6 management role values
  - Frontend enums.ts DEGREES array has 6 values matching backend TEACHER_DEGREES
  - Frontend enums.ts MANAGEMENT_ROLES array has 6 values matching backend MANAGEMENT_ROLES
  - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
1. Backend Joi validation test: Create a minimal import document with createdAt/updatedAt and pass to validateTeacherImport — must return no error
2. Backend constants check: TEACHER_DEGREES.length === 6, MANAGEMENT_ROLES.length === 6
3. Frontend label check: grep ImportData.tsx for "מורה חדש" confirms conditional rendering
4. Frontend enum sync: DEGREES and MANAGEMENT_ROLES in enums.ts match backend constants exactly
5. TypeScript compilation: no errors in frontend build
</verification>

<success_criteria>
- validateTeacherImport accepts documents with createdAt/updatedAt (Bug 3 fixed — import creates teachers)
- TEACHER_DEGREES includes 'תואר שלישי' and 'מוסמך בכיר' in both backend and frontend (Bug 2 fixed)
- MANAGEMENT_ROLES includes 'ריכוז אחר (פרט)' and 'תיאור תפקיד' in both backend and frontend (Bug 2 fixed)
- Import preview shows "מורה חדש" for teacher imports (Bug 1 fixed)
- No TypeScript compilation errors in frontend
</success_criteria>

<output>
After completion, create `.planning/phases/08-fix-import-teacher-bugs-wrong-labels-missing-enums-failed-creation/08-01-SUMMARY.md`
</output>
