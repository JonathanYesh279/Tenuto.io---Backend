---
phase: 78-activity-rescheduling
plan: 03
type: execute
wave: 3
depends_on: ["78-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Day change works for lesson (timeBlock with student) -- already working, verify preserved"
    - "Day change works for empty timeBlock -- new via moveActivity targetDay"
    - "Day change works for rehearsal -- new via updateRehearsal with date + dayOfWeek"
    - "Day change works for theory lesson -- new via updateTheoryLesson dayOfWeek"
    - "Conflict detection catches room overlap across all source types"
    - "Client-side conflict preview shows warning before save attempt"
    - "Rehearsal day change produces consistent date/dayOfWeek on document"
  artifacts: []
  key_links: []
---

<objective>
Verify all activity types can be rescheduled to different day/room/time from the Activity Detail Modal, including conflict preview and data consistency.

Purpose: End-to-end verification that the backend extensions, frontend wiring, conflict preview, and date calculations work correctly together for all source types.

Output: Confirmed working rescheduling for all activity types with conflict preview.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/78-activity-rescheduling/78-01-SUMMARY.md
@.planning/phases/78-activity-rescheduling/78-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Full activity rescheduling (day/room/time) for all activity types via the Activity Detail Modal in the Room Schedule page. Changes include:
- Day selector now visible for ALL activity types (not just lessons)
- Empty timeBlocks can change day via moveActivity + targetDay
- Rehearsals can change day via updateRehearsal + date + dayOfWeek (date calculated to match target day)
- Theory lessons can change day via updateTheoryLesson + dayOfWeek
- Theory conflict detection now checks cross-source (rehearsals + timeBlocks)
- Client-side conflict preview shows warning banner before save when overlapping activities detected
- Single-instance rehearsal rescheduling (moves one occurrence, not the whole pattern)
  </what-built>
  <how-to-verify>
1. Navigate to Room Schedule page (לוח חדרים)
2. Pick a day that has activities visible

**Test 1 — Lesson (timeBlock with student):**
- Click a lesson activity (blue card with student name)
- In the modal, change the Day selector to a different day
- Change the room and/or time
- Click "שמור שינויים" (Save)
- Navigate to the target day to verify the lesson moved
- Expected: Lesson appears on the new day with new room/time

**Test 2 — Empty timeBlock (no student assigned):**
- Click an empty timeBlock activity (blue card without student)
- Change the Day selector to a different day
- Click Save
- Navigate to the target day to verify it moved
- Expected: Empty block appears on the new day

**Test 3 — Rehearsal:**
- Click a rehearsal activity (purple card)
- Change the Day selector to a different day
- Change room and/or time
- Click Save
- Navigate to the target day to verify it moved
- Expected: Rehearsal appears on the new day with new room/time
- ALSO CHECK: In MongoDB (or via API), verify the rehearsal document has BOTH `date` and `dayOfWeek` pointing to the same day (no date/dayOfWeek mismatch)

**Test 4 — Theory lesson:**
- Click a theory lesson activity (amber/yellow card)
- Change the Day selector to a different day
- Click Save
- Navigate to the target day to verify it moved
- Expected: Theory lesson appears on the new day

**Test 5 — Client-side conflict preview:**
- Click any activity, change the day/room/time to a slot that already has another activity
- Expected: A warning banner appears BELOW the edit fields BEFORE you click Save, showing the conflicting activity name and time
- The Save button should still be clickable (warning is non-blocking)

**Test 6 — Server-side conflict detection:**
- Try saving an activity to a day/room/time that has a confirmed conflict
- Expected: Toast error showing conflict details (teacher name, time) after save attempt

**Test 7 — Same-day changes still work:**
- Click any activity, change only room or time (keep same day)
- Expected: Works as before (regression check)
- Expected: No conflict preview warning if target slot is empty
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All 7 test scenarios pass as described above.
</verification>

<success_criteria>
- Day selector visible for all activity types
- Day+room+time changes work for lessons, empty timeBlocks, rehearsals, and theory lessons
- Rehearsal day change maintains date/dayOfWeek consistency
- Client-side conflict preview warns before save
- Server-side conflict detection catches overlaps across source types
- Same-day edits still work (no regression)
</success_criteria>

<output>
After completion, create `.planning/phases/78-activity-rescheduling/78-03-SUMMARY.md`
</output>
