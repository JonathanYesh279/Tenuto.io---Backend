---
phase: 78-activity-rescheduling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/room-schedule/room-schedule.service.js
  - api/room-schedule/room-schedule.validation.js
  - services/conflictDetectionService.js
autonomous: true

must_haves:
  truths:
    - "moveActivity accepts targetDay and changes day for empty timeBlocks only"
    - "moveActivity conflict check uses targetDay when different from original day"
    - "moveActivity updates Hebrew day name on timeBlock when day changes"
    - "Theory lesson updates check cross-source conflicts (rehearsals + timeBlocks)"
  artifacts:
    - path: "api/room-schedule/room-schedule.service.js"
      provides: "Extended moveActivity with targetDay support for timeBlocks only"
      contains: "targetDay"
    - path: "api/room-schedule/room-schedule.validation.js"
      provides: "targetDay in moveBodySchema"
      contains: "targetDay"
    - path: "services/conflictDetectionService.js"
      provides: "Cross-source conflict detection for theory lessons"
      contains: "checkCrossSourceRoomConflicts"
  key_links:
    - from: "api/room-schedule/room-schedule.service.js"
      to: "api/room-schedule/room-schedule.validation.js"
      via: "targetDay field in moveBodySchema"
      pattern: "targetDay.*Joi"
    - from: "api/theory/theory.controller.js"
      to: "services/conflictDetectionService.js"
      via: "cross-source conflict validation on update"
      pattern: "checkCrossSource|validateSingleLesson"
---

<objective>
Extend backend APIs to support day changes for empty timeBlocks via moveActivity, and add cross-source conflict detection for theory lesson updates.

Purpose: Currently moveActivity only changes room/time (same day), and theory conflict detection only checks within theory_lesson collection. This plan adds targetDay support to moveActivity for timeBlocks (the only source type routed through moveActivity for day changes), and cross-source conflict detection for theory updates.

Design decision: moveActivity handles day changes ONLY for timeBlocks (empty and with lessons). Rehearsals and theory lessons route day changes through their own update APIs (rehearsalService.updateRehearsal, theoryService.updateTheoryLesson) — not through moveActivity. This avoids dead code in moveActivity for rehearsal/theory day-change logic that would never be called from the frontend.

Output: Backend APIs that accept day changes for timeBlocks via moveActivity, plus cross-source conflict detection for theory lesson updates.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@api/room-schedule/room-schedule.service.js
@api/room-schedule/room-schedule.validation.js
@api/room-schedule/room-schedule.controller.js
@services/conflictDetectionService.js
@services/rehearsalConflictService.js
@api/theory/theory.controller.js
@api/theory/theory.service.js
@api/rehearsal/rehearsal.service.js
@config/constants.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend moveActivity with targetDay parameter (timeBlock only)</name>
  <files>
    api/room-schedule/room-schedule.validation.js
    api/room-schedule/room-schedule.service.js
  </files>
  <action>
1. In room-schedule.validation.js, add `targetDay` to moveBodySchema as optional:
   - `targetDay: Joi.number().integer().min(0).max(5).optional()` with appropriate messages
   - When targetDay is provided, it means "move this activity to a different day"

2. In room-schedule.service.js moveActivity function:
   - Destructure `targetDay` from moveData (alongside existing fields)
   - After determining `numericDay` (the activity's current day), calculate `effectiveDay`:
     - `const effectiveDay = (targetDay !== undefined && targetDay !== null) ? targetDay : numericDay`
   - Change Step 2 conflict pre-check to use `effectiveDay` instead of `numericDay`:
     - `const schedule = await getRoomSchedule(effectiveDay, options)`
   - In Step 3 per-source updates, when `effectiveDay !== numericDay` (day is changing):
     - For `timeBlock`: also set `teaching.timeBlocks.$.day` to `DAY_NAMES[effectiveDay]` (Hebrew day name from config/constants.js)
     - For `rehearsal` and `theory`: do NOT add day-change logic here. These source types route day changes through their own update APIs (rehearsalService.updateRehearsal, theoryService.updateTheoryLesson). moveActivity for rehearsal/theory only handles same-day room/time moves.
   - In Step 4, return `getRoomSchedule(effectiveDay, options)` instead of `numericDay` so the frontend sees the target day's updated schedule
  </action>
  <verify>
    Server starts without errors: `npm start` (check for syntax errors).
    Review the diff to confirm:
    - targetDay is optional in validation schema
    - Conflict check uses effectiveDay
    - Only timeBlock source type has day change logic
    - Rehearsal/theory sources do NOT have day-change logic in moveActivity
    - Return uses effectiveDay
  </verify>
  <done>
    moveActivity API accepts optional targetDay parameter; when provided, conflict checks run against the target day's schedule, and the timeBlock's day field is updated (Hebrew day name). Rehearsal/theory day changes are NOT handled here (they use their own update APIs).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add cross-source conflict detection for theory lesson updates</name>
  <files>
    services/conflictDetectionService.js
  </files>
  <action>
1. In services/conflictDetectionService.js, add a new method `checkCrossSourceRoomConflicts(lessonData, excludeId, options)` to the ConflictDetectionService class:
   - This method checks for room conflicts against rehearsals and timeBlocks (not just theory_lesson)
   - Follow the pattern from rehearsalConflictService.js:
     a. Parse `lessonData.date` to get dayOfWeek and Hebrew day name (using VALID_DAYS from config/constants)
     b. Query `rehearsal` collection for same tenantId, same dayOfWeek, same location, overlapping times (use doTimesOverlap)
     c. Query `teacher` collection for timeBlocks on same Hebrew day, same location, overlapping times
     d. Return array of cross-source conflicts with source type, teacher/conductor name, times
   - Import VALID_DAYS from config/constants.js, and getDayOfWeek/createAppDate from utils/dateHelpers.js

2. Update `validateSingleLesson` method to also call `checkCrossSourceRoomConflicts`:
   - After existing roomConflicts and teacherConflicts, call:
     `const crossSourceConflicts = await this.checkCrossSourceRoomConflicts(lessonData, excludeId, options)`
   - Merge crossSourceConflicts into roomConflicts array
   - Update hasConflicts and totalConflicts accordingly

3. The theory controller already calls `ConflictDetectionService.validateSingleLesson` on both create and update. The cross-source checks will automatically be included. No changes to theory controller needed.

4. IMPORTANT: The existing theory-only checkRoomConflicts method must remain unchanged (it checks theory-vs-theory). The new method adds rehearsal and timeBlock checks on top.
  </action>
  <verify>
    Server starts without errors. Manually verify by reading the file that:
    - checkCrossSourceRoomConflicts exists and queries rehearsal + teacher collections
    - validateSingleLesson merges cross-source conflicts
    - No existing methods were broken
  </verify>
  <done>
    Theory lesson create/update conflict detection now catches room overlaps with rehearsals and timeBlocks, not just other theory lessons. Cross-source conflicts returned in the same format as existing room conflicts.
  </done>
</task>

</tasks>

<verification>
- moveActivity validation accepts targetDay (0-5, optional)
- moveActivity with targetDay changes the timeBlock's day AND checks conflicts on target day
- moveActivity does NOT include day-change logic for rehearsal or theory source types
- Theory create/update returns 409 when room conflicts with rehearsal or timeBlock exist
- All existing behavior preserved when targetDay is not provided
</verification>

<success_criteria>
- moveActivity endpoint accepts targetDay and correctly updates day for timeBlocks only
- Theory conflict detection catches cross-source room overlaps (rehearsals + timeBlocks)
- No dead code for rehearsal/theory day changes in moveActivity
- No regression in existing same-day move or reschedule-lesson functionality
</success_criteria>

<output>
After completion, create `.planning/phases/78-activity-rescheduling/78-01-SUMMARY.md`
</output>
