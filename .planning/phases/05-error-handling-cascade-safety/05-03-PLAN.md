---
phase: 05-error-handling-cascade-safety
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - services/cascadeDeletionService.js
  - services/cascadeDeletionAggregation.service.js
  - api/admin/cascade-deletion.service.js
  - api/admin/student-deletion-preview.service.js
autonomous: true

must_haves:
  truths:
    - "Every MongoDB query in cascadeDeletionService.js includes tenantId"
    - "Every aggregation pipeline in cascadeDeletionAggregation.service.js starts with $match { tenantId }"
    - "The admin cascade-deletion.service.js scopes all queries by tenantId"
    - "student-deletion-preview.service.js generates tenant-scoped previews"
    - "deletion_snapshots records include tenantId"
  artifacts:
    - path: "services/cascadeDeletionService.js"
      provides: "Tenant-scoped collection-based cascade deletion"
      contains: "tenantId"
    - path: "services/cascadeDeletionAggregation.service.js"
      provides: "Tenant-scoped aggregation pipelines for integrity validation"
      contains: "tenantId"
    - path: "api/admin/cascade-deletion.service.js"
      provides: "Tenant-scoped admin cascade deletion with preview"
      contains: "tenantId"
    - path: "api/admin/student-deletion-preview.service.js"
      provides: "Tenant-scoped deletion preview"
      contains: "tenantId"
  key_links:
    - from: "services/cascadeDeletionService.js"
      to: "deletion_snapshots collection"
      via: "insertOne with tenantId"
      pattern: "tenantId.*snapshot"
    - from: "services/cascadeDeletionAggregation.service.js"
      to: "All collection aggregation pipelines"
      via: "$match with tenantId as first stage"
      pattern: "\\$match.*tenantId"
---

<objective>
Add tenant scoping to the secondary cascade deletion service, aggregation service, admin cascade service, and deletion preview service.

Purpose: These four files collectively handle cascade deletion via collection-based approach, data integrity aggregation pipelines, admin-facing cascade API, and deletion impact preview. ALL have zero tenantId references in any query. They can read/modify data across all tenants during cascades, integrity checks, and previews.

Output: All four files fully tenant-scoped with tenantId in every DB query.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-error-handling-cascade-safety/05-RESEARCH.md
@services/cascadeDeletionService.js
@services/cascadeDeletionAggregation.service.js
@api/admin/cascade-deletion.service.js
@api/admin/student-deletion-preview.service.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tenantId to cascadeDeletionService.js and cascadeDeletionAggregation.service.js</name>
  <files>services/cascadeDeletionService.js, services/cascadeDeletionAggregation.service.js</files>
  <action>
  **File 1: services/cascadeDeletionService.js**

  Import requireTenantId:
  ```javascript
  import { requireTenantId } from '../middleware/tenant.middleware.js';
  ```

  Function signature changes:
  1. `cascadeDeleteStudent(studentId, options)` -- add `context` to options: `options.context`
     - At start: `const tenantId = requireTenantId(options?.context?.tenantId);`
     - Pass tenantId to createDeletionSnapshot and executeStudentCascade

  2. `createDeletionSnapshot(studentId, tenantId)` -- add tenantId parameter
     - Add tenantId to every query:
       - `collection.findOne({ _id: studentId, tenantId })` for student
       - `collection.find({ ..., tenantId })` for teacher, orchestra, rehearsal, theory_lesson, bagrut, activity_attendance
     - Add tenantId to the snapshot object: `snapshot.tenantId = tenantId`
     - The deletion_snapshots insertOne: add tenantId to snapshot doc

  3. `executeStudentCascade(studentId, session, options)` -- add tenantId to options or as separate param
     - Add tenantId to EVERY updateOne, updateMany, deleteMany, deleteOne filter:
       - student updateOne (teacherAssignments): add tenantId to filter `{ _id: studentId, tenantId }`
       - teacher updateMany: add tenantId
       - orchestra updateMany: add tenantId
       - rehearsal updateMany: add tenantId
       - theory_lesson updateMany: add tenantId
       - bagrut updateMany and deleteMany: add tenantId
       - activity_attendance deleteMany: add tenantId
       - student deleteOne/updateOne (final): add tenantId

  4. `cleanupOrphanedReferences(dryRun, tenantId)` -- add tenantId parameter
     - Add tenantId to all find queries for student, teacher, orchestra
     - Add tenantId to cleanup updateOne calls in the transaction
     - requireTenantId guard at entry

  5. `validateDeletionImpact(studentId, tenantId)` -- add tenantId parameter
     - Add tenantId to student findOne and all countDocuments calls

  6. `rollbackDeletion(snapshotId, tenantId)` -- add tenantId parameter
     - Add tenantId to snapshot findOne
     - Add tenantId to all replaceOne calls in the transaction
     - Add tenantId to snapshot updateOne (mark as used)

  7. `generateDeletionAuditLog(studentId, operations, metadata)` -- add tenantId to metadata
     - Add tenantId to the auditLog object before insertOne

  **File 2: services/cascadeDeletionAggregation.service.js**

  All methods need a `tenantId` parameter (or context). Add tenantId as the first pipeline $match stage in every aggregation.

  1. `findOrphanedStudentReferences(tenantId)`:
     - requireTenantId guard at entry
     - Add `{ $match: { isActive: true, tenantId } }` as first stage in ALL 7 aggregation pipelines:
       - student aggregation (active students)
       - teacher aggregation (orphan detection)
       - orchestra aggregation
       - rehearsal aggregation: add `{ $match: { tenantId } }` before $unwind
       - theory_lesson aggregation: add `{ $match: { tenantId } }` before $unwind
       - bagrut aggregation: add tenantId to existing $match
       - activity_attendance aggregation: add tenantId to existing $match
     - Also scope the activeStudentIds query by tenantId

  2. `detectBidirectionalInconsistencies(tenantId)`:
     - requireTenantId guard
     - Add tenantId to all 5 aggregation pipeline $match stages
     - Add tenantId to $lookup sub-pipelines (important: lookups can cross tenants without this)
       - studentTeacher: add tenantId to outer $match
       - teacherStudent: add tenantId to outer $match
       - orchestraStudent: add tenantId to outer $match AND to $lookup pipeline
       - studentOrchestra: add tenantId to outer $match AND to $lookup pipeline
       - bagrut: add tenantId to outer $match AND to $lookup pipeline

  3. `generateCascadeDeletionImpactReport(studentId, tenantId)`:
     - requireTenantId guard
     - Add tenantId to student findOne
     - Add tenantId to all 6 aggregation $match stages (teacher, orchestra, rehearsal, theory_lesson, bagrut, activity_attendance)

  4. `validateDataIntegrity(tenantId)`:
     - requireTenantId guard
     - Add tenantId to countDocuments calls
     - Pass tenantId to all internal method calls

  5. `findDuplicateReferences(tenantId)`:
     - Add tenantId to $match stage

  Import requireTenantId:
  ```javascript
  import { requireTenantId } from '../middleware/tenant.middleware.js';
  ```
  </action>
  <verify>
  Run: `grep -c 'tenantId' services/cascadeDeletionService.js` -- should return 25+ (was 0).
  Run: `grep -c 'tenantId' services/cascadeDeletionAggregation.service.js` -- should return 30+ (was 0).
  Run: `grep 'requireTenantId' services/cascadeDeletionService.js` -- should find at least 1.
  Run: `grep 'requireTenantId' services/cascadeDeletionAggregation.service.js` -- should find at least 4.
  Run: `node --check services/cascadeDeletionService.js && node --check services/cascadeDeletionAggregation.service.js` -- both pass.
  </verify>
  <done>Both cascadeDeletionService.js and cascadeDeletionAggregation.service.js have tenantId in every DB query. Aggregation pipelines start with $match { tenantId }. $lookup sub-pipelines include tenant filtering. requireTenantId guards validate at entry points.</done>
</task>

<task type="auto">
  <name>Task 2: Add tenantId to admin cascade-deletion.service.js and student-deletion-preview.service.js</name>
  <files>api/admin/cascade-deletion.service.js, api/admin/student-deletion-preview.service.js</files>
  <action>
  **File 1: api/admin/cascade-deletion.service.js**

  Import requireTenantId:
  ```javascript
  import { requireTenantId } from '../../middleware/tenant.middleware.js';
  ```

  1. `previewCascadeDeletion(studentId, options)` -- expect `options.tenantId` or `options.context`
     - At start: `const tenantId = requireTenantId(options?.tenantId || options?.context?.tenantId);`
     - Add tenantId to student findOne: `{ _id: ObjectId.createFromHexString(studentId), tenantId }`
     - Add tenantId to each countDocuments query in the relationship loop
     - Note: This file uses collection names from COLLECTION_RELATIONSHIPS constant (like 'privateAttendance', 'privateLessons') which may not exist in the actual DB. This is fine -- countDocuments on non-existent collections returns 0. Just add tenantId to every query filter.

  2. `executeCascadeDeletion` (if exported) -- add tenantId to all queries
     - Follow same pattern as previewCascadeDeletion

  3. Any `rollback` or `cleanup` functions -- add tenantId to all queries

  For all functions: the tenantId must be added to every filter object passed to findOne, find, countDocuments, updateMany, deleteMany, etc.

  **File 2: api/admin/student-deletion-preview.service.js**

  Import requireTenantId:
  ```javascript
  import { requireTenantId } from '../../middleware/tenant.middleware.js';
  ```

  1. `generateDeletionPreview(studentId, tenantId)` -- add tenantId parameter
     - requireTenantId guard at entry
     - Pass tenantId to getStudentInfo
     - Pass tenantId to analyzeStudentRelationships

  2. `getStudentInfo(studentId)` (internal helper) -- add tenantId
     - Add tenantId to student findOne query

  3. `analyzeStudentRelationships(studentId, ...)` -- add tenantId
     - Add tenantId to every countDocuments/find query for each relationship in STUDENT_RELATIONSHIPS

  4. Any other exported or internal functions that query the DB -- add tenantId to filters

  Note: Some collection names in STUDENT_RELATIONSHIPS may use different names than the actual collections (e.g., 'theory' vs 'theory_lesson'). Do not change collection names -- just add tenantId to whatever queries exist.
  </action>
  <verify>
  Run: `grep -c 'tenantId' api/admin/cascade-deletion.service.js` -- should return 10+ (was 0).
  Run: `grep -c 'tenantId' api/admin/student-deletion-preview.service.js` -- should return 10+ (was 0).
  Run: `grep 'requireTenantId' api/admin/cascade-deletion.service.js` -- should find at least 1.
  Run: `grep 'requireTenantId' api/admin/student-deletion-preview.service.js` -- should find at least 1.
  Run: `node --check api/admin/cascade-deletion.service.js && node --check api/admin/student-deletion-preview.service.js` -- both pass.
  </verify>
  <done>Admin cascade service and deletion preview service are fully tenant-scoped. Every DB query includes tenantId. requireTenantId guards at entry points. Preview/impact analysis cannot leak cross-tenant data.</done>
</task>

</tasks>

<verification>
1. grep for tenantId in all 4 files -- each should have 10+ occurrences (was 0 for all)
2. No DB query in any of the 4 files lacks tenantId
3. requireTenantId imported and called at entry points in all 4 files
4. All 4 files pass `node --check`
</verification>

<success_criteria>
- Every findOne, find, countDocuments, updateMany, deleteMany, insertOne in all 4 files includes tenantId
- deletion_snapshots records include tenantId
- Aggregation pipelines start with $match { tenantId }
- $lookup sub-pipelines include tenant filtering
- requireTenantId guards validate at all public entry points
</success_criteria>

<output>
After completion, create `.planning/phases/05-error-handling-cascade-safety/05-03-SUMMARY.md`
</output>
